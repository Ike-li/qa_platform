# 测试质量运营台账

更新日期：2026-06-01

## Flaky 台账

当前无未关闭 flaky。新增 flaky 必须先登记再处理，不能直接靠重试掩盖。

| ID | 测试 | 失败环境 | 复现命令 | 最近证据 | Owner | 状态 | 截止条件 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| FLAKY-000 | 无活跃项 | - | - | 2026-05-27 本轮相关 unit/integration 均可重复通过 | 测试平台维护者 | closed | 出现首次非确定失败时新增条目 |

趋势记录：

| 日期 | required integration | unit | warning 摘要 | 处理结论 |
| --- | --- | --- | --- | --- |
| 2026-06-01 | N/A（质量门禁维护分层 / release-quality helper 收敛） | release quality docs contract self-check 1 passed；release quality docs maintenance contract targeted 2 passed；release quality docs runtime/external moved-index contract targeted 1 passed；release quality docs contract full 381 passed；targeted ruff/py_compile passed；git diff --check passed | 无新增 warning；`docs/testing-strategy.md` 新增“质量门禁维护分层”，明确行为正确性优先由业务测试证明、release-quality contract 只保护发版证据/台账口径/高风险防回退；`test_release_quality_docs_contract.py` 新增 `_read` 加缓存，并把 `_quality_ops_row` / `_quality_ops_row_containing` / `_quality_ops_rows_containing` / `_test_block` / `_block_between` / `_after` / `_marked_block` / `_marked_block_or_tail` 用于近期 performance/real_auth follow-up，scheduler/frontend/retry/SQL follow-up 契约继续切到共享 helper；通用 helper 移入 `tests/unit/release_quality_contract_helpers.py`；documentation/status 契约拆到 `tests/unit/test_release_quality_docs_status_contract.py`；current gate split self-check 契约拆到 `tests/unit/test_release_quality_docs_current_gate_contract.py`，docs maintenance helper/static self-check 契约拆到 `tests/unit/test_release_quality_docs_maintenance_contract.py`，docs runtime/external moved-index 契约拆到 `tests/unit/test_release_quality_docs_runtime_external_index_contract.py`；executor/engine 契约拆到 `tests/unit/test_release_quality_executor_engine_contract.py`；plugin runner/registry 契约拆到 `tests/unit/test_release_quality_plugin_runner_contract.py`；release gate/workflow 契约拆到 `tests/unit/test_release_quality_release_gate_contract.py`；resource API/control-plane 契约拆到 `tests/unit/test_release_quality_resource_api_contract.py`；audit/audit-events 契约拆到 `tests/unit/test_release_quality_audit_contract.py`；pipeline/repository 契约拆到 `tests/unit/test_release_quality_pipeline_repository_contract.py`；frontend/API 契约拆到 `tests/unit/test_release_quality_frontend_contract.py`；API guardrails 契约拆到 `tests/unit/test_release_quality_api_guardrails_contract.py`；observability/logging 契约拆到 `tests/unit/test_release_quality_observability_contract.py`；analytics/API 契约拆到 `tests/unit/test_release_quality_analytics_contract.py`；migration/env-vars 契约拆到 `tests/unit/test_release_quality_migration_contract.py`；current gate evidence 契约拆到 `tests/unit/test_release_quality_current_gate_evidence_contract.py`；current gate E2E/static evidence 契约拆到 `tests/unit/test_release_quality_current_gate_e2e_contract.py`；current gate runner/source evidence 契约拆到 `tests/unit/test_release_quality_current_gate_runner_contract.py`；current gate runner/artifact evidence 契约拆到 `tests/unit/test_release_quality_current_gate_runner_artifact_contract.py`；current gate failure-path evidence 契约拆到 `tests/unit/test_release_quality_current_gate_failure_path_contract.py`；current gate validation/error evidence 契约拆到 `tests/unit/test_release_quality_current_gate_validation_contract.py`；current gate settings/domain validation evidence 契约拆到 `tests/unit/test_release_quality_current_gate_settings_domain_validation_contract.py`；current gate summary evidence 契约拆到 `tests/unit/test_release_quality_current_gate_summary_contract.py`，current gate summary freshness evidence 契约拆到 `tests/unit/test_release_quality_current_gate_summary_freshness_contract.py`；current gate config evidence 契约拆到 `tests/unit/test_release_quality_current_gate_config_contract.py`；current gate source/plugin evidence 契约拆到 `tests/unit/test_release_quality_current_gate_source_contract.py`；current gate API/request evidence 契约拆到 `tests/unit/test_release_quality_current_gate_api_contract.py`；current gate API boundary evidence 契约拆到 `tests/unit/test_release_quality_current_gate_api_boundary_contract.py`；current gate pipeline/API evidence 契约拆到 `tests/unit/test_release_quality_current_gate_pipeline_api_contract.py`；current gate auth/security evidence 契约拆到 `tests/unit/test_release_quality_current_gate_auth_security_contract.py`；current gate access-control evidence 契约拆到 `tests/unit/test_release_quality_current_gate_access_control_contract.py`；current gate data/runtime evidence 契约拆到 `tests/unit/test_release_quality_current_gate_data_runtime_contract.py`；current gate runtime-ops evidence 契约拆到 `tests/unit/test_release_quality_current_gate_runtime_ops_contract.py`；worker notification channel 契约拆到 `tests/unit/test_release_quality_notification_channels_contract.py`；worker notification execution 契约拆到 `tests/unit/test_release_quality_worker_notifications_contract.py`；worker lifecycle/retry/reclaim 契约拆到 `tests/unit/test_release_quality_worker_lifecycle_contract.py`；git source/worker Git 契约拆到 `tests/unit/test_release_quality_git_source_contract.py`；API integration/write-state 契约拆到 `tests/unit/test_release_quality_api_integration_contract.py`；project/webhook surface 契约拆到 `tests/unit/test_release_quality_project_surface_contract.py`；performance smoke 契约拆到 `tests/unit/test_release_quality_performance_contract.py`；worker external-stack 契约拆到 `tests/unit/test_release_quality_worker_external_stack_contract.py`；real auth results/artifacts 契约拆到 `tests/unit/test_release_quality_real_auth_results_contract.py`；auth/API token 契约拆到 `tests/unit/test_release_quality_auth_contract.py`；run/SSE/artifact API 契约拆到 `tests/unit/test_release_quality_run_api_contract.py`；runtime scheduler/log stream 契约拆到 `tests/unit/test_release_quality_runtime_scheduler_contract.py`；notification channel 重复 HTTP case 切片合并到 `_notification_channel_sections` / `_notification_channel_http_case_blocks`；65 个只切台账行契约改用 `_quality_ops_row`，40 个复杂 row/direct/helper 台账切片也改用 `_quality_ops_row`，471 个 helper 外 direct `.read_text(...)` 调用统一改用 `_read(...)`，224 个 quality-ops `next(line...)` 查行统一改用 `_quality_ops_row`，145 个双边界 `.split(...)[1].split(...)[0]` 切片统一改用 `_block_between`，13 个单边界 `.split(..., 1)[1]` 切片统一改用 `_after`，446 个 `.index`/`.find` 手工区块定位统一改用 `_marked_block`/`_marked_block_or_tail`，158 个 helper 外 `_read(QUALITY_OPS)` 整表读取改用 `_quality_ops_row_containing`，9 个多行 quality-ops 台账拼接改用 `_quality_ops_rows_containing`，self-check AST 计数收敛到 `_contract_maintenance_counts`，剩余 `QUALITY_OPS` 读取统一走缓存 `_read(QUALITY_OPS)`，旧式 direct `QUALITY_OPS.read_text` / `row_start` 模式上限下调为 0/0，helper 外 direct `.read_text(encoding="utf-8")` 上限固定为 0，helper 外 `_read(QUALITY_OPS)` 调用上限下调到 0，helper 外 `.index`/`.find` 调用上限下调到 0 | 从“继续补强”转向收敛：release-quality contract 已超过 22k 行，继续复制 `read_text`、`next(line...)`、`split(...)[1]`、`.index(...)` 或整表 `QUALITY_OPS` 包含式断言会让质量门禁自身变脆。现先收敛新切片入口并把 helper 外单边 split、index/find 与整表 quality-ops 读取上限固定为 0；后续新增门禁先合并同类、优先复用 helper，把长期策略放回 `testing-strategy`，避免台账变成逐断言流水账 |
| 2026-06-01 | `RUN_INTEGRATION_TESTS=1 RUN_PERFORMANCE_TESTS=1 tests/integration/test_performance_smoke.py::test_audit_events_large_filtered_page_api_token_p99_smoke -q` 1 passed；performance full 31 passed | release quality docs contract targeted 2 passed；release quality docs contract full 359 passed；targeted ruff/py_compile passed；git diff --check passed | 无新增 warning；audit events large filtered page 现在把 23 次成功查询写入的 `audit_events.list` 自审计全部收敛到 `self_audit_projections`，固定 tenant/user/action/resource/before_state/after_state 和查询分页摘要 | 测试 review 复扫发现该性能用例响应体已完整等值，但自审计副作用仍是 `count_self_audits()==before+23` 加最新一条 `after_state`；如果 warmup 或 timed loop 中间某条自审计漏写 tenant/user、action/resource 或分页摘要漂移，旧断言会漏。现升级为 audit large filtered page self-audit exact-list 契约；数量断言保留为无额外同类自审计的副作用边界 |
| 2026-06-01 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_auth_results_artifacts.py::test_real_sse_logs_resume_after_last_event_id_uses_ticket_rbac_and_redis tests/integration/test_real_auth_results_artifacts.py::test_real_sse_events_resume_after_last_event_id_uses_ticket_rbac_and_redis -q` 2 passed；real_auth full 30 passed | release quality docs contract targeted 3 passed；release quality docs contract full 358 passed；targeted ruff/py_compile passed；git diff --check passed | 无新增 warning；real auth SSE logs/events resume 现在把 owner/run.read/status-stream 回放收敛到 `expected_log_frames` / `expected_sse_frames` 完整 frame projection；done sentinel、id、data、status/previous/timestamp 都按列表等值 | 测试 review 复扫发现这两条虽然已解析 EventSource frame，但仍用 `{event.get("id") ...}` 集合负断言证明首个事件未回放；如果响应顺序漂移、done sentinel 夹带 id、run.read 读面与 owner 读面不一致，旧断言信号分散。现升级为 SSE resume projection exact 契约；外部 token reuse/empty-scope exact body 保留 |
| 2026-06-01 | `RUN_INTEGRATION_TESTS=1 RUN_PERFORMANCE_TESTS=1 tests/integration/test_worker_execute.py::test_real_worker_persists_artifacts_and_archived_logs -q -rs` 1 skipped（缺 compose services api, worker）；worker_execute full 8 skipped（缺 compose services api, worker） | release quality docs contract targeted 3 passed；release quality docs contract full 357 passed；targeted ruff/py_compile passed；git diff --check passed | 无新增 warning；external-stack worker archived logs 管理员 JWT 与 `run.read` token 读面现在统一经 `_archive_page_projection` 固定两页 `{page,per_page,total,data_count}`，并要求 total 等于两页合并日志行数；token 读面必须复用同一 `expected_archive_pages` | 测试 review 复扫发现归档日志已用 exact line 和 bulk index 锁住内容，但 pagination envelope 仍是 `archive_body["page"]` / `per_page` / token 同类分散断言；如果 total 漂移、token 读面的分页壳不同、或第二页 data_count 与 total 不一致，旧断言失败信号分散。现升级为 archive page projection exact 契约；`len(body.get("data", [])) == 1000` 继续作为第一页填满的外部栈 readiness |
| 2026-06-01 | `RUN_INTEGRATION_TESTS=1 RUN_PERFORMANCE_TESTS=1 tests/integration/test_worker_execute.py::test_external_stack_worker_e2e_slo_smoke tests/integration/test_worker_execute.py::test_real_worker_persists_artifacts_and_archived_logs -q -rs` 2 skipped（缺 compose services api, worker）；worker_execute full 8 skipped（缺 compose services api, worker） | release quality docs contract targeted 4 passed；release quality docs contract full 356 passed；targeted ruff/py_compile passed；git diff --check passed | 无新增 warning；external-stack worker artifact 最终读面现在统一经 `_artifact_page_projection` 锁住 `data/page/per_page/total`：SLO smoke 固定 total=2、default `per_page=20` 与 JUnit/log storage path，真实 worker 管理员 JWT 和 `run.read` token 读面固定同一个 `expected_artifact_page`，包含 JUnit/HTML/log/Allure 四个 artifact | 测试 review 复扫发现这些用例已经有 `{name,type,storage_path}` projection，但最终仍保留 `artifacts_body["total"] == len(expected_artifacts)` / token 同类数量断言；如果 pagination envelope 漂移、默认 per_page 变化、token 读面和管理员读面 total 不一致，旧断言信号分散。现升级为 artifact page projection exact 契约；`_has_exact_artifact_names` 与 `lambda body: body["total"] == len(expected_artifacts)` 继续作为外部栈 readiness，最终契约由 page projection、download 内容、secret/no-over-limit 负断言负责 |
| 2026-06-01 | `RUN_INTEGRATION_TESTS=1 RUN_PERFORMANCE_TESTS=1 tests/integration/test_performance_smoke.py::test_dequeue_waiting_prioritizes_newer_high_runs_over_older_low_backlog_slo_smoke -q` 1 passed；performance full 31 passed | release quality docs contract targeted 2 passed；release quality docs contract full 355 passed；targeted ruff/py_compile passed；git diff --check passed | 无新增 warning；performance priority backlog 现在把 high-priority ARQ 调用固定为按 `created_at` 排序的 `_expected_execute_run_call(run_id, "queue:high")` 完整列表，post-dequeue Run row 收敛为 `post_dequeue_projection_by_id` 等值，audit after-dequeue 改为包含 `resource_id/action/resource_type/after_state` 的精确列表等值 | 测试 review 复扫发现该用例仍把 high calls 拆成 run id 集合、job id 集合和 queue 集合，且 post-dequeue row 用逐字段抽查、audit 用 `{(resource_id, action)}` 集合；如果 queue 与 run 绑定串错、调用顺序漂移、重复写同一 audit，或 low run 被误入队后又被集合去重，旧断言信号偏弱。现升级为 exact ARQ order / row projection / audit exact-list 契约；polling 中的 `all(...)` 仍判定为异步等待条件，SLO samples 长度由 high run 列表自然约束 |
| 2026-06-01 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_auth_results_artifacts.py::test_artifact_list_api_token_requires_run_read_scope -q` 1 passed；real_auth full 30 passed；worker_execute full 8 skipped（缺 compose services api, worker） | release quality docs contract targeted 2 passed；release quality docs contract full 354 passed；targeted ruff/py_compile passed；git diff --check passed | 无新增 warning；real auth artifact list 正向 `run.read` 分支现在用显式 `artifact_created_at` 固定 DB 排序，并把 page/per_page/total/data 全部收敛到 `expected_body` 完整等值；旧的 `data: body["data"]` 响应壳自填充、按 id 排序投影与 `expected_artifact_items` 二段比较被移除 | 测试 review 复扫发现该用例虽已固定 ArtifactResponse 字段值，但仍没有验证服务端真实列表顺序和完整 pagination envelope；如果 API 改成按 id 排序、顶层夹带字段、或 data 顺序漂移，旧断言可能继续绿。现升级为 exact body/order 契约；worker 的 `_has_exact_artifact_names`、`_archive_contains_all`、bulk log page length 判断仍判定为外部栈异步 readiness，最终仍由 `_artifact_projection`、exact line、bulk index 与 secret redaction 断言负责 |
| 2026-06-01 | `RUN_INTEGRATION_TESTS=1 RUN_PERFORMANCE_TESTS=1 tests/integration/test_performance_smoke.py::test_artifact_list_api_p99_smoke tests/integration/test_performance_smoke.py::test_real_api_token_concurrent_read_paths_p99_smoke -q` 2 passed；`RUN_INTEGRATION_TESTS=1 tests/integration/test_real_auth_results_artifacts.py::test_archived_logs_api_replays_s3_jsonl_after_real_rbac tests/integration/test_real_auth_results_artifacts.py::test_artifact_upload_enforces_limits_before_real_db_rows -q` 2 passed；performance full 31 passed；real_auth full 30 passed | release quality docs contract targeted 4 passed；release quality docs contract full 353 passed；targeted ruff/py_compile passed；git diff --check passed | 无新增 warning；performance artifact list p99 现在用显式 `created_at` 固定 DB 排序，并把 page/per_page/total/data 全部收敛到 `expected_body` 完整等值；并发 run.read artifact list 从 `data: body["data"]` 壳检查 + id 排序投影升级为 `expected_artifact_body` 完整分页体；real auth archived logs 默认页与 page=2 都固定完整分页体，artifact upload limit DB rows 从 `total == 2` + `{row.name}` 升级为 `{total, rows}` 投影等值 | 测试 review 复扫发现这些用例虽然已比字段集合强，但仍把“数量正确/名称映射正确/用响应自身 data 填回响应壳”当作契约；如果 artifact list 顺序漂移、pagination envelope 夹带/缺字段、artifact row type/mime/path 串错或 archive 默认 per_page 漂移，旧断言信号不够强。现升级为 exact response/projection 契约；worker 的 `_has_exact_artifact_names` 与 `_archive_contains_all` 本批判定为合理保留的异步 readiness，最终仍由 `_artifact_projection`、exact line 和拒绝 exact body 断言负责 |
| 2026-06-01 | `RUN_INTEGRATION_TESTS=1 RUN_PERFORMANCE_TESTS=1 tests/integration/test_performance_smoke.py::test_archived_log_replay_large_page_p99_smoke tests/integration/test_performance_smoke.py::test_artifact_list_api_p99_smoke tests/integration/test_performance_smoke.py::test_artifact_list_large_collection_page_p99_smoke tests/integration/test_performance_smoke.py::test_audit_events_list_api_p99_smoke tests/integration/test_performance_smoke.py::test_audit_events_large_filtered_page_api_token_p99_smoke tests/integration/test_performance_smoke.py::test_real_api_token_concurrent_read_paths_p99_smoke --collect-only` 6 tests collected；`RUN_INTEGRATION_TESTS=1 tests/integration/test_real_auth_results_artifacts.py::test_archived_logs_api_replays_large_page_without_presign_after_real_rbac tests/integration/test_real_auth_results_artifacts.py::test_run_artifacts_list_paginates_real_db_without_s3_side_effects tests/integration/test_real_auth_results_artifacts.py::test_run_results_api_filters_real_rows_and_recovers_after_duplicate --collect-only` 3 tests collected；`RUN_INTEGRATION_TESTS=1 tests/integration/test_worker_execute.py::test_worker_lost_retry_completes_with_artifacts_and_archived_logs --collect-only` 1 test collected；相关 full collect：performance 31、real_auth 30、worker_execute 8；targeted actual：performance field-set 6 passed、SSE/log-stream/summary 4 passed、real_auth 3 passed、worker_lost 1 skipped（缺 compose services api, worker）；full actual：performance 31 passed、real_auth 30 passed、worker_execute 8 skipped（缺 compose services api, worker） | release quality docs contract targeted 6+1 passed；release quality docs contract full 352 passed；targeted ruff/py_compile passed；git diff --check passed | 无新增 warning；performance smoke 的 archived logs/artifact/audit/concurrent read 路径不再保留完整 body 等值后的 `expected_*_fields` 与 `[set(item) ...]` 冗余守门，audit events 改为 `_audit_event_response` 完整 DTO 分页 body 等值；log stream roundtrip 固定完整 `{id,stream,line}` entry，execution summary 纳入 `failed_tests/failed_tests_omitted`，live SSE client 固定 `trust_env=False` 隔离本地代理；real auth archived logs/page-two artifacts/rollback results 删除重复字段集合守门，其中 rollback results 通过显式 `created_at` 固定结果顺序；worker_lost retry 不再用 `any(attempt == 2)` 抽到第一条，而是精确要求唯一 attempt=2 并投影 retry Run 的 project/pipeline/environment/status/trigger/priority/git_ref/summary/error | 测试 review 复扫发现这些用例已逐步有完整响应体或字段到值投影，但仍残留“字段集合对/数量对/有一条 attempt=2”的旧形态；如果返回 item 夹带额外字段、audit DTO 字段值串错、rollback 结果顺序漂移，或 reclaimer 误创建两条 retry run，旧断言会给出弱信号甚至只抓第一条。现升级为 direct response/projection/exact-once retry 契约；`_has_exact_artifact_names`、`_archive_contains_all` 等保留为异步轮询 readiness，最终契约仍由后续 projection/exact line 断言负责；本地 live SSE 502 复现后确认由 client 环境隔离不足触发，已用目标用例 2 passed 和 performance full 31 passed 回归 |
| 2026-06-01 | N/A（RBAC dependency SQL column-bound params 契约） | `tests/unit/test_api/test_rbac_deps.py` 15 passed；release quality docs contract full 351 passed；targeted ruff/py_compile passed | 无新增 warning；RBAC dependency tests 现在把 project visibility lookup 与 project_member role lookup 编译为 PostgreSQL SQL，并固定 `project.id/project.tenant_id`、`project_member.project_id/user_id/tenant_id` 的 column-bound 参数映射；sequence helper 同时固定 execute 次数 | 测试 review 复扫发现这些用例此前把 `statement.compile().params.values()` 转成 set，只检查 project/user/tenant 值出现过；如果 tenant_id 与 user_id 绑定错列、project visibility 查询夹带 user_id，或 execute 次数漂移，旧测试仍可能绿。现升级为 RBAC SQL column-bound params 契约，避免权限依赖测试只证明“几个 UUID 出现在参数集合里” |
| 2026-06-01 | N/A（ScheduleRepository due SQL column-bound params 契约） | `tests/unit/test_project_repository_contracts.py::test_find_due_schedules_uses_skip_locked_schedule_row_lock` 1 passed；project repository contracts full 1 passed；release quality docs contract full 350 passed；targeted ruff/py_compile passed | 无新增 warning；ScheduleRepository due SQL 契约现在从 rendered SQL 逐列提取 `schedule.next_run_at <=` 与 `LIMIT` 的绑定参数名，并断言 params 只能包含 due_at 与 limit 两个绑定 | 测试 review 复扫发现该用例仍用 `now in params.values()` 和 `params["param_1"]`；如果 `now` 绑定到错误表达式、limit 参数名变化或查询夹带额外参数，旧测试只能证明“某个时间值出现在 params 里”。现升级为 due SQL column-bound params 契约，避免 schedule repository 锁行测试只证明“参数值集合看起来对” |
| 2026-06-01 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_auto_retry_real_db.py::test_execute_run_setup_docker_infra_error_uses_real_executor_and_schedules_retry tests/integration/test_auto_retry_real_db.py::test_execute_run_setup_script_failure_is_not_retried_real_executor -q` 2 passed；auto_retry_real_db full 7 passed | release quality docs contract full 349 passed；targeted ruff/py_compile passed | 无新增 warning；auto retry 真实 executor setup 日志现在用 `_assert_log_line_once` 固定 `Repository cloned successfully` 和 `Running setup script...` 各精确出现一次，覆盖 setup Docker infra retry 和 setup script non-retry 两条路径 | 测试 review 复扫发现这两条真实链路只用 `assert "... " in log_lines`；如果 setup 阶段重复执行、日志重复刷写，或未来维护把“至少出现过”当成不重试证据，旧测试仍可能绿。现升级为 setup log exact-once 契约，避免 auto retry setup 边界测试只证明“两条熟悉日志出现过” |
| 2026-06-01 | N/A（Frontend create payload optionality exact contract） | `tests/unit/test_frontend_api_contract.py::test_frontend_create_payloads_keep_backend_required_fields` 4 passed；frontend build passed；release quality docs contract full 348 passed；targeted ruff/py_compile passed | 无新增 warning；frontend create payload contract 现在同时固定后端 required 字段必须是 TS 非可选、后端 non-required 字段必须是 TS 可选；`ProjectCreatePayload.git_auth_method/default_branch/root_path` 已跟随后端默认值改为 optional，同时创建表单仍保留自己的默认值 | 测试 review 复扫发现该契约此前只断字段名集合和类型，required 只检查必填字段非可选，未检查后端可选字段不能被前端误建为必填；因此 `git_auth_method/default_branch/root_path` 这类有后端默认值的字段漂移时旧测试仍绿。现升级为 optionality exact contract，避免前端 API contract 只证明“字段名和类型大概一致” |
| 2026-06-01 | N/A（RunRepository scheduler SQL column-bound params 契约） | `tests/unit/test_run_repository_contracts.py` 4 passed；release quality docs contract full 347 passed；targeted ruff/py_compile passed | 无新增 warning；RunRepository scheduler SQL 契约现在从 rendered SQL 逐列提取 `run.status =`、`run.status IN`、`run.project_id =`、`LIMIT` 的绑定参数名，并断言 params 只能包含这些列级绑定；active status IN 保留无序集合语义但同时固定长度为 3 | 测试 review 复扫发现该文件仍有 `RunStatusEnum.QUEUED in params.values()` 和直接读 `status_1/status_2/project_id_1/param_1` 的形态；如果 status 绑定到错误表达式、project 过滤列漂移、limit 参数夹带或 SQLAlchemy 生成名变化，旧测试容易只证明“某个枚举值出现在 params 里”。现升级为 column-bound params 契约，避免 repository SQL 测试只证明“参数值集合看起来对” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 RUN_PERFORMANCE_TESTS=1 tests/integration/test_performance_smoke.py::test_webhook_trigger_enqueue_slo_smoke tests/integration/test_performance_smoke.py::test_schedule_tick_enqueue_slo_smoke tests/integration/test_performance_smoke.py::test_dequeue_waiting_runs_slo_smoke tests/integration/test_performance_smoke.py::test_dequeue_waiting_prioritizes_newer_high_runs_over_older_low_backlog_slo_smoke -q` 4 passed | targeted docs contract passed；targeted ruff/py_compile passed；targeted performance passed | 无新增 warning；webhook trigger、schedule trigger、waiting dequeue、priority backlog 现在都用真实 DB Run row / AuditEvent 投影等值：row status/priority/git_ref/git_sha/retry_group/queue/job/metadata 必须完整匹配，audit tenant/user/action/resource/before/after_state 必须完整匹配，并继续证明 webhook reserved metadata 和 delivery_id 不进入审计 | 测试 review 复扫发现这组性能门禁仍有“先用 id 集合证明拿到行，再逐字段抽查”的形态；如果 webhook metadata 被请求侧 reserved key 污染、schedule audit 缺少 `enqueued`、dequeue queue/job 写错、priority backlog 漏写 audit 或 response body 与 audit 脱节，旧测试失败信号偏弱。现升级为 performance enqueue/dequeue full projection contract，避免性能测试只证明“队列动作够快且 id 集合对” |
| 2026-05-31 | N/A（FairScheduler mark_enqueued direct kwargs helper follow-up） | `tests/unit/test_worker/test_scheduler.py` 14 passed；targeted docs contract passed；targeted ruff/py_compile passed | 无新增 warning；FairScheduler 成功入队/出队路径现在统一 `_assert_mark_enqueued`，完整等值 `{queue_name,arq_job_id,enqueued_at}` 并保留 timezone-aware 检查，覆盖 manual priority、schedule、explicit trigger override、dequeue capacity stop 与 project-capacity skip 后续出队 | 测试 review 复扫发现台账已声称 exact enqueue metadata，但源码仍在多处逐字段抽查 `repo.mark_enqueued.await_args.kwargs[...]`；如果 `mark_enqueued` 夹带额外 kwargs、queue/job/time 字段部分漂移，或 helper 回退成局部字段抽查，旧测试不够敏感。现升级为 direct kwargs helper follow-up，避免 scheduler exact metadata 测试只证明“几个 kwargs 子字段对” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_worker_execute.py --collect-only` 8 tests collected | targeted docs contract passed；targeted ruff/py_compile passed | 无新增 warning；external-stack worker artifact 读面现在通过 `_artifact_projection` 固定完整 `{name,type,storage_path}` 列表，覆盖管理员 JWT、`run.read` token 与 performance SLO artifact 三个读面，继续保留 artifact download 内容校验与 secret/no-over-limit 负断言 | 测试 review 复扫发现 artifact 读面虽然已从子集升级到名称集合，但仍可能只证明“名字对”；如果 artifact type 与 storage_path 绑定漂移、token 读面返回同名错误对象，或 performance SLO 多收临时文件，旧断言失败信号仍偏弱。现升级为 artifact projection exact contract，避免外部栈 artifact 测试只证明“文件名集合看起来对” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 RUN_PERFORMANCE_TESTS=1 tests/integration/test_performance_smoke.py::test_write_run_api_p99_smoke tests/integration/test_performance_smoke.py::test_trigger_run_enqueue_slo_smoke -q` 2 passed | targeted docs contract passed；targeted ruff/collect-only passed；targeted performance passed | 无新增 warning；performance smoke 的 write-run 与 trigger-enqueue 路径现在把真实 DB Run row 投影为 `{tenant_id,project_id,pipeline_id,environment_id,status,trigger_type,triggered_by,git_ref,priority,retry_group_id,enqueued/arq_job_id,queue_name}`，并把 `run.trigger` audit 投影为完整 `{tenant_id,user_id,action,resource_type,before_state,after_state}` 等值 | 测试 review 复扫发现这两条性能门禁仍用 `assert set(rows_by_id) == set(created_runs)` / `assert set(audits_by_run_id) == set(...)` 再逐字段抽查；如果漏一条 audit、row 写错 retry_group/queue 或 response body 与 audit after_state 脱节，旧测试失败信号偏弱。现升级为 performance write/trigger DB-audit direct projection contract，避免性能测试只证明“创建速度达标且 run id 集合对” |
| 2026-05-31 | N/A（Notification log write exception attempted log direct kwargs follow-up） | `tests/unit/test_worker/test_notifications.py::TestEvaluateAndNotify::test_duplicate_log_integrity_error_after_send_is_treated_as_existing_delivery tests/unit/test_worker/test_notifications.py::TestEvaluateAndNotify::test_non_duplicate_log_integrity_error_propagates_after_send tests/unit/test_worker/test_notifications.py::TestEvaluateAndNotify::test_unexpected_log_write_failure_propagates_after_send` 3 passed；worker notifications full 35 passed；release quality docs contract full 344 passed；targeted ruff passed | 无新增 warning；notification log 写入异常三分支现在都固定 `get_by_delivery(run_id, rule_id, channel_type="webhook")` 与 attempted `log_repo.create` 完整 `{project_id,run_id,rule_id,channel_type,status,error_message}`，duplicate 唯一约束分支 commit，FK/RuntimeError 分支不 commit | 测试 review 复扫发现这些异常语义用例已能区分 duplicate/FK/RuntimeError，但 attempted log 仍只断言 `log_repo.create.assert_awaited_once()`；如果 create 尝试写错 project/run/rule/channel、把 sent 写成 failed，或 delivery key 漂移，旧测试仍可能只证明“send 后碰过日志仓储”。现升级为 attempted log direct kwargs 契约，避免异常语义测试发现不了待写日志内容漂移 |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_auth_results_artifacts.py::test_artifact_list_api_token_requires_run_read_scope tests/integration/test_real_auth_results_artifacts.py::test_artifact_upload_recurses_allure_report_with_real_db_rows -q` 2 passed | targeted docs contract passed；targeted ruff/collect-only passed；targeted integration passed | 无新增 warning；real auth artifact list 正向 `run.read` 读面现在固定完整 ArtifactResponse item 投影 `{id,run_id,type,name,storage_path,size_bytes,mime_type,expires_at,created_at}`，Allure report 递归上传集成用例固定 DB artifact row `{run_id,type,name,storage_path,size_bytes,mime_type}` 与 S3 object bytes 完整等值 | 测试 review 复扫发现这些真实 artifact 路径仍有 `assert set(artifacts_by_id)` / `assert set(by_name)` 加局部字段抽查；如果 API 返回同名错 type/path、created_at 漂移、Allure JS MIME 或 S3 bytes 写错，旧测试只能证明“名字集合对且几项字段看起来对”。现升级为 real auth artifact projection exact contract，避免 artifact 集成测试为了覆盖率而只数名字 |
| 2026-05-31 | N/A（RunExecutor artifact_repo missing S3-only direct kwargs follow-up） | `tests/unit/test_engine/test_executor.py::TestUploadArtifacts::test_upload_skips_artifact_row_when_repo_missing` 1 passed；executor full 42 passed；release quality docs contract full 342 passed；targeted ruff passed | 无新增 warning；artifact repository 缺失但 S3 client 可用的降级路径现在固定 `put_object` 完整 kwargs 等值：bucket、storage key、Body 本地文件对象均不能夹带或缺失，并继续要求 `Uploaded artifact: x.xml` 日志对应同一个 run id | 测试 review 复扫发现该用例虽已补 bucket/key/body 抽查，但仍不是完整 kwargs 投影；如果未来 `put_object` 夹带 ContentType、ACL、错误 metadata，或缺失 Body 时靠局部字段继续绿，失败信号仍偏弱。现升级为 direct kwargs 契约，避免 artifact_repo missing 测试只证明“几个 S3 字段看起来对” |
| 2026-05-31 | N/A（Pytest/Go runner subprocess kwargs direct helper 契约） | `tests/unit/test_plugins/test_pytest_runner.py::TestPytestRunTests::test_env_vars_passed_through tests/unit/test_plugins/test_pytest_runner.py::TestPytestRunTests::test_working_dir_passed_through tests/unit/test_plugins/test_go_test_runner.py::TestGoTestRunTests::test_env_vars_passed_through` 3 passed；pytest runner full 45 passed；go runner full 34 passed；release quality docs contract full 342 passed；targeted ruff passed | 无新增 warning；Pytest/Go run_tests helper 现在由 release-quality 契约确认：完整 argv、`cwd/stdout/stderr/env` kwargs 等值、env 覆盖/None 分支、`process.communicate()` await 均不能退回字段集合守门 | 测试 review 发现实现层已改成 `_assert_spawn_call` direct equality，但防回退契约仍在要求旧的 `assert set(call_args.kwargs)` 字符串；这会让质量门禁把弱断言当成目标形态。现修正为 helper/direct equality 契约，避免 Pytest/Go runner exact 测试被契约拉回“kwargs key 集合看起来对” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_auth_results_artifacts.py::test_audit_events_api_token_requires_audit_read_scope_without_self_audit_on_denial tests/integration/test_real_auth_results_artifacts.py::test_auth_refresh_logout_audits_do_not_store_tokens_or_passwords tests/integration/test_real_auth_results_artifacts.py::test_refresh_survives_refresh_token_revoke_outage --collect-only` 3 tests collected | targeted docs contract 3 passed；targeted ruff passed | 无新增 warning；real auth API token scope denial exact body 契约保留 `{"detail": "Insufficient permissions"}`、no self-audit、no-S3 get/presign 与 release quality docs contract full 256 passed 证据；真实 auth/audit 集成用例现在把 audit-events self-audit after_state、auth refresh/logout 审计 action 次数与 jti before/after_state、refresh token revoke Redis kwargs 都改为完整 payload/Counter/kwargs 投影，避免重复审计行、额外字段或值漂移被字段集合断言掩盖 | 测试 review 复扫发现这些路径仍有 `assert set(dict)` 型守门；它们只能证明键名集合，不能发现重复 audit 行被 dict 覆盖、after_state 键值脱节或 Redis revoke 参数夹带；scope denial 旧写法也只证明“拒绝了且没碰 S3/没写自审计”。现升级为 auth audit direct projection 契约，避免认证/审计安全测试只证明“字段集合看起来对” |
| 2026-05-31 | N/A（Python/Go runner subprocess kwargs direct helper follow-up） | `tests/unit/test_plugins/test_pytest_runner.py::TestPytestRunTests::test_env_vars_passed_through tests/unit/test_plugins/test_pytest_runner.py::TestPytestRunTests::test_working_dir_passed_through tests/unit/test_plugins/test_go_test_runner.py::TestGoTestRunTests::test_env_vars_passed_through` 3 passed；targeted docs contract 2 passed；targeted ruff passed | 无新增 warning；Pytest/Go runner run_tests 现在和 Jest/Playwright 一样经 `_assert_spawn_call` 固定完整 argv 与 `{cwd,stdout,stderr,env}` kwargs 等值，env 覆盖/None 分支只检查业务差异 | 测试 review 复扫发现台账虽已标记 subprocess exact，但 Pytest/Go 实现仍保留 `assert set(call_args.kwargs)`；如果未来 subprocess kwargs 夹带参数或 helper 退回字段集合守门，旧契约信号偏弱。现补齐 direct helper 防回退，避免 runner 测试只证明“kwargs key 集合看起来对” |
| 2026-05-31 | N/A（JS runner subprocess kwargs direct helper 契约） | `tests/unit/test_plugins/test_jest_runner.py::TestJestRunTests tests/unit/test_plugins/test_playwright_runner.py::TestPlaywrightRunTests` 15 passed；jest/playwright runner full 57 passed；release quality docs contract full 341 passed；targeted ruff passed | 无新增 warning；Jest/Playwright run_tests 现在统一通过 `_assert_spawn_call` 固定完整 argv，并用 `call_args.kwargs == {"cwd","stdout","stderr","env"}` 直接等值阻止额外 subprocess kwargs 混入，env 断言只保留 runner 专属 JUnit path 与用户 env 覆盖值 | 测试 review 发现台账此前已声称 JS runner subprocess exact，但实现仍在多处使用 `assert set(call_args.kwargs)` 加局部字段抽查；如果 subprocess 夹带额外 kwargs、helper 退化为字段集合守门，旧测试失败信号偏弱。现升级为 direct helper 契约，避免 JS runner exact 测试只证明“kwargs key 集合看起来对” |
| 2026-05-31 | N/A（RunExecutor artifact row direct projection 契约） | `tests/unit/test_engine/test_executor.py::TestUploadArtifacts::test_upload_writes_artifact_rows_with_inferred_type_and_mime tests/unit/test_engine/test_executor.py::TestUploadArtifacts::test_upload_recurses_allure_report_directories` 2 passed；executor full 42 passed；release quality docs contract full 340 passed；targeted ruff passed | 无新增 warning；artifact upload 现在把普通结果文件和 Allure 目录的 S3 upload 顺序、Body 相对路径、artifact row 完整 `{run_id,type,name,storage_path,size_bytes,mime_type}` 列表直接等值，XML MIME 保留跨平台允许值 | 测试 review 发现 artifact 上传用例仍靠 `set(recorded)` 和局部字段抽查；如果 artifact row 顺序、type、size、mime、storage_path 与 S3 key 脱节，或 Allure 子文件只名字集合对了，旧测试失败信号偏弱。现升级为 artifact row direct projection 契约，避免 artifact 上传测试只证明“名字集合对且几项字段看起来对” |
| 2026-05-31 | N/A（RunExecutor failed_tests cap direct list projection 契约） | `tests/unit/test_engine/test_executor.py::TestExecutorCommitsAfterStateTransitions::test_execute_caps_failed_test_names_in_summary` 1 passed；targeted docs contract passed；targeted ruff passed | 无新增 warning；executor 失败用例截断 summary 现在直接等值前 20 条 `failed_tests` 完整 `{suite,name,status}` 列表，并继续固定 `failed_tests_omitted == 5` | 测试 review 复扫发现该用例仍用 `len(summary["failed_tests"]) == 20` 加首尾抽查；如果中间失败用例顺序、名称或状态漂移，旧测试可能仍绿。现升级为 failed_tests cap direct list projection 契约，避免通知 summary 截断测试只证明“数量对且首尾看起来对” |
| 2026-05-31 | N/A（OpenTelemetry FastAPI kwargs direct dict projection 契约） | `tests/unit/test_observability/test_tracing.py::test_instrument_fastapi_app_once` 1 passed；tracing full 8 passed；release quality docs contract full 339 passed；targeted ruff passed | 无新增 warning；FastAPI tracing instrumentation 现在先用 identity 锁住 `tracer_provider`，再把其余 kwargs 作为完整 dict 等值断言，继续固定 excluded_urls、server_request_hook、request header capture/sanitize 配置和只装配一次 | 测试 review 发现 instrumentation 用例仍靠 `assert set(fastapi_kwargs)` 加逐字段抽查；如果额外 kwargs 被夹带、header capture 默认被打开或 sanitize 列表漂移，旧写法失败信号偏弱且容易退回字段集合守门。现升级为 kwargs direct dict projection 契约，避免 OpenTelemetry FastAPI 测试只证明“kwargs 字段集合看起来对” |
| 2026-05-31 | N/A（Run cancel status event direct payload/timestamp 契约） | `tests/unit/test_api/test_runs.py::test_cancel_run` 1 passed；runs full 51 passed；release quality docs contract full 339 passed；targeted ruff passed | 无新增 warning；run cancel 成功路径现在先把 status event timestamp 解析为 timezone-aware UTC，再用完整 event dict 等值锁住 run_id/status/previous/timestamp，并继续证明 status hash 复用同一 timestamp | 测试 review 发现该用例虽已有完整 event dict，但仍保留 `assert set(event)` 字段集合守门；如果后续维护退回字段集合、timestamp 变成本地/naive 时间，或失败信息只停留在“字段集合对”，旧写法会制造 Redis event 已精确覆盖的错觉。现升级为 cancel status event direct payload/timestamp 契约，避免取消事件测试只证明“字段集合看起来对” |
| 2026-05-31 | N/A（RunExecutor setup mount direct projection 契约） | `tests/unit/test_engine/test_executor.py::TestRunSetupContainerised::test_setup_runs_via_backend_not_subprocess tests/unit/test_engine/test_executor.py::TestRunSetupContainerised::test_setup_timeout_capped_at_600s tests/unit/test_engine/test_executor.py::TestRunSetupContainerised::test_setup_uses_pipeline_timeout_when_below_cap tests/unit/test_engine/test_executor.py::TestRunSetupContainerised::test_setup_nonzero_exit_raises tests/unit/test_engine/test_executor.py::TestRunSetupContainerised::test_setup_timeout_raises` 5 passed；release quality docs contract full 339 passed；targeted ruff passed | 无新增 warning；setup success/failure container specs 现在把 workspace mount 投影为唯一 `[{source,target,read_only}]`，继续锁住 image/command/env/resource_limits/network/security/user/labels 与 lifecycle/异常语义 | 测试 review 发现既有 exact spec 契约仍靠 `len(spec.mounts) == 1` 与 `spec.mounts[0]` 分散断言；如果额外挂载夹带、mount 顺序/字段漂移或未来维护删掉其中一条索引断言，旧写法失败信号偏弱。现升级为 setup mount direct projection 契约，避免 setup sandbox 测试只证明“有一个挂载且索引 0 看起来对” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_infra/test_soft_delete.py --collect-only` 7 tests collected | targeted docs contract passed；targeted ruff/py_compile passed | 无新增 warning；soft-delete infra 的 repository list 删除后可见性现在直接断言 `items == []` 与 `total == 0`，不再用 `len(items) == 0` 间接证明空页 | 测试 review 复扫发现基础 soft-delete 集成用例仍有数量型空列表断言；如果 repository 返回特殊 list-like、未来维护只保留数量检查或失败时需要定位返回对象，旧断言信号偏弱。现升级为 soft-delete infra exact empty list 契约，避免软删除基础设施测试只证明“数量为 0” |
| 2026-05-31 | N/A（Cancel watcher handler exception direct error projection 契约） | `tests/unit/test_engine/test_cancel.py::TestWatchForCancel::test_swallows_handler_exceptions` 1 passed；release quality docs contract full 339 passed；targeted ruff passed | 无新增 warning；cancel watcher handler 异常路径现在把唯一 ERROR 投影为 `{levelno,message,exc_type,exc_message}`，继续证明第一条 cancel 异常后 task 不退出、第二条 cancel 仍触发 callback、pubsub 正常 unsubscribe/aclose | 测试 review 发现台账虽写着 exact continuation/log，但测试和契约仍靠 `len(error_records) == 1` 与 `error_records[0]`；如果重复刷 ERROR、exc_info 丢失或日志字段漂移，旧写法失败定位弱且容易退回数量证明。现升级为 direct error projection 契约，避免取消 watcher 异常测试只证明“有一条错误日志看起来对” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_worker_execute.py --collect-only` 8 tests collected | targeted docs contract passed；targeted ruff/py_compile passed | 无新增 warning；external-stack worker 10-container SLO 现在把触发出的 run id 保留为列表，显式断言重复 id 列表为空，再用 `run_id_set` 等待运行容器并要求 `running_count == run_count`，终态状态继续精确等于 `{run_id: "done"}` | 测试 review 复扫发现该 smoke 仍用 `len(run_ids) == run_count` 和 `running_count >= run_count`；如果 API 重复返回 run id、容器计数意外超过目标或 set 去重掩盖重复触发，旧测试失败信号偏弱。现升级为 worker 10-container run-id/running direct projection 契约，避免并发 worker SLO 只证明“数量至少够了” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 RUN_PERFORMANCE_TESTS=1 tests/integration/test_performance_smoke.py --collect-only` 31 tests collected | targeted docs contract passed；targeted ruff/py_compile passed | 无新增 warning；performance smoke 的 waiting dequeue 现在固定完整 row projection、ARQ 调用序列与 priority/created_at 顺序，priority backlog run row/audit 改为按 run_id 的完整投影等值，SSE ticket audit 改为完整投影列表等值且继续证明 ticket/token 不入审计 | 测试 review 复扫发现这些性能 smoke 仍有 `assert set(rows_by_id) == set(waiting_runs)` / `len(our_calls) == len(waiting_runs)` / `len(audits) == ...`；如果入队 job 参数漂移、priority 顺序错、audit 多写/漏写或 SSE ticket audit 字段泄露，只看数量再局部抽查失败信号偏弱。现升级为 performance smoke dequeue/audit direct projection 契约，避免性能门禁只证明“数量看起来对且速度够快” |
| 2026-05-31 | N/A（Run status event direct payload/log projection 契约） | `tests/unit/test_engine/test_events.py` 4 passed；release quality docs contract full 336 passed；targeted ruff passed | 无新增 warning；status event 主路径、no-previous 和 Redis failure 分支现在把 xadd event 直接等值为完整 payload dict，Redis failure warning 投影为唯一 `{levelno,message,exc_type,exc_message}` | 测试 review 发现台账已声称 exact stream/hash，但测试和防回退契约仍保留 `assert set(event)` 与 `len(records)` / `records[0]`；如果 payload 夹带字段、日志重复或异常上下文丢失，旧写法容易继续绿或失败定位弱。现升级为 direct payload/log projection 契约，避免状态事件测试只证明“字段集合和 warning 数量看起来对” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_cancel_e2e.py::test_cancel_at_stage_boundary_skips_next_stage tests/integration/test_cancel_e2e.py::test_cancel_recovered_when_published_before_subscribe --collect-only` 2 tests collected | targeted docs contract passed；targeted ruff/py_compile passed | 无新增 warning；cancel E2E stage-boundary 现在把所有 `create_execution` await args 投影为唯一 `{"labels": {"run_id", "stage"}, "command"}` 列表，直接固定只启动 stage-1 且命令为 `sleep 1`；pre-subscribe 分支继续 `assert_not_awaited()` | 测试 review 复扫发现 stage-boundary cancel 虽已锁住 stage-1 spec，但源码仍用 `len(create_execution_spy.await_args_list) == 1` 加最后一次 await args；如果多启动一次 stage-2 或 await args 历史被误读，失败信息仍偏数量化。现升级为 cancel E2E create_execution direct projection 契约，避免取消竞态测试只证明“调用次数看起来对” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_repository_matrix.py --collect-only` 9 tests collected | targeted docs contract passed；targeted ruff/py_compile passed | 无新增 warning；real repository matrix 的 tenant user list 与 project run list 不再用 `len(users) == 1` / `len(project_runs) == 1` 证明唯一项，改为完整 id/tenant/username/email 与 run scope 投影 | 测试 review 发现这些真实 PG repository 用例已固定 total，但仍用数量加局部字段证明分页结果；如果分页返回错用户、混入额外行或 project run 作用域漂移，旧失败信息弱。现升级为 repository matrix direct projection 契约，避免真实 repository 列表测试只证明“数量看起来对” |
| 2026-05-31 | N/A（Audit events list exact pagination body 契约） | `tests/unit/test_api/test_audit.py::test_audit_events_list_applies_filters_pagination_and_self_audits_summary` 1 passed；release quality docs contract full 335 passed；targeted ruff passed | 无新增 warning；audit-events list 单测现在固定完整分页 body：`data`、`page=3`、`per_page=2`、`total=7` 与完整 AuditEventResponse item 必须整体等值，继续锁住 filter repository kwargs 与 self-audit summary | 测试 review 发现该用例虽然已补到完整 AuditEventResponse 字段集合，但顶层 pagination 仍是 `body["page"]` / `body["per_page"]` / `body["total"]` 分散断言，且 `body["data"]` 单独比较；如果响应夹带额外顶层字段、pagination envelope 漏字段，或字段集合守门退回局部 set 检查，旧测试仍可能绿。现升级为 audit-events list exact pagination body 契约，避免审计列表测试只证明“data 和几个分页数字分别看起来对” |
| 2026-05-31 | N/A（Project list redundant field-set guard removed） | `tests/unit/test_api/test_projects.py::test_list_projects` 1 passed；release quality docs contract full 335 passed；targeted ruff passed | 无新增 warning；project list 单测已用完整 `body == {...}` 固定分页 body、完整 ProjectResponse item、tenant filter、offset/limit 和 name order，删除重复的 `assert set(body["data"][0]) == {...}` 字段集合守门 | 测试 review 发现该用例在完整 body 等值之后仍保留字段集合断言；它不能发现完整 body 已发现不了的问题，反而容易让列表测试继续堆“字段集合覆盖率”噪音。现升级为 project list no redundant field-set guard 契约，避免项目列表测试同时保留严格 body 与重复 set 守门 |
| 2026-05-31 | N/A（API token list exact pagination body 契约） | `tests/unit/test_auth/test_auth_routes.py::TestTokenRoutes::test_list_tokens` 1 passed；release quality docs contract full 335 passed；targeted ruff passed | 无新增 warning；API token list 单测现在固定完整分页 body：`data`、`page=2`、`per_page=1`、`total=1` 与完整 API token item 必须整体等值，继续精确排除 token/secret_hash 并保留 revoked token 仍出现在列表中的语义 | 测试 review 发现该用例虽然已补到完整 token metadata，但顶层 pagination 仍是 `body["page"]` / `body["per_page"]` / `body["total"]` 分散断言，`data` 单独比较，且字段集合用 `set(body["data"][0])` 另做守门；如果响应夹带额外顶层字段、pagination envelope 漏字段，或字段集合检查退回局部 token_id/name/is_revoked 抽查，旧测试仍可能绿。现升级为 API token list exact pagination body 契约，避免 token 列表测试只证明“data 和几个分页数字分别看起来对且没看到 secret” |
| 2026-05-31 | N/A（Pipeline list redundant field-set guard removed） | `tests/unit/test_api/test_pipelines.py::test_list_pipelines_returns_paginated_response` 1 passed；release quality docs contract full 335 passed；targeted ruff passed | 无新增 warning；pipeline list 单测已用完整 `data == {...}` 固定分页 body、完整 PipelineResponse item、嵌套 stages/selector/trigger_config/collectors/retry_policy 与 created_at/updated_at，删除重复的 `assert set(data["data"][0]) == {...}` 字段集合守门 | 测试 review 发现该用例在完整 body 等值之后仍保留字段集合断言；它不能发现完整 body 已发现不了的问题，反而容易把测试维护引向“为了覆盖字段再测一次”。现升级为 pipeline list no redundant field-set guard 契约，避免列表测试同时保留严格 body 与重复 set 守门 |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_auth_audit_failure_paths.py --collect-only` 12 tests collected | targeted docs contract passed；targeted ruff/py_compile passed | 无新增 warning；auth audit outage helper 现在把同 action 的 WARNING 投影为唯一 `{message,exc_type,exc_message}`，register/refresh strict rate-limit Redis IP bucket 用单元素解包固定唯一 key，refresh cookie rotation 用 6 个 token 的顺序解包和集合等值证明每次都轮换且原始 cookie 不进 Redis key | 测试 review 发现这些真实 auth 集成用例虽然已覆盖真实 Redis/PG/audit，但仍用 `len(records) == 1`、`len(keys) == 1` 和 `len(seen_refresh_tokens) == 6` 证明数量；如果同 action 额外打一条 warning、bucket 泄漏 key 变成多条、或 refresh cookie 重复轮换但集合数量检查难定位，旧测试失败信息弱。现升级为 auth rate-limit/audit direct projection 契约，避免认证安全测试只证明“数量看起来对” |
| 2026-05-31 | N/A（Schedule list exact pagination body 契约） | `tests/unit/test_api/test_schedules.py::test_list_schedules_uses_project_access_and_pagination` 1 passed；release quality docs contract full 335 passed；targeted ruff passed | 无新增 warning；schedule list 单测现在固定完整分页 body：`data`、`page=2`、`per_page=1`、`total=1` 与完整 ScheduleResponse item 必须整体等值，继续锁住 tenant-scoped project lookup 与 `schedule.list_by_project` offset/limit | 测试 review 发现该用例虽然已补到 timezone/missed_fire_policy/quiet_windows/last_run_at/next_run_at/last_error/created_at 与字段集合，但顶层 pagination 仍是 `data["page"]` / `data["per_page"]` / `data["total"]` 分散断言，且 `data["data"]` 单独比较；如果响应夹带额外顶层字段、pagination envelope 漏字段，或字段集合守门退回局部 set 检查，旧测试仍可能绿。现升级为 schedule list exact pagination body 契约，避免调度列表测试只证明“data 和几个分页数字分别看起来对” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 RUN_PERFORMANCE_TESTS=1 tests/integration/test_performance_smoke.py --collect-only` 31 tests collected；`RUN_INTEGRATION_TESTS=1 tests/integration/test_auto_retry_real_db.py --collect-only` 7 tests collected | targeted docs contract passed；targeted ruff/py_compile passed | 无新增 warning；performance smoke 与 auto-retry 剩余 len 断言现在改为直接投影/解包：schedule run 用单元素解包，log stream roundtrip 固定 20 条完整 `{id,stream,line}`（id 动态但 entry 完整等值），concurrent self-audit 固定 7 条 before/after_state，clone failure source.clone 参数用三元解包锁住 URL/ref/workdir | 测试 review 发现这些用例此前用 `len(...) == N` 证明数量；如果 log stream 返回错行但数量对、self-audit after_state 漂移、source.clone 多传/少传参数，或 schedule 误产出重复 run，旧测试失败信息弱甚至可能漏掉关键错位。现升级为 residual len direct projection 契约，避免剩余集成 smoke 只证明“数量看起来对” |
| 2026-05-31 | N/A（Project members list exact pagination body 契约） | `tests/unit/test_api/test_project_members.py::test_list_project_members_returns_rows` 1 passed；release quality docs contract full 334 passed；targeted ruff passed | 无新增 warning；project members list 单测现在固定完整分页 body：`data`、`page=2`、`per_page=1`、`total=2` 与两个完整 ProjectMemberResponse item 必须整体等值，继续锁住 tenant-scoped project lookup 与 `list_by_project_tenant` offset/limit | 测试 review 发现该用例虽然已补到顺序、created_at 与完整 data，但顶层 pagination 仍是 `body["page"]` / `body["per_page"]` / `body["total"]` 分散断言，且 `data` 单独比较；如果响应夹带额外顶层字段、pagination envelope 漏字段，或维护时退回角色集合/created_at 非空检查，旧测试仍可能绿。现升级为 project members list exact pagination body 契约，避免成员列表测试只证明“data 和几个分页数字分别看起来对” |
| 2026-05-31 | N/A（Credential list exact pagination body 契约） | `tests/unit/test_api/test_credentials.py::test_list_credentials_returns_response_without_plaintext` 1 passed；release quality docs contract full 333 passed；targeted ruff passed | 无新增 warning；credential list 单测现在固定完整分页 body：`data`、`page=2`、`per_page=1`、`total=2` 与两个完整 CredentialResponse item 必须整体等值，字段集合精确排除 value/encrypted_value | 测试 review 发现该用例虽然已补到完整 credential metadata，但顶层 pagination 仍是 `body["page"]` / `body["per_page"]` / `body["total"]` 分散断言，`data` 单独比较，且字段集合用 `frozenset(entry)` 另做守门；如果响应夹带额外顶层字段、pagination envelope 漏字段，或字段集合检查退回局部不泄密断言，旧测试仍可能绿。现升级为 credential list exact pagination body 契约，避免凭据列表测试只证明“data 和几个分页数字分别看起来对且没看到明文” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 RUN_PERFORMANCE_TESTS=1 tests/integration/test_performance_smoke.py --collect-only` 31 tests collected | targeted docs contract passed；targeted ruff/py_compile passed | 无新增 warning；performance enqueue SLO 的 manual trigger、webhook trigger、schedule tick 三条路径现在用 `_expected_execute_run_call` 固定每个 run 的完整 ARQ 调用序列：`("execute_run", str(run_id))`、目标 queue、`run:{id}` job id 与 `_defer_by=0` | 测试 review 发现这三条性能 smoke 已证明 DB row、audit 和 SLO 时间，但队列侧只用 `len(arq.calls) == 10`（schedule 再看 queue 集合）；如果 job id 漂移、args 漏 run id、defer 参数错误，旧测试仍可能绿。现升级为 performance enqueue exact ARQ call sequence 契约，避免入队 SLO 只证明“调用了 10 次队列” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_worker_execute.py --collect-only` 8 tests collected | targeted docs contract passed；targeted ruff/py_compile passed | 无新增 warning；external-stack worker 10-container SLO 现在把终态状态投影为完整 dict：`final_statuses == {run_id: "done" for run_id in run_id_set}`，不再分开断 key 集合与 `all(status == "done")` | 测试 review 发现 10-container smoke 的终态检查虽然能发现非 done，但失败信息只给布尔聚合，且 key/值契约分散；如果维护时删掉 key 集合检查或需要定位哪条 run 漂移，旧写法不够直接。现升级为 worker 10-container exact final statuses 契约，避免并发 worker SLO 只证明“所有状态看起来都是 done” |
| 2026-05-31 | N/A（Environment list exact pagination body 契约） | `tests/unit/test_api/test_environments.py::test_list_environments_uses_pagination_and_decrypts_items` 1 passed；release quality docs contract full 332 passed；targeted ruff passed | 无新增 warning；environment list 单测现在固定完整分页 body：`data`、`page=3`、`per_page=5`、`total=11` 与完整 EnvironmentResponse item 必须整体等值，继续证明 env_vars 解密后返回且 audit 无副作用 | 测试 review 发现该用例虽然已补到 `setup_script`/`created_at` 与字段集合，但顶层 pagination 仍是 `body["page"]` / `body["per_page"]` / `body["total"]` 分散断言，且 `data` 单独比较；如果响应夹带额外顶层字段、pagination envelope 漏字段，或字段集合守门退回局部 set 检查，旧测试仍可能绿。现升级为 environment list exact pagination body 契约，避免环境列表测试只证明“data 和几个分页数字分别看起来对” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 RUN_PERFORMANCE_TESTS=1 tests/integration/test_performance_smoke.py --collect-only` 31 tests collected | targeted docs contract passed；targeted ruff/py_compile passed | 无新增 warning；performance smoke 的 archived log 大页、artifact list、large artifact page、audit events 两个分页、concurrent run.read archived logs、artifact list 与 artifact download URL 都改为直接投影：日志字段集合、artifact/audit DTO 字段集合、并发 artifact list 完整 ArtifactResponse item、S3 presign 23 次与并发 7 次完整 `{method,params,expires_in}` 调用序列都必须精确匹配 | 测试 review 发现这些性能门禁虽然已有完整响应窗口和 SLO 采样，但仍用多处 `assert all(...)` / `assert set(artifacts_by_id)` 证明字段或 presign 子字段；如果预签名调用顺序/TTL/Key 漂移、字段集合检查退化，或并发读 artifact list 只剩 id 集合对，旧失败只给布尔值，且容易被看成“只为覆盖率”。现升级为 performance smoke direct projection 契约，避免性能 smoke 只证明“跑得快且大概有字段” |
| 2026-05-31 | N/A（Notification rule list exact pagination body 契约） | `tests/unit/test_api/test_notifications.py::test_list_rules_uses_project_access_pagination_and_returns_rules` 1 passed；release quality docs contract full 330 passed；targeted ruff passed | 无新增 warning；notification rule list 单测现在固定完整分页 body：`data`、`page=2`、`per_page=1`、`total=3` 与完整 NotificationRuleResponse item 必须整体等值 | 测试 review 发现该用例虽然已经补到完整 item 字段集合，但顶层 pagination 仍是 `body["page"]` / `body["per_page"]` / `body["total"]` 分散断言，且 `data` 单独比较；如果响应夹带额外顶层字段、pagination envelope 漏字段，或字段集合守门退回局部 set 检查，旧测试仍可能绿。现升级为 notification rule list exact pagination body 契约，避免列表测试只证明“data 和几个分页数字分别看起来对” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_auth_results_artifacts.py --collect-only` 30 tests collected | targeted docs contract passed；targeted ruff/py_compile passed | 无新增 warning；real auth results/artifacts/logs 三处字段集合校验不再藏在 `assert all(...)` generator 里：archived logs 大分页投影为 100 个 `{"stream","line"}` 集合，artifact page two 投影为两组完整 ArtifactResponse 字段，rollback 后 results 投影为两组完整 TestResultResponse 字段 | 测试 review 发现这些真实 API 用例主体已经在断完整响应体，但字段集合守门仍用 `assert all(set(item) == ...)`；后续维护时容易退化成只证明“每项大概都有字段”，失败定位也只剩一个布尔值。现升级为 real auth field-set direct projection 契约，避免字段集合检查为了覆盖率而存在 |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 QAP_TEST_OOM=1 tests/integration/test_oom_e2e.py --collect-only` 3 tests collected | targeted docs contract passed；targeted ruff/py_compile passed | 无新增 warning；OOM opt-in e2e 现在把 RunExecutor 日志固定为精确单次行：`Starting stage: oom`、`Pipeline failed with code {exit_code}`、一条 `Resource termination` 字段日志投影，以及 `Run completed: timeout`；termination reason/exit_code/duration_ms/oom_killed/timed_out 必须与 DB summary 对齐，且投影列表必须精确等于单元素期望 | 测试 review 发现 OOM 真实 Docker 用例已固定 DB summary 与 Redis timeout 事件，但日志曾用 `assert any(... in line ...)` 查三个片段，随后仍用 `len(termination_lines) == 1` 加索引检查资源终止日志；如果 OOM 日志重复、pipeline failure 行缺失、termination 字段漂移，或 timeout terminal 行拼接额外上下文，旧测试失败信息弱。现升级为 OOM e2e exact resource termination log direct projection 契约，避免 release-candidate OOM 证据只证明“状态对且日志里有几个关键词” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_worker_execute.py --collect-only` 8 tests collected | release quality docs contract full 328 passed；targeted ruff/py_compile passed | 无新增 warning；external-stack setup failure archived logs 现在把管理员 JWT 与 `run.read` token 读面都固定为三条精确日志行：`Repository cloned successfully`、`Running setup script...`、`setup-boundary-failure`，每条必须只出现一次 | 测试 review 发现 setup failure 黑盒用例已固定 run detail exact error，但归档日志仍用 `any(... in line ...)` 片段匹配；如果 setup 日志重复、stderr marker 被相似 debug 行替代，或 `run.read` token 读面漏掉 clone/setup/stderr 关键行，只要片段出现旧测试仍可能绿。现升级为 external-stack setup failure archive exact line/read-scope 契约，避免 setup failure 测试只证明“归档里大概出现过三段文本” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_worker_execute.py --collect-only` 8 tests collected | targeted docs contract passed；targeted ruff/py_compile passed | 无新增 warning；worker_lost retry 与 priority queue external-stack 用例现在把管理员 JWT 和 `run.read` token 的归档日志都固定为精确三行：`Repository cloned successfully`、`Uploaded artifact: junit.xml`、`Run completed: done`，每条都只能出现一次 | 测试 review 发现这两个黑盒用例此前只用 `any(... in line ...)` 确认归档日志片段；如果 retry/priority run 的归档日志重复刷上传、缺少 run.read token 的关键行，或用相似 debug 行替代 done/clone 语义，只要片段出现旧测试仍可能绿。现升级为 worker retry/priority archive exact line 契约，避免 worker_lost 与 priority queue 测试只证明“归档里有三个熟悉短语” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_worker_execute.py --collect-only` 8 tests collected | targeted docs contract passed；targeted ruff/py_compile passed | 无新增 warning；external-stack worker persist artifacts/archive 用例现在把管理员 JWT 与 `run.read` token 的归档日志读面都固定为精确行：clone 成功、stdout/stderr/bulk secret 脱敏、四个 artifact upload、超额 artifact skip、run done 均按预期出现且只出现一次 | 测试 review 发现该黑盒用例此前虽证明 artifact 名称集合、MinIO 超额对象不存在和 secret 不泄露，但归档日志仍用 `any(... in line ...)` 片段匹配；如果 upload 日志重复、skip reason 拼错、token 读面缺失脱敏行或 done 行被相似 debug 文本替代，只要片段还在旧测试仍可能绿。现升级为 external-stack worker archive exact line/read-scope 契约，避免真实 worker 归档读面测试只证明“几个关键片段大概出现过” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 .venv/bin/python -m pytest tests/integration/test_real_auth_results_artifacts.py::test_run_read_token_replays_logs_and_presigns_artifact_without_extra_s3_reads tests/integration/test_real_auth_results_artifacts.py::test_artifact_download_url_uses_real_auth_rbac_and_db_row -q` 2 passed | release quality docs contract full 325 passed；targeted ruff passed | 无新增 warning；real auth artifact list 读面现在在 run.read API token 组合链路与 JWT artifact download 链路中都固定完整 ArtifactResponse 分页体：id/run_id/type/name/storage_path/size_bytes/mime_type/expires_at/created_at/page/per_page/total 全部等值，并继续证明 artifact list 不触发 S3 read/presign、download 才 presign | 测试 review 发现这两条真实 RBAC/PostgreSQL artifact 用例此前只断言 `total`、`data[0].id` 和 `data[0].storage_path`；如果 artifact list 漏 type/name/size/mime/created_at、分页壳漂移、run_id 串错，或 response 夹带额外字段，只要 id 与 storage_path 还对旧测试仍可能绿。现升级为 real auth artifact list exact response 契约，避免真实 artifact 读面测试只证明“能看到某个 artifact 的 id 和路径” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 .venv/bin/python -m pytest tests/integration/test_worker_resource_termination.py::test_executor_resource_termination_persists_timeout_status_and_redis_event -q` 1 passed | targeted docs contract passed；targeted ruff/py_compile passed | 无新增 warning；worker resource termination 集成用例现在把 RunExecutor 日志固定为字段级契约：stage start 与 pipeline failed 精确行各一次，`Resource termination` 字段日志投影必须精确等于单元素期望且 reason/exit_code/duration_ms/oom_killed/timed_out 与 summary 完全一致，terminal timeout 精确行各一次 | 测试 review 发现该真实 DB/Redis 用例此前只用 `any(... in line ...)` 证明日志片段存在，后来仍用 `len(termination_lines) == 1` 加索引检查资源终止日志；如果 termination 日志重复、reason 正确但 exit_code/duration/bool 字段漂移、stderr failure 行缺失，或 terminal 行拼接了额外上下文，旧测试失败信息弱。现升级为 worker resource termination exact log fields direct projection 契约，避免资源终止测试只证明“DB summary 对了且日志里有几个关键词” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_worker_execute.py --collect-only` 8 tests collected | targeted docs contract passed；targeted ruff/py_compile passed | 无新增 warning；external-stack pytest package worker smoke 现在把 archived logs 的 `Starting stage: test` 与 `Run completed: done` 固定为精确日志行且各出现一次，继续保留真实 run done、passed result、JUnit artifact 与下载内容证据 | 测试 review 发现该基础真实 worker smoke 此前用 `assert any(... in line ...)` 查归档日志；如果归档重复写 terminal 行、阶段名变成相似 debug 文本、或日志行拼上额外上下文，只要片段还在旧测试仍可能绿。现升级为 external-stack pytest package archive exact line 契约，避免真实 worker smoke 只证明“归档里有两个看起来像关键字的片段” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_worker_execute.py --collect-only` 8 tests collected | targeted docs contract passed；targeted ruff/py_compile passed | 无新增 warning；external-stack worker SLO smoke 现在要求首个 live SSE 中 `Starting stage: pytest` 与 `worker-e2e-first-log-*` 都是精确日志行，归档 readiness 等待 `done_marker` 与 `Run completed: done` 的精确行集合，归档断言再固定 first/done/completed 三条关键日志各出现一次 | 测试 review 发现该 performance 黑盒此前用 `any(... in line ...)` 同时做等待和断言；如果 done marker 被相似 debug 行替代、归档重复写关键行、live SSE 只含相似阶段文本，或 archived logs 缺 first marker，只要片段出现旧测试仍可能绿。现升级为 external-stack worker SLO exact marker/archive sequence 契约，避免 worker SLO 测试只证明“关键日志片段大概出现过” |
| 2026-05-31 | N/A（P3 analytics custom pagination exact empty response 契约） | `tests/unit/test_api/test_p3.py::TestAnalytics::test_trends_with_custom_pagination tests/unit/test_api/test_p3.py::TestAnalytics::test_flaky_with_offset` 2 passed；P3 full 60 passed；release quality docs contract full 322 passed；targeted ruff passed | 无新增 warning；P3 analytics trends/flaky 自定义分页单测现在固定空页完整响应体：`data=[]` 与 pagination offset/limit/total 全部等值，并锁住 repository kwargs 精确字段集合与默认 `days=30` cutoff 时间窗口 | 测试 review 发现这两条用例此前只抽查 `body["pagination"]` 三个子字段和 offset/limit kwargs；如果响应夹带 data、pagination 壳漂移、漏 total，或 repository kwargs 多传/漏传 project_id/min_runs/cutoff，只要三个分页数字还对旧测试仍可能绿。现升级为 P3 analytics custom pagination exact empty response/kwargs 契约，避免自定义分页测试只证明“pagination 子字段看起来对” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_worker_execute.py --collect-only` 8 tests collected | targeted docs contract passed；targeted ruff/py_compile passed | 无新增 warning；external-stack live SSE worker 用例现在要求 `Starting stage: pytest` 为精确日志行，start marker 在首段 SSE 中用 `[_sse_log_line(event) ...] == [start_marker]` 固定精确出现一次并单元素解包 marker event，resume 后 start marker 精确不重放，end marker 精确出现一次；归档日志中 start/end/`Run completed: done` 也分别精确出现一次 | 测试 review 发现该黑盒用例此前通过 `any(...)`/`all(...)` 做片段匹配，后来仍用 `len(first_marker_events) == 1` 加 `[0]` 证明首段 marker 唯一；如果 SSE resume 重放旧 marker、marker 重复出现、end marker 被相似行替代，或归档里重复/误拼关键日志，旧测试仍可能绿且失败定位弱。现升级为 external-stack worker live SSE exact marker sequence direct projection 契约，避免真实 worker SSE 测试只证明“日志里大概出现过这些片段” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 .venv/bin/python -m pytest tests/integration/test_real_auth_results_artifacts.py::test_artifact_upload_enforces_limits_before_real_db_rows -q` 1 passed | targeted docs contract passed；targeted ruff passed | 无新增 warning；artifact upload limit 集成用例现在固定真实 S3 object dict 与四条 `write_log` 序列：上传 a.txt、stderr 跳过超 size 的 b.txt、上传 c.txt、stderr 跳过超 count 的 d.txt，run_id/line/stream 全部等值 | 测试 review 发现该真实 DB 用例此前已能证明只落 a/c 两条 artifact，但日志仍用 `assert any(...)` 找片段；如果 b/d 顺序漂移、跳过原因串错、超限日志写到 stdout、或成功上传日志缺失，只要某个片段还出现旧测试仍可能绿。现升级为 artifact upload limit exact log/S3 sequence 契约，避免 artifact limit 集成测试只证明“DB/S3 大致没写超限文件且日志里有几个词” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 .venv/bin/python -m pytest tests/integration/test_api_endpoints.py::TestAnalytics::test_get_trends tests/integration/test_api_endpoints.py::TestAnalytics::test_get_flaky -q` 2 passed | release quality docs contract full 318 passed；targeted ruff passed | 无新增 warning；Analytics trends/flaky 真实 API 集成用例现在固定完整 response body：趋势的 data 与 `pagination.offset/limit/total`、flaky 的 data 与默认 `limit=50` 全部等值，并继续证明 soft-deleted run/test result 不污染聚合 | 测试 review 发现这两条聚合集成此前只对 trends 的 pagination 三字段分散断言、flaky 只看 `pagination.total` 与 data；如果 pagination 壳多字段/漏字段、flaky 默认 limit 漂移、或响应 envelope 夹带调试字段，旧测试仍可能绿。现升级为 analytics trends/flaky integration exact response 契约，避免聚合集成测试只证明“聚合 data 看起来对” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_worker_execute.py --collect-only` 8 tests collected | targeted docs contract passed；targeted ruff/py_compile passed | 无新增 warning；external-stack clone/setup failure no-retry helper 现在把轮询到的 retry attempt 异常投影为 `unexpected_attempts == []`，失败信息同时给出 unexpected attempts 与完整 observed attempts | 测试 review 发现 `_assert_project_has_no_retry_runs` 此前用 `assert all(attempt == 1 for attempt in attempts)`；如果失败路径误创建 attempt=2 retry run，断言只给布尔结果，排查时还要从轮询历史里手动找异常值。现升级为 direct unexpected-attempt list 契约，避免 worker 失败不重试测试只证明“所有 attempt 看起来都是 1” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 uv run pytest tests/integration/test_audit_events_api.py::test_audit_events_tenant_isolation_and_cross_tenant_ids_return_404 -q` 1 passed | targeted docs contract passed；targeted ruff passed | 无新增 warning；audit-events 跨租户隔离成功列表现在使用唯一 action 固定完整 AuditEventResponse 分页 body：只返回 tenant A 的 event，tenant/user/action/resource/before/after/ip/user_agent/created_at/page/per_page/total 全部等值；跨租户 404 no-leak 也改为显式泄露列表 | 测试 review 发现该安全集成用例的成功列表路径仍只做 id 集合包含/排除，跨租户泄露检查也用 `isdisjoint` 只给布尔结果；如果列表响应字段漏传、分页壳漂移、同 action 的 tenant B 事件混入或 404 文本泄露具体 ID，旧测试不够敏感或失败定位不清。现升级为 audit-events cross-tenant exact list/no-leak 契约，避免安全测试只证明“看到 A 的 id 且没看到 B 的 id” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 .venv/bin/python -m pytest tests/integration/test_api_endpoints.py::TestNotificationRules::test_list_notification_rules_marks_historical_invalid_conditions -q` 1 passed | release quality docs contract full 315 passed；targeted ruff passed | 无新增 warning；Notification rule historical invalid condition 集成用例现在固定完整分页响应：dirty rule 的 id/project/name/enabled/conditions/channels/template/created_at、page/per_page/total 全部等值；invalid condition 的完整 reason、raw_field、raw_operator 与 canonical webhook `config.url` 一起锁住 | 测试 review 发现该历史兼容用例此前只通过 `items_by_id` 抽查 invalid/raw 字段和 reason 子串；如果分页壳、enabled/template/created_at、canonical channel、operator allow-list 文案或 nested invalid path 漂移，只要几个局部字段还在旧测试仍可能绿。现升级为 notification invalid condition integration exact response 契约，避免历史脏数据兼容测试只证明“某条规则里有 invalid 标记” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 uv run pytest tests/integration/test_api_endpoints.py::TestPipelines::test_create_pipeline tests/integration/test_api_endpoints.py::TestPipelines::test_update_pipeline_accepts_current_contract -q` 2 passed | targeted docs contract passed；targeted ruff passed | 无新增 warning；Pipeline create/update 集成 smoke 现在固定完整 PipelineResponse body：id/project_id/name/stages/selector/trigger_config/collectors/timeout/retry/enabled/created_at/updated_at 全部等值，并保留 stage `continue_on_error=False` 与 selector/trigger 默认壳 | 测试 review 发现该用例此前虽已固定 stage/timeout，但仍通过多条局部 `body[...]` 抽查响应；如果响应漏 `project_id`、`created_at/updated_at`、selector 默认字段或 trigger `dedup_window_seconds`，或者夹带额外字段，旧测试仍可能绿。现升级为 Pipeline integration full response 契约，避免 pipeline 正向集成只证明“几个嵌套字段看起来对” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 uv run pytest tests/integration/test_api_endpoints.py::TestNotificationRules::test_list_notification_rules_normalizes_legacy_channel_shape -q` 1 passed | targeted docs contract passed；targeted ruff passed | 无新增 warning；Notification rule legacy channel 集成用例现在把旧 DB JSON `webhook_url` 行的列表响应固定为完整分页 body：id/project_id/name/enabled/conditions/channels/template/created_at 全部等值，并禁止响应 channel 保留旧 `webhook_url` key | 测试 review 发现该兼容用例此前只用 `items_by_id` 抽查 legacy rule 的 channels；如果分页壳、enabled/conditions/template/created_at 漂移，或 API 同时输出 canonical `config.url` 和旧 `webhook_url`，旧测试仍可能绿。现升级为 legacy notification channel exact response 契约，避免历史 JSON 兼容测试只证明“列表里某条规则的 channels 大致归一化了” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 .venv/bin/python -m pytest tests/integration/test_api_endpoints.py::TestNotificationRules::test_create_notification_rule tests/integration/test_api_endpoints.py::TestNotificationRules::test_update_notification_rule -q` 2 passed | release quality docs contract full 314 passed；targeted ruff passed | 无新增 warning；Notification rule create/update 集成 smoke 现在固定完整 NotificationRuleResponse，并用 `_notification_rule_audit_state` 证明 create/update AuditEvent 的 tenant/user/resource/before_state/after_state 完整脱敏；同时校验 DB 行的 project/name/enabled/conditions/channels/template 与响应一致 | 测试 review 发现这两条真实通知规则写入口此前只抽查 name/enabled/channels 局部；如果响应漏 conditions/template/created_at、DB channels 与响应归一化不一致，或 audit 漏 before/after、泄露 channel config，只要几个局部字段仍对旧测试仍可能绿。现升级为 Notification rule integration exact response/audit 契约，避免 notification CRUD smoke 只证明“规则名和启用状态看起来对” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 uv run pytest tests/integration/test_api_endpoints.py::TestCrossTenantIsolation::test_cross_tenant_project_access -q` 1 passed | targeted docs contract passed；targeted ruff passed | 无新增 warning；Project cross-tenant 404 集成用例现在固定 cross-tenant project 与随机 project 的同一 `NOT_FOUND` envelope，并把外部 project/tenant/user ID 与随机探测 ID 的 no-leak 断言升级为显式泄露列表 | 测试 review 发现该用例此前虽然覆盖了 existence oracle，但外部 ID 泄露检查使用 `all(...)` 只给布尔结果；如果响应里泄露某个具体标识，失败信息不指向泄露值或响应面，容易让安全回归测试退化成“看起来没泄露”。现升级为 cross-tenant 404 direct no-leak list 契约，避免跨租户测试只证明“404 body 大致一致” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 .venv/bin/python -m pytest tests/integration/test_api_endpoints.py::TestRuns::test_trigger_run tests/integration/test_api_endpoints.py::TestRuns::test_get_run -q` 2 passed | release quality docs contract full 311 passed；targeted ruff passed | 无新增 warning；Run trigger/get 集成 smoke 现在复用 `_expected_run_response` 固定完整 RunResponse，trigger 分支额外证明 DB run 的 pipeline/environment/status/trigger_type/priority/user/git/retry_group 全部落库，并要求 `run.trigger` AuditEvent 的 tenant/user/resource/before_state/after_state 完整等于响应 | 测试 review 发现这两条真实 Run 读写入口此前只抽查 pipeline/environment/status/git/priority 或 id/pipeline_id；如果响应漏 pipeline_name、attempt、时间字段、summary/error，或 trigger 响应正确但 DB retry_group/audit 漂移，旧测试仍可能绿。现升级为 Run trigger/get integration exact response/audit 契约，避免 Runs CRUD smoke 只证明“创建了 queued run 或能按 id 读到某个 run” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 .venv/bin/python -m pytest tests/integration/test_webhook_branch_dedup.py::test_webhook_terminal_same_commit_can_trigger_again -q` 1 passed | targeted docs contract passed；targeted ruff passed | 无新增 warning；webhook terminal same-commit 集成用例现在固定第一次与第二次 201 的完整 RunResponse，并把 DB 里同一 dedup_key 下的 done/queued 两条 run 投影为 tenant/project/pipeline/environment/status/trigger/git/triggered_by/attempt/dedup_key/retry_group_id 完整等值 | 测试 review 发现该用例此前只抽查两次响应的 git_ref/git_sha/status，并用 `all(...)` 证明两条 DB run 的 git_ref/git_sha/triggered_by；如果第二次响应漏字段、terminal run 的 retry_group/dedup_key 丢失、pipeline/environment 串错，旧测试仍可能绿。现升级为 terminal same-commit exact response/DB projection 契约，避免去重回归测试只证明“同 commit 能再建一条 run” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 .venv/bin/python -m pytest tests/integration/test_api_endpoints.py::TestRuns::test_list_runs_filters_pipeline_git_ref_and_created_range -q` 1 passed | targeted docs contract passed；targeted ruff passed | 无新增 warning；Run list pipeline/git_ref/created range 集成用例现在用 `_expected_run_response` 固定 pipeline_id 过滤、git_ref 过滤和 created_at range 过滤的完整分页响应：RunResponse 字段、pipeline_name、page/per_page/total 与默认 `-created_at` 顺序全部等值 | 测试 review 发现该用例虽然已比旧版本更强，但仍只断言 total、id 顺序和 `all(pipeline_id)`；如果响应漏 `tenant_id/pipeline_name/priority/timestamps`、分页壳漂移、git_ref/range 分支返回字段串错，只要 id 列表仍对旧测试仍可能绿。现升级为完整 RunResponse pagination 契约，避免 run list filter 集成测试只证明“筛出了这些 id” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 .venv/bin/python -m pytest tests/integration/test_api_endpoints.py::TestProjectsCRUD::test_list_projects_search_orders_results_by_name -q` 1 passed | release quality docs contract full 308 passed；targeted ruff passed | 无新增 warning；Project search sort 集成用例现在保存三次 create 的完整 ProjectResponse，并断言搜索响应精确等于 `{"data": sorted(created_projects, key=name), "page": 1, "per_page": 10, "total": 3}` | 测试 review 发现该用例此前只比较返回 name 数组是否排序；如果分页壳、total、ProjectResponse 字段、tenant scope 或额外匹配项漂移，只要名字顺序仍对旧测试仍可能绿。现升级为 Project search exact pagination/sort 契约，避免搜索排序测试只证明“几个名字按字母排了” |
| 2026-05-31 | N/A（Project webhook filtered unit exact response/audit 契约） | `tests/unit/test_api/test_p3.py::TestWebhookTrigger::test_webhook_trigger_filtered_branch_returns_200_without_run` 1 passed；targeted docs contract passed；targeted ruff passed | 无新增 warning；项目 webhook filtered 单测现在固定完整响应 `{"status":"filtered","reason":"branch_not_allowed"}`，证明 pipeline/environment/dedup/run create/retry group 全部短路，并要求 `webhook.filtered` audit after_state 精确等值且不含项目 git_url、攻击者 git_url 或 dedup_key | 测试 review 发现该 unit 用例此前只断言 `resp.json()["status"]` 和部分 audit 字段；如果 filtered 响应夹带内部字段、实现继续查 pipeline/env/dedup，或 audit 泄露 repo URL/dedup key，只要 status 仍是 filtered 旧测试仍可能绿。现升级为 response/audit/side-effect 精确契约，避免项目 webhook filtered 单测只证明“返回了 filtered” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 .venv/bin/python -m pytest tests/integration/test_api_endpoints.py::TestProjectsCRUD::test_get_project tests/integration/test_api_endpoints.py::TestProjectsCRUD::test_update_project -q` 2 passed | release quality docs contract full 306 passed；targeted ruff passed | 无新增 warning；Project get/update 集成 smoke 现在通过 `_expected_seed_project_response` 固定完整 ProjectResponse：tenant/name/slug/git_auth/default_branch/root_path/shallow_clone/settings/silent_windows/status/timestamps 全部等值；update 分支额外证明 DB name 已更新，并要求 `project.update` AuditEvent 的 tenant/user/resource/before_state/after_state 完整匹配响应契约 | 测试 review 发现项目详情与普通更新此前只断言 `id/name`；如果详情漏 git 配置、settings、silent_windows、状态或时间字段，或更新接口响应正确但 DB/audit 漂移，旧测试仍可能绿。现升级为 Project get/update integration exact response/audit 契约，避免 Projects CRUD smoke 只证明“能按 id 返回或改掉一个名字” |
| 2026-05-31 | N/A（special-regressions env var UI scoped card 精确契约） | `npx playwright test tests/e2e/special-regressions.spec.ts --project=chromium --list` 7 tests listed；targeted docs contract passed；frontend lint passed | 未启动真实浏览器链路；静态 contract 锁住断言形状；环境变量遮蔽 E2E 现在先创建 decoy environment，再通过具名 environment region 锁定目标卡片，要求目标卡片内唯一 KEY/VALUE 输入分别为 `API_TOKEN` 与目标 secret，并限定 show/hide 只作用于目标卡片 | 测试 review 发现该 E2E 此前用 `page.locator('input[placeholder="KEY"]').first()` / `VALUE.first()` 和全页 secret 文本断言；如果列表排序变化、多环境串行、或 show value 按钮点到其它环境，只要第一个输入框碰巧像目标旧测试仍可能绿。现升级为 scoped card 精确契约，避免环境变量 UI 测试只证明“页面某处第一个输入框看起来对” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 .venv/bin/python -m pytest tests/integration/test_silent_windows.py::test_manual_trigger_ignores_silent_windows tests/integration/test_silent_windows.py::test_webhook_trigger_ignores_silent_windows -q` 2 passed | release quality docs contract full 305 passed；targeted ruff passed | 无新增 warning；silent window manual/webhook bypass 集成用例现在用 `_expected_trigger_run_response` 固定完整 RunResponse：id/tenant/project/pipeline/pipeline_name/environment/status/trigger_type/priority/triggered_by/git/attempt/timestamps/summary/error 全部等值，并继续保留 run count、DB run metadata/retry_group/queue、webhook dedup 与 `run.trigger` audit after_state 证据 | 测试 review 发现这两条真实静默窗口绕过路径此前虽已补到 DB 和 audit，但响应仍只抽查 tenant/project/pipeline/environment/status/trigger/git；如果 RunResponse 漏 `pipeline_name`、`priority`、`attempt`、时间字段或错误字段，只要几个局部字段还在旧测试仍可能绿。现升级为 silent-window bypass exact RunResponse 契约，避免绕过静默窗口测试只证明“创建了某个 queued run 且审计等于这个局部响应” |
| 2026-05-31 | N/A（JWT no-jti pass-through exact no-blacklist 契约） | `tests/unit/test_auth/test_auth_routes.py::TestRevokedTokenMiddleware::test_jti_missing_token_passes_through` 1 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；legacy 无 `jti` access token pass-through 现在固定完整响应体 `{"user_id": user_id}`，并证明不查询 Redis revoked blacklist、不回显原 JWT | 测试 review 发现该用例此前只断言 status 200 与局部 `user_id`；如果实现对无 jti token 仍查 `jwt:revoked:*`、响应夹带 token/debug 字段，或响应壳漂移，只要 `user_id` 还在旧测试仍可能绿。现升级为 no-jti pass-through exact/no-blacklist 契约，避免 revoked-token 兼容测试只证明“legacy token 没被拒绝” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 .venv/bin/python -m pytest tests/integration/test_api_endpoints.py::TestPipelines::test_create_pipeline tests/integration/test_api_endpoints.py::TestPipelines::test_update_pipeline_accepts_current_contract -q` 2 passed | release quality docs contract full 303 passed；targeted ruff passed | 无新增 warning；pipeline create/update 集成 smoke 现在要求响应 `stages` 完整等于规范化 payload stages（含 `continue_on_error=False`），并固定 `timeout_seconds` 回显；继续保留 selector/trigger_config/collectors/retry_policy 的嵌套响应断言 | 测试 review 发现 pipeline 集成正向路径此前只抽查 stage plugin/phase，并用 `startswith("python -m pip")` 接受命令前缀；如果 stage name、command 尾部、config key、第二个 stage、默认 continue_on_error 或 timeout 丢失，只要几个局部字段还在旧测试仍可能绿。现升级为 Pipeline integration stage/timeout exact response 契约，避免 pipeline smoke 只证明“创建/更新接口返回了几个像样的嵌套片段” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 .venv/bin/python -m pytest tests/integration/test_environment_env_vars_encryption.py::test_environment_env_vars_create_fetch_update_are_encrypted_at_rest -q` 1 passed | release quality docs contract full 302 passed；targeted ruff passed | 无新增 warning；真实 Environment env_vars create/fetch/update 加密链路现在固定 create/get/update 三次完整 EnvironmentResponse：id/project/name/base_image/setup/memory/cpu/disk/artifact/network/env_vars/cache/created_at 全部等值，DB 侧继续证明 API_TOKEN/secret 与 NEW_TOKEN/rotated-secret 不落明文 | 测试 review 发现该 required integration 此前只断言响应 `env_vars` 与 DB 不含明文；如果响应壳漏字段、默认资源限制/网络策略漂移、get/update 与 create 返回不一致，或 update 串掉其它字段，只要变量值能读回旧测试仍可能绿。现升级为 Environment env_vars encrypted at rest exact response 契约，避免加密链路测试只证明“密文存储且 env_vars 字段能读回” |
| 2026-05-31 | N/A（Health liveness exact response body 契约） | `tests/unit/test_api/test_health.py::test_health_returns_lightweight_liveness_contract` 1 passed；health full 6 passed；targeted ruff passed；release quality docs contract full 301 passed | 无新增 warning；`/health` liveness 单测现在固定完整响应 body 只能包含 `status/version/uptime`，其中 status 为 `ok`、version 等于 app.version，uptime 继续走两位小数秒字符串校验 | 测试 review 发现 `/health` 此前只逐字段抽查 status/version/uptime；如果响应多出 checks/debug、漏字段，或 liveness 与 readiness 壳混用，只要三项局部还能索引旧测试仍可能绿。现升级为 Health liveness exact response body 契约，避免健康检查测试只证明“几个字段看起来对” |
| 2026-05-31 | N/A（Environment disk limit update exact response body 契约） | `tests/unit/test_api/test_environments.py::test_update_environment_updates_and_clears_disk_limit` 1 passed；environments targeted ruff passed；release quality docs contract full 300 passed | 无新增 warning；environment disk_mb 更新与清除分支现在固定两次完整 EnvironmentResponse：id/project/name/base_image/setup/memory/cpu/disk/artifact/network/env_vars/cache/created_at 均逐项等值，继续保留 tenant-scoped lookup、resource_limits update 和 update audit 证据 | 测试 review 发现该用例虽已锁住 repository 与 audit 关键参数，但响应仍只断言 `resp.json()["disk_mb"]`；如果更新接口漏字段、默认 resource limit 响应漂移、env_vars/cache/created_at 丢失，或清空 disk 后其它字段串错，旧测试仍可能绿。现升级为 Environment disk limit update exact response body 契约，避免资源限制更新测试只证明“disk_mb 局部字段对了” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 .venv/bin/python -m pytest tests/integration/test_audit_events_api.py::test_audit_events_list_access_writes_audit_event -q` 1 passed | release quality docs contract full 335 passed；targeted ruff/py_compile passed | 无新增 warning；audit-events list access 自审计集成用例现在固定默认空列表响应 `{"data":[],"page":1,"per_page":20,"total":0}`，并把写入的 `audit_events.list` 行解包为唯一 `{resource_id,before_state,after_state}` 投影，`resource_id/before_state` 为 None，`after_state` 对 actor/action/resource/time/page/per_page/total 完整等值且不含 data | 测试 review 发现该用例此前只断言 200 与自审计 page/per_page，后来仍用 `len(events) == 1` 证明只写一条；如果 API 返回了自审计前后不一致的分页壳、total 漂移、写了多条审计，或 after_state 丢失 filter/total 字段，旧测试失败信息弱。现升级为 audit-events list access exact response/self-audit direct projection 契约，避免 list access 测试只证明“访问会写自审计” |
| 2026-05-31 | N/A（Notification template update response/audit exact 契约） | `tests/unit/test_api/test_notifications.py::test_update_rule_clears_template_when_null_submitted tests/unit/test_api/test_notifications.py::test_update_rule_keeps_template_when_omitted` 2 passed；notifications targeted ruff passed；targeted docs contract passed | 无新增 warning；notification rule 的 `template: null` 与省略 template 更新分支现在固定完整 NotificationRuleResponse、`NOTIFICATION_EDIT` 权限、tenant project lookup、rule lookup、repo update kwargs，以及 update audit before/after 的 redacted template `{present,length}` 转换 | 测试 review 发现这两条模板语义用例此前只断言 status 200、内存 rule.template 和局部 response template；如果响应壳漏字段、权限/RBAC 漏掉、audit 没写或 template 明文泄露，只要 template 字段局部正确旧测试仍可能绿。现升级为响应/权限/审计精确契约，避免 notification template update 测试只证明“模板字段看起来被清空或保留” |
| 2026-05-31 | N/A（Run results filter empty page exact 契约） | `tests/unit/test_api/test_runs.py::test_get_run_results_filters_by_suite tests/unit/test_api/test_runs.py::test_get_run_results_filters_by_keyword tests/unit/test_api/test_runs.py::test_get_run_results_combines_suite_and_keyword tests/unit/test_api/test_runs.py::test_get_run_results_escapes_keyword_like_wildcards` 4 passed；runs targeted ruff passed；targeted docs contract passed | 无新增 warning；run results 的 suite/q/status 过滤单测现在同时固定空结果分页响应体 `{"data":[],"page":1,"per_page":20,"total":0}`、默认 offset/limit 和 SQL filter 投影 | 测试 review 发现这些过滤用例此前只断言 200 与 repo filters；如果路由响应壳漏字段、page/per_page/total 漂移，或默认分页参数串错，只要过滤条件仍传给 repo 旧测试仍可能绿。现升级为空页响应+分页入参精确契约，避免 run results filter 测试只证明“过滤条件大概传下去了” |
| 2026-05-31 | N/A（Rate limit pass-through response identity 契约） | `tests/unit/test_api/test_rate_limit.py::TestRateLimitMiddlewareDispatch::test_different_tokens_independent_quotas tests/unit/test_api/test_rate_limit.py::TestRateLimitMiddlewareDispatch::test_third_token_passes_when_others_exhausted tests/unit/test_api/test_rate_limit.py::TestRateLimitMiddlewareDispatch::test_no_trusted_proxy_xff_ignored_in_key tests/unit/test_api/test_rate_limit.py::TestRateLimitMiddlewareDispatch::test_uuid_path_segments_share_one_bucket` 4 passed；rate limit full 28 passed；release quality docs contract full 296 passed；targeted ruff passed | 无新增 warning；rate-limit 放行路径现在固定返回对象 identity：未超限 token、无 trusted proxy 的 direct IP、UUID 路径归一化的两个请求都必须原样返回 downstream response，同时保留 429 envelope、Redis bucket key 与 no raw token/run id 证据 | 测试 review 发现这些限流用例已锁住 bucket key、429 body 和 downstream 调用次数，但放行分支仍只看 `status_code == 200`；如果 middleware await 了 downstream 后又包装/替换响应，或两个 UUID 请求返回错 downstream 对象，旧测试仍可能绿。现升级为 Rate limit pass-through response identity 契约，避免限流放行测试只证明“下游被调用且最终状态码是 200” |
| 2026-05-31 | N/A（worker-backed E2E UI row/artifact 精确契约） | `npx playwright test tests/e2e/real-run-trigger.spec.ts --project=chromium --list` 1 test listed；targeted docs contract passed；targeted ruff passed | 未重跑真实 worker-backed 浏览器链路；静态 contract 锁住 UI 断言形状；run detail 的 Test Results 页面现在用结果行逐 `td` 锁定用例名与 `Passed` 状态，Artifacts 页面锁定 `junit.xml` heading、`junit` 类型/大小行与下载按钮 | 测试 review 发现 worker-backed E2E 的 API summary/result/artifact 已是精确契约，但 UI 侧仍用全页 `getByText("Passed").first()`、`getByText(testCaseName)` 和 `getByText("junit.xml")`；如果 Passed 出现在摘要卡、用例名出现在日志，或 artifact 名出现在其它区域，旧 UI 断言仍可能绿。现把页面证据绑定到结果表行和产物卡片，避免真实 E2E 只证明“页面某处出现了目标文本” |
| 2026-05-31 | N/A（Admin status exact response body 契约） | `tests/unit/test_api/test_admin.py` 4 passed；release quality docs contract full 295 passed；targeted ruff passed | 无新增 warning；`/admin/status` 正常与 zero-run 分支现在都固定完整响应 body：queue_depth、in_flight、success_rate_1h、total_runs_1h 必须逐项等值，同时保留队列/运行中/终态分母/成功分子的查询契约 | 测试 review 发现 admin status 已锁住 repository 查询参数，但响应仍逐字段抽查，zero-run 分支只断言 `success_rate_1h == 1.0`；如果状态页漏字段、字段名漂移或 total_runs_1h/in_flight 串错，旧测试仍可能绿。现升级为 Admin status exact response body 契约，避免系统状态页测试只证明“查询正确且某个指标看起来对” |
| 2026-05-31 | N/A（Pytest runner shell command exact helper 契约） | `tests/unit/test_plugins/test_pytest_runner.py::TestPytestBuildCommand` 6 passed；pytest runner full 45 passed；release quality docs contract full 294 passed；targeted ruff passed | 无新增 warning；Pytest public `build_command` helper 现在用完整 expected argv 反推出 shell command，并要求 `command == "cd /workspace && " + shlex.quote(argv...)`，同时再用 `shlex.split` 证明带空格参数会还原为原 argv | 测试 review 发现 pytest runner 虽已从 `in cmd` 升级到 argv 比较，但 helper 仍只用 `startswith("cd /workspace && ")` 接受工作目录前缀；如果 shell 字符串夹带额外前缀/后缀、带空格参数未正确 quote，或 public command 与 private argv 不再一一对应，旧 helper 仍可能漏掉。现升级为 Pytest runner shell command exact helper 契约，避免 runner public command 测试只证明“前缀对且解析后 argv 大概对” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_worker_execute.py --collect-only` 8 tests collected | release quality docs contract full 293 passed；targeted ruff/py_compile passed | 无新增 warning；external-stack worker_lost 原始 run 现在固定 `error_message` 必须以 `worker_lost: heartbeat expired for ` 开头，提取出的 worker id 必须来自本轮删除的 Redis heartbeat key，且符合 `worker-[0-9a-f]{8}` | 测试 review 发现 worker_lost 重试黑盒用例此前只检查 `worker_lost` 关键词；如果 run detail 混入 debug context、指向错误 worker，或实现只写了一个泛化错误短语，旧测试仍可能绿。现升级为 external-stack worker_lost original error exact heartbeat 契约，避免失联重试集成测试只证明“错误关键词出现过” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_auto_retry_real_db.py::test_execute_run_infra_exception_marks_failed_and_schedules_retry_real_db tests/integration/test_auto_retry_real_db.py::test_execute_run_setup_docker_infra_error_uses_real_executor_and_schedules_retry tests/integration/test_auto_retry_real_db.py::test_execute_run_setup_script_failure_is_not_retried_real_executor tests/integration/test_auto_retry_real_db.py::test_execute_run_clone_failure_is_not_retried_real_executor tests/integration/test_auto_retry_real_db.py::test_reclaim_resources_worker_lost_marks_failed_publishes_event_and_schedules_retry_real_db` 5 passed；auto_retry_real_db full 7 passed | release quality docs contract full 292 passed；targeted ruff passed | 无新增 warning；auto retry 真实 DB/Redis 失败边界现在固定持久化 `error_message`：Docker infra、setup create infra、setup script exit、clone URL userinfo 脱敏、worker_lost heartbeat 五条路径均为完整等值 | 测试 review 发现这些真实链路此前已证明重试/不重试和队列副作用，但错误消息只做关键词包含；如果 `error_message` 混入 stderr、debug hint、原始 URL userinfo 或 worker 上下文，只要关键词仍在旧测试仍可能绿。现升级为 auto retry real DB failure exact error 契约，避免自动重试集成测试只证明“失败原因大概出现过” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_worker_execute.py --collect-only` 8 tests collected | release quality docs contract full 291 passed；targeted ruff/py_compile passed | 无新增 warning；external-stack setup 失败黑盒用例现在固定 run detail 的 `error_message == "Setup script failed (exit 1)"`，并证明 setup stderr 片段 `setup-boundary-failure` 只出现在归档日志读面，不进入 run detail 响应 | 测试 review 发现 setup 失败路径此前只用包含式检查 `Setup script failed (exit 1)`；如果 API 把 setup stderr、调试信息或日志片段拼进 run detail 的 `error_message`，旧测试仍可能绿。现升级为 setup failure run detail exact error 契约，避免外部栈失败测试只证明“错误短语出现过” |
| 2026-05-31 | N/A（RunExecutor setup failure spec exact 契约） | `tests/unit/test_engine/test_executor.py::TestRunSetupContainerised::test_setup_nonzero_exit_raises tests/unit/test_engine/test_executor.py::TestRunSetupContainerised::test_setup_timeout_raises` 2 passed；executor full 42 passed；release quality docs contract full 290 passed；targeted ruff passed | 无新增 warning；setup 非零退出和 timeout 失败路径现在除 exact RuntimeError、start/wait/cleanup、active execution reset 外，也固定失败容器 `ExecutionSpec` 的 image/command/env/resource_limits/network/security/user/mount/labels | 测试 review 发现 setup 失败路径此前能证明异常后会 cleanup，但不能证明失败容器仍使用与成功路径一致的 sandbox spec；如果失败路径绕过 env/resource_limits、workspace mount、user 或 labels，只要 wait 返回失败/timeout 旧测试仍可能绿。现升级为 setup failure spec exact 契约，避免失败测试只证明“异常和 cleanup 正常” |
| 2026-05-31 | N/A（Environment env_vars update exact response/audit 契约） | `tests/unit/test_api/test_environments.py::test_update_environment_reencrypts_env_vars_and_redacts_audit` 1 passed；environments full 27 passed；release quality docs contract full 289 passed；targeted ruff passed | 无新增 warning；env_vars update 现在固定完整 EnvironmentResponse、tenant-scoped project lookup、`environment.update(env, env_vars=...)` 且无额外 kwargs、密文 envelope 不含新旧 key/secret 并可按 env id 解密、`environment.update` audit before/after 完整等值且只记录 env_vars redacted count | 测试 review 发现该用例虽已证明重加密和 audit 不含明文，但仍只逐字段抽查响应/审计；如果响应漏 `created_at/network/resource_limits`、repo update 夹带额外字段、audit before/after 字段漂移或只保留 env_vars 片段，旧测试仍可能绿。现升级为 env_vars update exact response/audit 契约，避免环境变量更新测试只证明“密文看起来不泄密” |
| 2026-05-31 | N/A（Check schedules enqueue failure exact commit order 契约） | `tests/unit/test_worker/test_check_schedules.py::TestCheckSchedules::test_handles_enqueue_failure` 1 passed；check_schedules full 9 passed；release quality docs contract full 288 passed；targeted ruff passed | 无新增 warning；enqueue failure 现在固定 create run、set retry group、第一次 commit、enqueue 返回 False、run.trigger audit、update_after_fire(enqueue failed)、第二次 commit 的完整顺序；继续锁住 run.create、enqueue、audit 和 schedule update 参数 | 测试 review 发现该分支虽已固定 update/audit payload，但仍只用 `ctx_session.commit.await_count == 2` 证明事务边界；如果实现把 enqueue 放到 run 提交前、audit/update 与提交顺序漂移，或两次 commit 发生在错误位置，旧测试仍可能绿。现升级为 enqueue failure exact commit order 契约，避免失败路径测试只证明“提交过两次” |
| 2026-05-31 | N/A（Worker-lost reclaim stale update no-side-effect 契约） | `tests/unit/test_engine/test_reclaim.py::test_mark_worker_lost_returns_false_skips_event_and_cleanup tests/unit/test_engine/test_reclaim.py::test_redis_probe_failure_is_fail_open` 2 passed；reclaim full 11 passed；release quality docs contract full 287 passed；targeted ruff passed | 无新增 warning；mark_worker_lost 条件更新失败现在固定 heartbeat key、`mark_worker_lost(run.id, worker_id, message)` 参数，且不 publish failed event、不触发 retry callback、不 cleanup；Redis heartbeat probe 异常固定 fail-open，仍只查目标 heartbeat key、不 mark、不 cleanup | 测试 review 发现这些分支此前只断言返回 0、没有 Redis xadd/cleanup 或没有 mark；如果条件更新使用错 worker/message 后返回 False，或失败开放路径查错 key/误清理容器，旧测试仍可能绿。现升级为 worker-lost stale update no-side-effect 契约，避免 reclaimer 早退测试只证明“最后没清理容器” |
| 2026-05-31 | N/A（Worker auto retry reject no-side-effect 契约） | `tests/unit/test_worker/test_auto_retry.py::TestAttemptRetry::test_returns_false_when_should_retry_false tests/unit/test_worker/test_auto_retry.py::TestAttemptRetry::test_returns_false_when_original_not_found tests/unit/test_worker/test_auto_retry.py::TestAttemptRetry::test_non_infra_exception_not_retried` 3 passed；auto retry full 24 passed；release quality docs contract full 286 passed；targeted ruff passed | 无新增 warning；auto retry 拒绝路径现在固定 get_by_id、refresh pipeline、`_should_retry` 参数和 no create/no scheduler/no commit 副作用；原 run 不存在时固定不 refresh、不创建 retry run、不构造 scheduler、不 commit | 测试 review 发现这些分支此前只断言返回 False 和 create 未调用；如果未找到 run 后仍 refresh/commit，policy 拒绝后仍构造 scheduler，或非 infra 异常仍触发重试副作用，旧测试仍可能绿。现升级为 auto retry reject no-side-effect 契约，避免拒绝路径测试只证明“最后没 create retry run” |
| 2026-05-31 | N/A（FairScheduler capacity short-circuit exact side-effect 契约） | `tests/unit/test_worker/test_scheduler.py::test_enqueue_marks_waiting_when_global_capacity_is_full tests/unit/test_worker/test_scheduler.py::test_enqueue_marks_waiting_when_project_capacity_is_full` 2 passed；scheduler full 14 passed；release quality docs contract full 285 passed；targeted ruff passed | 无新增 warning；全局容量满现在固定只查 global count、不查 project count、不入 ARQ、不写 mark_enqueued/get_by_arq_job_id；项目容量满现在固定先查 global 再查 project、只写 waiting、不入 ARQ、不写 enqueued/dedup 查询 | 测试 review 发现这两条此前只断言返回 False 和 `mark_waiting(run.id)`；如果全局满仍继续查项目、容量满仍触发 ARQ 或误写 enqueued metadata，旧测试仍可能绿。现升级为 capacity short-circuit exact side-effect 契约，避免 scheduler 容量测试只证明“最后变成 waiting” |
| 2026-05-31 | N/A（FairScheduler ARQ conflict exact dedup 契约） | `tests/unit/test_worker/test_scheduler.py::test_enqueue_job_conflict_is_idempotent_for_same_enqueued_run tests/unit/test_worker/test_scheduler.py::test_enqueue_job_conflict_marks_waiting_for_different_run` 2 passed；scheduler full 14 passed；release quality docs contract full 284 passed；targeted ruff passed | 无新增 warning；ARQ 返回 `None` 的 dedup/id conflict 分支现在固定完整 `enqueue_job("execute_run", str(run.id), queue:medium, run:{id}, _defer_by=0)`、`get_by_arq_job_id("run:{id}")`，同一已入队 run 不写 waiting/mark_enqueued，不同 run 冲突只写 waiting reason | 测试 review 发现这两条此前只看 `scheduler.enqueue()` True/False 和部分 waiting 副作用；如果 dedup 查询 key 漂移、ARQ job id 不再等于 `run:{id}`、冲突时误写 `mark_enqueued`，或同一 run idempotent 分支仍落 waiting，旧测试仍可能绿。现升级为 ARQ conflict exact dedup 契约，避免 scheduler 冲突测试只证明“返回值方向大概对” |
| 2026-05-31 | N/A（Notification channel failure exact ChannelResult 契约） | notification channel failure selectors 21 passed；channels full 39 passed；release quality docs contract full 283 passed；targeted ruff passed | 无新增 warning；Email missing config，Webhook 非公网解析/URL shape/credentials/HTTP 500/timeout/network/missing URL，DingTalk timeout/API errcode/HTTP 500/invalid JSON object，WeCom timeout/API errcode/HTTP 500/invalid JSON object，以及 unknown route 现在固定 exact `ChannelResult` 或 exact `_validate_webhook_url` 返回值 | 测试 review 发现这些失败路径此前多为 `result.success is False` 加错误子串/`lower()`；如果错误文案夹带 upstream body、secret、debug hint，或错误类型从 missing/credentials/timeout/API/HTTP 串线，只要子串还在旧测试仍可能绿。现升级为 Notification channel failure exact ChannelResult 契约，避免渠道失败测试只证明“失败了且错误里有关键词” |
| 2026-05-31 | N/A（Notification HTTP success exact ChannelResult 契约） | `tests/unit/test_worker/test_channels.py::TestWebhookChannel::test_custom_method tests/unit/test_worker/test_channels.py::TestWebhookChannel::test_custom_headers tests/unit/test_worker/test_channels.py::TestDingtalkChannel::test_signs_request_when_secret_configured tests/unit/test_worker/test_channels.py::TestDingtalkChannel::test_markdown_message tests/unit/test_worker/test_channels.py::TestWecomChannel::test_markdown_message` 5 passed；channels full 39 passed；release quality docs contract full 282 passed；targeted ruff passed | 无新增 warning；Webhook custom method/headers、Dingtalk signed/markdown、WeCom markdown 成功路径现在不仅固定 HTTP args/kwargs，也固定完整 `ChannelResult(success=True)`，确保成功分支没有夹带 error | 测试 review 发现这些 HTTP 渠道用例虽然已锁住 endpoint/params/json/headers，但返回值仍只断言 `result.success is True`；如果实现错误地返回 `ChannelResult(success=True, error="...")` 或复用带 error 的成功对象，旧测试仍可能绿。现升级为 Notification HTTP success exact ChannelResult 契约，避免渠道测试只证明“请求打对了且 success 布尔值为真” |
| 2026-05-31 | N/A（JS runner command prefix exact helper 契约） | `tests/unit/test_plugins/test_jest_runner.py::TestJestBuildCommand tests/unit/test_plugins/test_playwright_runner.py::TestPlaywrightBuildCommand` 31 passed；release quality docs contract full 281 passed；targeted ruff passed | 无新增 warning；Jest/Playwright command helper 现在把 `npx` 前缀拆出并完整等值断言：`cd /workspace && mkdir -p <dir> && JEST_JUNIT_OUTPUT_FILE=...` / `PLAYWRIGHT_JUNIT_OUTPUT_FILE=...`，默认、custom junit、args、test_paths 和 full config 均会校验工作目录、报告目录和 env var 位置 | 测试 review 发现上一轮 JS runner 命令已锁住 argv，但 helper 仍用 `command.startswith("cd /workspace && ")` 和 `ENV_VAR in command`；如果 args/path 分支漏掉 `mkdir -p results`、把 env var 放到错误位置或夹带额外前缀，只要 argv 仍能解析旧测试仍可能绿。现升级为 JS runner command prefix exact helper 契约，避免命令测试只证明“npx argv 对、前缀大概存在” |
| 2026-05-31 | N/A（Go runner JUnit failure/skip exact subtree 契约） | `tests/unit/test_plugins/test_go_test_runner.py::TestGoTestRunTests::test_run_with_failures tests/unit/test_plugins/test_go_test_runner.py::TestGoTestRunTests::test_run_with_skipped` 2 passed；release quality docs contract full 280 passed；targeted ruff passed | 无新增 warning；Go runner failure/skip 用例现在固定 `testsuites` 汇总、唯一 `testsuite` 属性、`testcase` classname/name/time、唯一子节点 tag/attribute/text，分别锁住 `failure message=go test failed` 与 `skipped message=go test skipped` | 测试 review 发现这两条用例此前虽然检查了汇总数字和 message，但 failure/skipped 输出文本仍只用 `in (node.text or "")`；如果 JUnit 子树夹带额外输出、测试耗时/包名漂移，或 failure/skipped 节点重复混入，旧测试仍可能绿。现升级为 Go runner JUnit failure/skip exact subtree 契约，避免报告测试只证明“失败/跳过文本片段出现过” |
| 2026-05-31 | N/A（GitSource clone failure exact redacted message 契约） | `tests/unit/test_plugins/test_git_source.py::TestGitSourceClone::test_clone_auth_failure_redacts_token_from_error tests/unit/test_plugins/test_git_source.py::TestGitSourceClone::test_clone_failure_redacts_url_userinfo_without_explicit_secret` 2 passed；release quality docs contract full 279 passed；targeted ruff passed | 无新增 warning；显式 token auth 失败与 URL userinfo 失败现在都固定完整异常文本 `git clone failed (exit 128): fatal: Authentication failed for 'https://***@github.com/org/repo.git'`，并继续证明 token、embedded secret、`x-access-token` 不回显 | 测试 review 发现这两条失败脱敏用例此前只断言 secret 不在异常里且 redacted URL 片段出现；如果异常同时夹带额外 debug、原始 userinfo、或重复 stderr，只要片段还在旧测试仍可能绿。现升级为 GitSource clone failure exact redacted message 契约，避免脱敏测试只证明“有一个脱敏片段” |
| 2026-05-31 | N/A（GitSource auth env path/ssh argv exact 契约） | `tests/unit/test_plugins/test_git_source.py::TestGitSourceClone::test_clone_https_token_uses_askpass_not_url_userinfo tests/unit/test_plugins/test_git_source.py::TestGitSourceClone::test_clone_ssh_key_uses_temp_identity_file` 2 passed；release quality docs contract full 278 passed；targeted ruff passed | 无新增 warning；token auth 现在把 `GIT_ASKPASS` 解析成 `Path`，固定文件名 `askpass.sh` 与 `qap-git-auth-*` 临时目录；SSH auth 现在用 `shlex.split(GIT_SSH_COMMAND)` 固定完整 argv：`ssh -i <identity> -o IdentitiesOnly=yes -o StrictHostKeyChecking=accept-new`，并固定 identity 文件名与临时目录前缀 | 测试 review 发现 GitSource auth env 虽已锁住 clone argv，但 askpass 仍只用 `.endswith("askpass.sh")`、SSH command 仍只用 `.startswith("ssh -i ")` / `.endswith(...)`；如果命令中间夹带额外 option、identity 路径错名、或临时目录不再受控，旧测试仍可能绿。现升级为 GitSource auth env path/ssh argv exact 契约，避免认证注入测试只证明“字符串首尾看起来对” |
| 2026-05-31 | N/A（API token Argon2 PHC parameter exact 契约） | `tests/unit/test_auth/test_token_service.py::TestCreateApiToken::test_hash_is_argon2id` 1 passed；release quality docs contract full 277 passed；targeted ruff passed | 无新增 warning；`create_api_token` 的真实 hash 测试现在用 `extract_parameters(hash_value)` 固定 PHC 参数：`Type.ID`、version 19、salt_len 16、hash_len 32、time_cost 3、memory_cost 65536、parallelism 1，并继续证明生成 token 第三段 secret 可验证、完整 token/user_id 不可验证 | 测试 review 发现该用例此前只断言 `hash_value.startswith("$argon2id$")`；如果实现把 Argon2 参数降级、hash_len 变短、parallelism/memory/time 漂移，旧测试仍会绿。现升级为 API token Argon2 PHC parameter exact 契约，避免安全敏感 hash 测试只证明“看起来是 Argon2id” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 .venv/bin/python -m pytest tests/integration/test_real_auth_results_artifacts.py::test_archived_logs_api_returns_404_when_s3_object_missing -q` 1 passed；`RUN_INTEGRATION_TESTS=1 RUN_PERFORMANCE_TESTS=1 .venv/bin/python -m pytest tests/integration/test_performance_smoke.py::test_archived_log_missing_object_p99_smoke -q` 1 passed | test quality contracts 5 passed；release quality docs contract full 276 passed；targeted ruff passed | 无新增 warning；真实 archived logs missing S3 object 404 与 performance p99 warmup/sample 循环现在固定完整 `{"error":{"code":"NOT_FOUND","message":"Archived logs not found","details":[]}}` envelope，并证明响应不回显 run_id；测试质量门也能拦住 `error = response.json()["error"]` 后继续断 `error["code"]` / `error["message"]` 的别名式弱断言 | 测试 review 发现这条真实 archived logs 404 仍把 error 子对象赋值后只检查 code/message；如果 details 夹带 run_id、debug hint 或响应壳退化，旧测试仍可能绿，且旧静态门没拦住别名写法。现升级为 Archived logs missing object exact envelope/alias gate 契约，避免错误响应测试换个变量名继续只测子字段 |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 .venv/bin/python -m pytest tests/integration/test_api_endpoints.py::TestProjectsCRUD::test_delete_project tests/integration/test_api_endpoints.py::TestNotificationRules::test_delete_notification_rule -q` 2 passed | release quality docs contract full 275 passed；targeted ruff passed | 无新增 warning；真实 project 与 notification rule delete 后 GET 现在固定完整 `NOT_FOUND` envelope：`Project not found` / `Notification rule not found`，并证明响应不回显 project_id、rule_id、原始 git URL、git userinfo 或 webhook URL | 测试 review 发现这两条真实删除集成路径已有 204 空响应、软删除与审计证据，但删除后读取仍只断言 `get_resp.status_code == 404`；如果错误体漂移、details 夹带探测 ID、或响应文本泄露删除前 URL，旧测试仍可能绿。现升级为 CRUD delete follow-up 404 exact envelope/no-leak 契约，避免删除集成测试只证明“删完再读是 404” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_auth_audit_failure_paths.py::test_auth_login_rate_limit_uses_real_redis_token_hash_bucket tests/integration/test_auth_audit_failure_paths.py::test_auth_register_rate_limit_uses_real_redis_ip_bucket_and_audit_count tests/integration/test_auth_audit_failure_paths.py::test_auth_refresh_rate_limit_uses_real_redis_ip_bucket_and_audit_count tests/integration/test_auth_audit_failure_paths.py::test_auth_api_token_create_rate_limit_uses_real_redis_token_bucket tests/integration/test_auth_audit_failure_paths.py::test_auth_sse_ticket_rate_limit_uses_real_redis_token_hash_bucket` 5 passed | release quality docs contract full 274 passed；targeted ruff passed | 无新增 warning；真实 auth strict rate-limit 五条集成路径现在统一用 `_assert_rate_limited_response` 固定 429、`Retry-After` strict auth window 与完整 `{"error":{"code":"TOO_MANY_REQUESTS","message":"Rate limit exceeded. Please try again later."}}` envelope，并保留真实 Redis bucket、原始 token/password/cookie 不进 key、限流请求不写额外 audit 的证据 | 测试 review 发现这些真实 Redis 限流用例此前只检查 `limited.json()["error"]["code"]`；如果 429 body message 漂移、夹带 debug/key/token hint、或响应壳退化但 code 字段仍在，旧测试仍可能绿。现升级为 Auth real rate-limit 429 exact envelope 契约，避免真实限流集成测试只证明“第 6 次被限流且 code 字段正确” |
| 2026-05-31 | N/A（Auth middleware exact exception detail sweep 契约） | `tests/unit/test_auth_middleware.py::test_get_current_user_rejects_missing_authorization_header tests/unit/test_auth_middleware.py::test_authenticate_jwt_rejects_expired_tokens tests/unit/test_auth_middleware.py::test_authenticate_api_token_rejects_malformed_token_without_database tests/unit/test_auth_middleware.py::test_authenticate_api_token_rejects_when_database_unavailable` 4 passed；auth middleware full 45 passed；release quality docs contract full 273 passed；targeted ruff passed | 无新增 warning；missing Authorization、expired JWT、malformed API token、API token DB unavailable 四条中间件拒绝路径现在固定 exact detail：`Missing Authorization header`、`Token has expired`、`Invalid API token format`、`API token authentication not available`，并保留不查 blacklist/不打开 DB 的副作用证据 | 测试 review 发现这几条此前只用 `detail.lower()` 子串匹配；如果错误文本夹带 token、claim、数据库状态、debug hint，或不同认证路径文案串线，只要关键词还在旧测试仍可能绿。现升级为 Auth middleware exact exception detail sweep 契约，避免认证入口测试只证明“错误大概提到了 authorization/expired/format/unavailable” |
| 2026-05-31 | N/A（API token invalid records exact denial detail 契约） | `tests/unit/test_auth_middleware.py::test_authenticate_api_token_rejects_invalid_records` 8 passed；auth middleware full 45 passed；release quality docs contract full 272 passed；targeted ruff passed | 无新增 warning；API token 无记录、已撤销、owner/tenant 失效、secret 错误统一固定 `Invalid or revoked API token`，secret 匹配后过期才固定 `API token has expired`，并证明错误 detail 不回显 token_id、secret 或完整 bearer token | 测试 review 发现该 8 分支参数化用例此前只用 detail 子串匹配；如果失效原因被细分、过期顺序回退、或错误体夹带 token id/secret/debug 信息，旧测试仍可能绿。现升级为 API token invalid records exact denial detail 契约，避免鉴权拒绝测试只证明“detail 大概含 invalid/expired” |
| 2026-05-31 | N/A（Rate limit multi-token 429 exact response 契约） | `tests/unit/test_api/test_rate_limit.py::TestRateLimitMiddlewareDispatch::test_third_token_passes_when_others_exhausted` 1 passed；release quality docs contract full 271 passed；targeted ruff passed | 无新增 warning；三个 bearer token 隔离用例现在对两个耗尽 token 都复用 `_assert_rate_limited_response`，固定 429、`Retry-After` 使用普通 API window、`TOO_MANY_REQUESTS` error envelope，同时保留第三个 token 进入 downstream 与 Redis bucket key 精确序列 | 测试 review 发现该用例此前只断言 token A/B 返回 429；如果其中一个 429 响应缺 `Retry-After`、错误体漂移为空/调试字段，或窗口误用 strict auth window，旧测试仍可能绿。现升级为 Rate limit multi-token 429 exact response 契约，避免 token 隔离测试只证明“两个耗尽 token 被限流” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_auth_results_artifacts.py::test_artifact_download_soft_deleted_row_returns_404_without_presign tests/integration/test_real_auth_results_artifacts.py::test_artifact_download_api_token_cross_tenant_returns_same_404` 2 passed | release quality docs contract full 270 passed；targeted ruff passed | 无新增 warning；真实 artifact download 的 soft-deleted 与跨租户 404 现在不仅与随机 UUID 响应一致，还固定完整 `{"error":{"code":"NOT_FOUND","message":"Artifact not found","details":[]}}` envelope，并保留 no-presign/no-get、artifact id/storage path 不回显证据 | 测试 review 发现这两条 required integration 此前只比较“目标 artifact 与随机 UUID 的响应相等”；如果二者一起漂移为带 artifact_id、storage_path、tenant hint 或 debug details 的 404，旧测试仍可能绿。现升级为 artifact download real 404 exact envelope 契约，避免枚举防护测试只证明“两边一样” |
| 2026-05-31 | N/A（Error subfield equality static gate 契约） | `tests/unit/test_test_quality_contracts.py::test_python_tests_do_not_assert_error_subfields_as_whole_response` 1 passed；release quality docs contract full 269 passed；targeted ruff passed | 无新增 warning；仓库级测试质量门现在用 AST 扫描 unit/integration Python 测试，禁止把 response JSON 的 `detail`/`error` 子字段直接当作完整响应断言，同时保留 validation projection 等有意的结构化错误投影 | 测试 review 已把现有字段级错误体等值断言清零；如果未来新增拒绝路径只断言错误子字段，可能重新漏掉顶层 ID、hint、debug 或配置路径泄漏。现升级为 Error subfield equality static gate，避免这类弱断言回流 |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_webhook_branch_dedup.py::test_webhook_without_pipeline_returns_409_and_creates_no_run tests/integration/test_webhook_branch_dedup.py::test_webhook_without_environment_returns_409_and_creates_no_run` 2 passed；`RUN_INTEGRATION_TESTS=1 tests/integration/test_cancel_e2e.py::test_repeated_http_cancel_is_idempotent --collect-only` 1 test collected；cancel e2e py_compile passed | `tests/unit/test_api/test_notifications.py::test_get_rule_not_found tests/unit/test_api/test_notifications.py::test_notification_rule_item_routes_hide_other_project_rule_without_side_effects tests/unit/test_api/test_project_members.py::test_update_nonexistent_member_returns_404_without_side_effects tests/unit/test_api/test_project_members.py::test_remove_nonexistent_member_returns_404` 4 passed；release quality docs contract full 268 passed；targeted ruff passed | 无新增 warning；剩余字段级错误体断言已收敛：webhook 缺 pipeline/environment 409、重复 cancel 第二次 409、notification rule 404、project member 404 现在都固定完整 body/envelope，并保留 no-run/no-side-effect/no-leak 证据 | 测试 review 扫描确认这些用例此前仍用 `json()["detail"]` 或 `json()["error"]`；如果错误体顶层夹带 project/member/rule/run 标识、配置 hint 或 debug 字段，旧测试仍可能绿。现升级为 remaining rejection exact body 契约，避免拒绝路径测试只证明“子字段文案正确” |
| 2026-05-31 | N/A（Route rejection detail exact body 契约） | `tests/unit/test_api/test_runs.py::test_cancel_already_terminal tests/unit/test_api/test_p3.py::TestWebhookTrigger::test_webhook_trigger_signed_project_requires_signature tests/unit/test_api/test_p3.py::TestWebhookTrigger::test_webhook_trigger_archived_project_409 tests/unit/test_api/test_projects.py::test_unauthenticated` 4 passed；release quality docs contract full 267 passed；targeted ruff passed | 无新增 warning；terminal cancel、项目 webhook 缺签名、归档项目 webhook trigger、未认证 project list 现在都固定完整 `{"detail": ...}` body，并保留不 cancel、不 publish Redis、不查 pipeline/environment/run、不写 audit、不触碰 project repository 的副作用证据 | 测试 review 发现这些路由拒绝用例已有副作用断言，但用户可见错误体仍只断言 `json()["detail"]`；如果响应体夹带 run/project/webhook 识别信息、签名 hint 或 debug 字段，旧测试仍可能绿。现升级为 route rejection detail exact body 契约，避免拒绝路径测试只证明“状态码和 detail 文案对” |
| 2026-05-31 | N/A（API token revoke hidden 404 exact body 契约） | `tests/unit/test_auth/test_auth_routes.py::TestTokenRoutes::test_revoke_token_not_found tests/unit/test_auth/test_auth_routes.py::TestTokenRoutes::test_revoke_token_hides_other_users_token_without_side_effects` 2 passed；release quality docs contract full 266 passed；targeted ruff passed | 无新增 warning；API token revoke 的不存在 token 与他人 token 现在都固定完整 body `{"detail": "Token not found"}`，并保留只查 token_id、不 revoke、不写 audit、不 commit、rollback 的副作用证据 | 测试 review 发现 API token revoke 防枚举用例此前只断言 `resp.json()["detail"]`；如果 404 响应体夹带 token_id、owner hint、revoked/existence reason 或 debug 字段，旧测试仍可能绿。现升级为 API token revoke hidden 404 exact body 契约，避免 token 防枚举测试只证明“detail 文案相同” |
| 2026-05-31 | N/A（Audit events unit exact envelope 契约） | `tests/unit/test_api/test_audit.py::test_audit_events_list_masks_cross_tenant_matches_without_self_audit tests/unit/test_api/test_audit.py::test_audit_events_rejects_invalid_text_filters_without_repo_lookup tests/unit/test_api/test_audit.py::test_audit_events_rejects_reversed_time_range_without_repo_lookup` 6 passed；release quality docs contract full 265 passed；targeted ruff passed | 无新增 warning；audit-events 单元层跨租户 404 与参数 422 现在固定完整 ErrorResponse body，分别为 `{"error":{"code":"NOT_FOUND","message":"Audit event not found","details":[]}}` 和 `VALIDATION_ERROR` envelope，并保留不查仓储/不写 `audit_events.list` 自审计证据 | 测试 review 发现 audit-events 单测此前只断言 `resp.json()["error"]`；如果 query validation 或跨租户掩蔽响应顶层夹带 resource_id、tenant_id、raw params、hint/debug 字段，旧测试仍可能绿。现升级为 audit-events unit exact envelope 契约，避免单元测试只证明“error 子对象正确且状态码对” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_auth_results_artifacts.py::test_archived_logs_api_returns_503_when_storage_unconfigured_after_rbac tests/integration/test_real_auth_results_artifacts.py::test_artifact_download_url_uses_real_auth_rbac_and_db_row` 2 passed | `RUN_INTEGRATION_TESTS=1 RUN_PERFORMANCE_TESTS=1 tests/integration/test_performance_smoke.py::test_archived_log_storage_unavailable_p99_smoke tests/integration/test_performance_smoke.py::test_artifact_download_storage_unavailable_p99_smoke` 2 passed；release quality docs contract full 264 passed；targeted ruff passed | 无新增 warning；storage-unavailable 的 archived logs 与 artifact download 503 现在在 required integration 和 performance smoke warmup/sample 循环里都固定完整 body `{"detail": "Archived logs are not available"}` / `{"detail": "Artifact download is not available"}`，并保留真实 RBAC/PostgreSQL 后才在 S3 client 边界短路 | 测试 review 发现这些路径此前只断言 `json()["detail"]`；如果 503 响应体夹带 bucket、storage path、artifact id、配置 hint 或 debug 字段，旧测试仍可能绿。现升级为 storage-unavailable 503 exact body 契约，避免可用性烟测只证明“返回了 503 且 detail 文案对” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_audit_events_api.py::test_audit_events_refused_queries_do_not_write_self_audit tests/integration/test_audit_events_api.py::test_audit_events_tenant_isolation_and_cross_tenant_ids_return_404` 2 passed | release quality docs contract full 263 passed；targeted ruff passed | 无新增 warning；audit-events 跨租户 404 现在固定顶层完整 body `{"error":{"code":"NOT_FOUND","message":"Audit event not found","details":[]}}`，同时保留 member/viewer 403 exact body、跨租户 tenant/user/project/event id 不回显、拒绝查询不写 `audit_events.list` 自审计 | 测试 review 发现 audit-events 安全集成用例此前只断言 `resp.json()["error"]`；如果错误体顶层夹带 resource_id、tenant_id、hint 或 debug 字段，旧测试仍可能绿。现升级为 audit-events cross-tenant exact envelope 契约，避免安全测试只证明“error 子对象正确” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_auth_results_artifacts.py::test_real_sse_logs_resume_after_last_event_id_uses_ticket_rbac_and_redis` 1 passed | release quality docs contract full 262 passed；targeted ruff passed | 无新增 warning；真实 logs SSE 缺 ticket 401 现在固定完整 body `{"detail": "Invalid or expired SSE ticket"}`，并保留同用例对 Last-Event-ID 只回放第二条 stderr frame、owner ticket 复用 401、`run.read` API token 续读、empty-scope 403 的 exact body/事件帧证据 | 测试 review 发现该 required integration 已覆盖真实 JWT/Postgres/Redis 与 SSE resume，但缺 ticket 分支仍只断言 `json()["detail"]`；如果错误体夹带 ticket、run_id、内部 reason，或 FastAPI/异常壳漂移，旧测试仍可能绿。现升级为真实 logs SSE missing ticket exact body 契约，避免 required integration 只证明“缺票会 401 且 detail 文案对” |
| 2026-05-31 | N/A（SSE route rejection exact body 契约） | `tests/unit/test_api/test_sse.py::test_logs_sse_no_ticket_returns_401_without_touching_redis tests/unit/test_api/test_sse.py::test_events_sse_no_ticket_returns_401_without_touching_redis tests/unit/test_api/test_sse.py::test_stream_logs_same_tenant_no_project_perm_returns_403 tests/unit/test_api/test_sse.py::test_stream_events_same_tenant_no_project_perm_returns_403` 4 passed；release quality docs contract full 261 passed；targeted ruff passed | 无新增 warning；SSE logs/events 缺 ticket route 现在固定完整 body `{"detail": "Invalid or expired SSE ticket"}`，同租户无 project permission 的 logs/events 现在固定完整 body `{"detail": "Insufficient permissions"}`，并保留缺 ticket 不触碰 Redis GETDEL、403 前只查 tenant run 且不读 Redis stream/status | 测试 review 发现这些 SSE route 单测此前只断言 `resp.json()["detail"]`；如果响应体夹带额外字段、错误壳漂移，或 logs/events 两条 route body 不一致，旧测试仍可能绿。现升级为 SSE route rejection exact body 契约，避免 route 单测只证明“detail 字段文案对” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_worker_execute.py --collect-only` 8 tests collected | release quality docs contract full 260 passed；targeted ruff/py_compile passed | 无新增 warning；external-stack worker denied exact body 契约现在固定 live SSE 缺票/复用 401 为 `{"detail": "Invalid or expired SSE ticket"}`，empty-scope live/archive/detail/artifact list/download 与 clone/setup failure archived logs 403 为 `{"detail": "Insufficient permissions"}`，并保留 worker secret、artifact/log path、JUnit 内容、clone URL secret 与日志片段不回显证据 | 测试 review 发现 external-stack 黑盒用例已覆盖真实 worker/API/MinIO/RBAC 路径和 no-leak 片段，但多个拒绝分支仍只看 401/403；如果 SSE ticket 错误体漂移、scope 拒绝体夹带日志 key/artifact path，或不同读面拒绝语义不一致，旧测试仍可能绿。现升级为 external-stack worker 拒绝路径 exact body 源码契约，避免黑盒测试只证明“状态码拒绝且没明显泄漏片段” |
| 2026-05-31 | N/A（Auth refresh failed audit exact response 契约） | `tests/unit/test_auth/test_auth_routes.py::TestAuditRefresh::test_refresh_failed_emits_audit` 1 passed；release quality docs contract full 259 passed；targeted ruff passed | 无新增 warning；auth refresh failed audit 单测现在固定 401 body `{"detail": "Invalid refresh token"}`、refresh cookie 清理、`auth.refresh_failed` audit 完整 kwargs，且继续证明原始 refresh token 不进 audit payload | 测试 review 发现该用例此前只证明 invalid refresh token 会写失败审计；如果响应 detail 漂移、refresh cookie 未清、或用户可见错误体泄露 token，旧测试仍可能绿。现升级为 refresh failed audit exact response/cookie 契约，避免审计单测只证明“失败审计写了” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_auth_results_artifacts.py::test_real_jwt_created_api_token_authenticates_updates_last_used_and_revokes` 1 passed | release quality docs contract full 258 passed；targeted ruff passed | 无新增 warning；real API token revoke rejection exact body 契约现在固定被撤销 API token 再访问 `/projects` 返回 `{"detail": "Invalid or revoked API token"}`，同时保留 token secret 不落库、last_used 更新、revoke 204 空 body、真实 create/revoke audit 不含 token/secret/secret_hash 证据 | 测试 review 发现该真实生命周期用例已证明 revoke 后 401，但没有锁住防枚举错误体；如果 revoked token 错误体漂移成 expired/revoked 细分、泄露 token_id/secret，或后续中间件把撤销 token 当普通 unauthorized，旧测试仍可能绿。现升级为 revoked API token exact rejection 契约，避免生命周期集成测试只证明“撤销后不能访问” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_auth_results_artifacts.py::test_api_token_scope_matrix_enforced_by_real_routes` 1 passed | release quality docs contract full 257 passed；targeted ruff passed | 无新增 warning；API token scope matrix exact denial body 契约现在固定 project.read 写 project 拒绝、run.trigger 写 schedule 拒绝、run.read 读 projects 拒绝、empty-scope trigger run 拒绝均返回 `{"detail": "Insufficient permissions"}`，同时保留 project 未落库、schedule count 不变、run.trigger 正向 Run 行匹配证据 | 测试 review 发现 scope matrix 已从状态码表升级出副作用证据，但四个拒绝分支仍只看 403；如果权限层错误体漂移、不同 endpoint 拒绝语义不一致，或错误体夹带 denied slug/pipeline id，旧测试仍可能绿。现升级为 scope matrix exact denial body 契约，避免矩阵只证明“这些分支都 403” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_auth_results_artifacts.py::test_audit_events_api_token_requires_audit_read_scope_without_self_audit_on_denial tests/integration/test_real_auth_results_artifacts.py::test_archived_logs_api_token_requires_run_read_scope tests/integration/test_real_auth_results_artifacts.py::test_artifact_list_api_token_requires_run_read_scope tests/integration/test_real_auth_results_artifacts.py::test_artifact_download_api_token_requires_run_read_scope` 4 passed | release quality docs contract full 256 passed；targeted ruff passed | 无新增 warning；real auth API token scope denial exact body 契约现在固定 audit-events、archived logs、artifact list、artifact download 的 `project.read`/empty-scope 等拒绝路径均返回 `{"detail": "Insufficient permissions"}`，并保留 no self-audit、no-S3 get/presign、artifact/log/audit 查询不回显证据 | 测试 review 发现这些 required integration 已覆盖真实 API token、RBAC、PostgreSQL 与 S3 test double，但拒绝路径主要只断言 403 和“不泄漏”；如果权限层改成别的错误体、错误体夹带 action/resource/log key/artifact path，或不同读面拒绝体不一致，旧测试仍可能绿。现升级为真实 API token scope 拒绝 exact body 契约，避免 required integration 只证明“拒绝了且没碰 S3/没写自审计” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 RUN_PERFORMANCE_TESTS=1 tests/integration/test_performance_smoke.py::test_archived_log_denied_no_s3_read_p99_smoke tests/integration/test_performance_smoke.py::test_artifact_download_denied_no_presign_p99_smoke tests/integration/test_performance_smoke.py::test_run_read_empty_scope_denied_logs_and_artifacts_no_s3_p99_smoke` 3 passed | release quality docs contract full 255 passed；targeted ruff passed | 无新增 warning；performance log/artifact denial exact body 契约现在固定 archived-log 跨租户/随机 404 为 `{"error":{"code":"NOT_FOUND","message":"Run not found","details":[]}}`，artifact download 跨租户/随机 404 为 `{"error":{"code":"NOT_FOUND","message":"Artifact not found","details":[]}}`，empty-scope run.read 三条读面 403 为 `{"detail": "Insufficient permissions"}`，并保留 no-S3 get/presign 与敏感 metadata 不回显证据 | 测试 review 发现这些性能烟测此前已能证明 RBAC 拒绝不会提前触碰 S3，但 archived log/artifact download 404 只复用随机响应模板，empty-scope 只看 403；如果错误 envelope 漂移、download 与 logs 文案串错，或 403 body 回显 artifact/log 路径，旧测试仍可能绿。现升级为 log/artifact 拒绝路径 exact body 契约，避免 smoke 只证明“拒绝后没有 S3 副作用” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 RUN_PERFORMANCE_TESTS=1 tests/integration/test_performance_smoke.py::test_artifact_list_denied_no_metadata_p99_smoke tests/integration/test_performance_smoke.py::test_audit_events_denied_queries_no_self_audit_p99_smoke tests/integration/test_performance_smoke.py::test_audit_events_api_token_denied_no_self_audit_p99_smoke` 3 passed | release quality docs contract full 254 passed；targeted ruff passed | 无新增 warning；performance smoke denied exact error body 契约现在固定 artifact/audit 403 拒绝路径的 `{"detail": "Insufficient permissions"}`，跨租户 artifact 404 为 `{"error":{"code":"NOT_FOUND","message":"Run not found","details":[]}}`，跨租户 audit 404 为 `{"error":{"code":"NOT_FOUND","message":"Audit event not found","details":[]}}`，并保留 artifact metadata、audit query 与 self-audit 不泄漏证据 | 测试 review 发现这些性能烟测此前证明拒绝路径足够快、不会回显部分敏感内容或写自审计，但 403/404 错误体仍可漂移；如果拒绝响应泛化、误把 run/audit id 或 action 写进错误体，或跨租户 404 文案泄露实现细节，旧测试仍可能绿。现升级为性能拒绝路径 exact error body 契约，避免 smoke 只证明“拒绝得快且状态码对” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_auth_audit_failure_paths.py::test_login_nonexistent_user_returns_401_when_audit_write_fails tests/integration/test_auth_audit_failure_paths.py::test_refresh_with_invalid_token_returns_401_not_500 tests/integration/test_auth_audit_failure_paths.py::test_refresh_invalid_token_returns_401_when_audit_write_fails` 3 passed | release quality docs contract full 253 passed；targeted ruff passed | 无新增 warning；真实 auth failure/audit-outage 的 401 路径现在固定 response body：login 为 `{"detail": "Invalid credentials"}`，refresh 为 `{"detail": "Invalid refresh token"}`，同时保留 refresh cookie 清理、audit warning 与 audit count 不变证据 | 测试 review 发现这些路径此前证明不会 500、会清 cookie/写 warning/不写半成功 audit，但没有锁住用户可见错误体；如果错误 detail 泛化、泄露内部 audit outage 或 refresh 错误文案漂移，旧测试仍绿。现升级为 auth 失败 401 exact body 契约，避免认证失败集成测试只证明“状态码和后端副作用对” |
| 2026-05-31 | N/A（全局 201 创建响应 body 防回退契约） | `tests/unit/test_api/test_credentials.py::test_create_credential_audit_does_not_leak_plaintext tests/unit/test_api/test_p3.py::TestWebhooks::test_webhook_trigger_creates_run tests/unit/test_api/test_p3.py::TestWebhooks::test_webhook_trigger_preserves_project_git_auth_metadata tests/unit/test_api/test_projects.py::test_create_project_adds_creator_as_project_admin tests/unit/test_api/test_runs.py::test_trigger_run_includes_git_auth_metadata_without_plaintext tests/unit/test_auth/test_auth_routes.py::TestAuditRegister::test_register_success_emits_audit` 6 passed；release quality docs contract full 252 passed；targeted ruff passed | 无新增 warning；credential/webhook/project/run/auth register 的 201 成功路径现在固定响应 body 或 token/user 投影，且 release docs contract 用 AST 扫描禁止任何测试函数只断言 201 而不校验响应内容 | 测试 review 发现这些创建成功路径此前主要证明仓储/audit/enqueue 副作用；如果 response DTO 漏字段、串错 id/tenant/pipeline、register token/user 投影漂移，旧测试仍绿。现升级为全局 201 body 防回退契约，避免创建类测试只证明“写入副作用发生且状态码是 Created” |
| 2026-05-31 | N/A（全局 204 响应空 body 防回退契约） | `tests/unit/test_auth/test_auth_routes.py::TestTokenRoutes::test_revoke_token tests/unit/test_auth/test_auth_routes.py::TestAuditLogout::test_logout_emits_audit_without_storing_tokens tests/unit/test_auth/test_auth_routes.py::TestAuditLogout::test_logout_without_token_still_emits_audit tests/integration/test_auth_audit_failure_paths.py::test_logout_without_authorization_returns_204_not_500 tests/integration/test_auth_audit_failure_paths.py::test_logout_without_authorization_returns_204_when_audit_write_fails tests/integration/test_real_api_write_state.py::test_project_api_audits_lifecycle_without_git_url_userinfo_leak tests/integration/test_real_api_write_state.py::test_credentials_api_persists_encrypted_value_rotates_and_soft_deletes tests/integration/test_real_auth_results_artifacts.py::test_real_jwt_created_api_token_authenticates_updates_last_used_and_revokes tests/integration/test_real_auth_results_artifacts.py::test_auth_refresh_logout_audits_do_not_store_tokens_or_passwords tests/integration/test_real_db_persistence.py::test_pipeline_api_persists_rows_and_soft_delete_hides_from_repository` 10 passed；release quality docs contract full 251 passed；targeted ruff passed | 无新增 warning；auth revoke/logout 与真实 project/credential/API-token/logout/pipeline delete 成功路径现在也固定 204 空 body，且 release docs contract 用 AST 扫描禁止任何测试函数只断言 204 而不校验响应内容 | 测试 review 发现 204 status-only 已从核心 API delete 单测扩散到 auth 与真实集成路径；如果接口误返回 body，旧测试仍绿。现升级为全局 204 空 body 防回退契约，避免项目继续积累“只证明状态码”的 204 测试 |
| 2026-05-31 | N/A（API delete/remove 204 空 body 契约） | `tests/unit/test_api/test_credentials.py::test_delete_credential_happy_path tests/unit/test_api/test_environments.py::test_delete_environment_removes_existing_environment_and_audits tests/unit/test_api/test_notifications.py::test_delete_rule tests/unit/test_api/test_pipelines.py::test_delete_pipeline_removes_existing_pipeline_and_audits tests/unit/test_api/test_project_members.py::test_remove_member tests/unit/test_api/test_projects.py::test_delete_project tests/unit/test_api/test_schedules.py::test_delete_schedule` 7 passed；release quality docs contract full 250 passed；targeted ruff passed | 无新增 warning；credential/environment/notification/pipeline/project-member/project/schedule 的 unit delete/remove 成功路径现在固定 204 且 `resp.content == b""`，同时保留 repository 与 audit 副作用断言 | 测试 review 发现这些单测此前只看 204 和 DB/audit 调用；如果 route 误返回 `null`、错误 JSON 或调试 payload，旧测试仍绿。现补齐 204 空 body 契约，避免 delete/remove 单测只证明“状态码和副作用看起来对” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_worker_execute.py --collect-only` 8 tests collected | release quality docs contract full 249 passed；targeted ruff/py_compile passed | 无新增 warning；external-stack worker clone/setup failure 的 artifact 空列表现在固定完整 page=1/per_page=20/total=0/data=[] 响应壳 | 测试 review 发现 clone/setup failure 黑盒用例已证明失败不重试、不泄露 secret、失败日志读面受 RBAC 保护，但 artifact 列表只断言 total=0；如果失败路径 artifact list 响应壳漂移、默认分页参数被改坏或 data 夹带异常内容，旧测试仍绿。现升级为空 artifact 完整分页契约，避免 worker 失败路径只证明“没有 artifact 总数” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_auth_results_artifacts.py::test_run_artifacts_list_paginates_real_db_without_s3_side_effects` 1 passed | release quality docs contract full 248 passed；targeted ruff passed | 无新增 warning；真实 run artifacts 分页 required integration 现在固定 page=2/per_page=2 的完整 ArtifactResponse 窗口、字段集合、越界页完整响应壳，并继续证明 artifact list 是 DB-only、不触发 S3 get/presign | 测试 review 发现该用例此前只比对 id/name/storage_path/size_bytes 列表和空页 page/per_page/total/data 分字段；如果 type/run_id/mime_type/expires_at/created_at 漏传或串错、响应夹带字段、越界页响应壳漂移，旧测试仍绿。现升级为真实 artifact 列表完整分页契约，避免 artifact list required integration 只证明“分页窗口几个字段看起来对” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_audit_events_api.py::test_audit_events_tenant_isolation_and_cross_tenant_ids_return_404` 1 passed | release quality docs contract full 247 passed；targeted ruff passed | 无新增 warning；audit-events 跨租户隔离用例的成功空列表路径现在固定 page=99/per_page=7/total=0/data=[] 完整响应，且仍要求成功空查询只新增 1 条 `audit_events.list` 自审计 | 测试 review 发现该安全集成用例已锁住 404 脱敏和拒绝不写自审计，但 resource_type 空结果只断言 total/data；如果 API 忽略请求分页、per_page 回退默认值或空列表响应壳漂移，旧测试仍绿。现升级为空列表完整响应契约，避免 audit-events 跨租户测试只证明“过滤后没有数据” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_auth_results_artifacts.py::test_auth_tokens_list_is_paginated` 1 passed | release quality docs contract full 246 passed；targeted ruff passed | 无新增 warning；真实 auth API token 空分页现在固定完整 page=99/per_page=2/total=3/data=[] 响应壳 | 测试 review 发现 token 分页第二页已精确，但空页路径仍只断言 data/total；如果 API 忽略请求页码、per_page 回退默认值或分页壳字段漂移，旧测试仍绿。现升级为空页完整响应契约，避免 token 空分页测试只证明“空列表且总数对” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_api_write_state.py::test_project_member_and_credential_lists_are_paginated` 1 passed | release quality docs contract full 245 passed；targeted ruff passed | 无新增 warning；真实 DB credentials 与 project members 空分页现在固定完整 page=99/per_page=2/total=3/data=[] 响应壳 | 测试 review 发现该用例的第二页响应已精确，但空页路径仍只断言 data/total；如果 API 忽略请求页码、per_page 回退默认值或分页壳字段漂移，旧测试仍绿。现升级为空页完整响应契约，避免 credentials/members 空分页测试只证明“空列表且总数对” |
| 2026-05-31 | N/A（Run list exact response/default sort 契约） | `tests/unit/test_api/test_runs.py::test_list_runs` 1 passed；release quality docs contract full 244 passed；targeted ruff passed | 无新增 warning；run list 基础成功路径现在固定完整 RunResponse 分页壳，包含 tenant/project/pipeline/environment/pipeline_name/status/priority/trigger/git/timestamps/summary/error 字段；同时锁住 `page=1/per_page=10 -> offset=0/limit=10`、默认 `run.created_at DESC` 排序、tenant/status filter 和读列表不写 audit | 测试 review 发现该单测此前只断言 `resp.json()["total"] == 1` 和第一条 `status`；如果 RunResponse 漏字段、tenant_id 与过滤租户不一致、默认排序漂移、分页参数传错、或列表读面误写 audit，旧测试仍可能绿。现升级为 Run list exact response/default sort 契约，避免 run list 单测只证明“有一条 queued run” |
| 2026-05-31 | N/A（Run results exact page/RBAC 契约） | `tests/unit/test_api/test_runs.py::test_get_run_results` 1 passed；release quality docs contract full 243 passed；targeted ruff passed | 无新增 warning；run results 基础成功路径现在固定完整 TestResultResponse 分页壳，包含 id/run_id/suite/name/status/duration_ms/error_message/stack_trace/tags/metadata；同时锁住 `page=3/per_page=7 -> offset=14/limit=7`、tenant-scoped run lookup、RBAC `RUN_READ`、run_id filter 和读列表不写 audit | 测试 review 发现该单测此前只断言 `resp.json()["total"] == 0`；如果 route 漏掉 RUN_READ、分页 offset 算错、run_id filter 丢失、响应漏字段/串 run，或读结果列表误写 audit，旧测试仍可能绿。现升级为 Run results exact page/RBAC 契约，避免结果列表单测只证明“能返回一个空分页 total” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_api_write_state.py::test_single_run_cancel_persists_state_redis_event_and_audit_row tests/integration/test_real_api_write_state.py::test_batch_run_apis_persist_state_and_audit_rows` 2 passed | release quality docs contract full 242 passed；targeted ruff passed | 无新增 warning；真实 Run single/batch cancel 与 batch retry 写路径现在固定完整 RunResponse 投影、batch response、DB 终态/cancel timestamps、Redis status stream/hash 完整 payload 与同一 timestamp，并要求 `run.cancel` / `run.batch_cancel` / `run.batch_retry` AuditEvent 的 tenant/user/resource/before_state/after_state 完整匹配；batch retry after_state 精确等于 retry_run_id/source_run_id/status/attempt | 测试 review 发现这两个集成用例此前只抽查 response status、Redis event status/previous 和 audit status；如果 RunResponse 漏字段、status hash 与 stream timestamp 不一致、audit before/after 漏字段或用错版本、审计归属写错、batch retry after_state 错绑新旧 run，旧测试仍可能绿。现升级为 run cancel/batch real API exact response/Redis/audit 契约，避免取消/重试真实写路径测试只证明“状态大概 cancelled 且有两条 audit action” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_api_write_state.py::test_schedule_and_run_apis_persist_next_run_metadata_and_audit_rows` 1 passed | release quality docs contract full 241 passed；targeted ruff passed | 无新增 warning；真实 Schedule create/update/delete 与 manual run 触发组合路径现在固定完整 ScheduleResponse、delete 204 空响应、真实 Schedule 行 next_run/missed_fire_policy 更新、Run metadata/retry_group 入库，以及三条 `schedule.*` AuditEvent 与 `run.trigger` AuditEvent 的 tenant/user/resource/before_state/after_state 完整匹配；schedule delete before_state 精确等于更新后的响应 | 测试 review 发现该集成用例此前虽然已比较 create/update audit payload，但 schedule 响应本身未精确固定、delete audit 只抽 cron/enabled，run.trigger audit 也未验证 tenant/resource 归属；如果 delete before_state 漏字段、update next_run_at 串错、run audit 写错 resource_type/resource_id，旧测试仍可能绿。现升级为 schedule/run real API exact response/audit 契约，避免调度真实写路径测试只证明“schedule 审计大概写了，run 也触发了” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_api_write_state.py::test_notification_rule_api_persists_updates_and_hides_soft_deleted_rule` 1 passed | release quality docs contract full 240 passed；targeted ruff passed | 无新增 warning；真实 Notification rule create/update/delete 写路径现在固定完整 NotificationRuleResponse、email/webhook channel canonical config、delete 204 空响应、repository 软删除不可见，并要求三条 `notification_rule.*` AuditEvent 的 tenant/user/resource 完整匹配；create/update/delete before/after_state 精确等于 API 响应投影：conditions 保留、channels 只留 redacted/count/types，template 只留 redacted/present/length，webhook URL 和模板 secret 不进审计 | 测试 review 发现该集成用例此前只抽查 DB channel config、soft-delete 空列表和 audit channels/template 少量字段；如果响应 channel 形状漂移、template 明文进入审计、audit before_state 用错版本、审计归属写错、delete 响应夹带 body，旧测试仍可能绿。现升级为 notification rule real API lifecycle exact response/audit/no-secret 契约，避免通知规则真实写路径测试只证明“channel 被归一化且审计里 channels 被打码” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_api_write_state.py::test_environment_api_persists_encrypted_env_vars_limits_and_soft_delete` 1 passed | release quality docs contract full 239 passed；targeted ruff passed | 无新增 warning；真实 Environment create/update/delete 写路径现在固定完整 EnvironmentResponse、delete 204 空响应、DB env_vars 非明文、resource_limits 持久化与 repository 软删除不可见；三条 `environment.*` AuditEvent 的 tenant/user/resource 完整匹配，before/after_state 精确等于 API 响应中 env_vars 替换为 `{"redacted": true, "count": 1}` 的投影，且原始/轮换 token 不进入任何审计 payload | 测试 review 发现该集成用例此前只抽查 env_vars 响应、disk_mb、DB 加密和 audit env_vars 红action 片段；如果响应漏字段、resource_limits 响应/DB 不一致、delete 返回 body、audit 归属写错、before/after_state 串错或 token 藏到其它审计字段，旧测试仍可能绿。现升级为 environment real API lifecycle exact response/audit/no-secret 契约，避免环境真实写路径测试只证明“变量加密了且审计里 env_vars 被打码” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_api_write_state.py::test_pipeline_api_persists_audit_states_for_lifecycle` 1 passed | release quality docs contract full 238 passed；targeted ruff passed | 无新增 warning；真实 Pipeline create/update/delete 写路径现在固定三条 `pipeline.*` AuditEvent 的 tenant/user/resource 完整匹配，delete 204 空响应，并要求 create/update/delete before/after_state 精确等于 API 响应的递归脱敏投影：URL userinfo 保留 host/path 后脱敏，secret/token/credential/Authorization 等 key 统一变为 `{"redacted": true}` | 测试 review 发现该集成用例此前虽然覆盖多类敏感字段，但仍是一串字段抽查；如果 audit before_state 漏字段、after_state 夹带额外字段、update/delete 使用错版本响应、审计归属写错，或 tokens 列表/trigger_config 某个分支未脱敏，旧测试仍可能绿。现升级为 pipeline real API lifecycle exact audit projection 契约，避免 pipeline 审计测试只证明“几个敏感字段被处理过” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_api_write_state.py::test_project_member_api_persists_audit_states_for_role_changes` 1 passed | release quality docs contract full 237 passed；targeted ruff passed | 无新增 warning；真实 Project member add/update/remove 写路径现在固定 add/update 完整 ProjectMemberResponse、remove 204 空响应、软删除后 repository 不可见，并要求三条 `project_member.*` AuditEvent 的 tenant/user/resource 完整匹配；add/update after_state 精确等于 API 响应，update/remove before_state 固定 user_id+role，remove after_state 为 None | 测试 review 发现该集成用例此前只看 add 响应 role、DB role/soft-delete、action 集合和少量 audit role 字段；如果 username/email/created_at 响应串错、update 返回旧 role、204 响应夹带 body、审计 resource_type/resource_id 写错或 after_state 只写子集，旧测试仍可能绿。现升级为 project member real API exact response/audit 契约，避免成员真实写路径测试只证明“角色变了且审计里有 role” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_api_write_state.py::test_project_api_audits_lifecycle_without_git_url_userinfo_leak` 1 passed | release quality docs contract full 236 passed；targeted ruff passed | 无新增 warning；真实 Project create/update/delete 写路径现在固定完整 ProjectResponse 字段集合与默认值，update 响应只允许 description/git_url/status/updated_at 变化；三条 `project.*` AuditEvent 均要求 tenant/user/resource 完整匹配，create/update/delete before/after_state 分别精确等于脱敏 git_url 后的响应投影，且 userinfo token 不进入任何审计 payload | 测试 review 发现该用例此前只抽查 API git_url、DB status、审计 action 集合以及审计 git_url/status 片段；如果响应漏字段、默认值漂移、update 改坏不相关字段、审计归属写错、before/after_state 漏字段，或 userinfo token 藏到其它审计字段，旧测试仍可能绿。现升级为 project real API lifecycle exact response/audit/redaction 契约，避免项目真实写路径测试只证明“项目能改删且审计里有脱敏 URL” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_webhook_branch_dedup.py::test_signed_webhook_success_audits_and_protects_reserved_metadata tests/integration/test_webhook_branch_dedup.py::test_webhook_filtered_branch_returns_200_and_creates_no_run` 2 passed | release quality docs contract full 235 passed；targeted ruff passed | 无新增 warning；项目级 signed webhook 真实集成路径现在固定 Run tenant/project/pipeline/environment/triggered_by/git_ref/git_sha/dedup_key 与 reserved metadata 不可覆盖，`run.trigger` 审计完整匹配 tenant/user/resource/before_state/after_state；project-level filtered 分支固定精确 filtered response、no-run 边界和 `webhook.filtered` 审计归属，审计 payload 不含项目 repo URL、攻击者 URL、credential 或 default_branch | 测试 review 发现 signed webhook 成功用例此前只抽查 run trigger_type/git_sha/metadata 和 audit user/after_state，filtered 分支只断言 `resp.json()["status"]`；如果 payload 污染 pipeline/environment/triggered_by、audit 归属写错、before_state 被误填、错误体夹带额外字段，或攻击者 metadata 进入审计，旧测试仍可能绿。现升级为 project webhook reserved metadata integration exact audit/no-leak 契约，避免 webhook 安全测试只证明“创建了 run 或返回 filtered” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_webhook_branch_dedup.py::test_provider_github_push_filtered_branch_uses_api_v1_alias_and_system_audit tests/integration/test_webhook_branch_dedup.py::test_webhook_same_commit_second_trigger_returns_duplicate` 2 passed | release quality docs contract full 234 passed；targeted ruff passed | 无新增 warning；真实 webhook filtered/duplicate 集成路径现在固定 filtered response 与 duplicate response、no-run / single-run 副作用边界，且 `webhook.filtered` / `webhook.duplicate` AuditEvent 的 tenant/user/resource/before_state/after_state 完整等值；审计 payload 不含 repo URL、secret 或 dedup_key | 测试 review 发现此前 filtered 只证明 response/no-run/after_state，duplicate 只证明 200/单 run/部分 audit；如果 audit 归属写错 tenant/resource、before_state 被误填、duplicate dedup_key 泄露或 secret/repo URL 进入审计，旧测试仍可能绿。现升级为 webhook decision audit ownership/no-leak 契约，避免 webhook 分支过滤/去重测试只证明“不建或少建了 run” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_silent_windows.py::test_cron_tick_in_silent_window_skips_run_writes_audit_and_preserves_last_run_at` 1 passed | release quality docs contract full 233 passed；targeted ruff passed | 无新增 warning；真实 schedule silent-window 集成路径现在固定不创建 schedule run、Schedule.last_run_at/last_error 不变、next_run_at 不被推进，并要求 `schedule_skipped_silent_window` AuditEvent 的 tenant/user/resource/before_state/after_state 完整等值；window start/end 必须以 JSON `Z` 时间精确序列化 | 测试 review 发现该真实集成用例此前只抽查 run count、last_run_at、audit schedule_id 和 window reason；如果真实 DB 路径误推进 next_run_at、写错 tenant/resource、before_state 被误填、窗口 start/end 序列化漂移，或漏掉 no-run 证据，旧测试仍可能绿。现升级为 schedule silent-window integration exact no-fire/audit 契约，避免静默窗口集成测试只证明“没新建 run 且审计里有 reason” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_silent_windows.py::test_project_update_api_persists_silent_windows_in_settings` 1 passed | `tests/unit/test_api/test_projects.py::test_update_project_serializes_silent_windows_into_settings` 1 passed；release quality docs contract full 232 passed；targeted ruff passed | 无新增 warning；Project audit state 现在递归脱敏 `settings` 中的敏感 key，真实 silent_windows API 更新路径固定完整 ProjectResponse、DB settings 完整保留 allowed_branches/webhook_secret/silent_windows、`project.update` AuditEvent tenant/user/resource 完整匹配，before/after_state 精确等于脱敏后的 ProjectResponse，且 webhook_secret 原值不进 audit | 测试 review 发现项目审计 `_to_audit_state` 此前只脱敏 git_url userinfo；该集成用例也只抽查 response/settings/audit 中的 reason 与 allowed_branches。如果 `Project.settings.webhook_secret` 被写入 audit、before_state 漏字段、settings 被覆盖，或 silent window 时间字符串漂移，旧测试仍可能绿。现修复 Project settings audit redaction，并升级为 silent_windows integration exact response/settings/audit/no-secret 契约，避免静默窗口入口测试只证明“reason 被放进某个地方” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_environment_env_vars_encryption.py::test_environment_env_vars_aad_mismatch_returns_500_and_audits` 1 passed | release quality docs contract full 231 passed；targeted ruff passed | 无新增 warning；真实 env_vars AAD mismatch 集成路径现在固定 500 body `{"detail": "Environment env vars decrypt failed"}` 且响应不回显 secret；`environment.env_vars_decrypt_failed` audit.event 的 tenant/user/resource/action 精确匹配，before_state 为 None，after_state 精确为 decrypt/project_id/environment_id/error_type=ValueError，并且 failure payload 不含 `TOKEN`、`bound-to-source` 或 source environment id | 测试 review 发现该用例此前只抽查 500、audit action、operation 和明文 value 不出现；如果响应泄露底层错误、audit 丢 tenant/user/resource、environment_id 错写 source id、error_type 漂移，或变量名 `TOKEN` 进审计，旧测试仍可能绿。现升级为 env_vars AAD mismatch integration exact failure audit/no-secret 契约，避免测试只证明“坏 AAD 会 500 且审计大概写了 decrypt” |
| 2026-05-31 | N/A（RunExecutor skipped transition exact structured log 契约） | `tests/unit/test_engine/test_executor.py::TestMarkRunningSkippedLog::test_executor_logs_when_mark_running_skipped tests/unit/test_engine/test_executor.py::TestMarkRunningSkippedLog::test_executor_logs_when_mark_collecting_skipped` 2 passed；release quality docs contract full 230 passed；targeted ruff passed | 无新增 warning；mark_running/mark_collecting cancel-race 日志现在固定各只产生一条 `qaplatform.engine.executor` INFO，message 为 `run_status_transition_skipped`，record.run_id 等于当前 run，from_/to 分别为 preparing→running 与 running→collecting，reason 精确保留 concurrent cancel 语义 | 测试 review 发现这两条用例此前 patch 全局 `logging.Logger.info` 后只取第一条匹配日志并抽查 from_/to；如果 logger 名或级别漂移、重复刷日志、run_id/reason 丢失，旧测试仍可能绿。现升级为 caplog exact structured record 契约，避免 skipped transition 日志测试只证明“某处打过一条同名日志” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_api_endpoints.py::TestProjectsCRUD::test_delete_project` 1 passed | release quality docs contract full 229 passed；targeted ruff passed | 无新增 warning；真实 project delete 集成路径现在固定 204 空响应、软删除行存在、`project.delete` AuditEvent 的 tenant/user/resource 完整匹配，before_state 精确等于删除前 ProjectResponse 但 git_url userinfo 脱敏为 `https://***@example.com/r.git`，after_state 为 None，且审计 payload 不含 git-user、git-pass 或原始 URL | 测试 review 发现该集成用例此前只抽查 audit before_state 的 id/slug；如果 delete 审计丢 tenant/user、before_state 漏字段、after_state 被误写，或含 userinfo 的 git_url 进入审计，旧测试仍可能绿。现升级为 project delete integration exact audit/redacted-url 契约，避免项目删除集成测试只证明“项目被软删且审计里有 id/slug” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_api_endpoints.py::TestNotificationRules::test_delete_notification_rule` 1 passed | release quality docs contract full 228 passed；targeted ruff passed | 无新增 warning；真实 notification rule delete 集成路径现在固定 204 空响应、软删除行存在、`notification_rule.delete` AuditEvent 的 tenant/user/resource 完整匹配，before_state 精确等于完整删除前审计状态：id/project_id/name/enabled/conditions/channels/template/created_at，after_state 为 None，且审计 payload 不含 webhook URL、`webhook_url`、`config` 或 `"url"` | 测试 review 发现该真实集成用例此前只抽查 audit before_state 的 id/channel redacted 和 URL 字符串不出现；如果 audit 漏 tenant/user、before_state 漏 enabled/conditions/template/created_at、after_state 被误写，或 channel config 以别的字段名泄露，旧测试仍可能绿。现升级为 notification rule delete integration exact audit/no-channel-leak 契约，避免通知规则删除集成测试只证明“规则被软删且审计里 URL 字符串没出现” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_auth_audit_failure_paths.py::test_login_nonexistent_user_returns_401_not_500` 1 passed | release quality docs contract full 227 passed；targeted ruff passed | 无新增 warning；真实 unknown-user login 失败路径现在固定 401 body `{"detail": "Invalid credentials"}`，并要求 `auth.login_failed` AuditEvent 的 tenant_id/user_id 均为 None，after_state 精确等于 `{"reason": "invalid_credentials", "username": username}`，且不泄露 password | 测试 review 发现该真实集成用例此前只用 `invalid_credentials in str(row.after_state)`；如果审计 reason 字段名漂移、username 丢失、误写 user_id/tenant_id、夹带 password 或响应体泛化，旧测试仍可能绿。现升级为 login unknown-user exact audit/no-secret 契约，避免 unknown-user 登录失败测试只证明“没有 500 且审计里出现过 invalid_credentials” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_api_write_state.py::test_credentials_api_persists_encrypted_value_rotates_and_soft_deletes` 1 passed | release quality docs contract full 226 passed；targeted ruff passed | 无新增 warning；真实 Credentials create/rotate/delete 集成写路径现在固定 create 与 rotate 响应完整等于 CredentialResponse 元数据：id/project_id/name/type/created_by/created_at，并递归证明响应与三条 audit payload 都不含 `value`、`encrypted_value`、`secret-v1`、`secret-v2` 或当前 ciphertext repr；create/rotate/delete audit 的 before/after_state 也分别精确等于 None/响应体/响应体/None | 测试 review 发现该真实写路径此前只断言 create 响应顶层没有 `"value"`，audit 也靠 `repr` 片段排除少数字符串并抽查 name；如果明文藏到嵌套字段、响应漏掉 created_by/created_at、audit before/after_state 漂移，或 `encrypted_value` 字段进入 payload，旧测试仍可能绿。现升级为 credential real API exact response/no-secret 契约，避免凭据集成测试只证明“数据库加密成功且顶层没看到 value” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_auth_results_artifacts.py::test_refresh_survives_refresh_token_revoke_outage` 1 passed | release quality docs contract full 335 passed；targeted ruff/py_compile passed | 无新增 warning；真实 refresh token revoke Redis outage 路径现在固定 `redis.set` 只按旧 refresh token jti 写 `jwt:revoked:{jti}` 与 `"1"`，TTL `ex` 落在有效范围内；同时把旧 refresh token jti 对应的 `qaplatform.api.v1.auth` WARNING 投影为唯一 `{message,jti,exc_type,exc_message}`：message 为 `token_revoke_failed`，`record.jti` 等于旧 refresh token jti，且 exc_info 保留原始 `RuntimeError("redis revoke unavailable")` | 测试 review 发现该真实集成用例此前只用 `caplog.text` 包含式断言，后来仍靠 `len(records) == 1` 和 `records[0]` 证明唯一 warning；如果 revoke 查错 jti、Redis 写入参数漂移、日志级别漂移、jti extra 丢失、异常上下文被吞掉，或同一路径重复刷 warning，旧测试仍可能绿且失败定位弱。现升级为 refresh revoke outage exact warning/redis direct projection 契约，避免 Redis blip 集成测试只证明“refresh 成功且日志里出现过 token_revoke_failed” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_auth_audit_failure_paths.py::test_login_nonexistent_user_returns_401_when_audit_write_fails tests/integration/test_auth_audit_failure_paths.py::test_refresh_invalid_token_returns_401_when_audit_write_fails tests/integration/test_auth_audit_failure_paths.py::test_logout_without_authorization_returns_204_when_audit_write_fails` 3 passed | release quality docs contract full 224 passed；targeted ruff passed | 无新增 warning；真实 auth audit 写入 outage 三条集成路径现在固定每条只记录一条 `qaplatform.api.v1.auth` WARNING，message 为 `audit_write_failed`，`record.action` 分别是 `auth.login_failed` / `auth.refresh_failed` / `auth.logout`，且 exc_info 保留原始 `RuntimeError("audit store unavailable")` | 测试 review 发现这些真实失败路径此前只用 `caplog.text` 包含式断言；如果 action extra 丢失、日志级别漂移、异常上下文被吞掉，或同一路径重复刷 warning，旧测试仍可能绿。现升级为 integration exact warning 契约，避免 auth audit outage 集成测试只证明“日志里出现过 audit_write_failed” |
| 2026-05-31 | N/A（write_audit failure exact warning 契约） | `tests/unit/test_api/test_audit.py::test_write_audit_swallows_repo_errors` 1 passed；audit unit full 13 passed；release quality docs contract full 223 passed；targeted ruff passed | 无新增 warning；`write_audit` 的 best-effort 失败路径现在固定只记录一条 `qaplatform.infra.audit` WARNING，message 精确包含 action/resource/id，且 exc_info 保留原始 `RuntimeError("audit table is down")`；同时保留完整 `repos.audit.create` kwargs 与不抛异常语义 | 测试 review 发现该用例此前只断言 `caplog.text` 包含 `audit write failed`；如果日志级别漂移、resource id/action 丢失、异常上下文被吞掉，或实现重复刷 warning，旧测试仍可能绿。现升级为 failure exact warning 契约，避免审计失败测试只证明“日志里出现过一个大概的错误片段” |
| 2026-05-31 | N/A（Cancel watcher handler exception exact continuation/log 契约） | `tests/unit/test_engine/test_cancel.py::TestWatchForCancel::test_swallows_handler_exceptions` 1 passed；cancel full 16 passed；release quality docs contract full 222 passed；targeted ruff passed | 无新增 warning；cancel watcher 的 handler 异常路径现在固定第一条 cancel 抛 `RuntimeError("boom")` 后 task 仍未结束，第二条 cancel 继续触发无参 callback，最终 `qaplatform.engine.cancel` ERROR 投影唯一，message 精确为 `cancel handler raised for run r` 且 exc_info 保留原始异常 | 测试 review 发现该用例此前只断言 `caplog.text` 包含错误文案；如果 handler 异常后 watcher 提前退出、重复刷错误日志、日志级别漂移，或异常上下文被吞掉，旧测试仍可能绿。现升级为 handler exception exact continuation/log 契约，避免取消 watcher 异常测试只证明“日志里有一句话且任务没炸” |
| 2026-05-31 | N/A（Auth revoked access middleware route exact rejection 契约） | `tests/unit/test_auth/test_auth_routes.py::TestRevokedTokenMiddleware::test_revoked_access_token_returns_401` 1 passed；auth routes full 57 passed；release quality docs contract full 221 passed；targeted ruff passed | 无新增 warning；路由级 revoked access token 中间件用例现在固定 401 body `{"detail": "Token has been revoked"}`、Redis blacklist exact `jwt:revoked:{jti}` 查询、不打开 DB session，且响应不回显 access token 或 jti | 测试 review 发现该用例此前只断言 detail 包含 `revoked`；如果实现查错 jti、拒绝路径误进 DB、错误体泛化，或把 token/jti 泄露到响应文本，旧测试仍可能绿。现升级为 route exact rejection/no-leak 契约，避免 revoked access route 测试只证明“返回了某个含 revoked 的 401” |
| 2026-05-31 | N/A（Run status event previous exact stream/hash 契约） | `tests/unit/test_engine/test_events.py::test_publish_status_event_writes_stream_and_hash` 1 passed；events full 4 passed；release quality docs contract full 220 passed；targeted ruff passed | 无新增 warning；带 previous 的 status event 主路径现在固定 xadd stream 与完整 payload dict、UTC timestamp 请求窗口、previous 值、hset status hash 与 stream event 复用同一个 timestamp | 测试 review 发现该用例此前只逐字段抽查 run_id/status/previous 且 timestamp 只证明有时区；如果 stream payload 夹带/漏掉字段、timestamp 使用非 UTC 或陈旧时间，或 hash timestamp 与 event timestamp 分叉，旧测试仍可能绿。现升级为 previous exact stream/hash 契约，避免状态事件主路径只证明“写了 running/preparing 这几个字段” |
| 2026-05-31 | N/A（Run status event Redis failure exact log/no-hash 契约） | `tests/unit/test_engine/test_events.py::test_publish_status_event_swallows_redis_errors` 1 passed；events full 4 passed；release quality docs contract full 219 passed；targeted ruff passed | 无新增 warning；Redis status event 失败分支现在固定 xadd stream 与完整 payload dict、UTC timestamp 请求窗口、hset 不执行，并要求 warning 投影唯一，message 精确包含 run id/status 且 exc_info 保留原始 `RuntimeError("redis down")` | 测试 review 发现该用例此前只用 `caplog.text` 包含式证明写过一段 warning；如果事件 timestamp 退成本地时区、日志重复刷屏、status 从日志里丢失，或异常上下文被吞掉，旧测试仍可能绿。现升级为 exact log/no-hash 契约，避免 Redis publish 失败测试只证明“没有抛异常且日志里有 run id” |
| 2026-05-31 | N/A（API token last_used success exact auth flow 契约） | `tests/unit/test_auth_middleware.py::test_authenticate_api_token_returns_current_user_and_updates_last_used` 1 passed；auth middleware full 45 passed；release quality docs contract full 218 passed；targeted ruff passed | 无新增 warning；`_authenticate_api_token` 的成功路径现在固定 token id 查询、`TokenService.verify_token(API_TOKEN_SECRET, token.secret_hash)`、完整 `CurrentUser` 等值、同一 token 的 `update_last_used(token, utc_datetime)` 时间窗口与空 kwargs、commit/no rollback | 测试 review 发现该用例此前只逐字段抽查 user、只证明 last_used 有 timezone 且 commit 被 await；如果成功身份漏 scopes/admin flag、last_used 使用非 UTC 或额外 kwargs、成功路径误 rollback，旧测试仍可能绿。现升级为 success exact auth flow 契约，避免 API token 成功认证测试只证明“返回用户字段大概对且更新过 last_used” |
| 2026-05-31 | N/A（GitHub provider webhook success exact route 契约） | `tests/unit/test_api/test_p3.py::TestProviderWebhookTrigger::test_provider_webhook_success_uses_system_identity_and_metadata` 1 passed；P3 full 60 passed；release quality docs contract full 217 passed；targeted ruff passed | 无新增 warning；GitHub provider webhook 成功路径现在固定 response body、repository URL 候选精确集合、dedup 预查、`run.create` 完整 kwargs、`set_retry_group_id`、`enqueue_run` 五个位置参数与空 kwargs，以及系统身份 `run.trigger` audit 的完整 payload | 测试 review 发现该用例此前只抽查一个 repo URL 候选、若干 `run.create` 字段和 audit action/resource/user；如果 provider URL 匹配面扩大、系统身份触发人漂移、metadata/dedup 入库错位、入队漏 settings，或 audit after_state 不等于响应体，旧测试仍可能绿。现升级为 exact route 契约，避免 GitHub provider webhook 成功测试只证明“201 且几个字段看起来对” |
| 2026-05-31 | N/A（Schedule silent-window audit exact payload 契约） | `tests/unit/test_worker/test_check_schedules.py::TestCheckSchedules::test_skips_project_silent_window_without_creating_run` 1 passed；check_schedules full 9 passed；release quality docs contract full 216 passed；targeted ruff passed | 无新增 warning；项目级 silent window 分支现在固定不查环境、不创建 run、不入队、不更新 fire 时间，且 `schedule_skipped_silent_window` audit 的 tenant/user/action/resource/before_state/after_state 完整等值，窗口 start/end 以 JSON `Z` 时间序列化 | 测试 review 发现该用例此前只抽查 audit action、schedule_id 和 window reason；如果审计丢 before_state、写错 resource、窗口 start/end 序列化漂移，或 after_state 夹带/漏掉字段，旧测试仍可能绿。现升级为 exact payload，避免 silent-window 单测只证明“跳过时写过一条大概对的审计” |
| 2026-05-31 | N/A（E2E UI row/cell exact text 契约） | `npx playwright test tests/e2e/auth-flow.spec.ts --project=chromium` 1 passed；`npx playwright test tests/e2e/auth-flow.spec.ts tests/e2e/real-login-flow.spec.ts --project=chromium --list` 2 tests listed；targeted quality contracts passed；targeted docs contract passed；targeted ruff passed | 仅有 `NO_COLOR` / `FORCE_COLOR` 环境提示；E2E spec 现在禁止 `.toContainText(` 回流，mock auth-flow 的 Recent Runs 与 Settings token 表格改为逐 `td` exact text 断言，real-login-flow 项目卡片改为标题可见性断言 | 测试 review 发现 mocked UI 冒烟大量使用 `toContainText`，只能证明目标词在同一行/卡片里出现过；如果状态/优先级/分支/权限范围渲染到错误列、行里夹带重复或冲突文案，旧 E2E 仍可能绿。现升级为行内单元格精确契约，避免 PR 级 E2E 只证明“某行里有这些词” |
| 2026-05-31 | N/A（Webhook signature compare_digest runtime 契约） | `tests/unit/test_api/test_webhook_signature.py::TestTimingSafety` 2 passed；webhook signature full 15 passed；release quality docs contract full 215 passed；targeted ruff passed | 无新增 warning；constant-time compare 测试现在 runtime patch `qaplatform.infra.webhook_signature.hmac.compare_digest`，固定 `verify_webhook_signature(secret, payload, supplied)` 会以 `(expected, supplied)` 调用 compare_digest；空 secret 早退路径固定不触发 compare_digest | 测试 review 发现该用例此前用 `inspect.getsource` 查找 `"compare_digest"` 字符串；如果字符串出现在注释、死代码或错误包装里，旧测试仍可能绿。现升级为运行时调用契约，避免 webhook 签名 timing safety 测试只证明“源码文本里出现过 compare_digest” |
| 2026-05-31 | N/A（Security headers exact map/HSTS 契约） | `tests/unit/test_api/test_security_headers.py` 2 passed；release quality docs contract full 214 passed；targeted ruff passed | 无新增 warning；SecurityHeadersMiddleware 现在用 `EXPECTED_SECURITY_HEADERS` 固定无 HSTS 时完整 header map，开启 HSTS 时固定完整 map 加 `Strict-Transport-Security: max-age=31536000; includeSubDomains` | 测试 review 发现该文件此前用 14 条用例重复检查单个 header 与 CSP substring；如果响应多/少 header、CSP 夹带额外 directive、HSTS 开关污染默认路径，旧的 substring/单字段测试仍可能绿且制造覆盖数量错觉。现收敛为 exact map/HSTS 契约，避免 security headers 测试只证明“几个安全头片段存在” |
| 2026-05-31 | N/A（Lifespan shutdown close/no-cancel exact 契约） | `tests/unit/test_lifespan.py::TestLifespanShutdownNoCancelAll::test_shutdown_does_not_cancel_concurrent_tasks tests/unit/test_lifespan.py::TestLifespanShutdownNoCancelAll::test_shutdown_calls_container_close tests/unit/test_lifespan.py::TestLifespanShutdownNoCancelAll::test_shutdown_does_not_raise_with_no_tasks` 3 passed；lifespan full 9 passed；release quality docs contract full 213 passed；targeted ruff passed | 无新增 warning；lifespan shutdown 现在固定不取消并发任务且任务仍 pending，由测试手动 cancel 收尾，并固定 `container.close()` 以零参数被 await 一次 | 测试 review 发现 shutdown 用例此前只证明 concurrent task 未进入 CancelledError、close 被 await 一次；如果 close 调用夹带参数、无任务路径和并发路径表现不一致，或实现把任务提前完成但不抛 CancelledError，旧测试仍可能绿。现升级为 close/no-cancel exact 契约，避免 shutdown 测试只证明“看起来没有 cancel-all 且 close 被碰过” |
| 2026-05-31 | N/A（Plugin registry discover failure exact continuation 契约） | `tests/unit/test_plugins/test_registry.py::test_discover_registers_loadable_entry_points_and_continues_after_failures` 1 passed；plugin registry full 7 passed；release quality docs contract full 212 passed；targeted ruff passed | 无新增 warning；plugin discovery 现在固定 `entry_points(group="qaplatform.plugins")`、broken/runner 两个 entry point 都只 load 一次、broken 只记录 ERROR 且不污染 registry，runner 继续注册为唯一 runner 并记录成功日志 | 测试 review 发现该用例此前只证明失败后仍能 `get_runner("dummy-runner")`；如果 discovery group 漂移、broken entry point 被重复加载、失败污染 collector/source registry，或成功/失败日志退化，只要 runner 最终存在旧测试仍可能绿。现升级为 failure exact continuation 契约，避免插件发现测试只证明“某个可用插件最后能取到” |
| 2026-05-31 | N/A（Frontend run terminal polling/archive 契约） | `tests/unit/test_frontend_api_contract.py` 35 passed；frontend build passed；release quality docs contract full 211 passed；targeted ruff passed | 无新增 warning；Run view model 现在保留 `is_terminal`，由后端终态 `done/failed/cancelled/timeout` 派生；run detail 轮询和 archived logs 开关使用该字段，不再用 UI `status` 判断 | 测试 review 发现后端 `done + total=0` 会被前端显示为 `Unknown`，但这仍是终态；旧 Hook 用归一化 UI status 判断轮询和归档日志，会让 empty collector 详情页持续 5s 轮询且不启用 archived logs。现把展示状态与后端终态分离，避免 Hook 测试只证明列表行显示 Unknown，却漏掉详情页行为 |
| 2026-05-31 | N/A（Heartbeat Redis set exact UTC/arity 契约） | `tests/unit/test_worker/test_tasks.py::TestHeartbeatLoop::test_heartbeat_writes_reclaimer_key_with_ttl_and_utc_timestamp tests/unit/test_worker/test_tasks.py::TestHeartbeatLoop::test_heartbeat_continues_after_redis_error` 2 passed；worker tasks full 19 passed；release quality docs contract full 210 passed；targeted ruff passed | 无新增 warning；heartbeat 写 Redis 现在复用 helper 固定每次 `redis.set` 只有 key/timestamp 两个 positional 参数、key shape、UTC offset `+00:00` timestamp，以及唯一 kwargs `{"ex": 90}` | 测试 review 发现 heartbeat 用例此前只验证 timestamp 有时区、单次路径只抽查 `ex` 值；如果实现写成本地时区、给 redis.set 夹带额外参数，或 TTL kwargs 多出字段，只要 key 与 `ex` 还在旧测试仍可能绿。现升级为 exact UTC/arity 契约，避免 heartbeat 测试只证明“写了一个带时区的心跳键” |
| 2026-05-31 | N/A（Worker active run PipelineConfig exact projection 契约） | `tests/unit/test_worker/test_tasks.py::TestArchiveBlocking::test_worker_executes_active_project` 1 passed；worker tasks full 19 passed；release quality docs contract full 209 passed；targeted ruff passed | 无新增 warning；active project 执行入口现在固定 `executor.execute(run, config)` 的无 kwargs 双参数调用，并把 PipelineConfig 投影为完整等值：image/stages/env_vars/resource_limits/network/timeout/setup_script/collectors/source_auth 全部锁住 | 测试 review 发现该用例此前只抽查 image、timeout、network、memory/cpu、第一个 collector 与 source_auth；如果 env_vars/setup_script/default artifact limit 漂移、collector 列表夹带额外项、或 executor 调用夹带 kwargs，只要被抽查字段仍对旧测试仍可能绿。现升级为 exact projection 契约，避免 worker active run 测试只证明“核心字段大概能传给 executor” |
| 2026-05-31 | N/A（Health uptime numeric seconds 精确契约） | `tests/unit/test_api/test_health.py` 6 passed；release quality docs contract full 208 passed；targeted ruff passed | 无新增 warning；`/health` 与 `/ready` 的 uptime 现在固定为两位小数的非负秒数字符串，必须带 `s` 后缀且整数/小数部分都可解析 | 测试 review 发现 health/readiness 用例此前只证明 `uptime` 以 s 结尾；如果实现返回 `oks`、`nans`、`infs`、缺小数精度或带非法数值，只要后缀还在旧测试仍可能绿。现升级为 numeric seconds 契约，避免运维健康检查测试只证明“字段像个秒单位字符串” |
| 2026-05-31 | N/A（worker-backed E2E summary 精确契约） | `npx playwright test tests/e2e/real-run-trigger.spec.ts --project=chromium --list` 1 test listed；release quality docs contract full 207 passed；targeted ruff passed | 无新增 warning；worker-backed E2E 现在固定真实 worker 终态 summary 必须精确等于 `{total:1, passed:1, failed:0, skipped:0, error:0, pass_rate:1}`，不再接受 `toMatchObject` 的超集响应 | 测试 review 发现该 E2E 已证明 worker run `done`、结果和 artifact 可见，但 summary 仍只抽查 total/passed/failed/error；如果 collector summary 漏掉 skipped/pass_rate、夹带错误聚合字段，或后端 summary schema 漂移，旧 E2E 仍可能绿。现把 summary 也纳入精确证据，避免 worker-backed E2E 只证明“几个计数看起来对” |
| 2026-05-31 | N/A（special-regressions filtered webhook response 精确契约） | `npx playwright test tests/e2e/special-regressions.spec.ts --project=chromium --list` 7 tests listed；release quality docs contract full 207 passed；targeted ruff passed | 无新增 warning；webhook branch-filter E2E 现在固定 filtered 响应必须精确等于 `{status:"filtered", reason:"branch_not_allowed"}`，不再接受 `toMatchObject` 的超集响应 | 测试 review 发现该 E2E 已验证 filtered branch 不出现在 Runs UI，但 API 断言仍只证明两个字段存在；如果后端开始夹带 run id、dedup key、内部 reason 或其它 schema 漂移，旧 E2E 仍可能绿。现和 unit/integration 的 exact webhook 响应契约对齐，避免 branch-filter E2E 只证明“看起来被过滤了” |
| 2026-05-31 | N/A（LogStream truncation exact byte/prefix 契约） | `tests/unit/test_engine/test_log_stream.py::TestLogStreamLineTruncation::test_oversize_line_truncated_with_marker tests/unit/test_engine/test_log_stream.py::TestLogStreamLineTruncation::test_truncation_preserves_utf8_boundary tests/unit/test_engine/test_log_stream.py::TestLogStreamLineTruncation::test_write_batch_truncates_each_entry` 3 passed；log stream full 18 passed；release quality docs contract full 207 passed；targeted ruff passed | 无新增 warning；LogStream 单条 ASCII、UTF-8 多字节和 batch stderr 截断现在固定 exact 输出：保留前缀到 `_MAX_LINE_BYTES - len(_TRUNCATION_MARKER)` byte budget，追加 `_TRUNCATION_MARKER`，ASCII 路径正好 4096 bytes，UTF-8 路径必须停在完整字符边界，batch 第二条 xadd payload 也必须等于同一 exact truncation | 测试 review 发现截断用例此前只断言 `len(...encode()) <= 4096` 和 `endswith("...[truncated]")`；如果实现保留尾部、保留内容过短、marker 预算计算漂移，或 batch 只是“某条 line 带 marker”，旧测试仍可能绿。现升级为 exact byte/prefix 契约，避免日志截断测试只证明“结果不超长且看起来被截断了” |
| 2026-05-31 | N/A（Worker pipeline collector exact list 契约） | `tests/unit/test_worker/test_tasks.py::test_build_pipeline_config_maps_pipeline_collectors tests/unit/test_worker/test_tasks.py::test_build_pipeline_config_defaults_to_junit_collector` 2 passed；worker tasks full 19 passed；release quality docs contract full 206 passed；targeted ruff passed | 无新增 warning；PipelineConfig collectors 映射现在固定自定义 junit 与 disabled coverage collector 的完整顺序列表，并证明 collectors=None 只生成一个默认 junit collector | 测试 review 发现 worker collector 映射用例此前只检查 `config.collectors[0]`；如果实现丢掉第二个 collector、忽略 `enabled=False`、或默认分支额外追加 collector，只要第一个 junit 仍相似旧测试仍可能绿。现升级为 collector exact list 契约，避免 worker collector 映射测试只证明“第一个 junit 看起来对” |
| 2026-05-31 | N/A（Pipeline nested payload exact persistence 契约） | `tests/unit/test_api/test_pipelines.py::test_create_pipeline_persists_nested_payload_and_writes_audit tests/unit/test_api/test_pipelines.py::test_update_pipeline_translates_partial_nested_updates` 2 passed；pipeline full 28 passed；release quality docs contract full 205 passed；targeted ruff passed | 无新增 warning；pipeline create/update 嵌套 payload happy path 现在固定 repository create/update 的完整 kwargs，包括 stages/selector/trigger_config/collectors/retry_policy/enabled 默认壳、raw credentialed clone URL、响应体回显配置，以及审计 after_state 继续脱敏 userinfo/token | 测试 review 发现这些用例此前主要抽查少量 create/update 字段，create 的 clone URL 只用 `endswith("@git.example/repo.git")`；如果实现把持久化 trigger_config 提前脱敏、漏掉 selector/trigger 默认字段、漏写 enabled/retry_policy，或响应体配置和 repo 入参不一致，旧测试仍可能绿。现升级为 exact persistence/redaction 契约，避免 pipeline 嵌套 payload 测试只证明“几个字段和脱敏片段看起来对” |
| 2026-05-31 | N/A（JWT blacklist lookup exact jti/fail-open 契约） | `tests/unit/test_auth_middleware.py::test_authenticate_jwt_passes_when_blacklist_lookup_fails tests/unit/test_auth_middleware.py::test_authenticate_jwt_rejects_when_token_actually_revoked` 2 passed；auth middleware full 45 passed；release quality docs contract full 204 passed；targeted ruff passed | 无新增 warning；JWT blacklist Redis 故障降级路径现在固定 `JWTService.is_revoked(jti)` 的 exact await 参数，并用 dataclass 等值锁住 legacy no-DB container 下返回的 CurrentUser；真实 revoked 路径固定同一 `jti` 查询、401 `Token has been revoked` 与 no DB session | 测试 review 发现这两条用例此前分别只断言返回对象字段、detail 包含 `revoked`；如果实现拿错 jti 查 blacklist、revoked 错误体漂移，或 fail-open 返回的用户身份字段只局部相似，旧测试仍可能绿。现升级为 blacklist exact jti/fail-open 契约，避免 JWT 黑名单测试只证明“Redis 被碰过/错误里有 revoked” |
| 2026-05-31 | N/A（RunExecutor mark_collecting race exact publish 契约） | `tests/unit/test_engine/test_executor.py::TestMarkRunningSkippedLog::test_executor_does_not_publish_collecting_when_mark_collecting_returns_false` 1 passed；executor full 42 passed；release quality docs contract full 203 passed；targeted ruff passed | 无新增 warning；mark_collecting 取消竞争用例现在固定只发布一次 `RUNNING` status event，参数必须是 `(redis=None, run_id, running, previous=preparing)`，同时锁住 `mark_collecting(run_id)`、不写 finish/fail 终态、不输出 `Collecting test results...` 日志 | 测试 review 发现该用例此前只用 `publish_status_event.assert_awaited_once()` 和 `args[2] == running`；如果事件 run_id/previous 漂移、误发布 collecting、mark_collecting 查错 run，或取消竞争后仍进入收集/终态写入，只要最后一次抽到 running 旧测试仍可能绿。现升级为 exact publish/no-terminal 契约，避免 executor 取消竞争测试只证明“发布过一个 running 事件” |
| 2026-05-31 | N/A（Notification existing delivery idempotent short-circuit 契约） | `tests/unit/test_worker/test_notifications.py::TestEvaluateAndNotify::test_existing_delivery_log_skips_duplicate_send` 1 passed；worker notifications full 35 passed；release quality docs contract full 202 passed；targeted ruff passed | 无新增 warning；existing delivery log 幂等分支现在固定先查 delivery key，命中后不加载 project_name、不发送 channel、不创建新 NotificationLog，并保持单次 commit 结束事务 | 测试 review 发现该用例此前只证明不 send、不 create；如果实现把 delivery 去重放到模板渲染或 project lookup 之后，旧测试仍可能绿但会引入不必要查询、未知模板变量噪声或重复评估副作用。现升级为 idempotent short-circuit 契约，避免重复通知测试只证明“最后没有再次发送” |
| 2026-05-31 | N/A（Migration 007 env_vars upgrade exact envelope 契约） | `tests/unit/test_migrations/test_007_encrypt_environment_env_vars.py::test_upgrade_encrypts_plain_env_vars_and_skips_existing_envelopes` 1 passed；migration 007 full 2 passed；release quality docs contract full 201 passed；targeted ruff passed | 无新增 warning；env_vars 加密迁移 upgrade 用例现在固定只更新 plain row 的 id 序列，写回 env_vars 字段集合必须精确等于 `__encrypted__` + `ciphertext`，marker 必须是 `qaplatform.env_vars.v1`，ciphertext 必须是非空字符串，并继续证明 envelope 可解密且不回显明文 key/value | 测试 review 发现该迁移用例此前只用 `len(conn.updates) == 1` 和解密回读证明“更新过一次”；如果迁移额外更新已加密行、写回 envelope 夹带多余字段、marker 漂移，或测试只看到一次更新但不是目标 plain row，旧测试定位不够敏感。现升级为 exact envelope/update-id 契约，避免迁移测试只证明“有一条环境变量能 roundtrip” |
| 2026-05-31 | N/A（Worker auto retry waiting exact order 契约） | `tests/unit/test_worker/test_auto_retry.py::TestAttemptRetry::test_waiting_retry_run_counts_as_scheduled` 1 passed；auto retry full 24 passed；release quality docs contract full 201 passed；targeted ruff passed | 无新增 warning；auto retry waiting-capacity 分支现在固定 get_by_id 原 run id、refresh pipeline、retry run create 完整参数、enqueue 返回 False 仍算 scheduled，以及 create -> enqueue(waiting) -> commit 顺序 | 测试 review 发现该用例此前只断言 result/create/enqueue/commit 各自发生；如果实现提交顺序漂移、跳过 pipeline refresh、查错 run id，或在 waiting enqueue 后漏提交，只要几个 mock 仍被碰过旧测试仍可能绿。现升级为 waiting exact order 契约，避免自动重试 waiting 分支测试只证明“waiting 分支碰过几个 mock” |
| 2026-05-31 | N/A（Run status event no-previous exact stream/hash 契约） | `tests/unit/test_engine/test_events.py::test_publish_status_event_omits_previous_when_none` 1 passed；events full 4 passed；release quality docs contract full 199 passed；targeted ruff passed | 无新增 warning；无 previous 的 queued status event 现在固定完整 xadd stream 与 payload dict、timezone-aware timestamp、hset status hash 与 stream event 复用同一个 timestamp | 测试 review 发现该用例此前只检查 `"previous" not in event`；如果 stream key 漂移、run_id/status/timestamp 丢失、hset 不写或使用另一 timestamp，只要 previous 缺失旧测试仍可能绿。现升级为 exact stream/hash 契约，避免 no-previous 状态事件测试只证明“payload 没有 previous 字段” |
| 2026-05-31 | N/A（OpenTelemetry FastAPI exact instrumentation/span 契约） | `tests/unit/test_observability/test_tracing.py::test_instrument_fastapi_app_once tests/unit/test_observability/test_tracing.py::test_instrument_fastapi_records_route_status_and_excludes_ops_paths` 2 passed；tracing full 8 passed；release quality docs contract full 198 passed；targeted ruff passed | 无新增 warning；FastAPI tracing 单测现在固定 `FastAPIInstrumentor.instrument_app(app, ...)` 的完整 kwargs 字段集合、excluded_urls、server_request_hook、request header capture/sanitize 配置和 app 只装配一次；真实 TestClient span 用例把 server spans 投影为 exact list，只允许 `/items/{item_id}` 产生一个 GET/200 span，且 captured request headers 必须为空 | 测试 review 发现这些用例此前只抽查 metrics exclusion、authorization sanitize 和 `len(...) == 1`；如果 health/ready/metrics 排除正则漂移、strip hook 丢失、header capture 被打开，或 ops 路径也产出 server span，只要局部字段还在旧测试仍可能绿。现升级为 exact instrumentation/span projection 契约，避免 tracing 测试只证明“FastAPI tracing 大概装上了” |
| 2026-05-31 | N/A（SSE ticket atomic result exact projection 契约） | `tests/unit/test_api/test_sse.py::test_authenticate_sse_ticket_consumes_atomically` 1 passed；sse full 23 passed；release quality docs contract full 197 passed；targeted ruff passed | 无新增 warning；并发复用同一个 SSE ticket 的用例现在把 `asyncio.gather` 的两个返回结果投影为 exact unordered list，固定一个 401 `Invalid or expired SSE ticket` 和一个完整 UserIdentity(user_id/role/tenant_id/scopes)，同时继续固定两次 `getdel` key、no `get`/`delete` fallback 和错误响应不回显 ticket | 测试 review 发现该用例此前先按类型分桶再用 `len(successes) == 1` / `len(failures) == 1` 证明数量；维护时容易退回只证明“一成一败”，而不是精确证明成功身份、失败错误体、无意外返回类型和 ticket 防泄漏。现升级为 exact result projection 契约，避免 SSE 并发 ticket 测试只为覆盖 atomic 分支而存在 |
| 2026-05-31 | N/A（RunExecutor artifact limit exact storage 契约） | `tests/unit/test_engine/test_executor.py::TestUploadArtifacts::test_upload_skips_artifacts_over_size_limit tests/unit/test_engine/test_executor.py::TestUploadArtifacts::test_upload_skips_artifacts_over_count_limit` 2 passed；executor full 42 passed；release quality docs contract full 196 passed；targeted ruff passed | 无新增 warning；artifact size/count limit 两条路径现在固定唯一 S3 upload 的完整 kwargs：bucket、完整 storage key、Body 本地文件，以及 artifact row 的 run_id/type/name/storage_path/size_bytes/mime_type exact 参数；跳过日志继续固定顺序和 stderr | 测试 review 发现这两条用例此前只用 `Key.endswith(...)` 和局部 artifact 字段断言，后续复扫又发现 kwargs 仍靠字段集合加分散断言；如果上传到错误 run key、Body 指向错文件、put_object 夹带多余参数、mime type 漂移，或 artifact row storage_path 与真实上传 key 不一致，只要文件名后缀和几个字段还在旧测试仍可能绿。现升级为 exact storage 契约，避免 artifact limit 测试只证明“超限文件被跳过且另一个文件大概上传” |
| 2026-05-31 | N/A（Project creator membership exact session add/flush 契约） | `tests/unit/test_api/test_projects.py::test_create_project_adds_creator_as_project_admin` 1 passed；projects full 59 passed；release quality docs contract full 195 passed；targeted ruff passed | 无新增 warning；project create 自动成员用例现在固定 session 只 `add` 一个 `ProjectMemberORM`，随后立刻 `flush`，并精确锁住 role/user_id/project_id/tenant_id/deleted_at | 测试 review 发现该回归用例此前用 `len(members) == 1` 从所有 session.add 对象里过滤 ProjectMember；如果实现额外 add 其他对象、漏掉 flush，或 role 依赖字符串转换后才看起来像 admin，旧测试表达不够敏感。现升级为 exact session add/flush 契约，避免“创建者自动成为项目管理员”测试只证明“某处出现过一个成员对象” |
| 2026-05-31 | N/A（Project credential binding update exact response/audit 契约） | `tests/unit/test_api/test_projects.py::test_update_project_validates_git_credential_binding` 1 passed；projects full 59 passed；release quality docs contract full 194 passed；targeted ruff passed | 无新增 warning；Project update 绑定 Git credential 成功路径现在固定完整 ProjectResponse、tenant-scoped project lookup、credential lookup exact 参数、`project.update(project, git_auth_method="token", credential_id=...)`，以及 `project.update` audit before/after 完整 payload | 测试 review 发现该用例此前只抽查 `update.await_args.kwargs["credential_id"]`；如果实现漏写 `git_auth_method`、响应 DTO 漏字段、审计缺失/漂移，或 project lookup tenant scope 回退，只要 credential_id 仍被传入旧测试仍可能绿。现升级为 exact response/audit 契约，避免凭据绑定成功测试只证明“credential_id 被更新” |
| 2026-05-31 | N/A（DockerBackend create execution exact config 契约） | `tests/unit/test_engine/test_docker_backend.py::TestDockerBackend::test_create_execution` 1 passed；docker backend full 33 passed；release quality docs contract full 193 passed；targeted ruff passed | 无新增 warning；DockerBackend create_execution 基础路径现在固定 `containers.create_or_replace` 的完整 kwargs，包括容器 name、Image/Cmd/Env/User、stdout/stderr attach、完整 HostConfig 沙箱字段集合、Tmpfs 和 Labels；默认路径还通过 dict 等值证明没有多余 StorageOpt | 测试 review 发现该用例此前虽然覆盖了很多字段，但仍是逐字段抽查；如果 HostConfig 额外开放字段、漏掉未抽查字段、Labels 字段集合漂移，或 `create_or_replace` 夹带额外 kwargs，只要被抽查字段仍对旧测试仍可能绿。现升级为 exact config 契约，避免 Docker 创建测试只证明“关键字段大概都在” |
| 2026-05-31 | N/A（Project duplicate slug exact short-circuit 契约） | `tests/unit/test_api/test_projects.py::test_create_project_duplicate_slug` 1 passed；projects full 59 passed；release quality docs contract full 192 passed；targeted ruff passed | 无新增 warning；Project create 重复 slug 拒绝路径现在固定完整 409 body、tenant-scoped `project.get_by_slug(tenant_id, "dup-slug")`，并证明 credential lookup、project create 和 audit 均在查重命中后短路 | 测试 review 发现该用例虽然已有 409/no-create/no-audit，但此前只断言 `get_by_slug` 被 await 一次和 `detail` 子字段；如果实现漏掉 tenant scope 改成宽查、先做 credential 校验、或错误响应夹带额外字段，只要状态码和 detail 文本还在旧测试仍可能绿。现升级为 exact short-circuit 契约，避免重复 slug 测试只证明“返回 409 且大概查过 slug” |
| 2026-05-31 | N/A（Audit events direct duplicate weak-test 清理） | `tests/unit/test_api/test_audit.py::test_audit_events_list_applies_filters_pagination_and_self_audits_summary tests/unit/test_api/test_audit.py::test_audit_events_list_masks_cross_tenant_matches_without_self_audit` 2 passed；audit API full 13 passed；release quality docs contract full 191 passed；targeted ruff passed | 无新增 warning；删除 `tests/unit/test_api/test_audit_events.py` 中直接调用 `list_audit_events()` 的两条重复弱测，T02 验收档案现在只指向更强的 HTTP route 单元契约；保留的 `test_audit.py` 已固定完整分页 body、完整 AuditEventResponse item、tenant/user/resource 自审计 payload、跨租户 404 envelope 和拒绝路径不写自审计 | 测试 review 发现 direct function 用例和 `test_audit.py` 的 HTTP route 用例覆盖同一路径，但前者只抽查 response total/id/after_state 与 audit action/resource_type/after_state；如果 ErrorResponse 壳、permission/dependency 接线、audit tenant/user/resource_id、响应字段集合或自审计 no-data 边界漂移，弱重复测试可能仍绿并制造“审计查询已有两套单测”的覆盖率幻觉。现删除弱重复测，避免 audit-events 测试为数量和覆盖率服务 |
| 2026-05-31 | N/A（Auth login tenant lookup/last_login exact 契约） | `tests/unit/test_auth/test_auth_routes.py::TestLogin::test_login_success tests/unit/test_auth/test_auth_routes.py::TestLogin::test_login_wrong_password tests/unit/test_auth/test_auth_routes.py::TestLogin::test_login_user_not_found tests/unit/test_auth/test_auth_routes.py::TestLogin::test_login_inactive_user` 4 passed；auth routes full 57 passed；release quality docs contract full 188 passed；targeted ruff passed | 无新增 warning；登录成功、错密码、用户不存在、停用用户四条分支现在固定 `_resolve_tenant_id(..., None)` 后必须以 resolver 返回的 tenant_id 调用 `user_repo.get_by_username`；成功分支还固定 last_login timestamp 落在请求窗口且 timezone-aware | 测试 review 发现这些用例此前只让 `get_by_username` 的 AsyncMock 返回目标用户/None，未校验 tenant scope；如果实现忽略 resolver tenant、把 username 查成跨租户宽查询，或 last_login 写固定/陈旧时间，只要响应/审计仍来自 mock 旧测试仍可能绿。现升级为 tenant lookup/last_login exact 契约，避免 auth login 测试只证明“登录结果和审计看起来对” |
| 2026-05-31 | N/A（JUnit upload report exact artifact/S3 契约） | `tests/unit/test_plugins/test_junit_collector.py::TestCollect::test_upload_report` 1 passed；junit collector full 24 passed；release quality docs contract full 190 passed；targeted ruff passed | 无新增 warning；JUnitCollector upload_report 现在固定完整 ArtifactData（name/type/local_path/mime_type）和 S3 `put_object` 完整 kwargs（Bucket/Key/Body/ContentType）等值断言 | 测试 review 发现该用例此前逐字段抽查 artifact 与 S3 kwargs；如果 artifact 多/漏字段、type/mime/local_path 漂移，或 S3 上传夹带额外参数/漏参数，只要几个字段仍相似旧测试仍可能绿。现升级为 exact artifact/S3 契约，避免 JUnit 上传测试只证明“上传过一个叫 junit.xml 的对象” |
| 2026-05-31 | N/A（Run trigger priority exact queue/audit 契约） | `tests/unit/test_api/test_runs.py::test_trigger_run_with_high_priority tests/unit/test_api/test_runs.py::test_trigger_run_default_priority` 2 passed；runs full 51 passed；release quality docs contract full 189 passed；targeted ruff passed | 无新增 warning；手动触发 high/default priority 两条路径现在固定完整 RunResponse、`RUN_TRIGGER` 权限、pipeline/project/default environment 查找、`run.create` 完整 kwargs、retry_group、ARQ queue:high/queue:medium 入队参数、mark_enqueued metadata，以及 `run.trigger` audit 完整等于响应 | 测试 review 发现这两条用例此前只断言响应 `priority` 和 create kwargs 里的 priority；如果高优先级仍入 medium 队列、默认 priority 漏写审计、RBAC 未执行、metadata 缺 git_url/default_branch，或响应 DTO 漏字段，只要 priority 数值仍对旧测试仍可能绿。现升级为 exact queue/audit 契约，避免 Run priority 正向测试只证明“priority 数字写进去了” |
| 2026-05-31 | N/A（Project silent_windows update success exact settings/audit 契约） | `tests/unit/test_api/test_projects.py::test_update_project_serializes_silent_windows_into_settings` 1 passed；projects full 59 passed；release quality docs contract full 188 passed；targeted ruff passed | 无新增 warning；project update silent_windows 成功路径现在固定保留既有 settings、序列化后的完整 silent window 对象、完整 ProjectResponse、`project.update(project, settings=...)` exact 参数、credential lookup 零触达，以及 `project.update` audit before/after 完整脱敏 payload；`webhook_secret` 原值不进入 audit before/after | 测试 review 发现该用例此前只抽查 settings/response 中的 `reason`；如果 allowed_branches/webhook_secret 被覆盖、start/end timezone 字符串漂移、响应 DTO 漏字段、误查 credential、audit before/after 只写子集，或 webhook_secret 原值进审计，只要 reason 仍对旧测试仍可能绿。现升级为 success exact settings/audit/no-secret 契约，避免项目静默窗口成功测试只证明“reason 被放进某个地方” |
| 2026-05-31 | N/A（Project member CRUD exact response/audit 契约） | `tests/unit/test_api/test_project_members.py::test_add_member_happy_path tests/unit/test_api/test_project_members.py::test_update_member_role tests/unit/test_api/test_project_members.py::test_remove_member` 3 passed；project members full 10 passed；release quality docs contract full 186 passed；targeted ruff passed | 无新增 warning；project member add/update/remove 正向路径现在固定完整 ProjectMemberResponse、tenant-scoped project/user/member lookup、create/update/delete 仓储 exact 参数，以及完整 audit tenant/user/action/resource/before_state/after_state | 测试 review 发现这些用例此前只抽查响应 `role`、audit `action` 与少量 state 字段；如果 username/email/created_at 响应漂移、tenant/user/resource 审计接线错误、update after_state 只写子集，或 delete after_state 不是 None，只要 role/action 仍对旧测试仍可能绿。现升级为 CRUD exact response/audit 契约，避免项目成员正向测试只证明“成员角色变了且审计大概写过” |
| 2026-05-31 | N/A（Project create audit route exact wiring 契约） | `tests/unit/test_api/test_audit.py::test_project_create_route_invokes_audit` 1 passed；audit API full 13 passed；release quality docs contract full 185 passed；targeted ruff passed | 无新增 warning；project create route wiring smoke 现在固定 patched `write_audit` 的 positional args 为 `(repos, user)`，kwargs 精确等于 `project.create/project/project_id/after=response body` | 测试 review 发现该用例此前只证明 `write_audit` 被 await 且 after 非空；如果路由传错 repos/user、action/resource_type/resource_id 漂移，或 after_state 与响应体不一致，只要 after 有值旧测试仍可能绿。现升级为 route exact wiring 契约，避免项目创建审计接线测试只证明“审计函数大概被调用过” |
| 2026-05-31 | N/A（LogStream archive success cursor/S3 exact 契约） | `tests/unit/test_engine/test_log_stream.py::TestLogStream::test_archive_logs_success` 1 passed；log stream full 18 passed；release quality docs contract full 184 passed；targeted ruff passed | 无新增 warning；LogStream 成功归档路径现在固定 Redis `xrange` cursor 序列 `- -> 2-1`、S3 `Bucket/Key/Body/ContentType` 精确参数、1h stream TTL 和清除 retry marker | 测试 review 发现该用例此前只在 `put_object.assert_awaited_once()` 后抽查 bucket/key/content-type 并解析 body；如果实现一直从起始游标读、游标没推进、上传 body/key 漂移，或 TTL/retry marker 没分流，只要预置 side effect 返回两批数据旧测试仍可能绿。现升级为 archive success cursor/S3 exact 契约，避免日志归档成功测试只证明“上传过一个能解析的 JSONL” |
| 2026-05-31 | N/A（Run trigger success exact response/audit/RBAC 契约） | `tests/unit/test_api/test_runs.py::test_trigger_run tests/unit/test_api/test_runs.py::test_trigger_run_stays_queued_and_audited_without_arq_pool` 2 passed；runs full 51 passed；release quality docs contract full 183 passed；targeted ruff passed | 无新增 warning；手动触发 run 的 queued 与 no-arq fallback 两条成功路径现在固定完整 RunResponse、`RUN_TRIGGER` 权限参数、`run.create` metadata/priority、retry_group、queue metadata 或 no-queue 副作用，以及 `run.trigger` audit tenant/user/before_state/after_state 完整等于响应 | 测试 review 发现这些用例此前只抽查 status/project_id/pipeline_id/environment/git_ref 和 audit status；如果响应 DTO 漏字段、权限 action 漂移、audit after_state 只写子集，或 no-arq 分支误写 queue metadata，只要 queued 状态还在旧测试仍可能绿。现升级为 success exact response/audit/RBAC 契约，避免手动触发成功测试只证明“Run 大概 queued 且审计大概写过” |
| 2026-05-31 | N/A（Notification consecutive failures exact log 契约） | `tests/unit/test_worker/test_notifications.py::TestEvaluateAndNotify::test_or_group_with_consecutive_failures_sends_notification` 1 passed；worker notifications full 35 passed；release quality docs contract full 182 passed；targeted ruff passed | 无新增 warning；OR/consecutive failures 通知路径现在固定 `_load_consecutive_failures(run_repo, project_id, run_id)`、delivery 去重 key、`_send_channel` 完整参数、`_assert_delivery_log` direct helper 与 sent log 投影 | 测试 review 发现该用例此前只证明连续失败数被加载、消息发过、日志写过一次；如果 delivery key 漂移、log 的 project/run/rule/channel 串错、status/error 变坏，只要 mock send 仍发生旧测试仍可能绿。现升级为 exact log 契约，避免连续失败通知测试只证明“OR 条件触发后发了消息” |
| 2026-05-31 | N/A（Analytics success exact kwargs/cutoff window 契约） | `tests/unit/test_api/test_analytics.py::test_trends_success_uses_project_rbac_and_repository_pagination tests/unit/test_api/test_analytics.py::test_flaky_success_uses_project_rbac_and_repository_filters tests/unit/test_api/test_analytics.py::test_test_history_success_uses_project_rbac_and_repository_filters` 3 passed；analytics API full 6 passed；release quality docs contract full 181 passed；targeted ruff passed | 无新增 warning；analytics trends/flaky/test-history 成功路径现在通过 4 元组解包固定 `RUN_READ` 权限完整调用、仓储 kwargs 字段集合、零 positional args，并用请求前后时间窗口证明 `days=7/21/14` 的 timezone-aware cutoff，同时保留完整响应体 | 测试 review 发现这些用例虽已覆盖响应映射，但仓储调用仍逐字段抽查，cutoff 只看 `tzinfo`，RBAC helper 还靠 `len(args) == 4` 证明参数数目；如果 cutoff 没减 days、额外/缺失 kwargs、RBAC 调错 action 或参数顺序，只要 mock 响应体不变旧测试仍可能绿。现升级为 success exact kwargs/cutoff window 契约，避免 analytics API success path 只证明“响应体来自 mock 且仓储大概被调用” |
| 2026-05-31 | N/A（Jest/Playwright success subprocess exact 契约） | `tests/unit/test_plugins/test_jest_runner.py::TestJestRunTests::test_run_success tests/unit/test_plugins/test_playwright_runner.py::TestPlaywrightRunTests::test_run_tests_success` 2 passed；jest/playwright runner full 57 passed；release quality docs contract full 180 passed；targeted ruff passed | 无新增 warning；JS runner 默认成功路径现在固定 subprocess 完整 argv、cwd、stdout/stderr pipe、默认 JUnit env 覆盖值和 `process.communicate()` await，同时继续证明 stdout/stderr、exit code 和 pass/fail 计数 | 测试 review 发现默认成功用例此前只断言 `mock_exec.assert_awaited_once()` 后抽查默认 JUnit env；如果命令少了 reporter、cwd 指错、stdout/stderr pipe 被删、未等待子进程，或默认 JUnit env 指向错误路径，只要 fixture XML 能被解析旧测试仍可能绿。现升级为 success subprocess exact 契约，避免 JS runner happy path 测试只证明“能解析预置成功报告” |
| 2026-05-31 | N/A（Schedule CRUD exact response/audit 契约） | `tests/unit/test_api/test_schedules.py::test_create_schedule tests/unit/test_api/test_schedules.py::test_update_schedule_recomputes_next_run tests/unit/test_api/test_schedules.py::test_delete_schedule` 3 passed；schedules full 11 passed；release quality docs contract full 179 passed；targeted ruff passed | 无新增 warning；schedule create/update/delete 正向用例现在固定 `SCHEDULE_EDIT` 权限调用、tenant-scoped project lookup、pipeline/schedule lookup、create/update/delete 仓储 exact 参数、完整 ScheduleResponse，以及 create/update/delete audit 的 tenant/user/action/resource/before_state/after_state；create/update 也固定 `compute_next_run_at` 输入与 next_run_at JSON 口径 | 测试 review 发现这些用例此前只抽查 cron/pipeline/timezone/next_run 或 audit action/resource 中少数字段；如果权限未执行、project lookup 查错 tenant、quiet_windows/last_run_at/last_error/created_at 漏传，或 audit before/after 不是完整响应，只要 cron 和 next_run 仍对旧测试仍可能绿。现升级为 CRUD exact response/audit 契约，避免 Schedule 正向测试只证明“调度接口成功且几个字段看起来对” |
| 2026-05-31 | N/A（Jest/Playwright env subprocess exact 契约） | `tests/unit/test_plugins/test_jest_runner.py::TestJestRunTests::test_env_vars_passed_through tests/unit/test_plugins/test_jest_runner.py::TestJestRunTests::test_junit_xml_config_wins_over_env_var tests/unit/test_plugins/test_playwright_runner.py::TestPlaywrightRunTests::test_run_tests_with_env_vars tests/unit/test_plugins/test_playwright_runner.py::TestPlaywrightRunTests::test_junit_xml_config_wins_over_env_var` 4 passed；jest/playwright runner full 57 passed；release quality docs contract full 178 passed；targeted ruff passed | 无新增 warning；JS runner env 与 JUnit env override 用例现在固定 subprocess 完整 argv、cwd、stdout/stderr pipe、runner 专属 JUnit env 覆盖值和 `process.communicate()` await | 测试 review 发现这些用例此前只断言 `mock_exec.assert_awaited_once()` 后抽查 env；如果 Jest/Playwright 命令漂移、cwd 指错、stdout/stderr pipe 被删、配置 JUnit env 没覆盖外部 env，或未等待子进程，只要目标 env 字段仍存在旧测试仍可能绿。现升级为 env subprocess exact 契约，避免 JS runner env 测试只证明“env 字段看起来对” |
| 2026-05-31 | N/A（Notification rule CRUD exact response/audit 契约） | `tests/unit/test_api/test_notifications.py::test_create_rule tests/unit/test_api/test_notifications.py::test_update_rule tests/unit/test_api/test_notifications.py::test_delete_rule` 3 passed；notifications API full 21 passed；release quality docs contract full 177 passed；targeted ruff passed | 无新增 warning；notification rule create/update/delete 基础正向用例现在固定 `NOTIFICATION_EDIT` 权限调用、tenant-scoped project lookup、get/create/update/delete 仓储 exact 参数、完整 NotificationRuleResponse，以及 create/update/delete audit 的 tenant/user/action/resource/before_state/after_state；审计状态只保留 channel types 与 template length，不泄露 webhook URL 或模板内容 | 测试 review 发现这些用例此前只抽查响应 name、delete 调用和 audit action/resource/name/channel redacted；如果 create 响应来自默认 fixture、权限未执行、project lookup 查错 tenant、audit 漏 user/before/after 或泄露 URL/template，只要 name/redacted 布尔值仍对旧测试仍可能绿。现升级为 CRUD exact response/audit 契约，避免通知规则正向测试只证明“接口成功且几个字段像是对的” |
| 2026-05-31 | N/A（Go run_tests env subprocess exact 契约） | `tests/unit/test_plugins/test_go_test_runner.py::TestGoTestRunTests::test_env_vars_passed_through` 1 passed；go runner full 34 passed；release quality docs contract full 176 passed；targeted ruff passed | 无新增 warning；Go runner env 用例现在固定 subprocess 完整 argv、cwd、stdout/stderr pipe、env 覆盖值，并要求 `process.communicate()` 被 await 后才写 JSON/JUnit 与返回结果 | 测试 review 发现该用例此前只断言 `mock_exec.assert_awaited_once()` 后抽查 env；如果实现丢掉 `-v -json ./...`、cwd、stdout/stderr pipe 或未等待子进程，只要 env 字段仍存在旧测试仍可能绿。现升级为 Go run_tests env subprocess exact 契约，避免 Go runner env 测试只证明“env 字段看起来对” |
| 2026-05-31 | N/A（Environment create disk defaults exact 契约） | `tests/unit/test_api/test_environments.py::test_create_environment_stores_disk_limit` 1 passed；environments full 27 passed；release quality docs contract full 175 passed；targeted ruff passed | 无新增 warning；environment create 最小 disk_mb 用例现在固定完整 EnvironmentResponse、tenant-scoped project lookup、`environment.create` 完整默认 kwargs（memory/cpu/artifact/network/cache/env_vars）、空 env_vars 加密 envelope 可解密为空，以及完整 `environment.create` audit kwargs 与 redacted env_vars count | 测试 review 发现该用例此前只断言响应 `disk_mb == 2048`、create 被 await、resource_limits 三个字段；如果默认 memory/cpu/network/cache 漂移、项目租户校验漏掉、空 env_vars 未加密、created_at/audit payload 丢字段，只要 disk_mb 仍在旧测试仍可能绿。现升级为 create disk defaults exact 契约，避免环境磁盘配额测试只证明“disk_mb 字段写进去了” |
| 2026-05-31 | N/A（Project CRUD exact response/audit 契约） | `tests/unit/test_api/test_projects.py::test_create_project tests/unit/test_api/test_projects.py::test_get_project tests/unit/test_api/test_projects.py::test_update_project tests/unit/test_api/test_projects.py::test_delete_project` 4 passed；projects full 59 passed；release quality docs contract full 174 passed；targeted ruff passed | 无新增 warning；project create/get/update/delete 正向用例现在固定完整 ProjectResponse、tenant-scoped lookup、create/update/delete 仓储 exact kwargs，以及 create/update/delete audit 的 tenant/user/action/resource/before_state/after_state；create audit 继续证明 git URL userinfo 脱敏且响应仍保留原始 git_url | 测试 review 发现这些用例此前只抽查响应 id/name 或 audit action/resource/name/git_url；如果 DTO 漏字段、时间格式漂移、create 默认参数丢失、get/delete 查错 tenant，或 audit payload 漏 user/before/after，只要被抽查字段仍对旧测试仍可能绿。现升级为 CRUD exact response/audit 契约，避免 Projects 正向测试只证明“路由成功且几个字段看起来对” |
| 2026-05-31 | N/A（Pytest run_tests subprocess env/cwd exact 契约） | `tests/unit/test_plugins/test_pytest_runner.py::TestPytestRunTests::test_env_vars_passed_through tests/unit/test_plugins/test_pytest_runner.py::TestPytestRunTests::test_working_dir_passed_through` 2 passed；pytest runner full 45 passed；release quality docs contract full 173 passed；targeted ruff passed | 无新增 warning；pytest run_tests env/cwd 用例现在固定 subprocess 完整 argv、cwd、stdout/stderr pipe、env 覆盖/None 分支，并要求 `process.communicate()` 被 await 后才返回 | 测试 review 发现这两条用例此前只断言 `mock_exec.assert_awaited_once()` 后抽查 env 或 cwd；如果实现丢掉 `--junitxml`、stdout/stderr pipe、`env=None` 默认分支或未等待子进程，只要目标字段仍存在旧测试仍可能绿。现升级为 subprocess env/cwd exact 契约，避免 pytest run_tests 测试只证明“env 或 cwd 字段看起来对” |
| 2026-05-31 | N/A（Credential delete audit complete payload 契约） | `tests/unit/test_api/test_credentials.py::test_delete_credential_happy_path` 1 passed；credentials full 14 passed；release quality docs contract full 172 passed；targeted ruff passed | 无新增 warning；credential delete happy path 现在固定 project/credential tenant-scoped lookup、`credential.delete(cred)`、完整 `credential.delete` audit kwargs：tenant/user/action/resource、before_state 完整 CredentialResponse、after_state None；并证明审计不含 `value`、`encrypted_value` 或 ciphertext bytes | 测试 review 发现该用例此前只证明 delete 被 await，并局部抽查 audit action/resource/name/type；如果 tenant/user/resource_id 漂移、before_state 漏 created_by/created_at、after_state 错写，或 ciphertext 进入 audit，只要 name/type 仍对旧测试仍可能绿。现升级为 complete payload 契约，避免 credential delete 审计测试只证明“删了对象且几个字段看起来对” |
| 2026-05-31 | N/A（Credential encrypt/rotate exact AAD response 契约） | `tests/unit/test_api/test_credentials.py::test_create_credential_encrypts_with_aad tests/unit/test_api/test_credentials.py::test_rotate_credential_re_encrypts_with_aad` 2 passed；credentials full 14 passed；release quality docs contract full 171 passed；targeted ruff passed | 无新增 warning；credential create/rotate 现在固定 `crypto.encrypt(value, context_id=f"credential:{project.id}:db_password")` 完整调用、project/credential tenant-scoped lookup、create/update 仓储参数和完整 CredentialResponse；rotate audit 也固定 tenant/user/action/resource/before/after_state，且不含明文、`encrypted_value` 或 ciphertext bytes | 测试 review 发现这两条用例此前只用 `encrypt.assert_called_once()` 后抽查 `call_args`，响应只负向检查没有明文；如果加密入参位置、AAD kwarg、project/credential 查询、响应 DTO 或 rotate audit 字段漂移，只要局部 call_args 看起来相似旧测试仍可能绿。现升级为 exact AAD response 契约，避免 credential 加密测试只证明“加密函数大概被调用且响应没看到 secret” |
| 2026-05-31 | N/A（Jest/Playwright failure subprocess exact 契约） | `tests/unit/test_plugins/test_jest_runner.py::TestJestRunTests::test_run_with_failures tests/unit/test_plugins/test_playwright_runner.py::TestPlaywrightRunTests::test_run_tests_with_failures` 2 passed；jest/playwright runner full 57 passed；release quality docs contract full 170 passed；targeted ruff passed | 无新增 warning；JS runner 失败路径现在固定 subprocess 完整 argv、cwd、stdout/stderr pipe、JUnit env 覆盖值和 `communicate()` await，同时继续证明非零 exit code 与失败计数/输出被保留 | 测试 review 发现失败用例此前预先写好 JUnit XML 后只断言解析结果；如果 runner 命令漂移、cwd 指错、JUnit env 未指向同一个报告路径，或未等待子进程，只要 fixture XML 还在旧测试仍可能绿。现升级为 failure subprocess exact 契约，避免失败路径测试只证明“能解析一个预置失败报告” |
| 2026-05-31 | N/A（Run detail exact response/RBAC 契约） | `tests/unit/test_api/test_runs.py::test_get_run` 1 passed；runs full 51 passed；release quality docs contract full 169 passed；targeted ruff passed | 无新增 warning；run detail 正向用例现在固定完整 RunResponse，覆盖 tenant/project/pipeline/environment/pipeline_name/status/priority/trigger/git/timestamps/summary/error 字段；同时锁住 `get_for_tenant(run.id, tenant_id)` 与 RBAC `RUN_READ` | 测试 review 发现该用例此前只断言响应 id 和 tenant-scoped lookup；如果详情 DTO 漏字段、pipeline_name/时间格式漂移，或实现漏掉项目级读权限，旧测试仍可能绿。现升级为 exact response/RBAC 契约，避免 run detail 测试只证明“能按 id 返回一个对象” |
| 2026-05-31 | N/A（Run notifications exact page/RBAC 契约） | `tests/unit/test_api/test_runs.py::test_get_run_notifications_returns_exact_page_and_rbac` 1 passed；runs full 51 passed；release quality docs contract full 168 passed；targeted ruff passed | 无新增 warning；run notifications 成功路径现在固定完整 NotificationLogResponse 分页壳，包含 id/project_id/run_id/rule_id/channel_type/status/error_message/sent_at；同时锁住 `page=3/per_page=4 -> offset=8/limit=4`、tenant-scoped run lookup、RBAC `NOTIFICATION_READ`，并证明列表读面不写 audit | 测试 review 发现该 route 此前只有缺失 run 短路覆盖，没有正向单测；如果实现漏掉通知读权限、分页参数算错、响应漏字段/串 run 或 rule，或读列表误写审计，旧测试完全发现不了。现补成 exact page/RBAC 契约，避免 run notifications 路由只靠“缺失资源不会触碰下游”来代表成功读面质量 |
| 2026-05-31 | N/A（Jest/Playwright missing JUnit still executes 契约） | `tests/unit/test_plugins/test_jest_runner.py::TestJestRunTests::test_missing_junit_xml_returns_zeros tests/unit/test_plugins/test_playwright_runner.py::TestPlaywrightRunTests::test_run_tests_no_junit_file` 2 passed；jest/playwright runner full 57 passed；release quality docs contract full 167 passed；targeted ruff passed | 无新增 warning；缺失 JUnit XML 的降级用例现在固定 runner subprocess 的完整 argv、cwd、stdout/stderr pipe、JUnit env 覆盖值，并要求 `process.communicate()` 被 await 后才返回全零计数 | 测试 review 发现这两条用例此前只断言返回 0 计数；如果实现完全跳过 `create_subprocess_exec`、没有等待子进程、或 JUnit env 指错导致真实报告永远缺失，只要返回全零旧测试仍可能绿。现升级为 still executes 契约，避免 missing-report 测试只证明“没报告时能给 0” |
| 2026-05-31 | N/A（Run archived logs S3 key/page/RBAC 契约） | `tests/unit/test_api/test_runs.py::test_get_archived_run_logs_reads_s3_jsonl` 1 passed；runs full 50 passed；release quality docs contract full 166 passed；targeted ruff passed | 无新增 warning；archived logs 成功单测现在请求 `page=2&per_page=1` 并固定完整分页响应，只回放第二条 stderr；同时锁住 tenant-scoped run lookup、RBAC `RUN_READ`、S3 `get_object(Bucket="qa-platform", Key=f"logs/{run.id}.jsonl")`，并证明不会生成 presign URL | 测试 review 发现该用例此前只检查 `total == 2` 和完整 data 内容；如果 route 忽略分页、读错 S3 key/bucket、漏掉权限校验，或日志回看误生成预签名 URL，只要两条 JSONL 被解析出来旧测试仍可能绿。现升级为 S3 key/page/RBAC 契约，避免 archived logs 成功单测只证明“能解析一段 JSONL” |
| 2026-05-31 | N/A（Jest/Playwright JUnit parent subprocess exact 契约） | `tests/unit/test_plugins/test_jest_runner.py::TestJestRunTests::test_run_tests_creates_custom_junit_parent_directory tests/unit/test_plugins/test_playwright_runner.py::TestPlaywrightRunTests::test_run_tests_creates_custom_junit_parent_directory` 2 passed；jest/playwright runner full 57 passed；release quality docs contract full 165 passed；targeted ruff passed | 无新增 warning；自定义 JUnit parent 目录用例现在不仅证明目录在 subprocess 启动前创建，也固定 `create_subprocess_exec` 的完整 argv、cwd、stdout/stderr pipe 和 runner 专属 JUnit env 覆盖值 | 测试 review 发现该用例此前只用 side-effect 写报告并断言 `mock_exec.assert_awaited_once()`；如果 Jest/Playwright 命令漂移、cwd 指错、JUnit env 未传或传回默认路径，只要 side-effect 写出报告旧测试仍可能绿。现升级为 subprocess exact 契约，避免 runner JUnit parent 测试只证明“目录存在且解析到了报告” |
| 2026-05-31 | N/A（RunExecutor setup success backend lifecycle 契约） | `tests/unit/test_engine/test_executor.py::TestRunSetupContainerised::test_setup_runs_via_backend_not_subprocess tests/unit/test_engine/test_executor.py::TestRunSetupContainerised::test_setup_timeout_capped_at_600s tests/unit/test_engine/test_executor.py::TestRunSetupContainerised::test_setup_uses_pipeline_timeout_when_below_cap` 3 passed；executor full 42 passed；release quality docs contract full 289 passed；targeted ruff passed | 无新增 warning；setup 成功路径现在固定 `ExecutionSpec` 的 image/command/env/resource_limits/network/security/user/mount/labels 与 600s cap，同时固定 `create_execution -> start -> wait -> cleanup` 使用同一个 execution id，并要求成功后 `_active_execution_id` 清空 | 测试 review 发现 timeout cap/below-cap 用例此前只证明 `create_execution` 被碰过、lifecycle id 和 timeout 参数正确；如果 setup command、env、workspace mount、resource limits 或 sandbox/user/labels 漂移，只要 timeout 数字仍对旧测试仍可能绿。现升级为 backend spec+lifecycle 契约，避免 setup 成功路径测试只证明“timeout 参数看起来对” |
| 2026-05-31 | N/A（Notification template rendering exact send/log 契约） | `tests/unit/test_worker/test_notifications.py::TestEvaluateAndNotify::test_template_rendering` 1 passed；worker notifications full 35 passed；release quality docs contract full 163 passed；targeted ruff passed | 无新增 warning；rule-level 模板渲染用例现在固定 rule-level 模板渲染后的完整 `_send_channel` 参数、`get_by_delivery` delivery key、`_assert_delivery_log` direct helper 与 commit | 测试 review 发现该用例此前只看消息里有 run_id/status/passed/failed 片段，后来仍保留字段集合守门；如果通知发到错误 channel/config、delivery 去重键漂移、日志字段漏写/夹带额外字段，或 commit 被删，只要几个变量仍被替换进字符串旧测试仍可能绿。现升级为 exact send/log 契约，避免模板渲染测试只证明“几个变量被替换进字符串” |
| 2026-05-31 | N/A（Run artifacts list exact response/DB-only 契约） | `tests/unit/test_api/test_runs.py::test_get_run_artifacts` 1 passed；runs full 50 passed；release quality docs contract full 165 passed；targeted ruff passed | 无新增 warning；run artifacts 列表用例现在固定完整 ArtifactResponse 分页壳，包含 id/run_id/type/name/storage_path/size_bytes/mime_type/expires_at/created_at；同时锁住 `page=3/per_page=7 -> offset=14/limit=7`、tenant-scoped run lookup、RBAC `RUN_READ`，并证明 DB-only 列表不触碰 S3 get/presign | 测试 review 发现该用例此前只断言 `total == 0`；如果 route 漏掉权限校验、分页 offset 算错、仓储查错 run、响应漏字段/夹带额外字段，或 artifact list 开始误触发对象存储，只要返回空 total 旧测试仍可能绿。现升级为 exact response/DB-only 契约，避免 run artifacts 单测只证明“能返回一个空分页” |
| 2026-05-31 | N/A（ChannelRouter exact result propagation 契约） | `tests/unit/test_worker/test_channels.py::TestChannelRouter::test_route_email tests/unit/test_worker/test_channels.py::TestChannelRouter::test_route_webhook tests/unit/test_worker/test_channels.py::TestChannelRouter::test_route_dingtalk tests/unit/test_worker/test_channels.py::TestChannelRouter::test_route_wecom tests/unit/test_worker/test_channels.py::TestChannelRouter::test_route_known_channel_propagates_failure_result tests/unit/test_worker/test_channels.py::TestRouteChannelFunction::test_delegates_to_router` 6 passed；channels full 39 passed；release quality docs contract full 161 passed；targeted ruff passed | 无新增 warning；ChannelRouter 现在固定每个一等 channel 的 `send(config, message)` exact await 参数、完整 `ChannelResult(success=True)`，并新增下游失败 `ChannelResult(success=False, error=...)` 原样透传；模块级 `route_channel` 也固定返回 router 的完整失败结果 | 测试 review 发现 router 用例此前主要看 `result.success is True`；如果路由层吞掉下游 error、把失败重写成成功、或 module-level wrapper 忽略 router 返回值，只要调用了对应 channel 且 success 看起来为真，旧测试仍可能绿。现升级为 exact result propagation 契约，避免通知路由测试只证明“调到了某个 channel 且成功布尔值为真” |
| 2026-05-31 | N/A（Credential create audit complete payload 契约） | `tests/unit/test_api/test_credentials.py::test_create_credential_audit_does_not_leak_plaintext` 1 passed；credentials full 14 passed；release quality docs contract full 160 passed；targeted ruff passed | 无新增 warning；credential create 审计脱敏用例现在固定完整 `repos.audit.create` kwargs：tenant/user/action/resource/before_state，以及 after_state 的 id/project_id/name/type/created_by/created_at；同时继续证明明文 secret、`encrypted_value` 和 ciphertext bytes 不进入 audit | 测试 review 发现该用例此前只抽查 after_state 的 name/type 并检查不含明文；如果 tenant/user/resource_id 漂移、before_state 错写、created_by/created_at 漏掉，或 audit payload 夹带额外字段，只要 name/type 和脱敏片段仍对旧测试仍可能绿。现升级为 complete payload 契约，避免 credential create 审计测试只证明“没看到明文且两个字段对” |
| 2026-05-31 | N/A（Email SMTP success wait_for/thread exact 契约） | `tests/unit/test_worker/test_channels.py::TestEmailChannel::test_success tests/unit/test_worker/test_channels.py::TestEmailChannel::test_ssl_port_465` 2 passed；channels full 38 passed；release quality docs contract full 159 passed；targeted ruff passed | 无新增 warning；EmailChannel 成功与 SSL 465 端口用例现在固定 `wait_for(thread_result, timeout=EmailChannel.TIMEOUT)`，并锁住 `to_thread` 包裹同一个 `_send_sync` 调用的 host/port/user/password/from/to 参数，成功响应使用完整 `ChannelResult(success=True)` | 测试 review 发现成功路径此前仍用 `wait_for.assert_awaited_once()` / `to_thread.assert_awaited_once()` 无参断言，SSL 465 用例也只抽查 port；如果实现把 `_send_sync` 直接 await、`wait_for` 包错 awaitable、timeout 参数漂移，或 SMTP 参数顺序串错，旧测试仍可能绿。现升级为 success wait_for/thread exact 契约，避免 Email SMTP 成功测试只证明“成功返回且线程 helper 被碰过” |
| 2026-05-31 | N/A（Logging renderer output shape 契约） | `tests/unit/test_logging.py` 2 passed；release quality docs contract full 158 passed；targeted ruff passed | 无新增 warning；logging json/console 两条配置测试现在用 root handler 类型/stdout/ProcessorFormatter 投影证明唯一 handler，并实际发出 structlog 事件：json 分支解析 stdout JSON 并锁住 event/request_id/level/logger，console 分支锁住 console 输出包含 event/component 且不退化成 JSON renderer | 测试 review 发现 logging 单测此前只看 root handler 数量、level 和 uvicorn handler 绑定；如果 json/console renderer 选错、structlog processor 丢 level/logger，或 console 分支误用 JSON renderer，只要 handler 数量仍为 1 旧测试仍可能绿。现升级为 renderer output shape 契约，避免 logging 配置测试只证明“装了一个 handler” |
| 2026-05-31 | N/A（Worker source auth credential lookup exact filters 契约） | `tests/unit/test_worker/test_tasks.py::test_build_source_auth_rejects_type_mismatch` 1 passed；worker tasks full 19 passed；release quality docs contract full 346 passed；targeted ruff passed | 无新增 warning；Git credential type mismatch 用例现在除了固定 exact RuntimeError 与 no-decrypt，还编译 credential lookup statement，锁住 `credential.id/project_id/tenant_id/deleted_at IS NULL` 过滤，并从 rendered SQL 逐列提取绑定参数，要求 `credential.id -> project.credential_id`、`credential.project_id -> project.id`、`credential.tenant_id -> project.tenant_id` 精确匹配 | 测试 review 发现该用例此前只证明 credential lookup 发生一次且不解密；后续复扫发现 `set(compiled.params.values())` 仍可能漏掉 `project_id` / `tenant_id` 绑定互换。如果实现漏掉 tenant/project 过滤、允许软删除 credential，或把租户/项目过滤串错但仍返回 type mismatch，旧测试仍可能绿。现升级为 source auth credential lookup column-bound exact filters 契约，避免 worker source auth 测试只证明“查过一条凭据并拒绝了解密” |
| 2026-05-31 | N/A（TokenService real hash repository args 契约） | `tests/unit/test_auth/test_token_service.py::TestCreateApiToken::test_hash_is_argon2id` 1 passed；token service full 21 passed；release quality docs contract full 156 passed；targeted ruff passed | 无新增 warning；`create_api_token` 的真实 Argon2 hash 用例现在解析真实生成的 public token，固定返回 record 身份、`repo.create` 完整 kwargs：user_id/name/token_id/secret_hash/scopes/expires_at，并继续证明 hash 只验证第三段 secret、不验证完整 token 或 user_id | 测试 review 发现该用例此前只证明仓储 create 被调用一次，再抽出 `secret_hash` 看起来是 Argon2；如果实现把错误 token_id 落库、丢 scopes/expiry、返回的 record 不是仓储结果，或 hash 对象碰巧是 Argon2-looking，旧测试仍可能绿。现升级为 real hash repository args 契约，避免 API token 创建服务测试只证明“仓储被碰过且 hash 看起来对” |
| 2026-05-31 | N/A（Email SMTP timeout wait_for exact 契约） | `tests/unit/test_worker/test_channels.py::TestEmailChannel::test_smtp_timeout_is_reported` 1 passed；channels full 38 passed；release quality docs contract full 156 passed；targeted ruff passed | 无新增 warning；EmailChannel SMTP timeout 用例现在让 `asyncio.wait_for` 抛 `TimeoutError`，固定 `wait_for(thread_result, timeout=EmailChannel.TIMEOUT)`，同时锁住 `to_thread` 包裹 `_send_sync` 的 host/port/user/password/from/to 参数与完整 `ChannelResult` | 测试 review 发现该用例此前让 `asyncio.to_thread` 自己抛 `TimeoutError`，只能证明异常被转换为 timeout 文案；如果实现移除 `wait_for`、timeout 参数漂移，或没有把同步 SMTP helper 放进线程池，旧测试仍可能绿。现升级为 Email SMTP timeout wait_for exact 契约，避免邮件 timeout 测试只证明“某处抛了 TimeoutError” |
| 2026-05-31 | N/A（FairScheduler queue choice exact enqueue metadata 契约） | `tests/unit/test_worker/test_scheduler.py::test_enqueue_falls_back_to_medium_for_invalid_manual_priority tests/unit/test_worker/test_scheduler.py::test_enqueue_schedule_uses_low_priority_queue tests/unit/test_worker/test_scheduler.py::test_manual_priority_matrix_targets_all_worker_queues tests/unit/test_worker/test_scheduler.py::test_enqueue_run_convenience_uses_explicit_trigger_type_for_queue` 6 passed；scheduler full 14 passed；release quality docs contract full 155 passed；targeted ruff passed | 无新增 warning；invalid manual priority、schedule trigger、manual priority matrix 与 explicit trigger override 用例现在固定完整 `arq.enqueue_job("execute_run", str(run.id), _queue_name, _job_id, _defer_by=0)`，并锁住 `mark_enqueued(run.id, queue_name, arq_job_id, timezone-aware enqueued_at)` | 测试 review 发现这些 FairScheduler 队列选择用例此前主要只看 `_queue_name`；如果 job id 漂移、`mark_enqueued` 没落库、queue metadata 写错，或 explicit trigger override 只把 ARQ 发到正确队列但 DB 仍写错队列名，旧测试仍可能绿。现升级为 queue choice exact enqueue metadata 契约，避免 scheduler 队列选择测试只证明“队列名看起来对” |
| 2026-05-31 | N/A（write_audit helper complete kwargs 契约） | `tests/unit/test_api/test_audit.py::test_write_audit_passes_through_to_repo tests/unit/test_api/test_audit.py::test_write_audit_serializes_pydantic_before_and_after tests/unit/test_api/test_audit.py::test_write_audit_swallows_repo_errors tests/unit/test_api/test_audit.py::test_write_audit_handles_dict_states` 4 passed；audit unit full 13 passed；release quality docs contract full 154 passed；targeted ruff passed | 无新增 warning；`write_audit` helper 的 pydantic/dict/None/error-swallow 四条用例现在固定完整 `repos.audit.create.await_args.kwargs`，包含 tenant/user/action/resource/before_state/after_state，避免额外 kwargs 或状态壳漂移 | 测试 review 发现这些 helper 用例此前只在 await 后逐字段抽查少量 kwargs；如果实现夹带额外字段、漏掉 before/after 的 `None` 壳、或某条序列化路径 resource/action 漂移，只要被抽查字段仍相似旧测试仍可能绿。现升级为 complete kwargs 契约，避免审计 helper 测试只证明“几个字段看起来对” |
| 2026-05-31 | N/A（RunExecutor artifact_repo missing S3-only exact upload 契约） | `tests/unit/test_engine/test_executor.py::TestUploadArtifacts::test_upload_skips_artifact_row_when_repo_missing` 1 passed；executor full 42 passed；release quality docs contract full 154 passed；targeted ruff passed | 无新增 warning；artifact repository 缺失但 S3 client 可用的降级路径现在固定 `put_object` 的 bucket、storage key、Body 本地文件，以及 `Uploaded artifact: x.xml` 日志对应同一个 run id | 测试 review 发现该用例此前只断言 `s3.put_object.assert_awaited_once()`，只能证明 S3-only 模式尝试上传一次；如果 bucket/key 漂移、上传了错误文件，或成功日志写错 run id，旧测试仍可能绿。现升级为 artifact_repo missing S3-only exact upload 契约，避免 artifact 存储降级测试只证明“上传函数被碰过一次” |
| 2026-05-31 | N/A（Lifespan/worker startup exact init sequence 契约） | `tests/unit/test_lifespan.py::TestLifespanInitializesS3::test_init_s3_called_after_init_arq tests/unit/test_lifespan.py::TestWorkerStartupInitializesS3::test_worker_startup_init_s3_after_init_arq` 2 passed；lifespan full 9 passed；release quality docs contract full 152 passed；targeted ruff passed | 无新增 warning；API lifespan 成功路径现在固定 `init_db -> init_redis -> init_arq -> init_s3 -> init_crypto -> ensure_plugin_registry`，worker startup 成功路径固定 `init_db -> init_redis -> init_arq -> init_s3 -> init_crypto`，并隔离 tracing provider 影响 | 测试 review 发现这两条启动顺序用例此前只证明 `init_s3` 在 `init_arq` 后出现；如果 `init_crypto` 被删、registry 提前/漏执行，或 DB/Redis/ARQ/S3 顺序被重排，只要 S3 仍在 ARQ 后旧测试仍可能绿。现升级为 exact init sequence 契约，避免生命周期测试只证明“S3 大概排在 ARQ 后面” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_api_write_state.py::test_manual_run_archived_project_returns_409_without_persisting` 1 passed | release quality docs contract full 151 passed；targeted ruff passed | 无新增 warning；真实写路径中 archived project 手动触发 run 的集成用例现在固定 409 body `{"detail": "Project is archived; new runs cannot be triggered"}`，并同时证明 manual Run 数量和 `run.create` 审计数量不变 | 测试 review 发现该真实集成此前只断言 `409` 和 detail 含 `archived`，再检查 Run 数量不变；如果错误体泛化、detail 壳漂移、或拒绝路径误写 `run.create` 审计，旧测试仍可能绿。现升级为 archived project manual run integration exact rejection 契约，避免真实拒绝路径测试只证明“返回了一个含 archived 的 409” |
| 2026-05-31 | N/A（API token last_used failure exact auth flow 契约） | `tests/unit/test_auth_middleware.py::test_authenticate_api_token_rolls_back_last_used_failure_but_allows_user` 1 passed；auth middleware full 45 passed；release quality docs contract full 150 passed；targeted ruff passed | 无新增 warning；`_authenticate_api_token` 的 last_used 更新失败降级用例现在固定 token id 查询、`TokenService.verify_token(API_TOKEN_SECRET, token.secret_hash)`、同一 token 的 `update_last_used(token, aware_datetime)`、rollback/no commit，以及返回用户的 user_id/tenant_id/scopes | 测试 review 发现该用例此前只断言 `verify_token.assert_called_once()`、role、rollback；如果实现用错解析出的 secret、拿错 token hash、更新另一条 token，或降级返回丢 scopes，只要 verify 被调用一次且最终 role 相似旧测试仍可能绿。现升级为 API token last_used failure exact auth flow 契约，避免鉴权降级路径测试只证明“校验函数被碰过且回滚了” |
| 2026-05-31 | N/A（Auth middleware bearer dispatch exact args 契约） | `tests/unit/test_auth_middleware.py::test_get_current_user_routes_api_tokens_to_api_token_auth tests/unit/test_auth_middleware.py::test_get_current_user_routes_non_api_tokens_to_jwt_auth` 2 passed；auth middleware full 45 passed；release quality docs contract full 149 passed；targeted ruff passed | 无新增 warning；`get_current_user` 的 API token/JWT 分发测试现在固定 `_authenticate_api_token(raw_bearer, container)` 与 `_authenticate_jwt(raw_bearer, container)` 的完整 await 参数，并继续证明另一条认证路径未触发 | 测试 review 发现这两条鉴权入口用例此前只用 `assert_awaited_once()` 证明正确认证函数被碰过；如果 bearer token 被截断/预处理、container 重新取错，或认证函数收到错误对象，只要 await 了一次旧测试仍可能绿。现升级为 bearer dispatch exact args 契约，避免认证中间件分发测试只证明“碰过对应认证函数” |
| 2026-05-31 | N/A（Release candidate gate profile 结构化契约） | `tests/unit/test_release_gate_workflow.py::test_release_candidate_gate_profiles_are_explicit` 1 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；release candidate gate profile 单测现在解析 workflow YAML，固定 `workflow_dispatch.inputs.gate` 的 description/required/default/type/options、schedule cron、release-gate 的 name/if/needs，以及 backend integration 的 `QAP_PERFORMANCE_GATE_PROFILE` env | 测试 review 发现该用例此前主要用全文字符串证明 `workflow_dispatch`、gate options、release gate job 和 env 片段出现过；如果 gate options 挂到错误输入、release-gate needs 顺序/集合漂移、schedule 结构错误，或 env 放错 job，只要文本仍存在旧测试仍可能绿。现升级为结构化 profile 契约，避免 CI 发版门测试只证明“配置文本出现过” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_auth_results_artifacts.py::test_real_sse_logs_resume_after_last_event_id_uses_ticket_rbac_and_redis` 1 passed | targeted SSE unit passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；真实 `/runs/{id}/logs` SSE Last-Event-ID 集成用例现在解析 EventSource frame，固定只回放第二条 stderr log、随后 `done` sentinel；同一用例还固定 owner ticket 复用 401 `Invalid or expired SSE ticket`、`run.read` API token 可续读同一帧、空 scope ticket 返回 403 `Insufficient permissions` | 测试 review 发现该真实 logs SSE 续传用例此前只用 `first_line not in resp.text`、`second_line in resp.text`、`event: done` 和 `status: done` 片段；如果响应夹带第一条事件、log frame 的 id/stream 漂移、done sentinel payload 退化、API token 续读路径只返回相似文本，或拒绝 detail 漂移，旧测试仍可能绿。现升级为 SSE logs real resume exact frame 契约，避免真实日志 SSE 测试只证明“目标日志文本出现过” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_auth_results_artifacts.py::test_real_sse_events_resume_after_last_event_id_uses_ticket_rbac_and_redis` 1 passed | targeted SSE unit passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；真实 `/runs/{id}/events` SSE Last-Event-ID 集成用例现在固定 Redis status stream 两条 payload 的 `{run_id,status,previous}` 与 timestamp 存在，并解析 EventSource frame，要求只回放第二条 `status_change`、随后 `done` sentinel，ticket 复用固定 401 `Invalid or expired SSE ticket` | 测试 review 发现该真实 SSE 续传用例此前只用 `len(events)==2` 和 `resp.text` 包含 `event/status/previous/done` 片段；如果回放夹带第一条事件、事件顺序错误、done sentinel payload 漂移、Redis previous/status 写错，或 ticket 复用错误体漂移，旧测试仍可能绿。现升级为 SSE events real resume exact frame 契约，避免真实 SSE 测试只证明“文本里出现过几个片段” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_auth_results_artifacts.py::test_auth_token_routes_require_authorization_without_side_effects` 1 passed | targeted ruff passed；targeted docs contract passed | 无新增 warning；真实 API token create/list/delete 无 Authorization 集成用例现在固定三条完整响应投影 `[{status_code, body}]` 均为 401 + `{"detail": "Missing Authorization header"}`，并继续证明 ApiToken 数量和 create/revoke audit 数量不变 | 测试 review 发现该真实认证入口此前只检查状态码列表和 `all("Missing Authorization header" in response.text ...)`；如果其中一路由错误体夹带额外字段、detail 壳漂移、只部分路由返回正确文案，或 401 响应混入不同 body，旧测试仍可能绿。现升级为 Auth token missing Authorization integration exact response 契约，避免真实认证拒绝测试只证明“三个响应文本里都有某句话” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_api_endpoints.py::TestNotificationRules::test_create_notification_rule_rejects_invalid_condition` 2 passed | targeted ruff passed；targeted docs contract passed | 无新增 warning；真实 notification rule invalid condition 集成用例现在固定 422 detail 的 `{type, loc, msg, input}` 单条投影，并继续证明对应 rule name 没有写入 PostgreSQL | 测试 review 发现同一行为的 unit 已经锁住精确 validation detail，但真实 FastAPI+DB 集成仍只用 `resp.text` 包含 `conditions[0].field must be one of`；如果错误 type/loc/input 漂移、同一次请求夹带额外 detail，或别名 `consecutive_failed_runs` 被误放行后其它错误文本仍出现，旧集成测试仍可能绿。现升级为 Notification invalid condition integration exact detail 契约，避免真实集成只证明“错误响应里有相似文字” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_auth_results_artifacts.py::test_auth_refresh_logout_audits_do_not_store_tokens_or_passwords` 1 passed | `tests/unit/test_auth/test_auth_routes.py::TestRefreshRevokesOldToken::test_refresh_revokes_old_refresh_token tests/unit/test_auth/test_auth_routes.py::TestAuditRefresh::test_refresh_success_emits_audit_with_jti` 2 passed；auth routes full 57 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；`auth.refresh` 现在先生成新 access/refresh token，再把真实非空 `new_jti` 写入 audit after_state；rotation 与 audit 专测均解码真实新 refresh cookie 的 jti，并要求它不同于 `old_jti` 且不把 refresh token 原文写入审计 | 测试 review 发现此前生产实现把 `new_jti` 固定写成 `None`，而单测也把 `None` 当正确契约；这类测试只能证明字段名存在，无法发现 refresh token 轮换链路失去审计追踪。现升级为 Auth refresh audit new_jti 真实追踪契约，避免 refresh 审计测试只证明“有一个 new_jti 字段” |
| 2026-05-31 | N/A（Run cancel success exact response/Redis/audit 契约） | `tests/unit/test_api/test_runs.py::test_cancel_run` 1 passed；runs full 50 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；run cancel 成功路径现在固定完整响应体、`cancel_if_current` 可取消状态集合、Redis cancel pub/sub、status stream/hash 的完整 payload 与同一 timestamp，以及 `run.cancel` 审计的完整 before/after/cancel_reason payload | 测试 review 发现该用例此前只抽查 response id/status、Redis event/hash 的 status/previous 和 audit 少数字段；如果响应 schema 夹带/漏字段、可取消状态少了 preparing/collecting、status hash 与 stream timestamp 不一致，或 audit tenant/user/before/after 漂移，旧测试仍可能绿。现升级为 Run cancel success exact response/Redis/audit 契约，避免取消成功测试只证明“状态大概变成 cancelled” |
| 2026-05-31 | N/A（Notification channel text success exact timeout/call 契约） | `tests/unit/test_worker/test_channels.py::TestWebhookChannel::test_success tests/unit/test_worker/test_channels.py::TestDingtalkChannel::test_success_text_message tests/unit/test_worker/test_channels.py::TestWecomChannel::test_success_text_message` 3 passed；channels full 38 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；Webhook/DingTalk/WeCom 基础 text 成功路径现在固定 `ChannelResult(success=True)`、`AsyncClient(timeout=...)`、endpoint、params/headers 与完整 json payload，不再只抽查 kwargs 子字段 | 测试 review 发现这些最常走的通知成功用例此前只看局部 `params/json/headers`；如果 timeout 被删、DingTalk/WeCom endpoint 常量被绕开、token/key 参数串错，或 Webhook payload 夹带额外字段，只要几个子字段仍存在旧测试仍可能绿。现升级为 Notification channel text success exact timeout/call 契约，避免基础通知渠道测试只证明“发过一个看起来像 text 的 payload” |
| 2026-05-31 | N/A（Webhook metadata reserved-key exact merge 契约） | `tests/unit/test_api/test_p3.py::TestWebhookTrigger::test_webhook_trigger_preserves_project_git_auth_metadata` 1 passed；P3 full 60 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；project-level webhook 现在用请求侧伪造的 `GIT_URL/git_auth_method/Credential_ID/default_branch/shallow_clone` 攻击输入，固定 `run.create` 完整 kwargs、dedup 预查参数与最终 metadata 精确等于项目 Git 配置加允许的 `delivery_id/trace_id` | 测试 review 发现该用例此前只抽查 `metadata["git_auth_method"]`、`credential_id`、`delivery_id`；如果 reserved key 大小写绕过、请求侧凭据污染 metadata、`shallow_clone/default_branch` 被覆盖，或 run.create 其它归属字段漂移，旧测试仍可能绿。现升级为 Webhook metadata reserved-key exact merge 契约，避免 webhook Git 凭据测试只证明“三个 metadata 字段看起来对” |
| 2026-05-31 | N/A（Email SMTP error redaction 契约） | `tests/unit/test_worker/test_channels.py::TestEmailChannel::test_connection_error_redacts_config_values` 1 passed；channels full 38 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；EmailChannel 的 SMTP/OSError 分支现在使用 `redact_sensitive_text` 只脱敏 `smtp_password`，保留 host 诊断信息，单测固定返回 `SMTP error: smtp://***@smtp.example.com refused; password=[REDACTED]` 且不泄露密码或 URL userinfo | 测试 review 发现 HTTP 渠道错误已有 token/secret 不回显契约，但 EmailChannel 此前直接返回 `SMTP error: {exc}`，测试也只用 `Connection refused` 片段断言；如果 SMTP/OSError 文本包含密码、带 userinfo 的连接串或认证上下文，旧测试仍可能绿并把密钥写入通知日志。现升级为 Email SMTP error redaction 契约，避免邮件渠道失败测试只证明“错误文本里有连接失败” |
| 2026-05-31 | N/A（Notification channel HTTP request exact args/kwargs 契约） | `tests/unit/test_worker/test_channels.py::TestWebhookChannel::test_custom_method tests/unit/test_worker/test_channels.py::TestWebhookChannel::test_custom_headers tests/unit/test_worker/test_channels.py::TestDingtalkChannel::test_signs_request_when_secret_configured tests/unit/test_worker/test_channels.py::TestDingtalkChannel::test_markdown_message tests/unit/test_worker/test_channels.py::TestWecomChannel::test_markdown_message` 5 passed；channels full 38 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；Webhook 自定义 method/headers、DingTalk 签名/markdown、WeCom markdown 现在固定 HTTP call 的完整 args 与 kwargs，覆盖 endpoint、method、params、headers 和 json payload，不再只检查局部字段 | 测试 review 发现这些渠道用例此前多只看 `kwargs["json"]` 或 params 子字段；如果发错 URL、漏 access_token/key、headers 丢失、签名请求 payload 串成 text/markdown 错形，只要局部字段仍存在旧测试可能绿。现升级为 Notification channel HTTP request exact args/kwargs 契约，避免渠道测试只证明“某个 json/params 片段被传过” |
| 2026-05-31 | N/A（Check schedules failure update/audit 精确契约） | `tests/unit/test_worker/test_check_schedules.py::TestCheckSchedules::test_handles_pipeline_not_found tests/unit/test_worker/test_check_schedules.py::TestCheckSchedules::test_handles_enqueue_failure` 2 passed；check_schedules full 9 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；missing pipeline 与 enqueue failure 现在固定 `update_after_fire` args/kwargs、`next_run_at`、commit 边界、无 env/run/enqueue 副作用，以及 skipped/run.trigger audit 的完整 payload；enqueue failure 还锁住 run.create、set retry group 和 enqueue 参数 | 测试 review 发现这两条失败路径此前只看 `last_error` 和少量 audit 字段；如果 next_run_at 漂移、schedule 更新写错 id、missing pipeline 仍创建 run/enqueue、enqueue failure audit 夹带/漏字段，旧测试仍可能绿。现升级为 Check schedules failure update/audit 精确契约，避免 schedule worker 失败测试只证明“错误字符串被写过” |
| 2026-05-31 | N/A（Analytics repository query exact kwargs/cutoff 契约） | `tests/unit/test_api/test_p3.py::TestAnalytics::test_trends_returns_paginated_response tests/unit/test_api/test_p3.py::TestAnalytics::test_flaky_returns_paginated_response tests/unit/test_api/test_p3.py::TestAnalytics::test_test_history_returns_repository_rows` 3 passed；P3 full 60 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；analytics trends/flaky/test-history 三条成功路径现在固定仓储查询 kwargs 完整等值，并用请求前后时间窗口证明默认 `days=30` 的 timezone-aware cutoff，同时继续锁住完整响应体与不走 `session.execute` | 测试 review 发现这些用例此前只逐字段抽查 project/offset/limit 和 `cutoff.tzinfo is not None`，后来仍保留字段集合守门；如果 cutoff 没减 days、min_runs/suite/name 漂移、仓储调用多带字段，或实现仍返回相同响应 mock，旧测试可能绿。现升级为 Analytics repository query exact kwargs/cutoff 契约，避免 analytics 成功测试只证明“响应体来自 mock 行且仓储大概被调用” |
| 2026-05-31 | N/A（Webhook trigger run create exact identity/enqueue 契约） | `tests/unit/test_api/test_p3.py::TestWebhookTrigger::test_webhook_trigger_creates_run` 1 passed；P3 full 60 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；webhook 成功触发现在固定 dedup 预查 kwargs、`run.create` kwargs 完整等值 `{tenant_id,project_id,pipeline_id,environment_id,git_ref,git_sha,triggered_by,trigger_type,metadata_,dedup_key}`，并锁住 `enqueue_run` 五个位置参数与空 kwargs | 测试 review 发现该用例此前只逐字段抽查部分 create kwargs，后来完整等值后仍保留字段集合守门；如果 run 归属/触发人串错、环境漂移、入队缺 settings，旧测试仍可能 201 或失败信号停在字段集合噪音。现升级为 Webhook trigger run create exact identity/enqueue 契约，避免 webhook 创建 run 测试只证明“run.create 被调用且几个字段看起来对” |
| 2026-05-31 | N/A（Notification failure log exact kwargs/error 契约） | `tests/unit/test_worker/test_notifications.py::TestEvaluateAndNotify::test_send_failure_logged tests/unit/test_worker/test_notifications.py::TestEvaluateAndNotify::test_template_render_failure_logged_without_sending` 2 passed；worker notifications full 35 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；notification send failure 与 template render failure 现在通过 `_assert_delivery_log` 固定 `log_repo.create` kwargs 完整等值，并把 failed log 投影成完整 `{project_id,run_id,rule_id,channel_type,status,error_message}`；send failure 精确到脱敏后的 `send failed for [REDACTED] with [REDACTED]`，template failure 精确到 `unknown template variable: unknown` | 测试 review 发现这两条失败路径此前逐字段抽查，send failure 还只用包含式检查 `send failed` 与脱敏片段，后来仍保留字段集合守门；如果 failed log 多写字段、project/run/rule/channel 串错、错误文本退化或泄露信息仍带着相似片段，旧测试仍可能绿。现升级为 Notification failure log exact kwargs/error 契约，避免通知失败日志测试只证明“失败被记录且大概脱敏” |
| 2026-05-31 | N/A（Notification matching rule log create exact kwargs 契约） | `tests/unit/test_worker/test_notifications.py::TestEvaluateAndNotify::test_matching_rule_sends_notification` 1 passed；worker notifications full 35 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；notification 最基础的 matching-rule 成功路径现在通过 `_assert_delivery_log` 固定 `log_repo.create` kwargs 完整等值，并把 `{project_id,run_id,rule_id,channel_type,status,error_message}` 投影成完整 sent log 契约，同时继续锁住 `get_by_delivery`、`_send_channel` 消息与 commit | 测试 review 发现该用例此前逐字段抽查 `log_repo.create`，后来仍保留字段集合守门；如果日志写入额外字段、status/error 漂移，或 project/run/rule/channel 串错但部分字段仍相似，旧测试定位弱且容易漏。现升级为 Notification matching rule log create exact kwargs 契约，避免基础通知发送测试只证明“发了消息并写过一条日志” |
| 2026-05-31 | N/A（Auth login failure helper 完整 payload 契约） | `tests/unit/test_auth/test_auth_routes.py::TestLogin::test_login_wrong_password tests/unit/test_auth/test_auth_routes.py::TestLogin::test_login_user_not_found tests/unit/test_auth/test_auth_routes.py::TestLogin::test_login_inactive_user` 3 passed；auth routes full 57 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；login wrong-password、user-not-found、inactive 三条失败路径现在通过 `_assert_login_failed_audit` 固定完整 payload `{tenant_id,user_id,action,resource_type,resource_id,after_state,ip_address,user_agent}`，继续证明不发 token/cookie、不更新 last_login | 测试 review 发现 login 成功审计已是完整 payload，但失败 helper 仍逐字段抽查且漏掉 ip/user-agent；如果 login_failed 客户端元数据丢失、payload 夹带多余敏感字段，旧测试仍可能绿。现升级为 Auth login failure helper 完整 payload 契约，避免 login 失败审计只证明“失败原因和用户名写进去了” |
| 2026-05-31 | N/A（Auth refresh failure helper 完整 payload 契约） | `tests/unit/test_auth/test_auth_routes.py::TestRefresh::test_refresh_with_access_token_rejected tests/unit/test_auth/test_auth_routes.py::TestRefresh::test_refresh_expired_token tests/unit/test_auth/test_auth_routes.py::TestRefreshRevokesOldToken::test_refresh_with_revoked_token_returns_401` 3 passed；auth routes full 57 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；refresh access-token-as-refresh、expired refresh token、revoked replay 三条失败路径现在通过 `_assert_refresh_failed_audit` 固定完整 payload `{tenant_id,user_id,action,resource_type,resource_id,after_state,ip_address,user_agent}`，继续证明原 token 不进入 audit，并锁住 cookie 清理与 audit session commit/no rollback | 测试 review 发现这些普通失败路径虽然已复用 helper 检查原因和 token 脱敏，但 helper 仍逐字段抽查且漏掉 ip/user-agent；如果失败审计客户端元数据丢失、payload 多出敏感字段，旧测试仍可能绿。现升级为 Auth refresh failure helper 完整 payload 契约，避免 refresh 失败审计只证明“失败原因写进去了” |
| 2026-05-31 | N/A（API token audit 完整 client metadata 契约） | `tests/unit/test_auth/test_auth_routes.py::TestTokenRoutes::test_create_token tests/unit/test_auth/test_auth_routes.py::TestTokenRoutes::test_revoke_token` 2 passed；auth routes full 57 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；API token create/revoke 主 CRUD 用例现在固定完整审计 payload，覆盖 tenant/user、resource_type/resource_id、before/after_state、ip_address/user_agent，并继续证明 token secret/full token/hash 不进 create audit | 测试 review 发现 API token 审计冗余弱测试已删除，但主 CRUD 用例仍逐字段抽查 action/resource/state，且漏掉客户端元数据；如果 tenant/user/resource_type、ip/user-agent 漂移，或 audit payload 夹带多余字段，旧测试仍可能绿。现升级为 API token audit 完整 client metadata 契约，避免 API token 审计只证明“关键字段大概存在” |
| 2026-05-31 | N/A（Auth logout audit 完整 client metadata 契约） | `tests/unit/test_auth/test_auth_routes.py::TestAuditLogout` 2 passed；auth routes full 57 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；logout 有/无 token 审计现在通过 helper 固定完整 payload `{tenant_id,user_id,action,resource_type,resource_id,after_state,ip_address,user_agent}`，继续证明 access/refresh token 原文不进入 audit，并锁住 refresh cookie 清理与 commit/no rollback | 测试 review 发现 logout 审计虽已覆盖 tenant/user、resource_id、had_access_token、token 脱敏和事务边界，但 helper 仍逐字段抽查且漏掉 ip/user-agent；如果客户端元数据丢失、审计调用夹带多余字段，旧测试仍可能绿。现升级为 Auth logout audit 完整 client metadata 契约，避免 logout 审计测试只证明“核心字段大概存在” |
| 2026-05-31 | N/A（Auth refresh audit 完整 client metadata 契约） | `tests/unit/test_auth/test_auth_routes.py::TestAuditRefresh::test_refresh_success_emits_audit_with_jti tests/unit/test_auth/test_auth_routes.py::TestAuditRefresh::test_refresh_failed_emits_audit` 2 passed；auth routes full 57 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；refresh 成功/失败审计现在固定完整 payload，覆盖 `tenant_id/user_id/action/resource_type/resource_id/before_state/after_state/ip_address/user_agent`，并继续证明 refresh token 原文不进入 audit | 测试 review 发现 refresh 成功审计专测此前逐字段抽查 action/resource/resource_id/tenant/user/jti，但未锁住 ip/user-agent，也未用完整 payload 防止多余字段漂移；失败审计只经 helper 检查核心字段和 token 脱敏。现升级为 Auth refresh audit 完整 client metadata 契约，避免 refresh 审计测试只证明“核心审计字段大概存在” |
| 2026-05-31 | N/A（Auth register audit 完整 payload/脱敏契约） | `tests/unit/test_auth/test_auth_routes.py::TestAuditRegister::test_register_success_emits_audit` 1 passed；auth routes full 57 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；register 成功审计现在固定完整 payload `{tenant_id,user_id,action,resource_type,resource_id,after_state,ip_address,user_agent}`、不含明文密码或 password_hash，并锁住 tenant/user repository 入参与 commit/no rollback | 测试 review 发现 register 成功审计用例此前只抽查 action/resource_type 和 after_state 的 username/email；若 tenant_id/user_id/resource_id/user_agent 漂移、审计泄露密码哈希，或事务未提交/误回滚，旧测试仍可能绿。现升级为 Auth register audit 完整 payload/脱敏契约，避免注册审计测试只证明“写过一个看起来像 register 的事件” |
| 2026-05-31 | N/A（Auth login audit 成功契约与失败冗余清理） | `tests/unit/test_auth/test_auth_routes.py::TestAuditLogin::test_login_success_emits_audit tests/unit/test_auth/test_auth_routes.py::TestLogin::test_login_wrong_password tests/unit/test_auth/test_auth_routes.py::TestLogin::test_login_user_not_found tests/unit/test_auth/test_auth_routes.py::TestLogin::test_login_inactive_user` 4 passed；auth routes full 57 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；login 成功审计现在固定完整 payload `{tenant_id,user_id,action,resource_type,resource_id,after_state,ip_address,user_agent}`、不含明文密码，并锁住 last_login update 与 commit/no rollback。login 失败审计由主失败路径用例负责，删除底部 `TestAuditLogin` 中只看 action/reason 的 user-not-found 与 wrong-password 重复弱测试 | 测试 review 发现 login 成功审计用例此前只抽查 action/resource_type/user_id 和 `ip_address` 存在；两个失败审计专测又与主失败路径重复，但只抽查 action/reason。若 tenant/resource_id/user_agent 漂移、成功审计泄露密码，或失败路径 token/cookie/last_login 副作用回归，旧组合仍可能制造“login audit 已覆盖”的错觉。现升级为 Auth login audit 成功契约与失败冗余清理，避免登录审计靠重复低信号测试凑覆盖 |
| 2026-05-31 | N/A（API token audit 冗余弱测试清理） | `tests/unit/test_auth/test_auth_routes.py::TestTokenRoutes::test_create_token tests/unit/test_auth/test_auth_routes.py::TestTokenRoutes::test_revoke_token` 2 passed；auth routes full 59 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；API token create/revoke 审计现在由主 CRUD 契约负责：create 锁住完整响应、repo create、`auth.api_token_create` 的 resource/after_state/脱敏/事务；revoke 锁住 get/revoke、`auth.api_token_revoke` 的 resource/before_state/事务。删除底部 `TestAuditApiTokens` 中只看 action/name/user_id 的重复弱测试 | 测试 review 发现 `TestAuditApiTokens` 与 `TestTokenRoutes` 重复覆盖同一路径，但前者只抽样审计字段，create 还使用 `tok123` 这种非生产 token_id record；如果 resource_id、scopes、脱敏或事务漂移，弱测试仍会绿且制造“审计已覆盖”的错觉。现升级为 API token audit 冗余弱测试清理，避免 API token 审计靠重复低信号测试凑覆盖 |
| 2026-05-31 | N/A（TokenService generated token 冗余 smoke 清理） | `tests/unit/test_auth/test_token_service.py::TestGenerateToken` 2 passed；targeted token service passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；TokenService generate_token 现在保留一条完整 public format/roundtrip 契约，唯一性契约改为 token_id/full_token/secret 的重复值投影必须为空，删除 `startswith`、三段数量、token_id 匹配、token_id 长度这些被完整格式测试覆盖的碎片 smoke | 测试 review 发现该类此前同一行为被拆成多条弱断言，数量看起来多，但每条只能证明一个局部字符串现象；如果 full token secret 固定复用、secret 格式漂移、重复值被集合长度断言吞掉上下文，或 parse roundtrip 失效，部分碎片测试仍会绿。现升级为 TokenService generated token 冗余 smoke 清理，避免 token 生成测试靠多条低价值用例制造覆盖感 |
| 2026-05-31 | N/A（Logout revoke Redis 参数契约） | `tests/unit/test_auth/test_auth_routes.py::TestLogoutRevokesTokens` 4 passed；targeted auth routes passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；logout access-only、refresh-only、双 token、无 token 路径现在固定 204 空响应与 refresh cookie 清理；access/refresh 撤销分别锁住 `redis.set` key/value 与 TTL 上界，双 token 路径继续锁住两次 revoke 顺序，无 token 路径锁住不写 Redis | 测试 review 发现这些用例此前有的只断言 blacklist key 出现在内存 store，有的没有证明 204 不夹带 body；如果实现写入错误 value、TTL 缺失/过长、access/refresh revoke 顺序混乱、无 token 路径误写 Redis，或 204 响应夹带 body，只要 key 曾被写入旧测试仍可能绿。现升级为 Logout revoke Redis 参数契约，避免 logout 撤销测试只证明“jti 进了某个字典” |
| 2026-05-31 | N/A（API token create exact response body 契约） | `tests/unit/test_auth/test_auth_routes.py::TestTokenRoutes::test_create_token` 1 passed；targeted auth routes passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；API token create 现在固定完整响应体 `{token_id, token, name, scopes, expires_at, created_at}`，`expires_at` 必须来自仓储 create 入参，`created_at` 固定为返回 record 时间，并继续证明响应不含 `secret_hash`/`hashed-secret`、hash 只接收 64 hex secret、audit 不泄露明文/哈希、事务 commit/no rollback | 测试 review 发现该用例此前只抽查 `token_id/token/name/scopes` 和 `secret_hash not in data`；如果响应漏掉 `created_at`/`expires_at`、时间格式漂移、夹带额外字段、hash 泄入响应文本，或响应 expiry 不是实际 create 入参，旧测试仍可能绿。现升级为 API token create exact response body 契约，避免 API token 创建测试只证明“几个关键字段看起来对且 data 里没有 secret_hash” |
| 2026-05-31 | N/A（Auth refresh rotation exact token/audit 契约） | `tests/unit/test_auth/test_auth_routes.py::TestRefreshRevokesOldToken::test_refresh_revokes_old_refresh_token` 1 passed；targeted auth routes passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；refresh rotation 成功路径现在固定新响应体、`token_type=bearer`、新 access token claims、新 refresh cookie claims、旧 refresh token 不再作为 cookie 返回、old jti blacklist lookup 与 `redis.set(..., ex<=refresh_ttl)`，并锁住 user lookup、`auth.refresh` audit 的 `old_jti/new_jti` 与 commit/no rollback | 测试 review 发现该用例此前只断言 200 和旧 jti 进入内存 store；如果实现没有轮换 refresh cookie、access claims 串错用户/租户/role、blacklist 写入缺 TTL、audit 丢 `old_jti`，或只在 store 里出现旧 key 但没有真正按 Redis 参数写入，旧测试仍可能绿。现升级为 Auth refresh rotation exact token/audit 契约，避免 refresh rotation 测试只证明“旧 jti 进了某个字典” |
| 2026-05-31 | N/A（Auth register conflict exact rollback 契约） | `tests/unit/test_auth/test_auth_routes.py::TestRegister::test_register_username_conflict` 1 passed；targeted auth routes passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；register username conflict 现在固定完整 409 响应体、无 access/refresh token、无 refresh cookie、只执行 tenant name lookup、不会 create tenant/user、不会构造 UserRepository/AuditEventRepository，并要求 session rollback 且不 commit | 测试 review 发现该用例此前只断言 409 与 `already taken` 子串；如果错误体漂移、冲突路径仍发 cookie/token、事务未 rollback、或提前构造 user/audit repository，只要 detail 里仍含相似文本旧测试仍可能绿。现升级为 Auth register conflict exact rollback 契约，避免注册冲突测试只证明“返回了一个包含 already taken 的 409” |
| 2026-05-31 | N/A（Auth success token claims 精确契约） | `tests/unit/test_auth/test_auth_routes.py::TestLogin::test_login_success tests/unit/test_auth/test_auth_routes.py::TestRegister::test_register_success tests/unit/test_auth/test_auth_routes.py::TestRefresh::test_refresh_success` 3 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；login/register/refresh 成功路径现在固定完整响应 body、`token_type=bearer`、用户响应体、access token 的 `sub/role/tenant_id/is_platform_admin/type/jti/exp/iat` claims，以及 refresh cookie token 的 `sub/type/jti/exp/iat`；测试 helper 默认用户也显式 `is_platform_admin=False` | 测试 review 发现这些用例此前主要断言 `access_token` 存在、`refresh_token` 不在 body、cookie 存在；如果 token claims 串错用户/租户/role、`token_type` 漂移、响应夹带额外字段、refresh cookie 里仍发给错误用户，甚至 `MagicMock.is_platform_admin` truthy 化成错误 admin claim，旧测试仍可能绿。现升级为 Auth success token claims 精确契约，避免认证成功测试只证明“发了某个 token” |
| 2026-05-31 | N/A（Notification legacy/invalid list exact response 契约） | `tests/unit/test_api/test_notifications.py::test_list_rules_normalizes_legacy_channel_response_shape tests/unit/test_api/test_notifications.py::test_list_rules_marks_historical_invalid_conditions_without_500` 2 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；notification rule list 的 legacy webhook channel 与 historical invalid condition 用例现在固定完整分页响应体、canonical `config.url`、`consecutive_failed_runs -> consecutive_failures` alias、invalid condition 的 `reason/raw_field/raw_operator`，并继续证明 project access、list pagination 与 no audit | 测试 review 发现这些用例此前只检查 `resp.json()["data"][0]["channels"]` / `conditions` 的局部片段；如果分页壳漂移、字段集合缺失、invalid reason 文案退化、raw_field/raw_operator 丢失，或 legacy alias 只在局部看起来正确，旧测试仍可能绿。现升级为 Notification legacy/invalid list exact response 契约，避免历史兼容列表测试只证明“嵌套片段被归一化了” |
| 2026-05-31 | N/A（Batch business outcome exact response body 契约） | `tests/unit/test_api/test_p3.py::TestBatchCancel::test_batch_cancel_success tests/unit/test_api/test_p3.py::TestBatchCancel::test_batch_cancel_not_found tests/unit/test_api/test_p3.py::TestBatchRetry::test_batch_retry_success tests/unit/test_api/test_p3.py::TestBatchRetry::test_batch_retry_not_terminal` 4 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；batch cancel/retry 的 success、not-found、not-terminal 业务分支现在固定完整响应体 `{processed, failed, errors}`，同时保留 cancel/status publish、retry create/enqueue、audit 的正负副作用契约 | 测试 review 发现这些用例此前只拆字段检查 `data["processed"]` / `data["failed"]` / `data["errors"]`；如果响应夹带 `details`、`trace_id`、额外 error context，或成功分支 `errors` 缺失/漂移，只要核心字段仍正确旧测试仍可能绿。现升级为 Batch business outcome exact response body 契约，避免 batch 业务结果测试只证明“关键计数字段看起来对” |
| 2026-05-31 | N/A（Batch internal failure exact response body 契约） | `tests/unit/test_api/test_p3.py::TestBatchCancel::test_batch_cancel_internal_failure_redacts_exception_without_side_effects tests/unit/test_api/test_p3.py::TestBatchRetry::test_batch_retry_internal_failure_redacts_exception_without_side_effects` 2 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；batch cancel/retry 内部异常用例现在固定完整响应体只包含 `processed=0`、`failed=1`、`errors=[<run_id>: operation failed]`，并继续证明 SQL/token/底层异常文本不回显、无 Redis/audit/retry 副作用 | 测试 review 发现这些用例此前拆字段检查 `data["processed"]` / `data["failed"]` / `data["errors"]`；如果响应夹带 `details`、`trace_id`、异常类型或原始错误上下文，只要三字段仍正确旧测试仍可能绿。现升级为 Batch internal failure exact response body 契约，避免内部错误脱敏测试只证明“核心三字段看起来对” |
| 2026-05-31 | N/A（SSE Last-Event-ID resume frame/cursor 精确契约） | `tests/unit/test_api/test_sse.py::test_stream_logs_accepts_last_event_id_query_param tests/unit/test_api/test_sse.py::test_stream_events_accepts_last_event_id_query_param` 2 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；logs/events SSE `last_event_id` query 续传用例现在同时固定 Redis xread cursor/count/block、回放事件 id、事件序列 `log/status_change -> done`、data JSON 与 done status | 测试 review 发现这两条用例此前只断言首个 `xread` cursor；如果实现读取游标正确但响应退成 heartbeat、漏掉 done sentinel、回放事件 id 错误、或 status/log payload 形状漂移，旧测试仍可能绿。现升级为 SSE Last-Event-ID resume frame/cursor 精确契约，避免 SSE 续传测试只证明“Redis 从某个游标读了” |
| 2026-05-31 | N/A（SSE stream frame/payload 精确契约） | `tests/unit/test_api/test_sse.py::test_logs_sse_streams_log_event_payload tests/unit/test_api/test_sse.py::test_events_sse_streams_status_event_payload tests/unit/test_api/test_sse.py::test_stream_logs_same_project_returns_200_event_stream` 3 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；logs/events SSE 正向用例现在解析 EventSource frame，固定事件序列为 `log/status_change -> done`、首个事件 id、data JSON payload 与 done status，并继续锁住 Redis xread cursor/count/block | 测试 review 发现这些用例此前只用 `resp.text` 包含 `id:`、`event:` 和 JSON 片段；如果事件顺序颠倒、done sentinel 丢失、data JSON 缺字段、log/status_change event 混淆，或响应里同时夹带错误事件，只要目标片段还在旧测试仍可能绿。现升级为 SSE stream frame/payload 精确契约，避免 SSE 正向测试只证明“文本里出现过某些片段” |
| 2026-05-31 | N/A（Schedule validation detail 精确契约） | `tests/unit/test_api/test_schedules.py::test_schedule_routes_reject_invalid_cron_before_side_effects tests/unit/test_api/test_schedules.py::test_schedule_routes_reject_invalid_timezone_before_side_effects tests/unit/test_api/test_schedules.py::test_schedule_routes_reject_invalid_quiet_windows_before_side_effects` 3 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；schedule cron_expr、timezone、quiet_windows 的 create/update 负向用例现在固定 `_validation_detail_lines(response)` 的单条 detail，并继续证明不查 project/pipeline/schedule、不调用 `compute_next_run_at`、不写 audit | 测试 review 发现这三条用例此前只用 `response.text` 包含式断言错误片段；如果同一次请求夹带额外 validation detail、loc/type 文案漂移，或 quiet_windows 错误壳从字段级退成笼统文本，只要目标片段还在旧测试仍可能绿。现升级为 Schedule validation detail 精确契约，避免 schedule 请求体验证测试只证明“某段错误文本出现过” |
| 2026-05-31 | N/A（Pipeline core schema validation detail 精确契约） | `tests/unit/test_api/test_pipelines.py::test_pipeline_routes_reject_invalid_schema_before_side_effects` 8 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；pipeline core schema create/update 负向用例现在为非 name 字段补合法 name，避免 create 只靠缺少 name 失败，并固定 `_validation_detail_lines(response) == [expected_detail]` 的单条 detail，覆盖 name、timeout_seconds、stage name/plugin、collector plugin | 测试 review 发现该用例此前只用 `expected_text in create_resp.text/update_resp.text`；非 name payload 在 create 路径还会同时触发 `body -> name: Field required`，旧测试无法发现目标字段校验丢失、detail 数量变多、loc/type 文案漂移。现升级为 Pipeline core schema validation detail 精确契约，避免 schema 负向测试只证明“某段错误文本出现过” |
| 2026-05-31 | N/A（Pipeline selector/retry/trigger_config validation detail 精确契约） | `tests/unit/test_api/test_pipelines.py::test_pipeline_routes_reject_invalid_selector_before_side_effects tests/unit/test_api/test_pipelines.py::test_pipeline_routes_reject_invalid_retry_policy_before_side_effects tests/unit/test_api/test_pipelines.py::test_pipeline_routes_reject_invalid_trigger_config_before_side_effects` 13 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；pipeline selector include/exclude/tags、retry_policy retry_on、trigger_config type/dedup_window_seconds 的 create/update 负向用例现在固定 `_validation_detail_lines(response) == [expected_detail]`，并锁住列表上限错误的完整数量文案 `after validation, not 101/21` | 测试 review 发现这些用例此前只用 `expected_detail in " | ".join(...)`；如果响应夹带额外 validation detail、loc/type 文案漂移，或列表上限文案丢掉实际数量，只要目标片段还在旧测试仍可能绿。现升级为 Pipeline selector/retry/trigger_config validation detail 精确契约，避免 pipeline 嵌套配置验证测试只证明“某段错误文本出现在某处” |
| 2026-05-31 | N/A（Unit all-assertion direct contract sweep） | `tests/unit/test_api/test_environments.py::test_environment_routes_return_503_when_crypto_unavailable tests/unit/test_auth/test_auth_routes.py::TestLogin::test_login_inactive_user tests/unit/test_auth/test_auth_routes.py::TestRefresh::test_refresh_missing_cookie tests/unit/test_api/test_rate_limit.py::TestRateLimitMiddlewareDispatch::test_different_tokens_independent_quotas tests/unit/test_api/test_rate_limit.py::TestRateLimitMiddlewareDispatch::test_third_token_passes_when_others_exhausted tests/unit/test_api/test_rate_limit.py::TestRateLimitMiddlewareDispatch::test_uuid_path_segments_share_one_bucket tests/unit/test_api/test_webhook_signature.py::TestGenerateAndVerify::test_signature_format_is_sha256_equals_hex` 7 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；unit 测试中的 `assert all(...)` 已清空：environment crypto 503 改为五个响应体直接列表匹配，auth refresh cookie 禁止项改为过滤出的 refresh cookie header 精确等于空列表，rate-limit bucket key 泄漏检查改为渲染后的 key 文本不含原 token/run id，webhook signature 格式改为 `sha256=[0-9a-f]{64}` fullmatch | 测试 review 发现这些用例此前把核心判断藏在 generator 里；如果 cookie header 大小写漂移、rate-limit key 正确但泄漏检查难以定位、或签名格式拆成 startswith/len/逐字符检查，失败定位和扫描质量都偏弱。现升级为直接契约，避免 unit 测试继续保留“all 里藏判断”的低可读性断言 |
| 2026-05-31 | N/A（Missing project route matrix ErrorResponse 直接列表契约） | `tests/unit/test_api/test_credentials.py::test_credential_routes_hide_missing_project_without_side_effects tests/unit/test_api/test_project_members.py::test_project_member_routes_hide_missing_project_without_side_effects tests/unit/test_api/test_notifications.py::test_notification_rule_routes_hide_missing_project_without_side_effects tests/unit/test_api/test_environments.py::test_update_delete_environment_return_same_404_without_side_effects tests/unit/test_api/test_environments.py::test_environment_routes_hide_missing_project_without_side_effects` 5 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；credentials、project members、notification rules、environments 的 missing project / hidden environment 路由矩阵现在直接断言完整 `NOT_FOUND` ErrorResponse 列表，覆盖每个入口的 `{code,message,details}`，并保留 tenant lookup 与零副作用断言 | 测试 review 发现这些用例此前通过 `assert all(body == bodies[0] for body in bodies)` 加首个 body 精确检查来间接证明统一响应；虽然语义成立，但扫描和维护时容易退回“只证明响应一致”。现改为 `bodies == [expected_body] * N` 的直接契约，避免隐藏资源矩阵测试只证明“所有路由返回同一个东西” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_cancel_e2e.py::test_cancel_at_stage_boundary_skips_next_stage tests/integration/test_cancel_e2e.py::test_cancel_recovered_when_published_before_subscribe --collect-only` 2 tests collected | targeted ruff passed；targeted docs contract passed | 未执行 heavy Docker 集成；collect-only/ruff/docs contract 无新增 warning；stage-boundary cancel 现在固定唯一 `create_execution` 的 spec 必须是 stage-1、同一 run_id、命令 `sleep 1`；pre-subscribe cancel 现在用 `assert_not_awaited()` 禁止任何容器启动 | 测试 review 发现这两条 E2E 此前只断言 `create_execution_spy.await_count == 1` / `== 0`；如果边界取消跳过 stage-1 却启动 stage-2 一次，或 pre-subscribe 分支只靠次数文案判断，旧断言表达力不足。现升级为 cancel E2E create_execution spec/zero-launch 精确契约，避免取消竞态测试只证明“容器启动次数看起来对” |
| 2026-05-31 | N/A（DockerBackend OOM inspect retry backoff 精确契约） | `tests/unit/test_engine/test_docker_backend.py::TestDockerBackend::test_wait_retries_oom_inspect_after_sigkill_exit` 1 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；SIGKILL 137 后 OOMKilled inspect retry 用例现在固定两次无参 `container.show()`，并要求第一次 `OOMKilled=False` 后只 sleep 一次 `0.25` 再重查，第二次 `OOMKilled=True` 后直接返回 | 测试 review 发现该用例此前只断言 `mock_container.show.await_count == 2`；如果实现去掉 retry backoff、忙等 Docker inspect，或在已发现 OOMKilled 后继续 sleep/重查，只要总 inspect 次数为 2 旧测试仍可能绿。现升级为 DockerBackend OOM inspect retry backoff 精确契约，避免 Docker wait OOM 测试只证明“inspect 了两次” |
| 2026-05-31 | N/A（RBAC project visibility/member lookup 序列精确契约） | `tests/unit/test_api/test_rbac_deps.py::TestRequireProjectPermission::test_missing_or_cross_tenant_project_raises_404_before_rbac tests/unit/test_api/test_rbac_deps.py::TestRequireProjectPermission::test_visible_project_member_without_membership_is_403` 2 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；require_project_permission 的 missing/cross-tenant 404 路径现在固定只执行 `{project_id, tenant_id}` visibility 查询；visible project 但无 membership 的 403 路径固定按顺序执行 visibility 查询后再执行 `{project_id, user_id, tenant_id}` membership 查询 | 测试 review 发现这两条用例此前分别只断言 `session.execute.await_count == 1` / `== 2`，再用 helper 做包含式参数检查；如果查询夹带额外参数、顺序漂移，或 visibility 查询意外带上 user_id 导致存在性泄漏风险，旧测试不够敏感。现升级为 RBAC project visibility/member lookup 序列精确契约，避免 RBAC 依赖测试只证明“查了一次/两次且大概包含关键参数” |
| 2026-05-31 | N/A（Worker claim none 无副作用契约） | `tests/unit/test_worker/test_tasks.py::TestClaimReleasesRowLock::test_no_commit_when_claim_returns_none` 1 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；claim_for_worker 返回 None 的早退路径现在固定不 commit、不 refresh、不查 project、不会发布 preparing/cancelled status event，也不会执行 RunExecutor | 测试 review 发现该用例此前只断言 `mock_session.commit.await_count == 0`；如果实现 claim 失败后误发状态事件、刷新不存在的 run、查 project 或触发 executor，只要没有 commit 旧测试仍可能绿。现升级为 Worker claim none 无副作用契约，避免 claim 失败测试只证明“没提交事务” |
| 2026-05-31 | N/A（Heartbeat transient Redis error 确定性循环契约） | `tests/unit/test_worker/test_tasks.py::TestHeartbeatLoop::test_heartbeat_continues_after_redis_error` 1 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；heartbeat transient Redis error 用例现在用受控 `asyncio.sleep` side effect 精确驱动失败后两次成功写入，再由第三次 sleep 抛出 CancelledError；同时固定三次 sleep interval、三次 Redis heartbeat key、UTC ISO timestamp 与 `ex=90` TTL | 测试 review 发现该用例虽然已校验每次 `redis.set` 参数，但仍依赖真实 `sleep(0.2)` 和 `redis.set.await_count >= 3` 下界；如果事件循环调度变慢会变脆，若实现额外写多次 heartbeat 旧测试也不会报错。现升级为确定性三轮循环契约，避免心跳容错测试只证明“跑够时间后至少写过几次” |
| 2026-05-31 | N/A（Worker early cancel release 顺序精确契约） | `tests/unit/test_worker/test_tasks.py::TestArchiveBlocking::test_worker_releases_claim_when_run_was_already_cancel_requested` 1 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；cancel_requested_at 早退用例现在固定 claim、第一次 commit、preparing status event、refresh、cancel_if_current、cancelled status event、release_worker、第二次 commit 的完整顺序，并让 project lookup 的 `session.execute` 一旦被触碰就失败 | 测试 review 发现该用例此前只断言 `mock_session.execute.await_count == 0` 和 `mock_session.commit.await_count == 2`；如果实现先查 project、把第一次 commit 挪到取消之后、漏发 preparing/cancelled 状态事件，或 release_worker 顺序漂移，只要总提交次数仍为 2 旧测试仍可能绿。现升级为 Worker early cancel release 顺序精确契约，避免 worker 早退取消测试只证明“没执行容器且提交过两次” |
| 2026-05-31 | N/A（Notification template/lazy log create 精确契约） | `tests/unit/test_worker/test_notifications.py::TestEvaluateAndNotify::test_channel_template_overrides_rule_template_and_loads_project_name tests/unit/test_worker/test_notifications.py::TestEvaluateAndNotify::test_project_name_is_not_loaded_when_templates_do_not_reference_it` 2 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；channel template override 与 lazy project-name 用例现在除了固定 `_send_channel` 消息，也固定两条 `log_repo.create` 的完整 `{project_id, run_id, rule_id, channel_type, status, error_message}` 投影 | 测试 review 发现这两条用例此前只断言 `log_repo.create.await_count == 2`；如果模板分支发送参数正确但日志落错 project/run/rule、status/error 漂移，或 lazy project-name 分支漏记某个 channel，旧测试仍可能绿。现升级为 Notification template/lazy log create 精确契约，避免通知模板测试只证明“消息发出且写了两条日志” |
| 2026-05-31 | N/A（Check schedules run trigger commit 顺序精确契约） | `tests/unit/test_worker/test_check_schedules.py::TestCheckSchedules::test_fires_due_schedule` 1 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；schedule happy-path 用例现在记录 create run、set retry group、第一次 commit、enqueue、audit、update_after_fire、第二次 commit 的完整操作序列，证明 run 在入队前已提交，schedule 更新和 audit 进入第二个事务 | 测试 review 发现该用例此前只断言 `ctx_session.commit.await_count == 2`；如果实现把 enqueue 放到 run commit 之前、漏掉 update_after_fire 后的提交，或把 audit/schedule update 顺序挪错，只要总提交次数仍为 2 旧测试仍可能绿。现升级为 Check schedules run trigger commit 顺序精确契约，避免 schedule worker 主路径测试只证明“提交过两次” |
| 2026-05-31 | N/A（RunExecutor container log redaction 写日志精确契约） | `tests/unit/test_engine/test_executor_redaction.py::TestExecutorLogRedaction::test_stream_container_logs_redacts_environment_secret_values` 1 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；container stdout/stderr secret redaction 用例现在固定 `log_stream.write_log` 的完整 await 序列：同一个 run id、stdout/stderr 两条脱敏后 line、对应 stream kwarg 都必须精确匹配 | 测试 review 发现该用例此前只断言 `log_stream.write_log.await_count == 2`，再用 `all(...)` 检查两条 line 不含 secret 且含 `[REDACTED]`；如果实现写错 run_id、stdout/stderr 对调、line 夹带额外内容或 stream kwarg 漂移，旧测试仍可能绿。现升级为 RunExecutor container log redaction 写日志精确契约，避免日志脱敏测试只证明“写了两条看起来脱敏的日志” |
| 2026-05-31 | N/A（Cancel watcher repeated message 持续监听契约） | `tests/unit/test_engine/test_cancel.py::TestWatchForCancel::test_callback_invoked_for_each_message` 1 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；重复 cancel message 用例现在用两个 Event 等待每次 callback，第一条和第二条消息处理后都断言 watcher task 仍未结束，且第一条后没有 unsubscribe/close；pubsub stream 关闭后才精确执行两次无参 callback、一次 unsubscribe 和 `aclose` | 测试 review 发现该用例此前只断言 `on_cancel.await_count == 2`；如果 watcher 在第二条消息后提前退出、提前关闭订阅，或 callback 参数漂移，只要最终回调次数仍为 2 旧测试仍可能绿。现升级为 Cancel watcher repeated message 持续监听契约，避免取消监听测试只证明“收到过两次回调” |
| 2026-05-31 | N/A（LogStream archive retry S3/TTL 精确契约） | `tests/unit/test_engine/test_log_stream.py::TestLogStream::test_retry_failed_archives_replays_recorded_runs` 1 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；archive retry 用例现在固定 retry set 两个 run 的 `xrange` cursor 序列，只上传成功 run 的 `logs/{run_id}.jsonl`，并要求 S3 `Bucket/Key/Body/ContentType` 精确匹配；成功 run 写 1h stream TTL 并清 retry marker，失败 run 写 24h TTL 并保留 retry marker | 测试 review 发现该用例此前只断言 `s3_client.put_object.await_count == 1`；如果 retry worker 上传错 bucket/key/body/content-type、读错 failed run stream，或成功/失败 TTL 与 retry marker 分流漂移，旧测试仍可能绿。现升级为 LogStream archive retry S3/TTL 精确契约，避免归档补偿测试只证明“某次重试上传了一次” |
| 2026-05-31 | N/A（FairScheduler dequeue capacity stop 元数据精确契约） | `tests/unit/test_worker/test_scheduler.py::test_try_dequeue_waiting_enqueues_until_capacity_is_full` 1 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；waiting dequeue 容量边界用例现在固定 `find_waiting(limit=10)`、global active count 两次、project active count 只检查第一条 waiting run、只将第一条 run 以 medium queue / `run:{id}` job id 入队，并要求 `mark_enqueued` 写入同一 queue/job/timezone-aware enqueued_at 且不调用 `mark_waiting` | 测试 review 发现该用例此前只断言 `repo.mark_enqueued.await_count == 1` 和 first run id；如果 dequeue 把第一条 run 发到错误队列、漏写 arq_job_id/queue_name/enqueued_at，或错误检查第二条 project quota，旧测试仍可能绿。现升级为 FairScheduler dequeue capacity stop 元数据精确契约，避免 waiting queue 测试只证明“容量满前入队了一个 run” |
| 2026-05-31 | N/A（LogStream batch truncation Redis 参数精确契约） | `tests/unit/test_engine/test_log_stream.py::TestLogStreamLineTruncation::test_write_batch_truncates_each_entry` 1 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；LogStream batch 截断用例现在固定 `pipeline(transaction=False)`、两条 `xadd` 的 stream key、payload stream/line、`maxlen=_MAXLEN`、`approximate=True`，并要求 pipeline `execute()` 被 await；第二条长 stderr line 继续证明 UTF-8 byte 长度不超过 4096 且保留 truncation marker | 测试 review 发现该用例此前只断言 `pipeline_mock.xadd.call_count == 2`，再抽查第一条 line 与第二条截断结果；如果 batch 写入漏掉 Redis trim 参数、写错 run stream key、把 stderr 写成 stdout，或忘记执行 pipeline，旧测试仍可能绿。现升级为 LogStream batch truncation Redis 参数精确契约，避免 batch 日志测试只证明“排了两条 xadd” |
| 2026-05-31 | N/A（RunExecutor artifact upload storage 精确契约） | `tests/unit/test_engine/test_executor.py::TestUploadArtifacts::test_upload_writes_artifact_rows_with_inferred_type_and_mime` 1 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；artifact 上传正向用例现在固定三次 S3 `put_object` 的 bucket、storage key 与 Body 本地相对路径，并要求 artifact rows 的 name/run_id/storage_path 集合精确对应同一批上传对象，继续锁住 junit/report/other 类型与 mime | 测试 review 发现该用例此前只断言 `s3.put_object.await_count == 3` 和 `artifact_repo.create.await_count == 3`，随后主要检查 DB row；如果对象上传到错 bucket/key、上传文件与记录的 storage_path 不一致，或 DB 夹带额外 artifact row，旧测试仍可能绿。现升级为 RunExecutor artifact upload storage 精确契约，避免 artifact 上传测试只证明“上传和记录都发生了三次” |
| 2026-05-31 | N/A（Environment disk limit update/audit 精确契约） | `tests/unit/test_api/test_environments.py::test_update_environment_updates_and_clears_disk_limit` 1 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；environment disk_mb 更新/清除现在固定两次 tenant-scoped project lookup、两次同 env_id lookup、两次 `environment.update(env, resource_limits=...)` 完整参数，并证明清除 disk_mb 时仍保留 `max_artifact_size_mb/max_artifacts_count`；两条 `environment.update` 审计也必须记录 1024→2048、2048→None 的 before/after disk 变化且 env_vars 只保留 redacted/count | 测试 review 发现该用例此前只断言 `environment.update.await_count == 2`、第一次 resource_limits 里有 `disk_mb`、第二次没有；如果 update 传错 env、漏 tenant scope、清除 disk 时丢掉 artifact 限制，或审计 before/after disk 漂移，旧测试仍可能绿。现升级为 Environment disk limit update/audit 精确契约，避免环境资源限制测试只证明“更新接口调用了两次” |
| 2026-05-31 | N/A（Schedule missing project tenant lookup 精确契约） | `tests/unit/test_api/test_schedules.py::test_schedule_routes_hide_missing_project_without_side_effects` 1 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；schedule list/create/get/update/delete 五个父项目缺失入口现在固定每次都以同一个 `project_id` 和 `mock_user.tenant_id` 调用 `project.get_for_tenant`，响应仍为标准 404 `Project not found` envelope，且不回显 project_id/schedule_id、不触碰 pipeline/schedule/audit/next_run 计算 | 测试 review 发现该用例此前只断言 `project.get_for_tenant.await_count == 5`；如果某个 CRUD 入口查错 project_id、漏掉 tenant scope，或 404 响应回显探测 ID，只要状态码和调用次数没变旧测试仍可能绿。现升级为 Schedule missing project tenant lookup 精确契约，避免 schedule 父资源隐藏测试只证明“五个入口都查过项目” |
| 2026-05-31 | N/A（SSE ticket atomic getdel 参数精确契约） | `tests/unit/test_api/test_sse.py::test_authenticate_sse_ticket_consumes_atomically` 1 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；并发复用同一个 SSE ticket 时现在固定只会有一个 UserIdentity 成功、一个 401 `Invalid or expired SSE ticket`，两次 Redis `getdel` 都必须命中同一个 `sse_ticket:{ticket}` key，且继续证明不会回退到 `get` + `delete` 非原子组合 | 测试 review 发现该用例此前用手写 `call_count == 2` 证明 getdel 被碰过；如果 key 拼错、第二次读取了不同 key、失败 detail 漂移，或 ticket 被回显，旧测试仍可能绿。现升级为 SSE ticket atomic getdel 参数精确契约，避免并发 ticket 测试只证明“两个调用一成一败” |
| 2026-05-31 | N/A（Admin status zero-run query 精确契约） | `tests/unit/test_api/test_admin.py` 4 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；`/admin/status` 正常与 zero-run 分支现在共用查询契约：queue depth 查询 `queued/preparing`，in-flight 查询 `running/collecting`，1h 分母查询 `done/failed/cancelled/timeout`，分子查询 `done`，且分母/分子必须使用同一个 timezone-aware `since` 对象 | 测试 review 发现 zero-run 用例此前只断言 `count_by_statuses` / `count_finished_since_by_statuses` await 次数；如果实现把 zero-run 分支的队列状态、终态分母或分子窗口查错，只要返回 `success_rate_1h == 1.0` 旧测试仍可能绿。现升级为 admin status zero-run query 精确契约，避免状态页 zero-run 测试只证明“没有运行时显示 100%” |
| 2026-05-31 | N/A（JWT tampered payload decode 精确契约） | `tests/unit/test_auth/test_jwt_service.py::TestDecodeToken::test_raises_on_tampered_payload` 1 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；篡改 payload 的 access token 现在固定抛出 `jwt.InvalidSignatureError`，`args == ("Signature verification failed",)`，且错误文本不包含原 token、篡改 token、`u1` 或 `viewer` claim | 测试 review 发现该用例此前只断言 `"Signature verification failed"` 出现在异常字符串里；如果异常类型退化、args 漂移，或错误文本回显 tampered token / subject / role，旧测试仍可能绿。现升级为 JWT tampered payload decode 精确契约，避免 JWT tamper 测试只证明“某段签名失败文案出现过” |
| 2026-05-31 | N/A（API token parser generated-shape 短路契约） | `tests/unit/test_auth/test_token_service.py tests/unit/test_auth_middleware.py tests/unit/test_auth/test_auth_routes.py::TestTokenRoutes::test_create_token` 71 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；`TokenService.parse_bearer_token` 现在只接受生成器同形的 `qap_<32 lowercase hex token_id>_<64 lowercase hex secret>`，畸形 qap token 固定 401 format 且不打开 DB，正常路径继续按 32 hex token_id 查库并用 64 hex secret 校验；auth create-token 路由测试也固定返回同形 token，并证明 hash 只接收 64 hex secret | 测试 review 发现 parser 测试此前把 `qap_abc123_secret456` 和带下划线 secret 视为有效，中间件测试也用 `qap_tokenid_secret` 覆盖成功路径，auth route 测试还用 `qap_tok123_plain-secret` 这类非生产 token 形状；如果畸形 token 继续打 DB、短 secret 被接受，或 create-token 路由测试返回一个后续无法认证的 token，旧测试仍可能绿。现升级为 API token parser generated-shape 短路契约，避免 API token parser/route 测试固化非生产生成格式 |
| 2026-05-31 | N/A（API token create secret hash 精确契约） | `tests/unit/test_auth/test_token_service.py::TestCreateApiToken::test_hash_is_argon2id` 1 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；create_api_token 现在固定存储的 `secret_hash` 使用 `$argon2id$` PHC 格式，且必须能用生成 token 第三段 secret 验证通过，同时完整 token 与 user_id 不能通过该 hash | 测试 review 发现该用例此前只断言 hash 字符串以 `$argon2` 开头；如果实现误 hash 完整 token、token_id/user_id，或 Argon2 variant 漂移，旧测试仍可能绿。现升级为 API token create secret hash 精确契约，避免 token 创建测试只证明“存了某个 Argon2-looking hash” |
| 2026-05-31 | N/A（API token generated format/entropy 精确契约） | `tests/unit/test_auth/test_token_service.py::TestGenerateToken::test_token_has_exact_public_format_and_entropy_lengths` 1 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；TokenService.generate_token 现在固定公开格式完整为 `qap_<32 hex token_id>_<64 hex secret>`，token_id 与 full token 第二段一致，secret 可按 hex 解析，并继续证明生成结果能被 `parse_bearer_token` roundtrip | 测试 review 发现 generate_token 用例此前拆成 startswith、三段数量、token_id 匹配和 token_id 长度，未锁住 secret 长度/hex/roundtrip；如果 secret 退化成短随机值、非 hex，或 full token 拼接格式半漂移，旧测试仍可能绿。现升级为 API token generated format/entropy 精确契约，避免 token 生成测试只证明“看起来像 qap token” |
| 2026-05-31 | N/A（Pipeline missing project ErrorResponse 精确契约） | `tests/unit/test_api/test_pipelines.py::test_pipeline_routes_hide_missing_project` 1 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；pipeline list/create/get/update/delete 在 project 不可见时现在固定五个响应体完整等于标准 `NOT_FOUND` ErrorResponse，并继续证明每次只查 project tenant scope、不触碰 pipeline repo 或 audit | 测试 review 发现该用例此前虽然覆盖 CRUD 矩阵和副作用短路，但响应断言仍兼容 `detail` 或 `error.message` 两种形状；如果统一错误处理退回 FastAPI 默认 detail、details 夹带探测信息，或部分路由响应壳漂移，旧测试仍可能绿。现升级为 pipeline missing project ErrorResponse 精确契约，避免 pipeline 隐藏项目测试只证明“Project not found 文案出现过” |
| 2026-05-31 | N/A（Notification multi-channel log create 精确契约） | `tests/unit/test_worker/test_notifications.py::TestEvaluateAndNotify::test_multiple_channels_per_rule` 1 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；notification 多渠道发送用例现在固定 email/webhook 两次 `log_repo.create` 的完整 `{project_id, run_id, rule_id, channel_type, status, error_message}` 投影，继续锁住 get_by_delivery、_send_channel 顺序和单次 commit | 测试 review 发现该用例此前只断言 `await_count == 2`、channel_type 列表和 `all(...)` 字段子集；如果某个 channel 的 project/run/rule 串错、status/error 只在部分调用漂移，或 log create 夹带额外语义变化，旧测试仍可能绿。现升级为 notification multi-channel log create 精确契约，避免多渠道通知测试只证明“发送了两个渠道且字段大概一致” |
| 2026-05-31 | N/A（Integration schema bootstrap fail message 精确契约） | `tests/unit/test_integration_schema_bootstrap_contract.py::test_integration_db_schema_fails_instead_of_metadata_fallback` 1 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；integration schema bootstrap 的 Alembic 失败路径现在固定 pytest fail message 完整等于 `alembic upgrade head failed for integration schema bootstrap (exit=1):\nbroken migration`，并继续证明不会调用 metadata fallback engine | 测试 review 发现该用例此前只用 `pytest.raises(..., match="alembic upgrade head failed")` 匹配错误片段；如果 exit code/stderr 被丢弃、错误上下文退化，或失败路径重新引入 metadata fallback 但仍抛出相似文案，旧测试仍可能绿。现升级为 integration schema bootstrap fail message 精确契约，避免集成 schema 门禁只证明“迁移失败时抛了相似错误” |
| 2026-05-31 | N/A（Worker/Git raises exact exception 契约） | `tests/unit/test_worker/test_settings_tasks.py::test_on_shutdown_closes_docker_even_when_container_close_fails tests/unit/test_worker/test_tasks.py::test_build_source_auth_rejects_type_mismatch tests/unit/test_worker/test_notifications.py::TestEvaluateAndNotify::test_unexpected_log_write_failure_propagates_after_send tests/unit/test_plugins/test_git_source.py::TestGitUrlValidation::test_rejects_url_schemes_even_when_userinfo_port_looks_like_ssh tests/unit/test_plugins/test_git_source.py::TestGitUrlValidation::test_rejects_unsupported_or_malformed_urls_without_dns_lookup` 7 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；worker shutdown / source auth / notification log write 失败路径现在固定同一个 RuntimeError 对象或 exact args；Git URL scheme/hostname validation 现在固定完整 ValueError args，并继续证明非法 URL 不触发 DNS lookup | 测试 review 发现这些用例此前只用 `pytest.raises(..., match=...)` 匹配错误片段；如果 worker 层重新包装异常、Git URL 错误类型/完整文案漂移、或 DNS 短路边界被削弱但仍抛出相似文案，旧测试仍可能绿。现升级为 Worker/Git raises exact exception 契约，避免 worker/git 测试只证明“抛出了相似错误文本” |
| 2026-05-31 | N/A（RunExecutor timeout/artifact log 精确契约） | `tests/unit/test_engine/test_executor.py::TestRunSetupContainerised::test_setup_outer_wait_for_triggers_graceful_stop tests/unit/test_engine/test_executor.py::TestUploadArtifacts::test_upload_skips_artifacts_over_size_limit tests/unit/test_engine/test_executor.py::TestUploadArtifacts::test_upload_skips_artifacts_over_count_limit tests/unit/test_engine/test_executor.py::TestRunStagesTimeoutGracePeriod::test_timeout_writes_reason_log` 4 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；setup timeout 现在固定 exact RuntimeError args、graceful_stop reason、cleanup 和 `write_log` 完整 await 序列；artifact size/count limit 与 stage timeout 现在固定完整日志顺序、stderr 字段和上传/跳过消息 | 测试 review 发现这些用例此前只用 `pytest.raises(..., match="timed out")` 或 `assert any(...)` 找日志片段；如果异常被重新包装成同文案、timeout 日志漏掉 stderr、artifact 上传/跳过日志顺序漂移或额外/缺失日志，旧测试仍可能绿。现升级为 RunExecutor timeout/artifact log 精确契约，避免执行器测试只证明“有相似异常/日志片段” |
| 2026-05-31 | N/A（Dependency session exception 精确契约） | `tests/unit/test_dependencies.py` 12 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；global/request DB session 异常路径现在固定 rollback 后透传同一个 RuntimeError 对象，未初始化 database 固定 exact args，继续证明成功路径 commit、异常路径不 commit、缺 DB 返回 503 | 测试 review 发现这些用例此前只用 `pytest.raises(..., match=...)` 匹配错误文本；如果依赖层重新包装异常、吞掉原异常再构造同文案 RuntimeError，或未初始化 DB 文案只局部相似，旧测试仍可能绿。现升级为 dependency session exception 精确契约，避免依赖事务测试只证明“抛出了相似错误文本” |
| 2026-05-31 | N/A（Run trigger/results validation helper 精确契约） | `tests/unit/test_api/test_runs.py::test_get_run_results_rejects_invalid_text_filters_before_side_effects tests/unit/test_api/test_runs.py::test_trigger_run_priority_validation tests/unit/test_api/test_runs.py::test_trigger_run_rejects_invalid_git_inputs_without_side_effects` 9 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；run results suite/q 文本过滤、trigger priority、trigger git_ref/git_sha 现在固定 422 detail 的 `{type, loc, msg, input}` 单条投影，覆盖 `string_too_short`、`string_too_long`、`less_than_equal`、`value_error`、`string_pattern_mismatch` | 测试 review 发现这些用例此前按 `query/body.<field>` 过滤错误后只看首条消息片段；如果 git_sha pattern、priority 上界、suite/q 长度、git_ref 空白或 input 漂移，或同一次请求夹带额外 detail，旧测试仍可能绿。现升级为 run trigger/results validation 精确契约，避免 run 测试只证明“某个字段有错误文本” |
| 2026-05-31 | N/A（Domain/discovery validation errors 精确契约） | `tests/unit/test_domain_models.py tests/unit/test_services/test_discovery.py` 18 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；domain `RetryPolicy` / `TriggerConfig` / `StageDefinition` / `CollectorDefinition` / `Pipeline` 与 discovery `TestSelector` 负向用例现在固定 Pydantic errors 的 `{type, loc, msg, input}` 单条投影，覆盖 `string_too_short`、`value_error`、`too_long`、`greater_than_equal` | 测试 review 发现这些用例此前把 `ValidationError` 转成字符串后只找字段名和消息片段；如果 loc tuple/index 漂移、错误 type 退化、list 原始 input 丢失、边界数量文案退化，或同一次初始化夹带额外 detail，旧测试仍可能绿。现升级为 domain/discovery validation 精确契约，避免模型发现测试只证明“异常文本里有相似字段和消息” |
| 2026-05-31 | N/A（Webhook trigger git input validation 精确契约） | `tests/unit/test_api/test_p3.py::TestWebhookTrigger::test_webhook_trigger_rejects_invalid_git_inputs_without_side_effects` 4 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；webhook trigger 的 blank git_ref、空/空白/超长 git_sha 现在固定 422 detail 的 `{type, loc, msg, input}` 单条投影，并继续证明不查 project/pipeline/environment/run、不创建 run、不写 audit | 测试 review 发现该用例此前先按 `body.<field>` 过滤错误，再只看首条消息片段；如果 git_sha 空串的 `string_too_short` 退化、空白 git_ref/git_sha 的 value_error 文案或 input 漂移、同一次请求夹带额外 detail，旧测试仍可能绿。现升级为 webhook git input validation 精确契约，避免 webhook 触发测试只证明“某个 git 字段有错误文本” |
| 2026-05-31 | N/A（Notification validation helper 精确契约） | `tests/unit/test_api/test_notifications.py::test_create_rule_rejects_duplicate_channel_types tests/unit/test_api/test_notifications.py::test_create_rule_rejects_unsupported_channel_type tests/unit/test_api/test_notifications.py::test_create_rule_rejects_non_object_channel_config tests/unit/test_api/test_notifications.py::test_rule_routes_reject_blank_name_before_side_effects tests/unit/test_api/test_notifications.py::test_create_rule_rejects_invalid_conditions tests/unit/test_api/test_notifications.py::test_create_rule_rejects_undocumented_condition_alias tests/unit/test_api/test_notifications.py::test_update_rule_rejects_invalid_conditions_before_side_effects tests/unit/test_api/test_notifications.py::test_update_rule_rejects_duplicate_channel_types` 8 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；notification create/update 的 duplicate channel、unsupported channel、非 object config、blank name、非法/别名 condition field、非法 condition operator 现在固定 422 detail 的 `{type, loc, msg, input}` 单条投影，并继续证明 validation 在 project/rule/audit 前短路 | 测试 review 发现通用 helper 此前只用 `any(...)` 找 `body.<field>` 和消息片段；如果错误 type/input 漂移、allowed channel/condition 文案退化、同一次请求夹带额外 detail，或 update/create 走到仓储副作用，旧测试仍可能绿。现升级为 notification validation helper 精确契约，避免通知规则测试只证明“某个 body 字段有相似错误文本” |
| 2026-05-31 | N/A（P3 analytics pagination validation 精确契约） | `tests/unit/test_api/test_p3.py::TestAnalytics::test_trends_rejects_invalid_pagination tests/unit/test_api/test_p3.py::TestAnalytics::test_flaky_rejects_invalid_pagination` 2 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；analytics trends/flaky 的 offset/limit 负向查询现在固定 422 detail 的 `{type, loc, msg, input}` 单条投影，明确 offset 是 `greater_than_equal >= 0`，trends limit 是 `less_than_equal <= 365`，flaky limit 是 `less_than_equal <= 200` | 测试 review 发现通用 helper 此前只用 `any(...)` 找 `query.<field>` loc；如果上界约束漂移、错误 type/msg/input 漂移，或一次请求夹带额外 validation detail，旧测试仍可能绿。现升级为 P3 analytics pagination validation 精确契约，避免分析接口分页测试只证明“某个 query 字段报错” |
| 2026-05-31 | N/A（Settings validation errors 精确契约） | `tests/unit/test_config.py` 66 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；Settings numeric bounds、trusted_proxies、默认/必填字符串空白、cors_origins、otel_sample_rate、encryption_keys、required missing、log_level/log_format 现在固定 Pydantic errors 的 `{type, loc, msg, input}` 单条投影 | 测试 review 发现这些配置用例此前只用 `any(...)` 找 loc 和消息片段；如果错误 type 漂移、原始 input 丢失、missing required 的输入映射漂移、log level 允许集合文案退化，或同一次配置初始化夹带额外 detail，旧测试仍可能绿。现升级为 Settings validation 精确契约，避免配置测试只证明“某字段有相似错误文本” |
| 2026-05-31 | N/A（Project silent_windows validation 精确契约） | `tests/unit/test_api/test_projects.py::test_update_project_rejects_invalid_silent_windows` 5 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；project update silent_windows 五类非法 payload 现在固定 422 detail 的 `{type, loc, msg, input}` 单条投影，覆盖 timezone-aware、end_at 顺序、最多 20 条、settings.silent_windows 类型，以及 settings 内嵌窗口错误落点 `body.settings.start_at` | 测试 review 发现该用例此前只用 loc 前缀和消息片段；如果 settings 内嵌错误 loc 漂移、列表长度错误 type/input 漂移、同一次请求夹带额外 detail，或 validation 没有在 project/credential/audit 前短路，旧测试仍可能绿。现升级为 project silent_windows validation 精确契约，避免项目静默窗口测试只证明“某个前缀位置有相似错误” |
| 2026-05-31 | N/A（Notification update channels validation 精确契约） | `tests/unit/test_api/test_notifications.py::test_update_rule_rejects_empty_channels` 1 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；notification rule update 空 channels 现在固定 422 detail 的 `{type, loc, msg, input}` 单条投影，明确 `body.channels` 是 `too_short` 且 input 保留 `[]`，并继续证明不查 project/rule、不 create/update/delete、不写 audit | 测试 review 发现该用例此前只用 `any(...)` 找 `body.channels` 和 `at least 1 item` 片段；如果 min_length 错误 type/msg/input 漂移、同一次请求夹带额外 detail，或 validation 没有在 rule repository 前短路，旧测试仍可能绿。现升级为 notification update channels validation 精确契约，避免通知规则测试只证明“空 channels 有某段错误文本” |
| 2026-05-31 | N/A（Credential create validation errors 精确契约） | `tests/unit/test_api/test_credentials.py::test_create_credential_rejects_invalid_body_without_side_effects` 1 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；credential create 的空/空白 name、空 value、非法 type 现在固定 422 detail 的 `{type, loc, msg, input}` 单条投影，并继续证明不查 project、不查 credential name、不加密、不创建 credential、不写 audit | 测试 review 发现该用例此前只用 `any(...)` 找字段和消息片段；如果空白 name 的 `value_error` 退化、非法 type 枚举文案漂移、input 不是原始 payload 值、同一次请求夹带额外 detail，或 validation 没有在加密/仓储前短路，旧测试仍可能绿。现升级为 credential create validation 精确契约，避免密钥入口测试只证明“某字段有个相似错误” |
| 2026-05-31 | N/A（Project member role validation 精确契约） | `tests/unit/test_api/test_project_members.py::test_project_member_routes_reject_invalid_role_without_side_effects` 1 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；project member add/update 非法 role 现在固定 422 detail 的 `{type, loc, msg, input}` 单条投影，明确 `owner` 只能触发 `body.role` Literal 错误，并继续证明不查 project/user/member、不 create/update、不写 audit | 测试 review 发现该用例此前只用 `any(...)` 找 `body.role` 和 `Input should be` 片段；如果 role 枚举文案漂移、错误 loc/input/type 变化、同一次请求夹带额外 detail，或 validation 没有在成员仓储前短路，旧测试仍可能绿。现升级为 project member role validation 精确契约，避免成员权限测试只证明“非法 role 有某段错误文本” |
| 2026-05-31 | N/A（Run validation errors 精确契约） | `tests/unit/test_api/test_runs.py::test_cancel_run_rejects_overlong_reason_without_side_effects tests/unit/test_api/test_runs.py::test_get_run_results_rejects_unknown_status` 2 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；run cancel reason 过长与 run results status 非法值现在固定 422 detail 的 `{type, loc, msg, input}` 单条投影，并继续证明不查 run/result、不 cancel、不发布 Redis、不写 audit | 测试 review 发现这些用例此前只筛出 `body.reason` 后看消息片段，或用 `any(...)` 确认 status 错误里包含 `passed/xfail`；如果错误 type 漂移、Literal 枚举文案退化、同一次请求夹带额外 detail，或 validation 没有在仓储/Redis/audit 前短路，旧测试仍可能绿。现升级为 run validation 精确契约，避免 run 请求体验证测试只证明“某段错误文本出现过” |
| 2026-05-31 | N/A（Auth request validation errors 精确契约） | `tests/unit/test_auth/test_auth_routes.py::TestLogin::test_login_rejects_invalid_body_without_side_effects tests/unit/test_auth/test_auth_routes.py::TestRegister::test_register_rejects_invalid_username tests/unit/test_auth/test_auth_routes.py::TestRegister::test_register_rejects_invalid_password tests/unit/test_auth/test_auth_routes.py::TestRegister::test_register_rejects_bad_email tests/unit/test_auth/test_auth_routes.py::TestTokenRoutes::test_create_token_rejects_invalid_body_without_side_effects` 21 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；login/register/API token create validation 现在固定 422 detail 的 `{type, loc, msg, input}` 单条投影，覆盖 username 长度/pattern、register email、login/register password、token name/scope 空/空白/超长 | 测试 review 发现这些用例此前只用 `any(...)` 找 loc 和消息片段；如果错误 type/loc/input 漂移、同一次请求夹带多条 detail、pattern/email 文案变化，或 token/register/login 未在 DB/token/audit 前短路，旧测试仍可能绿。现升级为 auth request validation 精确契约，避免认证入口测试只证明“某个字段出现过某段错误文本” |
| 2026-05-31 | N/A（Project validation errors 精确契约） | `tests/unit/test_api/test_projects.py::test_create_project_rejects_invalid_core_fields_without_side_effects tests/unit/test_api/test_projects.py::test_update_project_rejects_empty_core_fields_without_side_effects tests/unit/test_api/test_projects.py::test_create_project_rejects_invalid_allowed_branches tests/unit/test_api/test_projects.py::test_update_project_rejects_invalid_allowed_branches` 28 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；project create/update core fields 与 create/update allowed_branches validation 现在固定 422 detail 的 `{type, loc, msg, input}` 单条投影，覆盖 slug pattern、空字符串、空白文本和 settings.allowed_branches 类型/长度/元素约束 | 测试 review 发现这些用例此前只用 `any(...)` 找字段和消息片段或 `repr(detail)` 消息片段；如果错误 type 漂移、同一次请求夹带额外 detail、slug pattern 文案漂移、settings input 不是原始 payload，或 validation 没有在 repository/audit 前短路，旧测试仍可能绿。现升级为 project validation 精确契约，避免项目请求体验证测试只证明“某字段有个相似错误” |
| 2026-05-31 | N/A（Batch duplicate run_ids validation 精确契约） | `tests/unit/test_api/test_p3.py::TestBatchCancel::test_batch_cancel_rejects_duplicate_run_ids_without_side_effects tests/unit/test_api/test_p3.py::TestBatchRetry::test_batch_retry_rejects_duplicate_run_ids_without_side_effects` 2 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；batch cancel/retry 重复 run_id 用例现在固定 422 detail 的 `{type, loc, msg, input}` 单条投影，并继续证明不读取 run、不 cancel、不 create retry run、不 enqueue、不发布 Redis 事件、不写 audit | 测试 review 发现这些用例此前只用 `any(...)` 在错误列表里找 `duplicate run_ids are not allowed`；如果请求体错误夹带额外 detail、loc 漂移、input 不是重复 ID 列表，或 validation 没有在业务副作用前短路，旧测试仍可能绿。现升级为 batch duplicate validation 精确契约，避免批量操作测试只证明“有一条重复 ID 错误” |
| 2026-05-31 | N/A（Environment validation errors 精确契约） | `tests/unit/test_api/test_environments.py::test_environment_schema_rejects_unpinned_or_moving_base_images tests/unit/test_api/test_environments.py::test_environment_schema_rejects_empty_name_or_cache_key tests/unit/test_api/test_environments.py::test_environment_schema_rejects_blank_name_or_cache_key tests/unit/test_api/test_environments.py::test_environment_schema_rejects_non_positive_resource_limits tests/unit/test_api/test_environments.py::test_environment_routes_reject_empty_name_or_cache_key_without_side_effects` 13 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；environment schema/route validation 现在固定单条错误的 `{type, loc, msg, input}` 投影，明确 `cpu_cores` 是 `greater_than > 0`，整数资源限制是 `greater_than_equal >= 1`，路由短路错误也固定 body 字段与原始输入 | 测试 review 发现这些用例此前只用 `any(error["loc"] == ...)` 或消息片段；如果错误字段漂移、同一次请求额外报错、`cpu_cores` 与整数资源约束混淆，或路由错误没有停在请求体验证层，旧测试仍可能绿。现升级为 validation error 精确契约，避免 environment validation 测试只证明“某字段有个错误” |
| 2026-05-31 | N/A（Rate limit token bucket key 精确契约） | `tests/unit/test_api/test_rate_limit.py::TestRateLimitMiddlewareDispatch::test_different_tokens_independent_quotas tests/unit/test_api/test_rate_limit.py::TestRateLimitMiddlewareDispatch::test_third_token_passes_when_others_exhausted` 2 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；token A/B/C 隔离用例现在固定 Redis `zadd` bucket key 完整序列 `rate_limit:token:<sha16>:/api/v1/runs`，并继续证明 key 不含原始 bearer token | 测试 review 发现这些用例此前只用 `any(hash in key)` 证明 hash 片段出现；如果 key path 漂移、bucket 前缀错误、某个请求复用前一个 token bucket，或 Redis key 夹带原始 token，旧测试仍可能绿。现升级为 token bucket key 精确契约，避免 rate limit token 隔离测试只证明“某个 hash 出现在某个 key 里” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_auto_retry_real_db.py::test_attempt_retry_uses_api_max_attempts_and_persists_retry_run tests/integration/test_auto_retry_real_db.py::test_execute_run_infra_exception_marks_failed_and_schedules_retry_real_db tests/integration/test_auto_retry_real_db.py::test_execute_run_setup_docker_infra_error_uses_real_executor_and_schedules_retry tests/integration/test_auto_retry_real_db.py::test_reclaim_resources_worker_lost_marks_failed_publishes_event_and_schedules_retry_real_db` 4 passed | targeted ruff passed；targeted docs contract passed | 无新增 warning；auto retry 真实 DB/Redis 用例现在固定 ARQ enqueue 调用完整 `{args, _queue_name, _job_id, _defer_by}`，setup 阶段 workspace mount 投影，以及 worker_lost Redis event 字段集合和 timestamp 解析 | 测试 review 发现这些用例此前多用 `len(arq.calls) == 1`、`_job_id.startswith("run:")`、少量 kwargs 抽查和 `len(setup_spec.mounts) == 1`；如果任务名/queue/defer 漂移、setup mount 夹带额外挂载、或 Redis event 丢 timestamp/字段夹带，旧测试仍可能绿。现升级为自动重试队列副作用与事件投影精确契约，避免 auto retry 集成测试只证明“有一次入队/有一个事件” |
| 2026-05-31 | N/A（Run list repository args 精确契约） | `tests/unit/test_api/test_runs.py::test_list_runs tests/unit/test_api/test_runs.py::test_list_runs_member_user_uses_project_member_repository tests/unit/test_api/test_runs.py::test_list_runs_multi_status_filter tests/unit/test_api/test_runs.py::test_list_runs_single_status_uses_equality` 4 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；run list 的普通列表、成员项目范围、multi-status 与 single-status 用例现在固定 `list_filtered_for_tenant 参数`：tenant_id、project_ids、statuses、sort，且成员用户走 project member repository | 测试 review 发现这些用例此前用 SQL 片段或部分参数判断；如果 route 漏传 tenant_id、成员 project_ids、single/multi statuses、默认 sort，或成员用户绕过 project member repository，旧测试仍可能绿。现升级为 run list repository 参数精确契约，避免列表过滤测试只证明“响应壳存在或目标片段出现过” |
| 2026-05-31 | N/A（Run results repository args 精确契约） | `tests/unit/test_api/test_runs.py::test_get_run_results_filters_by_suite tests/unit/test_api/test_runs.py::test_get_run_results_filters_by_keyword tests/unit/test_api/test_runs.py::test_get_run_results_combines_suite_and_keyword tests/unit/test_api/test_runs.py::test_get_run_results_escapes_keyword_like_wildcards` 4 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；run results 的 suite、q、status+suite+q 与 wildcard escape 用例现在固定 `list_filtered_by_run 参数`：run_id、offset/limit、suite、status、query | 测试 review 发现这些用例此前只用 SQL 片段或部分参数判断；如果 route 漏传 q/suite/status、分页 offset/limit 漂移、run_id 丢失，或 wildcard query 字符串传递退化，旧测试仍可能绿。现升级为结果查询 repository 参数精确契约，避免 run results 过滤测试只证明“响应壳存在或某个片段出现过” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_notification_delivery.py` 7 passed | targeted ruff passed；targeted docs contract passed | 无新增 warning；notification delivery 真实 DB 用例现在把成功、幂等、channel template、OR/consecutive failures、template render failure、channel send failure 的 NotificationLog 固定为完整 `{run_id, rule_id, channel_type, status, error_message}` 列表 | 测试 review 发现这些用例此前用 `len(logs) == 1` 加少量字段检查；如果重复写日志、写错 rule/channel、失败错误文案漂移或成功日志残留 error_message，旧测试仍可能绿。现升级为 notification log 投影精确契约，避免通知集成测试只证明“有一条日志” |
| 2026-05-31 | N/A（Project list repository args 精确契约） | `tests/unit/test_api/test_projects.py::test_list_projects tests/unit/test_api/test_projects.py::test_list_projects_with_search tests/unit/test_api/test_projects.py::test_list_projects_filters_by_status` 3 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；project list 默认/search/status 用例现在固定 `list_filtered_by_tenant` 参数：tenant_id、offset/limit、status、query | 测试 review 发现这些用例此前只验证部分请求结果或旧 SQL filter 片段；如果 route 漏传 status/query、分页 offset/limit 漂移，或默认列表丢 tenant_id，旧测试仍可能绿。现升级为 repository 参数精确契约，避免 project list 测试只证明“响应壳存在或某个过滤片段存在” |
| 2026-05-31 | N/A（Ready health probe SQL/response 精确契约） | `tests/unit/test_api/test_health.py` 6 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；`/ready` ok、DB degraded、Redis degraded、双 degraded 现在固定完整响应壳、`checks` 内容、`uptime` 格式、DB probe SQL `SELECT 1` 与 Redis ping 次数 | 测试 review 发现这些用例此前多用 `len(container.session.statements) == 1`，只能证明 DB probe 被调用一次；如果 SQL 漂移、ok 响应缺 version/uptime，或 degraded 响应字段退化，旧测试仍可能绿。现升级为 readiness 响应与依赖探测精确契约，避免 health 测试只证明“探测被调用过” |
| 2026-05-31 | N/A（special-regressions E2E silent windows 响应精确契约） | `npx playwright test tests/e2e/special-regressions.spec.ts --project=chromium --list` 7 tests listed；targeted TypeScript passed；targeted docs contract passed | 未启动真实浏览器链路；静态 contract 锁住断言形状；`special-regressions.spec.ts` 不再保留 `toHaveLength` / arrayContaining / objectContaining | 测试 review 发现 silent windows UI E2E 保存后只用 `toHaveLength(1)` 加字段抽查，audit E2E 也有冗余长度断言；如果 silent_windows item 夹带/丢字段，或分页 exact body 已覆盖的 audit data 数量漂移，旧断言要么漏问题、要么重复弱断言。现 silent_windows 响应固定完整单项对象，audit 交给完整分页体比较，避免 E2E 只证明“有一条数据” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_silent_windows.py::test_cron_tick_outside_silent_window_creates_run tests/integration/test_silent_windows.py::test_cron_tick_enqueue_conflict_records_last_error_and_waiting_run` 2 passed | targeted ruff passed；targeted docs contract passed | 无新增 warning；schedule cron outside-window 与 enqueue-conflict 真实集成用例现在固定完整 schedule Run metadata、tenant/project/pipeline/environment/git/status/retry_group/queue/arq 字段，以及 `run.trigger` 审计 after_state | 测试 review 发现这两条用例此前只用 `len(runs) == 1` / `len(created) == 1` 加少量字段检查；如果 cron 创建了错误环境、未设置 retry group、队列状态漂移、等待队列误写 arq_job_id，或审计 after_state 缺字段，旧测试仍可能绿。现升级为真实 DB run 行与审计投影精确契约，避免静默窗口 schedule 测试只证明“创建了一个 run” |
| 2026-05-31 | N/A（LogStream live read entries 精确契约） | `tests/unit/test_engine/test_log_stream.py::TestLogStream::test_read_logs_with_data` 1 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；Redis Stream live read 用例现在固定完整 entries 列表与 `xread({stream: "0"}, count=50)` await 参数 | 测试 review 发现该用例此前只用 `len(entries)` 加首条字段和第二条 stream 抽查；如果第二条 line 丢失、entries 公共字段漂移，或 read count/last_id 参数错，旧测试仍可能绿。现升级为 live log 读面精确契约，避免 LogStream 测试只证明“读到了两条日志” |
| 2026-05-31 | N/A（JUnit collector result data 精确契约） | `tests/unit/test_plugins/test_junit_collector.py` 24 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；JUnit XML 解析、单 testcase 解析、默认 collect 与自定义 path collect 现在固定完整 `TestResultData` 列表/对象，覆盖 suite/name/status/duration/error/stack_trace | 测试 review 发现这些用例此前多用 `len(results)` 加少量字段检查；如果 suite/classname 错、duration 漂移、status 被误判、stack_trace 丢失或 collect 路径解析返回字段不完整，旧测试仍可能绿。现升级为结果对象级契约，避免 JUnit collector 测试只证明“返回了几条且某些字段看起来对” |
| 2026-05-31 | N/A（GitSource clone argv/auth env 精确契约） | `tests/unit/test_plugins/test_git_source.py::TestGitSourceClone` targeted passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；GitSource token/SSH/shallow/branch clone 用例现在固定完整 `git clone` argv，并锁住 token askpass username、SSH terminal prompt 与 SSH command 形状 | 测试 review 发现这些用例此前只检查 `--depth`、`1`、`--branch`、ref 或 `ssh -i` 片段是否存在；如果 clone URL/目标目录错、参数顺序漂移、默认 main 混入、或 auth env 缺少关键开关，旧测试仍可能绿。现升级为完整 clone 命令与 auth env 契约，避免 GitSource clone 测试只证明“几个参数在命令里” |
| 2026-05-31 | N/A（Go runner shell command 精确契约） | `tests/unit/test_plugins/test_go_test_runner.py::TestGoTestBuildCommand` 15 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；Go runner 的 shell command 现在固定 helper 注入、FIFO/tee 管道、stderr 回放、JUnit 转换命令、custom JUnit XML mkdir 与 `go test` argv 序列 | 测试 review 发现 Go runner build command 用例只检查片段包含；如果默认 `./...` 混入自定义 path、`go test` 重定向错、helper 转换参数错或 custom report 目录没有创建，旧测试仍可能绿。现升级为关键脚本行与 argv 精确契约，避免 Go runner 命令测试只证明“脚本里有那些词” |
| 2026-05-31 | N/A（Jest/Playwright runner command argv 精确契约） | `tests/unit/test_plugins/test_jest_runner.py::TestJestBuildCommand tests/unit/test_plugins/test_playwright_runner.py::TestPlaywrightBuildCommand` 31 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；Jest 与 Playwright runner 的 shell command 现在固定 mkdir、JUnit env var、reporter 位置、extra args、test paths 与完整 argv 序列 | 测试 review 发现 JS runner 用例也主要靠 `in cmd` 与少量负向字符串；如果 reporter 被拼成 `--reporter=junit,path`、默认路径和自定义路径同时出现、或空格参数未正确 shell quote，旧测试仍可能绿。现升级为完整命令契约，避免 JS runner 命令测试只证明“几个片段出现过” |
| 2026-05-31 | N/A（Pytest runner command argv 精确契约） | `tests/unit/test_plugins/test_pytest_runner.py::TestPytestBuildCommand tests/unit/test_plugins/test_pytest_runner.py::TestPytestBuildCommandInternal` 25 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；pytest runner 的 public shell command 与 private argv 构造测试现在固定完整参数序列，覆盖默认命令、extra args、marker、custom test path、custom executable、custom JUnit XML 与多 test_paths | 测试 review 发现这些 runner 用例大量使用 `in cmd`，只能证明参数片段出现；如果默认 `tests` 残留、参数顺序错、JUnit XML 重复、custom path 与默认 path 同时存在，旧测试仍可能绿。现升级为完整 argv 契约，避免 runner 命令测试只证明“关键字符串在里面” |
| 2026-05-31 | N/A（integration skip report 输出精确契约） | `tests/unit/test_integration_skip_report.py` 3 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；skip inventory 的 collect 与 JSON/Markdown writer 现在固定完整 entry 字段、category 计数、version/total 和 markdown 表格全文 | 测试 review 发现 skip report 单测只断言分类序列、少量 policy 和 markdown 子串；如果报告漏掉 classname/name/reason、写错 gate_policy、JSON version/entries 漂移或 markdown 表格坏掉，旧测试仍会绿。现升级为完整输出契约，避免 CI skip 证据测试只证明“有个标题和原因文本” |
| 2026-05-31 | N/A（special-regressions E2E result filters 精确契约） | `npx playwright test tests/e2e/special-regressions.spec.ts --project=chromium --list`；targeted docs contract passed | 未启动真实浏览器链路；静态 contract 锁住断言形状 | `special-regressions.spec.ts` 的 run results status/suite/q 三条 API 过滤路径现在固定完整 TestResultResponse 投影：id 仍按 UUID 动态校验，其余 run_id/suite/name/status/duration/error/stack/tags/metadata 与分页壳精确匹配。测试 review 发现旧 E2E 只断言 total 加 name/status；如果过滤返回错 run_id、丢 metadata/tags、错误信息漂移或分页默认漂移，旧测试仍会绿 |
| 2026-05-31 | N/A（special-regressions E2E 列表/审计精确契约） | `npx playwright test tests/e2e/special-regressions.spec.ts --project=chromium --list`；targeted docs contract passed | 未启动真实浏览器链路；静态 contract 锁住断言形状 | `special-regressions.spec.ts` 的 webhook branch filter/dedup 与 audit visibility E2E 现在固定 runs 分页壳、唯一 active webhook run、核心 RunResponse 字段，以及 project.create 审计查询的唯一 AuditEventResponse 投影；不再用 `filter(...).toHaveLength(1)` / `some(...).toBe(true)` 证明“目标项在里面”。测试 review 发现旧 E2E 无法发现 filtered branch 误落其他 run、run DTO 字段串错、或审计响应夹带额外 project.create 行 |
| 2026-05-31 | N/A（worker-backed E2E evidence 精确契约） | `npx playwright test tests/e2e/real-run-trigger.spec.ts --project=chromium --list`；targeted docs contract passed | 未启动真实 worker-backed 浏览器链路；静态 contract 锁住断言形状 | worker-backed E2E 的真实 worker 结果与 artifact API 证据现在固定为单项 `{name,status}` 与单项 `{name,type}`，不再用 `expect.arrayContaining`。测试 review 发现该 E2E 已证明 UI 触发、日志和 artifact 可见，但 API 证据只要求目标 result/artifact 至少存在；如果 worker 额外收集错误结果或多余 artifact，旧 E2E 仍会绿。现升级为精确证据，避免 worker-backed E2E 只证明“目标证据在里面” |
| 2026-05-31 | N/A（RBAC viewer 权限矩阵精确契约） | `tests/unit/test_auth/test_permissions.py::TestTenantRolePermissions::test_viewer_has_read_only_actions tests/unit/test_auth/test_permissions.py::TestProjectRolePermissions::test_viewer_is_read_only` 2 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；tenant viewer 与 project viewer 权限现在固定为精确 Action 集合，不再只检查现有 action 名称里包含 `read` | 测试 review 发现 viewer 权限用例只证明“已有权限都是 read-like”，如果删掉 `RUN_READ`、`MEMBER_READ` 或意外保留少量读权限，测试仍会绿。现升级为精确集合，避免权限矩阵测试只证明“剩下的权限看起来只读” |
| 2026-05-31 | N/A（backend integration artifact validator 错误列表精确契约） | `tests/unit/test_backend_integration_artifact_validator.py` 9 passed；targeted ruff passed；targeted docs contract passed | 无新增 warning；backend integration artifact validator 的 nodeid 缺失、collect/JUnit mismatch、多重 nodeid、failure/error、skip 与 nightly OOM strict skip 用例现在固定完整错误列表 | 测试 review 发现这些证据门禁测试多处只用 `startswith` / `any` / `in errors`，只能证明某类错误大概出现；如果 validator 额外输出噪声、漏掉成对错误或错误分类漂移，旧测试仍可能绿。现升级为 `errors == [...]`，避免 artifact validator 测试只证明“有个相似错误” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_audit_events_api.py::test_audit_events_owner_admin_can_list_member_viewer_forbidden tests/integration/test_audit_events_api.py::test_audit_events_filters_combine_and_sort_desc` 2 passed | targeted ruff passed；targeted docs contract passed | 无新增 warning；Audit events owner/admin 与过滤排序集成路径现在固定完整 AuditEventResponse，包含 tenant/user/action/resource/before_state/after_state/ip_address/user_agent/created_at，且两次成功查询各写 1 条 `audit_events.list` 自审计 | 测试 review 发现这些真实 API 用例已证明权限和排序，但成功响应只断言 data id 或 resource_type；如果 action、resource_id、created_at、IP/User-Agent 或响应壳漂移，旧测试仍会绿。现升级为完整分页响应契约，避免 audit-events 集成测试只证明“目标审计行大概返回了” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_worker_execute.py --collect-only` 8 tests collected | targeted ruff passed；targeted docs contract passed | 无新增 warning；external-stack worker 的 `run.read` token artifact 响应也固定为精确单项 `junit.xml`，不能只过滤出目标 artifact 后证明非空 | 测试 review 发现 worker-lost retry 与 priority queue 的 token 读面仍用 `token_junit_artifacts` 子集过滤；如果 token artifact 列表额外泄漏其他 artifact，旧断言仍会绿。现改为固定 `total == 1` 且名称序列精确等于 `["junit.xml"]`，避免 run.read token 测试只证明“junit 在列表里” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_worker_execute.py --collect-only` 8 tests collected | targeted ruff passed；targeted docs contract passed | 无新增 warning；external-stack worker 的 artifact/log 轮询现在改为目标内容驱动，artifact 必须等到精确名称集合，归档日志必须等到指定业务片段，不再用 `body["total"] >= ...` 提前通过 | 测试 review 发现外部栈 worker 多条路径用 `total >= 1/4` 作为 readiness predicate；如果轮询等到无关 artifact、部分日志或历史残留，后续断言才失败或被宽松集合掩盖。现新增 `_has_exact_artifact_names()` / `_archive_contains_all()`，把等待条件升级为真正的读面契约，避免 external-stack 测试只证明“已经有点数据” |
| 2026-05-31 | N/A（Release gate compose worker 权限精确契约） | `tests/unit/test_release_gate_workflow.py::test_release_candidate_backend_gate_runs_real_stack_and_slo_validation` 1 passed；targeted ruff passed | 无新增 warning；release gate workflow 单测现在解析 `docker-compose.yml`，固定 worker/worker-high/worker-low 三个服务的 queue、Docker socket mount、workspace mount 和 docker group，且确认非 worker 服务没有这些执行器权限 | 测试 review 发现旧断言只用字符串 `count(...) >= N` 证明 Docker socket、workspace 和 group id 出现够多次；如果某个 worker 漏挂、配置挂到 api/frontend，或 compose 残留多余权限，旧测试仍可能绿。现升级为结构化服务级契约，避免 release gate 只证明配置文本出现过 |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 RUN_PERFORMANCE_TESTS=1 tests/integration/test_performance_smoke.py::test_artifact_list_api_p99_smoke tests/integration/test_performance_smoke.py::test_artifact_list_large_collection_page_p99_smoke tests/integration/test_performance_smoke.py::test_audit_events_large_filtered_page_api_token_p99_smoke` 3 passed | targeted docs contract passed；targeted ruff passed | 无新增 warning；performance smoke 的 artifact list 与大集合分页现在固定完整 ArtifactResponse 窗口，包含 id/run_id/type/name/storage_path/size_bytes/mime_type/expires_at/created_at；audit large filtered page 固定 tenant/user/action/resource scope、index 顺序和 AuditEventResponse 字段集合 | 测试 review 发现剩余性能用例仍有 `len(data)` 型断言，artifact 列表虽已锁 name/storage/size，却不能发现 id/type/mime/created_at 漂移；audit 大页虽锁 index，却可漏掉 resource_id 或字段集合漂移。现升级为完整 DTO/字段集合契约，避免性能 smoke 只数分页条数 |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 RUN_PERFORMANCE_TESTS=1 tests/integration/test_performance_smoke.py::test_archived_log_replay_large_page_p99_smoke tests/integration/test_performance_smoke.py::test_audit_events_list_api_p99_smoke tests/integration/test_performance_smoke.py::test_real_api_token_concurrent_read_paths_p99_smoke` 3 passed | targeted docs contract passed；targeted ruff passed | 无新增 warning；performance smoke 的 archived logs 大页、audit events 列表和真实 API token 并发读现在在计时循环内同时固定完整分页窗口、audit index 顺序、tenant/user/resource scope 和字段集合 | 测试 review 发现这些 SLO 用例仍有 len/首尾/action-set 型断言，只能证明接口够快且大概返回目标数据；如果分页中间窗口错序、audit response 混入其他资源、或 archived log replay 丢 stream 字段，旧性能测试仍绿。现把性能 smoke 同步升级为正确性哨兵，避免性能门禁只证明“快”，却漏掉返回内容漂移 |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_auth_results_artifacts.py::test_archived_logs_api_replays_large_page_without_presign_after_real_rbac` 1 passed | targeted docs contract passed；targeted ruff passed | 无新增 warning；真实 archived logs API 大分页现在固定 page=15/per_page=100 的完整 100 条 data 必须精确等于归档 JSONL 源切片 `entries[1400:1500]`，并验证每项字段集合只含 stream/line | 测试 review 发现该用例已经证明大分页走 S3 get 而不 presign，但响应只断言 total/page/per_page/len 和首尾两条；如果中间行错序、stream 字段丢失或 JSONL replay 夹带额外字段，旧测试仍绿。现升级为完整分页窗口契约，避免 archived logs 大分页测试只证明“首尾看起来对” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_auth_results_artifacts.py::test_run_results_api_filters_real_rows_and_recovers_after_duplicate tests/integration/test_real_auth_results_artifacts.py::test_run_artifacts_api_hides_soft_deleted_real_rows` 2 passed | targeted docs contract passed；targeted ruff passed | 无新增 warning；真实 run results/artifacts API 现在固定过滤结果完整 TestResultResponse、rollback 后完整可见结果集，以及 artifact soft-delete 后完整 ArtifactResponse，并删除完整 body 等值后的冗余字段集合守门 | 测试 review 发现 run results 过滤用例只断言 total/name/status，rollback 后只比对 name 集合；artifact 软删除用例只断言 total/id/name。如果 response 漏掉 duration/tags/metadata/storage_path/mime_type/created_at，或过滤/软删除路径夹带字段漂移，旧测试仍绿。现升级为真实 API 完整响应契约，避免 run results/artifacts 测试只证明“目标行大概出现” |
| 2026-05-31 | N/A（Frontend/OpenAPI 字段集合精确契约） | `tests/unit/test_frontend_api_contract.py::test_frontend_response_models_include_backend_openapi_fields tests/unit/test_frontend_api_contract.py::test_frontend_create_payloads_keep_backend_required_fields tests/unit/test_frontend_api_contract.py::test_frontend_paginated_response_matches_backend_shape` 15 passed；targeted ruff passed | 无新增 warning；前端响应 DTO、create payload 与 PaginatedResponse 字段集合现在必须精确等于 OpenAPI schema，create payload 的可选字段也会做类型/nullability 校验 | 测试 review 发现前端契约仍用 `backend_fields <= frontend_fields` / `backend_required <= set(frontend_members)`，只能发现后端字段缺失，不能发现前端保留后端已删除字段、漏掉可选请求字段或 optional payload 类型漂移。现升级为字段集合双向一致，避免前端/API 契约测试只证明“关键字段至少存在” |
| 2026-05-31 | N/A（Release candidate E2E spec 清单精确契约） | `tests/unit/test_release_gate_workflow.py::test_release_candidate_e2e_gate_runs_all_specs_with_evidence_validation` 1 passed；targeted ruff passed | 无新增 warning；release gate 单测现在固定当前 E2E spec 文件清单必须精确等于 auth-flow、real-login-flow、real-run-trigger、special-regressions 四个文件，不再只要求这些文件是目录子集 | 测试 review 发现 release_candidate workflow 会用 `find tests/e2e -name '*.spec.ts'` 跑全量并校验证据，但单测只做子集断言；如果新增 spec 未同步分层/文档/基线，旧测试仍绿。现将目录清单升级为精确契约，迫使新增 E2E spec 同步更新 gate 文档和证据口径 |
| 2026-05-31 | N/A（TriggerRun payload 精确字段契约） | `tests/unit/test_frontend_api_contract.py::test_trigger_run_payload_maps_to_backend_run_trigger_contract` 1 passed；targeted ruff passed | 无新增 warning；TriggerRunPayload 现在固定只包含 pipeline_id、branch、git_sha、environment_id、priority，并继续证明 branch 映射到后端 `git_ref` | 测试 review 发现旧断言只要求这些字段是子集，不能发现前端保留已删除/后端不接受的触发字段。现升级为精确字段集合，避免 trigger run 前端契约只证明关键字段至少存在 |
| 2026-05-31 | N/A（NotificationRuleCreate required 精确契约） | `tests/unit/test_frontend_api_contract.py::test_notification_create_payload_matches_backend_required_field_contract` 1 passed；targeted ruff passed | 无新增 warning；NotificationRuleCreate 后端 required 现在必须精确等于 name/channels，同时继续验证 channels item、condition union 与前端 create payload 字段 | 测试 review 发现旧断言只要求 name/channels 是 required 子集，不能发现后端误把 enabled、conditions 或 template 改成必填导致前端表单 422。现升级为精确 required 集合，避免通知创建契约测试只证明关键必填字段存在 |
| 2026-05-31 | N/A（CI workflow job 拓扑精确契约） | `tests/unit/test_release_gate_workflow.py::test_ci_workflow_yaml_has_no_duplicate_mapping_keys` 1 passed；targeted ruff passed | 无新增 warning；CI workflow job 集合现在必须精确等于 lint-and-type-check、backend-test、backend-integration-test、frontend-api-contract、frontend-build、e2e-test、release-gate | 测试 review 发现旧断言只要求 backend/frontend/e2e/release 关键 job 是子集，漏掉 lint-and-type-check 与 frontend-build，删掉前端 typecheck/lint 或 build job 仍会通过。现升级为完整 job 拓扑契约，避免 CI workflow 测试只证明部分门禁存在 |
| 2026-05-31 | N/A（Performance SLO manifest 名称精确契约） | `tests/unit/test_release_gate_workflow.py::test_performance_slo_manifest_matches_ci_threshold_env` 1 passed；targeted ruff passed | 无新增 warning；release gate 单测现在固定 39 个 SLO 名称，并继续要求 manifest、baseline 与 workflow PERF_* 阈值环境变量完全一致 | 测试 review 发现旧断言只要求 17 个核心 SLO 是子集，删除 archived log、artifact denial、API token 并发读等边界 SLO 后只要同步删 baseline 仍会通过。现升级为 manifest 名称精确契约，新增/删除 SLO 必须同步更新发布证据口径 |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_worker_execute.py --collect-only` 8 tests collected | targeted ruff/py_compile passed；`tests/unit/test_release_quality_docs_contract.py::test_quality_ops_capture_worker_external_stack_artifact_names_exact_contract` 1 passed | 无新增 warning；external-stack worker 多 artifact 读面现在固定管理员 JWT 与 `run.read` token 只能看到 JUnit/HTML/log/Allure 四类 artifact 名称集合 | 测试 review 发现该黑盒用例虽然断言 total=4、超额 artifact 不存在，但名称集合仍用子集断言；如果 API data 混入额外 artifact 名称或读面过滤漂移，旧断言不够敏感。现升级为 artifact 名称精确集合，避免真实 worker artifact 测试只证明四个期望文件至少存在 |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_worker_execute.py --collect-only` 8 tests collected | targeted ruff/py_compile passed；`tests/unit/test_release_quality_docs_contract.py::test_quality_ops_capture_external_stack_performance_artifact_exact_contract` 1 passed | 无新增 warning；external-stack performance smoke 现在固定 artifact total=2，且 artifact 名称/类型/storage_path 必须精确等于 junit.xml 与 logs/e2e-slo.txt | 测试 review 发现 performance smoke 的 setup script 只写 JUnit 与 e2e-slo log 两个 artifact，但旧断言只要求至少出现这两个名称；如果 worker 额外收集临时文件或 artifact 类型/storage_path 漂移，旧测试仍可能通过。现升级为精确 artifact 读面，避免 SLO smoke 只证明关键 artifact 至少存在 |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_api_write_state.py::test_project_member_and_credential_lists_are_paginated` 1 passed | targeted docs contract passed；targeted ruff passed | 无新增 warning；真实 DB credentials 与 project members 分页测试现在固定 page=2/per_page=2/total=3，且第二页完整 data 响应必须精确等于最早创建的记录，完整 body 等值后不再重复字段集合守门 | 测试 review 发现该用例只断言第二页 len=1、credential 不回明文、member role=developer；如果列表排序漂移、id/project_id/user/email/created_at 串错，或响应夹带/漏掉字段，旧测试仍绿。现升级为真实 API 分页精确响应契约，避免 credentials/members 分页测试只证明“第二页有一条” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_auth_results_artifacts.py::test_auth_tokens_list_is_paginated` 1 passed | targeted docs contract passed；targeted ruff passed | 无新增 warning；真实 auth API token 分页测试现在固定第二页完整列表项，包含 token_id/name/scopes/expires_at/last_used_at/is_revoked/created_at，并用完整 body 等值排除一次性 token 明文 | 测试 review 发现旧用例只断言 len=1、name 属于创建集合、token 不出现，无法发现 scopes/expires_at/created_at 丢失、排序错、is_revoked/last_used_at 漂移或字段夹带；后续复扫也删除完整 body 后的重复字段集合守门。现升级为真实 API token 分页精确响应契约，避免 token 列表测试只证明“有一个熟悉名字且没看到 token” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_auth_results_artifacts.py::test_audit_events_api_token_requires_audit_read_scope_without_self_audit_on_denial` 1 passed | targeted docs contract passed；targeted ruff passed | 无新增 warning；真实 auth API token 访问 audit-events 正向路径现在固定完整分页响应与 AuditEventResponse body，包含 tenant/user/action/resource/before_state/after_state/ip_address/user_agent/created_at，并删除完整 body 后的重复字段集合守门 | 测试 review 发现该用例已证明 audit.read scope 才能读取且拒绝路径不写自审计，但成功响应只抽查 total/action/resource_id；如果 audit response 漏掉 before_state/user_agent/created_at、created_at 串错或分页壳漂移，旧测试仍绿。现升级为真实 audit-events token scope 精确响应契约，避免权限正向测试只证明“能看到那条审计” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_api_endpoints.py::TestAnalytics::test_get_test_history` 1 passed | targeted docs contract passed；targeted ruff passed | 无新增 warning；analytics test-history 真实 API integration 现在固定完整 response body，包含两个历史点的 run_id/run_created_at/run_status/status/duration_ms/error_message/git_ref 与 pagination，并删除完整 body 后的冗余字段集合守门 | 测试 review 发现该用例已证明 soft-deleted run 不污染历史点、顺序和部分字段，但仍只抽查 git_ref/duration/error；如果 run_created_at 格式、第一条 duration/error、pagination 壳或字段集合漂移，旧测试仍绿。现升级为 integration 级精确响应契约，避免 analytics 历史测试只证明“两个 run 大致按顺序返回” |
| 2026-05-31 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_api_endpoints.py::TestProjectsCRUD::test_list_projects tests/integration/test_api_endpoints.py::TestRuns::test_list_runs tests/integration/test_api_endpoints.py::TestPipelines::test_list_pipelines tests/integration/test_api_endpoints.py::TestNotificationRules::test_list_notification_rules` 4 passed | targeted docs contract passed；targeted ruff passed | 无新增 warning；core API list integration 现在固定 projects/runs/pipelines/notification-rules 完整 response body、排序和时间字段，并删除完整 body 等值后的冗余 `set(body["data"][0])` 字段集合守门 | 测试 review 发现这些列表 smoke 虽然断言 page/per_page/total 和部分业务字段，但仍用 `len(data)`、`items_by_id`、少数字段抽样和重复字段集合断言；如果默认排序反了、DTO 漏掉 updated_at/git_sha/conditions、嵌套 selector/trigger_config 默认字段漂移，旧测试仍绿。现升级为真实 API 列表精确响应契约，避免 core list integration 只证明“条数和几个字段看起来对” |
| 2026-05-31 | N/A（Project members created_at 精确响应契约） | `tests/unit/test_api/test_project_members.py::test_list_project_members_returns_rows` 1 passed；targeted ruff passed | 无新增 warning；project members 列表单测现在固定完整 data 响应，包含每个 member 对应的 created_at ISO 时间串 | 测试 review 发现该用例已固定 project_id/user_id/username/email/role，但 created_at 仍只证明“非空”；如果响应把两个成员的创建时间串错、格式漂移或统一填当前时间，旧测试仍绿。现改成完整 data 精确匹配，避免成员列表测试只证明时间字段存在 |
| 2026-05-31 | N/A（API token list 精确响应契约） | `tests/unit/test_auth/test_auth_routes.py::TestTokenRoutes::test_list_tokens` 1 passed；targeted ruff passed | 无新增 warning；API token list 单测现在固定完整 data 响应，包含 token_id/name/scopes/expires_at/last_used_at/is_revoked/created_at，且字段集合精确排除 token/secret_hash | 测试 review 发现旧用例只断言 token_id/name/is_revoked 和 secret 字段不出现，无法发现 scopes、expires_at、last_used_at 或 created_at 漏传/串错/格式漂移。现升级为完整列表项响应契约，避免 API token list 测试只证明“关键字段和不泄密看起来对” |
| 2026-05-31 | N/A（Credential list 精确响应契约） | `tests/unit/test_api/test_credentials.py::test_list_credentials_returns_response_without_plaintext` 1 passed；targeted ruff passed | 无新增 warning；credential list 单测现在固定完整 data 响应，包含 id/project_id/name/type/created_by/created_at，且字段集合精确排除 value/encrypted_value | 测试 review 发现旧用例只断言 len(data)=2 和敏感字段不出现，无法发现 id/project_id/name/type/created_by/created_at 漏传、串值或格式漂移。现升级为完整 metadata 响应契约，避免 credential list 测试只证明“条数对且没看到明文” |
| 2026-05-31 | N/A（Environment list 精确响应契约） | `tests/unit/test_api/test_environments.py::test_list_environments_uses_pagination_and_decrypts_items` 1 passed；targeted ruff passed | 无新增 warning；environment list 单测现在固定完整 data 响应，包含 setup_script 与 created_at，且字段集合精确匹配 EnvironmentResponse | 测试 review 发现旧用例逐项断言了多数业务字段，但没有锁住 setup_script、created_at 或字段集合；如果响应漏掉创建时间、夹带额外字段或把 created_at 统一填当前时间，旧测试仍绿。现升级为完整列表项响应契约，避免 environment list 测试只证明“核心配置值看起来对” |
| 2026-05-31 | N/A（Project list 精确响应契约） | `tests/unit/test_api/test_projects.py::test_list_projects` 1 passed；targeted ruff passed | 无新增 warning；project list 单测现在固定完整 ProjectResponse 列表项，包含 silent_windows、created_at、updated_at，并继续锁住 tenant filter、offset/limit 和 name order | 测试 review 发现默认项目列表旧用例只断言 total 与第一条 slug，无法发现 tenant_id、git 配置、silent_windows、状态或时间字段漏传/串值/格式漂移。现升级为完整响应契约，避免 project list 测试只证明“有一个 test-project” |
| 2026-05-31 | N/A（Notification rule list 精确响应契约） | `tests/unit/test_api/test_notifications.py::test_list_rules_uses_project_access_pagination_and_returns_rules` 1 passed；targeted ruff passed | 无新增 warning；notification rule list 单测现在固定完整 data 响应，包含 enabled/conditions/template/created_at，且字段集合精确匹配 NotificationRuleResponse | 测试 review 发现旧用例只抽查 id/project_id/name/channels，无法发现 enabled、conditions、template 或 created_at 漏传/串值/格式漂移。现升级为完整列表项响应契约，避免 notification rule list 测试只证明“规则名和渠道看起来对” |
| 2026-05-31 | N/A（Pipeline list 精确响应契约） | `tests/unit/test_api/test_pipelines.py::test_list_pipelines_returns_paginated_response` 1 passed；targeted ruff passed | 无新增 warning；pipeline list 单测现在固定完整 PipelineResponse 列表项，包含 stages/selector/trigger_config/collectors/retry_policy、enabled 与 created_at/updated_at | 测试 review 发现旧用例只断言 total 与第一条 name，无法发现嵌套 stage 默认字段、selector、trigger_config、collector、retry_policy 或时间字段漏传/串值/格式漂移。现升级为完整响应契约，避免 pipeline list 测试只证明“有一个 Smoke Pipeline” |
| 2026-05-31 | N/A（Schedule list 精确响应契约） | `tests/unit/test_api/test_schedules.py::test_list_schedules_uses_project_access_and_pagination` 1 passed；targeted ruff passed | 无新增 warning；schedule list 单测现在固定完整 data 响应，包含 timezone/missed_fire_policy/quiet_windows/last_run_at/next_run_at/last_error/created_at，且字段集合精确匹配 ScheduleResponse | 测试 review 发现旧用例只抽查 id/project_id/pipeline_id/cron_expr，无法发现时区、misfire 策略、静默窗口、上次/下次运行时间或错误状态漏传/串值。现升级为完整调度响应契约，避免 schedule list 测试只证明“cron 看起来对” |
| 2026-05-31 | N/A（Audit events list 精确响应契约） | `tests/unit/test_api/test_audit.py::test_audit_events_list_applies_filters_pagination_and_self_audits_summary` 1 passed；targeted ruff passed | 无新增 warning；audit-events list 单测现在固定完整 data 响应，包含 action/resource_type/before_state/ip_address/user_agent/created_at，且字段集合精确匹配 AuditEventResponse | 测试 review 发现该用例已证明过滤、分页和自审计摘要，但响应只抽查 id/tenant/user/resource/after_state；如果 action、resource_type、before_state、IP、User-Agent 或 created_at 漂移，旧测试仍绿。现升级为完整审计响应契约，避免 audit-events list 测试只证明“目标审计行大概返回了” |
| 2026-05-31 | N/A（P3 analytics test-history 精确响应契约） | `tests/unit/test_api/test_p3.py::TestAnalytics::test_test_history_returns_repository_rows` 1 passed；targeted ruff passed | 无新增 warning；P3 analytics test-history 单测现在固定完整 response body，包含 run_id/run_created_at/run_status/status/duration_ms/error_message/git_ref 与 pagination | 测试 review 发现新版 analytics 单测已锁住 test-history 完整响应，但旧 P3 用例仍只抽查 run_id/status/error_message；如果 run_created_at、run_status、duration_ms 或 git_ref 映射漂移，旧测试仍绿。现统一为完整响应契约，避免同一接口存在“严格新测 + 宽松旧测”的双重口径 |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_db_persistence.py::test_pipeline_api_persists_rows_and_soft_delete_hides_from_repository` 1 passed | targeted ruff passed | 无新增 warning；Pipeline API 真实 DB 用例现在断言软删除后 `PipelineRepository.list_by_project()` 返回 total=1 且 visible id 列表只含 seed pipeline，不再只检查被删 pipeline id 不在返回集合 | 测试 review 发现该用例已证明 create 持久化和 delete 写入 deleted_at，但 soft-delete 可见性只做 membership 负向；如果 repository 额外返回其他 pipeline、total 漂移或分页壳错误，旧测试仍绿。现把软删除后的 repository 可见集升级为精确 seed pipeline 契约 |
| 2026-05-30 | N/A（Project members list 精确响应契约） | `tests/unit/test_api/test_project_members.py::test_list_project_members_returns_rows` 1 passed；targeted ruff passed | 无新增 warning；project members 列表单测现在按顺序固定 project_id、user_id、username、email、role 两条响应，不再只检查 role 集合 | 测试 review 发现成员列表虽然已锁住 project/tenant 入参、分页参数和 total，但响应只断言 `{role}` 集合；如果 user_id 映射错、重复返回同一 member、username/email 丢失或列表顺序漂移，旧测试仍绿。现把列表响应升级为精确 data 契约，避免成员列表测试只证明“角色看起来齐了” |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_repository_matrix.py::test_result_and_artifact_repositories_query_paginate_and_recover` 1 passed | targeted ruff passed | 无新增 warning；ArtifactRepository 真实 PG 用例现在固定软删除 first.log 后 total=2，第一页 limit=1 按默认 `created_at desc` 返回最新 trace.zip，不再只断言 first_artifact 不在第一页 | 测试 review 发现该用例已有软删除、分页和唯一约束证据，但 artifact 页只证明被删行没混入；如果默认排序漂移、第一页返回旧 report.html，旧测试仍可能绿。现把第一页 id 升级为精确 trace artifact，避免 artifact repository 测试只证明“删掉的那条看不见” |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_auth_results_artifacts.py::test_api_token_scope_matrix_enforced_by_real_routes` 1 passed | targeted ruff passed | 无新增 warning；API token scope matrix 中 `project.read` token 的 `/projects` 响应现在固定 total=1 且 data id 列表只含当前 project，不再只证明目标 project 出现在列表里 | 测试 review 发现该真实 scope 矩阵虽然已证明只读 token 不能写、run.trigger token 能建 run、schedule 拒绝不落库，但 `project.read` 正向列表只用 membership；如果项目列表多返回其他 project 或 scope/tenant 过滤回退，旧测试仍绿。现把项目列表正向路径也升级为精确分页/隔离契约 |
| 2026-05-30 | N/A（Schedule due-query 精确返回契约） | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_repository_matrix.py::test_schedule_repository_due_query_ignores_soft_deleted_schedules` 1 passed；targeted ruff passed | 无新增 warning；Schedule due-query 真实 PG 用例现在断言返回 id 列表精确等于 `[visible_due.id]`，不再只检查 visible due 存在、soft-deleted/future 不存在 | 测试 review 发现台账已把该用例描述为“只返回未删除且到期的 schedule”，但测试实现仍是 membership 检查；如果查询额外夹带其他 due schedule，旧测试仍可能绿。现把 due-query 结果升级为精确列表契约，避免调度 repository 测试只证明“三个夹具里两个被过滤” |
| 2026-05-30 | N/A（Performance SLO workflow env 双向契约） | `tests/unit/test_release_gate_workflow.py::test_performance_slo_manifest_matches_ci_threshold_env` 1 passed；targeted ruff passed | 无新增 warning；release gate workflow 中的 `PERF_*` env 现在必须精确等于 SLO manifest 的 `threshold_env` 清单，不再只证明 manifest env 是 workflow env 子集 | 测试 review 发现旧断言 `threshold_envs <= workflow_envs` 只能发现缺失 env，无法发现 CI workflow 残留已从 manifest/baseline 删除的阈值变量；这会让 release gate 看似还有某条 SLO 阈值配置。现把 CI env 与 manifest 升级为双向一致契约，避免性能门禁测试只证明“关键阈值至少存在” |
| 2026-05-30 | N/A（PluginRegistry builtins 精确清单契约） | `tests/unit/test_plugins/test_registry.py::test_register_builtins_includes_expected_plugin_families` 1 passed；targeted ruff passed | 无新增 warning；PluginRegistry builtins 用例现在固定 runner_names 为 pytest/jest/go_test/playwright，collector_names 为 junit，source_names 为 git，不再只用子集/包含断言 | 测试 review 发现 builtins 注册是固定清单而非外部 discovery；如果实现意外多注册 runner、collector 或 source，旧测试仍绿。现把内置插件清单升级为完整名单契约，避免 registry 测试只证明“常见插件存在” |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_api_endpoints.py::TestRuns::test_list_runs_filters_pipeline_git_ref_and_created_range` 1 passed | targeted ruff passed | 无新增 warning；Run list pipeline/git_ref/created range 真实 API 用例现在锁住 pipeline_id 过滤 total=2 且按 `-created_at` 返回 later、target 两条，created_at range 过滤 total=1 且只返回 target | 测试 review 发现该用例只检查 target run 在 pipeline/range 返回集合中、first/later 不在部分集合中；如果 pipeline filter 漏掉 later run、total 漂移或默认排序反了，旧测试仍绿。现把过滤结果升级为精确 total、id 顺序和 range 单项契约 |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_db_persistence.py::test_project_repository_filters_tenant_paginates_and_excludes_soft_deleted` 1 passed | targeted ruff passed | 无新增 warning；ProjectRepository 真实 PG persistence 用例现在固定 tenant A total=2 且 id set 只含 seed project 与 keep project，tenant B total=1 且只含 second tenant project | 测试 review 发现该用例只断言 `total >= 2` / `tenant_b_total >= 1` 与目标 id 存在；如果 list_by_tenant 混入额外 project、soft-deleted project 或跨租户 project，旧测试仍可能绿。现把分页 total 和返回 id set 锁到夹具精确期望，避免 tenant repository 测试只证明“至少返回了目标数据” |
| 2026-05-30 | N/A（Heartbeat transient Redis error 参数契约） | `tests/unit/test_worker/test_tasks.py::TestHeartbeatLoop::test_heartbeat_continues_after_redis_error` 1 passed；targeted ruff passed | 无新增 warning；heartbeat transient Redis error 用例保留时间型 `redis.set.await_count >= 3` 下界，同时逐次校验 heartbeat key、UTC ISO timestamp 与 `ex=90` TTL | 测试 review 发现 heartbeat 容错用例只证明异常后循环继续；如果后续写错 Redis key、写入 naive timestamp 或漏 TTL，旧测试仍绿。现把每次 `redis.set` 参数也纳入断言，避免心跳测试只证明“调用过几次” |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_repository_matrix.py::test_run_retention_hard_delete_cascades_results_artifacts_and_events` 1 passed | targeted ruff passed | 无新增 warning；Run retention 真实 PG 测试现在固定 cutoff，删除前先计算 terminal 且过期的 eligible_before，并断言 `delete_terminal_older_than()` 返回值精确等于 eligible_before | 测试 review 发现 run retention 用例只要求 `deleted >= len(eligible_run_ids)`；如果 cleanup 误删其他 terminal run、或 rowcount 与实际 eligible 集合漂移，旧测试仍绿。现把 hard-delete cascade 测试从“至少删掉目标 run”升级为精确 rowcount/eligibility 契约 |
| 2026-05-30 | N/A（Worker claim row-lock commit 顺序契约） | `tests/unit/test_worker/test_tasks.py::TestClaimReleasesRowLock::test_commit_called_immediately_after_successful_claim` 1 passed；targeted ruff passed | 无新增 warning；worker claim 失败执行路径现在记录 `claim -> commit -> execute`，证明 PREPARING 行锁在进入长执行前释放，不再只断言 `session.commit` 至少调用一次 | 测试 review 发现该用例名称和注释都要求 claim 后第一时间 commit，但实际只看 `commit.await_count >= 1`；如果 commit 被挪到 executor 失败处理或 finally release_worker 后，旧测试仍绿。现锁住 claim/commit/execute 相邻顺序，避免 worker 行锁测试只证明“某处 commit 过” |
| 2026-05-30 | N/A（Executor state transition commit 顺序契约） | `tests/unit/test_engine/test_executor.py::TestExecutorCommitsAfterStateTransitions::test_commit_called_after_mark_running tests/unit/test_engine/test_executor.py::TestExecutorCommitsAfterStateTransitions::test_commit_called_after_mark_collecting` 2 passed；targeted ruff passed | 无新增 warning；executor RUNNING/COLLECTING 状态转换用例现在跟踪 `mark_running -> commit` 与 `mark_collecting -> commit` 的相邻顺序，不再只按 `commit.await_count >= 2` 证明总调用数 | 测试 review 发现这两条单测只证明 commit 总数够多；如果 RUNNING 的 commit 被推迟到 `update_execution_id` 后、或 COLLECTING 的 commit 被挪到终态写入后，旧测试仍可能绿。现把可见状态转换的事务边界锁到“下一步必须 commit”，避免 executor 事务测试只为覆盖率服务 |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_repository_matrix.py::test_user_repository_scopes_soft_delete_and_recovers_from_unique_violation tests/integration/test_real_repository_matrix.py::test_run_retention_hard_delete_cascades_results_artifacts_and_events` 2 passed | targeted ruff passed | 无新增 warning；User repository tenant list 现在固定 total=2，Run repository project/pipeline list 在新增结果/artifact 前固定 total=1 且返回 seed run id 精确列表 | 测试 review 发现真实 repository matrix 里还有 `total >= 2` / `project_total >= 1` / `pipeline_total >= 1`；如果 tenant list 混入额外用户、project/pipeline run list 混入额外 run，旧测试仍绿。现把可精确预期的分页 total 和 returned ids 锁住，避免 repository 列表测试只证明“至少有当前 seed 数据” |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_repository_matrix.py::test_audit_retention_hard_deletes_only_events_older_than_cutoff` 1 passed | targeted ruff passed | 无新增 warning；Audit retention 真实 PG 测试现在固定 cutoff，并断言 eligible_before 精确为 2、`delete_older_than()` 返回值等于 eligible_before，两个超期事件被硬删且未超期事件保留 | 测试 review 发现 audit retention 用例只要求 `deleted >= 2`；如果清理任务误删额外历史审计行或 repository rowcount 与真实删除数量不一致，旧测试仍绿。现把保留策略验证从“至少删了两个”升级为精确 eligibility/rowcount 契约，避免数据保留测试只证明 cleanup 有动作 |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_audit_events_api.py::test_audit_events_owner_admin_can_list_member_viewer_forbidden` 1 passed | targeted ruff passed | 无新增 warning；Audit events owner/admin 列表路径现在按 action/resource 精确查询，固定 page/per_page/total/data id，并断言两次成功查询各写 1 条 `audit_events.list` 自审计，member/viewer 拒绝仍不写自审计 | 测试 review 发现 owner/admin 集成用例只要求 `total >= 1` 且目标 id 在返回集合中；如果查询夹带其他审计行、分页 total 膨胀或成功自审计漏写，旧测试仍绿。现把权限正向路径也升级为精确分页/过滤/自审计契约，避免审计查询测试只证明“至少能看到某条记录” |
| 2026-05-30 | N/A（Webhook signature empty-secret helper 契约） | `tests/unit/test_api/test_webhook_signature.py::TestEmptySecret` 2 passed；targeted ruff passed | 无新增 warning；`verify_webhook_signature` 现在对空 secret 固定返回 False，项目未配置 secret 时是否跳过验签仍由路由层显式策略决定 | 测试 review 发现 helper 单测把空 secret + 任意签名断言成 True，等于把安全旁路写成低层验证契约；如果后续调用方忘记先判断 secret，旧测试会保护错误行为。现把 helper 收紧为“只负责验证”，避免签名测试只证明未配置 secret 时可以被任意 header 通过 |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 RUN_PERFORMANCE_TESTS=1 tests/integration/test_performance_smoke.py::test_artifact_list_api_p99_smoke` 1 passed | targeted ruff passed | 无新增 warning；Artifact list p99 smoke 的 80 条当前 run artifact 现在固定 total、len(data)、name set 和 size_bytes 精确匹配，继续断言 DB-only 不触发 S3 | 测试 review 发现 Artifact list 性能 smoke 只要求 `total >= 80` 和 expected names 为子集；如果列表混入其他 run 的 artifact、total 膨胀或 size 错配，旧测试仍绿。现把 SLO smoke 同时变成分页/隔离正确性哨兵，避免性能测试只证明“至少返回了这些行且够快” |
| 2026-05-30 | N/A（GitHub repo URL candidates 精确匹配契约） | `tests/unit/test_api/test_p3.py::test_github_repo_url_candidates_expand_common_clone_forms` 1 passed；targeted ruff passed | 无新增 warning；GitHub provider repository URL candidates 现在固定为 clone/html/ssh/git/full_name 推导出的 4 个精确候选，不允许额外 URL 混入 | 测试 review 发现 `_github_repo_url_candidates` 用例只用 `issubset`，只能证明常见候选存在，不能发现实现额外生成错误 URL、扩大 provider project 匹配面或误匹配其他项目。现锁住完整 set，避免 GitHub provider 匹配测试只证明“至少含有常见形式” |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_api_write_state.py::test_project_api_audits_lifecycle_without_git_url_userinfo_leak tests/integration/test_real_api_write_state.py::test_pipeline_api_persists_audit_states_for_lifecycle tests/integration/test_real_api_write_state.py::test_project_member_api_persists_audit_states_for_role_changes tests/integration/test_real_api_write_state.py::test_credentials_api_persists_encrypted_value_rotates_and_soft_deletes tests/integration/test_real_api_write_state.py::test_environment_api_persists_encrypted_env_vars_limits_and_soft_delete tests/integration/test_real_api_write_state.py::test_notification_rule_api_persists_updates_and_hides_soft_deleted_rule tests/integration/test_real_api_write_state.py::test_schedule_and_run_apis_persist_next_run_metadata_and_audit_rows tests/integration/test_real_api_write_state.py::test_batch_run_apis_persist_state_and_audit_rows` 8 passed | targeted ruff passed | 无新增 warning；真实写路径 lifecycle 审计 action 从 `issubset` 提升为精确列表，且按 resource/user 维度不允许额外 action 或重复 action 混入 | 测试 review 发现 project/pipeline/member/credential/environment/notification/schedule/batch 的真实 DB 写路径只证明关键审计 action 出现过；如果同一资源多写 `*.read`、重复写 update/delete，或 batch 多写额外 action，旧测试仍绿。现用 `_assert_audit_actions` 锁住精确 action 集合，避免审计测试只证明“至少写过几条” |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_api_write_state.py::test_schedule_api_rejects_cross_project_pipeline_without_persisting` 1 passed | `tests/unit/test_api/test_environments.py::test_get_environment_returns_same_404_for_missing_or_other_project tests/unit/test_api/test_environments.py::test_update_delete_environment_return_same_404_without_side_effects tests/unit/test_api/test_environments.py::test_environment_routes_hide_missing_project_without_side_effects` 3 passed；targeted ruff passed | 无新增 warning；Environment 缺失/跨项目 env、缺失 project，以及 Schedule 跨项目 pipeline 拒绝路径固定 exact `NOT_FOUND` envelope，不再兼容 legacy `detail or error.message` | 测试 review 发现这些 404 用例虽然已覆盖状态码、响应体一致和副作用边界，但仍通过 `body.get("detail") or error.message` 抽 message；如果统一错误处理退回 FastAPI 默认 `{"detail": ...}`，或 `details` 开始夹带探测 ID，旧测试仍绿。现锁住完整 envelope，避免隐藏资源/跨项目测试只证明 message 文案 |
| 2026-05-30 | N/A（SSE stream run 404 错误契约） | `tests/unit/test_api/test_sse.py::test_stream_logs_cross_tenant_returns_404 tests/unit/test_api/test_sse.py::test_stream_events_cross_tenant_returns_404 tests/unit/test_api/test_sse.py::test_stream_logs_run_not_found_returns_404` 3 passed | targeted ruff passed | 无新增 warning；SSE logs/events 的跨租户或缺失 run 固定 `NOT_FOUND` envelope，不回显 run_id，按 `run_id + tenant_id` 查询 run，并在拒绝时不读 Redis stream/status | 测试 review 发现 SSE 404 用例原先用 `detail or error.message` 兼容旧响应形态；如果统一错误处理退回 FastAPI 默认 `{"detail": "Run not found"}` 或实现没有按 tenant 过滤，旧测试仍可能绿。现锁住 exact envelope、no-id echo、tenant lookup 和 no-Redis 副作用，避免 SSE 安全测试只证明状态码/文案 |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_api_endpoints.py::TestRuns::test_list_runs` 1 passed | targeted ruff passed | 无新增 warning；默认 `/runs` 列表只返回当前 seed tenant 的 1 条 run，并锁住分页字段、run/project/pipeline/environment/user 映射、pipeline_name/status/priority/git_ref/attempt | 测试 review 发现 `test_list_runs` 原先只断 `data` 存在、`total >= 1` 和 seed run id；如果列表泄露其他 tenant、分页壳漂移，或 response DTO 把 pipeline/environment/user 映射错，旧测试仍可能绿。现把它升级为 Run list seeded item 集成契约，避免列表 smoke 只证明“某处出现过这个 id” |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_api_endpoints.py::TestProjectsCRUD::test_list_projects tests/integration/test_api_endpoints.py::TestPipelines::test_list_pipelines tests/integration/test_api_endpoints.py::TestNotificationRules::test_list_notification_rules` 3 passed | targeted ruff passed | 无新增 warning；projects/pipelines/notification-rules 列表 smoke 现在固定 page=1、per_page=20、精确 total/len(data)，并锁住 tenant/project scope 与 seed/created item 字段 | 测试 review 发现这些列表用例仍有 `assert "data" in body` 与 `total >= N`，只能证明响应壳和“至少有点数据”。在当前 seed fixture 下条数可精确预期，现升级为 Core list exact pagination 集成契约，避免分页漂移、跨租户/跨项目泄漏或列表 repository scope 回退时仍假绿 |
| 2026-05-30 | N/A（P3 analytics exact response 契约） | `tests/unit/test_api/test_p3.py::TestAnalytics::test_trends_returns_paginated_response tests/unit/test_api/test_p3.py::TestAnalytics::test_flaky_returns_paginated_response` 2 passed | targeted ruff passed | 无新增 warning；legacy P3 analytics trends/flaky 单测固定完整 response body，覆盖 trends 的 passed_runs/failed_runs/pass_rate 与 flaky 的 name/total_runs/passed_count/failed_count/flaky_rate，以及 pagination 全字段 | 测试 review 发现这两条 analytics 单测仍用 `assert "data" in body` 和局部字段抽样；如果响应 DTO 漏掉 failed_runs/passed_count、比率计算漂移，或 pagination 壳变化，旧测试仍可能绿。现锁住完整响应，避免 analytics 单测只证明有 data 壳 |
| 2026-05-30 | N/A（Run trigger pipeline/archived 错误契约） | `tests/unit/test_api/test_runs.py::test_trigger_run_pipeline_not_found tests/unit/test_api/test_runs.py::test_trigger_run_cross_tenant_pipeline_returns_same_404 tests/unit/test_api/test_runs.py::test_trigger_run_archived_project_returns_409` 3 passed | targeted ruff passed | 无新增 warning；Run trigger 缺失 pipeline 与跨租户 pipeline 固定返回 `NOT_FOUND` envelope 且不回显 pipeline/project 探测 ID；archived project 固定 409 `Project is archived; new runs cannot be triggered`，且不查 environment、不创建 run、不 enqueue、不写 audit | 测试 review 发现 Run trigger 拒绝路径虽然已有 no-side-effect 断言，但错误响应只抽 message 或检查包含 `archived`，无法发现统一 error envelope 漂移、错误体泄露 ID，或 archived 文案泛化。现锁住 exact response 与脱敏边界，避免 Run trigger 安全测试只证明状态码 |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_webhook_branch_dedup.py::test_provider_github_push_rejects_invalid_signature_without_run tests/integration/test_webhook_branch_dedup.py::test_webhook_archived_project_returns_409_and_creates_no_run tests/integration/test_webhook_branch_dedup.py::test_signed_webhook_rejects_missing_signature_without_creating_run tests/integration/test_webhook_branch_dedup.py::test_webhook_cross_tenant_project_returns_404` 4 passed | targeted ruff passed | 无新增 warning；Webhook 拒绝响应脱敏契约：provider 签名错误、项目级签名缺失、archived project、跨租户 project 现在固定 exact 错误响应，401/409 为 `detail`，404 为 `NOT_FOUND` envelope；401/404 拒绝响应不回显 webhook secret、project/git URL、project/tenant/user 探测 ID，且 no-run/no-decision-audit 副作用边界保持 | 测试 review 发现 webhook 拒绝路径虽已有 no-run 证据，但部分用例仍允许错误体漂移或泄露探测材料而绿。现补齐错误体与脱敏断言，避免 webhook 安全测试只证明状态码和 run count |
| 2026-05-30 | N/A（Rate limit strict Redis outage envelope） | `tests/unit/test_api/test_rate_limit.py::TestRateLimitMiddlewareDispatch::test_strict_auth_endpoint_fails_closed_when_redis_missing` 5 passed | targeted ruff passed | 无新增 warning；strict auth endpoint 在 Redis client 缺失时固定 503 `SERVICE_UNAVAILABLE` envelope、`Retry-After: 5`，并且不 await downstream handler | 测试 review 发现 strict fail-closed 用例已有 503 与 Retry-After，但没有锁住错误体；未来若错误体退化为空、泄露内部异常或与 API error contract 漂移，旧测试仍可能绿。现用 helper 统一校验 envelope，避免限流 Redis outage 测试只证明状态码 |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_cross_tenant_isolation.py` 14 passed | targeted ruff passed | 无新增 warning；跨租户隔离矩阵现在统一校验两边完整 `NOT_FOUND` envelope 相等、错误原因固定为 Project/Pipeline/Run/Artifact not found，且响应不回显被探测的 tenant B 或随机 UUID | 测试 review 发现该矩阵原先只比较 detail 相等且非空，仍可能漏掉统一 error envelope 漂移、details 字段泄露 ID、或错误消息泛化；后续复核又去掉 legacy `detail or error.message` 兼容。现用 `_assert_same_404_result` 锁住完整响应与脱敏边界，避免 existence-oracle 测试只证明状态码一致 |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_auth_audit_failure_paths.py::test_login_nonexistent_user_returns_401_not_500 tests/integration/test_auth_audit_failure_paths.py::test_auth_login_rate_limit_uses_real_redis_token_hash_bucket tests/integration/test_auth_audit_failure_paths.py::test_login_nonexistent_user_returns_401_when_audit_write_fails tests/integration/test_auth_audit_failure_paths.py::test_login_valid_user_wrong_password_returns_401_with_tenant_id` 4 passed | targeted ruff passed | 无新增 warning；auth failure/rate-limit 集成夹具现在使用符合 username pattern 和 32 字符上限的值，真实命中登录业务路径；unknown user 和 audit outage 固定 401，token-bucket login rate limit 前 5 次固定 401 且第 6 次 429，known-user wrong-password 固定 401 `Invalid credentials` 并锁住 `auth.login_failed` audit 精确 tenant_id/user_id、after_state reason/username，以及正确密码和错误密码都不进 audit payload | 测试 review 发现这些用例原本生成 `totally-nonexistent-user`、`audit-user-*`、`rate-limit-missing-*` 这类不符合登录 schema 的 username，部分还超过 32 字符，会在请求体验证阶段 422，根本没有证明 unknown-user、audit outage、rate-limit bucket 或“已知用户错密码”审计路径。现修正夹具并补强 audit payload/脱敏断言，避免认证审计测试只因未跑到目标分支而假绿/假红 |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_api_endpoints.py::TestCrossTenantIsolation::test_cross_tenant_project_access` 1 passed | targeted ruff passed | 无新增 warning；跨租户 project detail 与随机 project ID 固定返回同一 `NOT_FOUND` envelope，响应不回显外部 project/tenant/user ID，也不回显随机探测 ID | 测试 review 发现核心跨租户 project smoke 已比较 tenant B 与随机 UUID 的 404 体一致，但还没锁住统一 error envelope 和响应脱敏；如果实现把外部 ID 写进错误 details/message，旧测试仍可能绿。现用真实 FastAPI/PostgreSQL 证据锁住 existence oracle 不能泄露标识 |
| 2026-05-30 | N/A（JWT invalid/revoked 前置短路契约） | `tests/unit/test_auth_middleware.py::test_authenticate_jwt_rejects_when_token_actually_revoked tests/unit/test_auth_middleware.py::test_authenticate_jwt_rejects_expired_tokens tests/unit/test_auth_middleware.py::test_authenticate_jwt_rejects_invalid_tokens tests/unit/test_auth_middleware.py::test_authenticate_jwt_rejects_refresh_token_type` 4 passed；targeted ruff passed | 无新增 warning；revoked JWT 固定在 blacklist 命中后 401 且不打开 DB；expired、invalid 和 refresh token type 固定在 decode/type 阶段 401，不查 blacklist、不打开 DB | 测试 review 发现 JWT 负向用例主要断言 401 文案，无法发现实现把过期/坏格式/refresh token 或已撤销 token 继续推进 Redis/DB 当前用户回查。现用 no-DB/no-blacklist 副作用哨兵锁住前置短路，避免认证拒绝测试只证明状态码 |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_audit_events_api.py::test_audit_events_owner_admin_can_list_member_viewer_forbidden tests/integration/test_audit_events_api.py::test_audit_events_tenant_isolation_and_cross_tenant_ids_return_404` 2 passed | targeted ruff passed | 无新增 warning；audit-events owner/admin/member/viewer 与跨租户过滤集成用例现在锁住 403/404 响应体、跨租户 tenant/user/project/event id 不泄漏、拒绝查询不写 `audit_events.list` 自审计，成功空列表仍写自审计 | 测试 review 发现 audit-events 集成用例虽已有真实 DB 路径，但两个主路径只看 member/viewer 403、跨租户 actor/resource 404 和 empty data，无法发现错误来源漂移、404 泄露外部 ID，或拒绝探测写入 self-audit。现用真实 FastAPI/PostgreSQL 证据补齐响应、脱敏和副作用边界，避免安全集成测试只证明状态码 |
| 2026-05-30 | N/A（空测试 / assert True 静态门禁） | `tests/unit/test_test_quality_contracts.py` 4 passed；targeted ruff passed | 无新增 warning；静态契约现在扫描 `tests/unit` 与 `tests/integration` 的 Python 测试，禁止真实 `test_*` 函数只有 `pass`，禁止裸 `assert True`，并禁止 E2E spec 回流 `expect.arrayContaining` / `expect.objectContaining` / `toMatchObject` / `toBeTruthy()` / `toContainText` 这类存在性、超集或行级包含匹配器 | 测试 review 发现仓库已有大量高价值契约，但仍缺少最基础的“不能新增空测试或恒真断言”护栏；后续又发现 E2E 弱断言已被逐个清理，但静态门禁还没阻止 `toMatchObject` / `toBeTruthy()` / `toContainText` 回流。现把覆盖率型假测试和 E2E 假绿断言的最低成本形态变成会失败的质量门禁，守住 E2E API 证据不能退回“目标项在里面 / 响应大概包含 / 值非空或行里有词就行”的匹配方式，避免后续为了通过率/覆盖率新增无诊断价值用例 |
| 2026-05-30 | N/A（integration schema bootstrap QAP 环境隔离） | `tests/unit/test_integration_schema_bootstrap_contract.py` 3 passed；targeted ruff passed | 无新增 warning；`tests/integration/conftest.py` 在 Alembic 子进程 env 中先剔除外部 `QAP_*`，再写入 testcontainers PG、Redis、S3、JWT、encryption key 的完整测试默认值，包括 `QAP_S3_REGION=us-east-1` | 测试 review 发现 integration schema bootstrap 虽显式覆盖了 `QAP_DATABASE_URL`，但仍从整份 `os.environ` 拷贝开始，开发机/CI 残留的空白 `QAP_S3_REGION` 或其他 `QAP_*` 会让迁移假红，甚至让 Settings 读到非测试配置。现用契约证明外部 QAP 污染不会进入 Alembic，避免集成迁移测试依赖当前机器环境 |
| 2026-05-30 | N/A（backend integration JUnit 重复 nodeid 门禁） | `tests/unit/test_backend_integration_artifact_validator.py` 9 passed；targeted ruff passed | 无新增 warning；`validate_backend_integration_artifacts.py` 现在要求每个 JUnit testcase 必须且只能带一个 nodeid，拒绝重复 nodeid，并校验 testcase 数量与 nodeid 数量一致 | 测试 review 发现原证据校验器只比较 collect/JUnit nodeid 集合，能抓少跑或跑错，却抓不到 JUnit 通过重复 testcase 膨胀通过数量或单个 testcase 挂多个 nodeid 的假证据。现用两条红灯锁住重复/多重 nodeid，避免 backend integration 门禁只证明“集合覆盖了清单” |
| 2026-05-30 | N/A（Settings 单测环境隔离复核） | `tests/unit/test_config.py` 66 passed；`tests/unit/test_api/test_schedules.py tests/unit/test_api/test_health.py tests/unit/test_frontend_api_contract.py tests/unit/test_observability/test_tracing.py tests/unit/test_api/test_rate_limit.py tests/unit/test_config.py` 153 passed；`tests/unit/test_test_quality_contracts.py` 3 passed；targeted ruff passed；Playwright `--list` 10 tests；OpenAPI export smoke passed | 无新增 warning；配置相关单元测试现在显式使用 `_env_file=None`，`test_config.py` autouse 清理外部 `QAP_*` 环境；E2E backend app 保留真实 env default 启动语义但不读 `.env`，Playwright/global setup/`run-e2e.sh` 都显式填充本地 QAP 默认环境，helper 清理限流和 DB seed 只读取当前 Redis/DB/S3 URL；OpenAPI 导出也固定使用不读 `.env` 的契约配置 | 测试 review 发现 `test_config.py` 虽已断开 `.env` 仍可能读到开发机/CI 残留 `QAP_*`，schedules 单测裸 `Settings()` 依赖项目根 `.env` 才能启动，E2E helper 为清理 Redis 限流却构造完整 `Settings()`，可能清错 `.env` 指向的 Redis 或因无关必填配置失败；继续复核又发现本地 Playwright webServer、global setup migration/seed、`scripts/run-e2e.sh` 和前端 OpenAPI 导出仍可能靠 `.env` 启动。现把单元/E2E/契约导出配置输入收紧为用例自给自足，并用静态契约禁止 unit/shared conftest、E2E 支撑代码和 OpenAPI export 的隐式 dotenv 读取回流，避免配置/API/E2E/前端契约测试只在当前机器假绿 |
| 2026-05-30 | N/A（Default string config blank 启动期契约） | `tests/unit/test_config.py::test_default_string_settings_reject_blank_values tests/unit/test_config.py::test_otel_exporter_endpoint_blank_is_treated_as_unset tests/unit/test_config.py::test_cors_origins_reject_blank_entries tests/unit/test_config.py::test_cors_origins_strip_whitespace` 7 passed；coverage unit 1379 passed；targeted ruff passed | 无新增 warning；Settings 现在拒绝空白 S3 bucket/region、Docker host、OTel service name 与 CORS origin，合法 CORS origin 会 strip；空白 OTel exporter endpoint 继续按未配置处理以兼容 `.env` 默认 | 测试 review 发现旧配置测试只覆盖默认值能读出，无法发现这些默认字符串被 env 覆盖为空白时会延迟到 S3、Docker、OTel resource 或 CORS middleware 运行期失败；同时 `.env` 里空白 OTel exporter endpoint 本就是未配置语义。现用 5 个负向契约和 2 个归一化/兼容契约锁住边界，避免默认字符串测试只证明 happy defaults |
| 2026-05-30 | N/A（Trusted proxies CIDR 启动期契约） | `tests/unit/test_config.py::test_trusted_proxies_reject_invalid_cidrs tests/unit/test_config.py::test_trusted_proxies_accept_valid_cidrs_and_strip_whitespace` 4 passed；coverage unit 1372 passed；targeted ruff passed | 无新增 warning；Settings 现在拒绝空白或非法 `trusted_proxies` CIDR，并会规整合法 CIDR 两端空白 | 测试 review 发现 rate limit 用例只覆盖合法 CIDR 下的 XFF 解析，无法发现 `trusted_proxies` 写错时 `_parse_trusted_nets()` 只打一条 warning 并跳过配置，导致生产流量按代理 IP 桶限流或忽略真实客户端 IP。现用 3 个红灯和 1 个正向契约锁住启动期失败，避免 trusted proxy 测试只证明 happy CIDR |
| 2026-05-30 | N/A（Numeric config invalid bounds 启动期契约） | `tests/unit/test_config.py::test_numeric_settings_reject_invalid_bounds` 19 passed；coverage unit 1368 passed；targeted ruff passed | 无新增 warning；Settings 现在拒绝 DB/Redis/S3/JWT/timeout/rate-limit/retention 等关键数值配置的 0 或负值，`database_max_overflow=0` 保持合法 | 测试 review 发现旧配置测试只覆盖整数能从 env 读出正值，无法发现 pool/TTL/rate limit/retention 为 0 或负值会通过配置并延迟到 DB engine、Redis、JWT、限流或清理任务运行时失败。现用 19 个红灯锁住启动期失败，避免 numeric config 测试只证明 happy parse |
| 2026-05-30 | N/A（Log config invalid value 启动期契约） | `tests/unit/test_config.py::test_log_configuration_rejects_invalid_values` 4 passed；coverage unit 1349 passed；targeted ruff passed | 无新增 warning；Settings 现在拒绝非法或空白 `log_level` / `log_format`，合法 log level 归一为大写，合法 log format 归一为小写 | 测试 review 发现旧 logging 测试只覆盖 json/console happy path，无法发现 `QAP_LOG_LEVEL=verbose` 会拖到 `configure_logging()` 才失败，或 `QAP_LOG_FORMAT=yaml` 静默退成 console。现用 4 个红灯锁住启动期失败，避免 logging 配置测试只证明正常格式能启动 |
| 2026-05-30 | N/A（Required config blank values 启动期契约） | `tests/unit/test_config.py::test_required_string_fields_reject_blank_values` 6 passed；coverage unit 1345 passed；targeted ruff passed | 无新增 warning；Settings 现在拒绝空白 database_url、redis_url、S3 endpoint/access/secret 与 whitespace-only jwt_secret | 测试 review 发现旧必填配置用例只删除环境变量，无法发现空字符串或 32 个空格的 JWT secret 会通过配置并延迟到依赖初始化/运行时失败。现用 6 个红灯锁住启动期失败，避免 required config 测试只证明字段不存在会失败 |
| 2026-05-30 | N/A（Encryption keys rotation 配置边界契约） | `tests/unit/test_config.py::test_encryption_keys_rotation_values_are_validated tests/unit/test_dependencies.py::test_crypto_service_rejects_non_256_bit_keys_before_encrypt` 7 passed；coverage unit 1339 passed；targeted ruff passed | 无新增 warning；Settings 现在拒绝轮换 `encryption_keys` 的非 64 hex 值和 0-15 之外版本，CryptoService 构造阶段拒绝非 32 bytes key | 测试 review 发现旧配置测试只覆盖默认 `encryption_key`，无法发现轮换 key 短值/非 hex/非法版本会通过配置并延迟到首次加密才失败。现用 7 个红灯锁住启动期失败，避免密钥轮换测试只证明 happy rotation |
| 2026-05-30 | N/A（Env vars truncated ciphertext 解密容错契约） | `tests/unit/test_services/test_env_vars_crypto.py` 6 passed；coverage unit 1332 passed；targeted ruff passed | 无新增 warning；CryptoService 现在会在 AES-GCM 解密前拒绝短于 header/nonce/tag 的 envelope，`decrypt_env_vars` 对空/截断 ciphertext 返回统一脱敏 `ValueError` | 测试 review 发现旧用例只覆盖 AAD mismatch 与 key rotation，无法发现 base64 合法但 envelope 为空时冒出 `IndexError`、截断 tag 时冒出底层异常类型。现用 3 个红灯锁住统一错误边界，避免 env_vars crypto 测试只证明正常密文和错 AAD |
| 2026-05-30 | N/A（Go runner malformed JSON/elapsed 容错契约） | `tests/unit/test_plugins/test_go_test_runner.py` 34 passed；`tests/unit/test_plugins/test_pytest_runner.py tests/unit/test_plugins/test_jest_runner.py tests/unit/test_plugins/test_playwright_runner.py tests/unit/test_plugins/test_go_test_runner.py` 136 passed；coverage unit 1329 passed；targeted ruff passed；Go helper smoke passed | 无新增 warning；Go runner 现在会忽略合法 JSON 但非 object 的输出行，字符串/NaN/Infinity/负数 elapsed 统一归零，Python 本地路径和 worker shell helper 都生成合法 JUnit time | 测试 review 发现旧用例只覆盖 panic 这类非 JSON 行，无法发现 JSON array/string 会让 `_parse_cases()` 抛 `AttributeError`，也无法发现畸形 elapsed 会中断采集或写出负/非有限 JUnit duration。现用 5 个红灯锁住结果采集容错，避免 Go runner 测试只证明标准 go test NDJSON happy path |
| 2026-05-30 | N/A（内置 runner test_paths 工作区边界契约） | `tests/unit/test_plugins/test_pytest_runner.py tests/unit/test_plugins/test_jest_runner.py tests/unit/test_plugins/test_playwright_runner.py tests/unit/test_plugins/test_go_test_runner.py` 130 passed；coverage unit 1323 passed；targeted ruff passed | 无新增 warning；pytest/jest/playwright/go runner 的 `test_path`/`test_paths` 现在拒绝绝对路径、`..` 越界路径、Windows 绝对路径和空白路径，错误不回显原始路径 | 测试 review 发现既有 runner 路径安全只保护 JUnit/JSON 输出路径，测试输入路径仍可指向 `/tmp`、`../secret`、Windows 绝对路径或空白路径；旧用例只证明合法路径能拼进命令。现用 26 个参数化红灯锁住输入测试范围，避免 runner 测试只证明报告路径不越界 |
| 2026-05-30 | N/A（GitSource URL scheme/SSH 判定顺序契约） | `tests/unit/test_plugins/test_git_source.py` 28 passed；coverage unit 1297 passed；targeted ruff passed | 无新增 warning；GitSource URL 校验现在先处理显式 URL scheme，只有无 scheme 时才按 scp-like SSH 解析；`http://git@host:80/...` 与 `ftp://git@host:21/...` 会在 DNS 前拒绝 | 测试 review 发现旧 SSH 正则跑在 `urlparse` 前，带 userinfo+port 的非 https URL 会被误判成 SSH hostname，现有 `http://host/...` 用例抓不到。现用带 userinfo+port 的 http/ftp URL 证明不再被 SSH 正则绕过，避免 Git URL scheme 测试只覆盖朴素非法 URL |
| 2026-05-30 | N/A（GitSource rev-parse HEAD 解析契约） | `tests/unit/test_plugins/test_git_source.py` 26 passed；coverage unit 1295 passed；targeted ruff passed | 无新增 warning；GitSource clone 后 `git rev-parse HEAD` 非零退出或返回空 SHA 会明确失败，不再把空 revision 写进 SourceRevision | 测试 review 发现既有 clone happy path 大多 mock `_resolve_sha()`，只能证明 clone subprocess 被调用，无法发现 HEAD SHA 解析失败被吞。现用双 subprocess mock 锁住 clone 成功后 rev-parse 失败/空输出两条红灯，避免 GitSource 测试只证明 clone 命令 happy path |
| 2026-05-30 | N/A（GitSource non-public DNS 防 SSRF 契约） | `tests/unit/test_plugins/test_git_source.py` 24 passed；coverage unit 1293 passed；targeted ruff passed | 无新增 warning；GitSource URL 校验现在拒绝 DNS 解析到 loopback/link-local/unspecified/multicast/reserved 等非公网或非单播地址，统一在 git subprocess 前返回 `non-public IP` | 测试 review 发现既有 GitSource private-IP 用例只锁 127/169 段与 `private IP` 文案，无法发现 `0.0.0.0`、`224.0.0.1`、`2001:db8::1` 被放行。现用参数化 DNS 结果证明不完整 CIDR 清单不再形成假绿，避免 Git URL SSRF 测试只证明一类私网地址 |
| 2026-05-30 | N/A（Jest/Playwright JUnit parent 目录契约） | `tests/unit/test_plugins/test_jest_runner.py tests/unit/test_plugins/test_playwright_runner.py` 45 passed；coverage unit 1289 passed；targeted ruff passed | 无新增 warning；Jest/Playwright runner 使用自定义 `junit_xml` 时会在 subprocess 启动前创建 parent 目录，并继续由配置值覆盖外部 JUnit env var | 测试 review 发现既有 run_tests 用例都预先创建了 `results/` 或写平铺 XML，掩盖了绝对 `working_dir` 下 `if not junit_path.is_absolute()` 判断反向的问题；真实 nested report 路径会导致 runner 跑完但收不到 JUnit。现用 mock subprocess 断言 nested parent 已创建，避免 runner 测试只证明已有目录 happy path |
| 2026-05-30 | N/A（JUnit duration 输入容错契约） | `tests/unit/test_plugins/test_junit_collector.py` 24 passed；coverage unit 1287 passed；targeted ruff passed | 无新增 warning；JUnit testcase `time` 现在对空串、非数字、NaN、Infinity、负数统一归零，保留 testcase 结果，不让 collector 抛异常或写入负 duration | 测试 review 发现 collector 已覆盖 malformed XML 和 missing time，但没有覆盖“XML 合法、duration 字段畸形”的报告；真实 runner 产物只要一个 bad time 就会打断结果采集。现用 5 个参数化畸形 duration 锁住容错路径，避免 JUnit collector 测试只证明 happy XML 和明显 parse error |
| 2026-05-30 | N/A（DingTalk/WeCom JSON object 响应契约） | `tests/unit/test_worker/test_channels.py` 38 passed；coverage unit 1282 passed；targeted ruff passed | 无新增 warning；DingTalk/WeCom 上游 HTTP 200 响应现在要求 JSON body 必须是 object；JSON array/string 这类非 object 响应返回固定 `invalid JSON object` 失败，不抛 `AttributeError`，且不回显 access_token、secret 或 webhook_key | 测试 review 发现已有渠道测试覆盖 HTTP 500、errcode 和 JSON decode 失败，但没覆盖“能 decode、形状错误”的响应；真实外部渠道返回 array/string 时 worker 会抛异常，通知日志拿不到安全失败结果。现用两条上游畸形响应契约锁住 ChannelResult 失败路径，避免渠道测试只证明正常 object JSON 和明显 parse error |
| 2026-05-30 | N/A（Admin status terminal denominator 契约） | `tests/unit/test_api/test_admin.py` 4 passed；coverage unit 1280 passed；targeted ruff passed | 无新增 warning；`GET /api/v1/admin/status` 的 `success_rate_1h` 现在用 `done/failed/cancelled/timeout` 作为终态分母，只把 `done` 计入成功；12 个终态、8 个成功时返回 `0.6667` | 测试 review 发现旧 admin/status 用例把 total query 锁成 `done/failed`，导致取消和超时运行不进失败率；状态页会被“漂亮”的成功率误导，测试还会绿。现把终态分母写进 await 参数断言，避免状态页测试只证明四个数字有形状 |
| 2026-05-30 | N/A（Webhook URL userinfo 防凭据嵌入契约） | `tests/unit/test_worker/test_channels.py::TestWebhookChannel::test_rejects_url_credentials_before_dns_or_http_client` 1 passed；`tests/unit/test_worker/test_channels.py` 36 passed；coverage unit 1280 passed；targeted ruff passed | 无新增 warning；generic webhook URL 现在拒绝 `user:password@host`，并在 DNS 解析和 HTTP client 创建前短路；错误只返回固定文案，不回显 token/userinfo；总覆盖率 87.35%，statements 89.33%，branches 78.61% | 测试 review 发现 webhook success/error/timeout/custom method 用例多数 patch 掉 `_validate_webhook_url`，已有 SSRF 用例只证明 non-global IP 会被挡；无法发现 URL 内嵌凭据会继续进入 httpx request、日志或异常上下文。现用真实 `send()` 路径断言 no-DNS/no-HTTP/no-secret echo，避免 webhook channel 测试只证明“可发送”和“私网 IP 被挡” |
| 2026-05-30 | N/A（JWT current-user lifecycle 防陈旧权限契约） | `tests/unit/test_auth_middleware.py` 39 passed；`tests/unit/test_auth_middleware.py tests/unit/test_auth/test_auth_routes.py` 100 passed；coverage unit 1279 passed；targeted ruff passed | 无新增 warning；JWT 认证现在在有 DB session 时回查当前 user row，拒绝 missing/inactive/soft-deleted user、soft-deleted tenant、tenant/role 漂移；legacy 无 `jti` token 仍只在 blacklist 维度兼容；总覆盖率 87.34%，statements 89.33%，branches 78.59% | 测试 review 发现旧 middleware 只对 platform-admin claim 查 DB，普通 JWT 在账号停用、租户软删除或角色降级后仍可凭旧 access token 构造 `CurrentUser` 进入权限层。现把当前用户生命周期与角色/租户一致性纳入认证测试，避免 JWT 测试只证明签名和 claim shape |
| 2026-05-30 | N/A（API token owner status 防失效账号继续访问契约） | `tests/unit/test_auth_middleware.py` 32 passed；coverage unit 1272 passed；targeted ruff passed | 无新增 warning；API token 认证现在 secret 匹配后还会拒绝 inactive owner、soft-deleted owner 和 soft-deleted tenant，且不更新 last_used；总覆盖率 87.33%，statements 89.32%，branches 78.56% | 测试 review 发现 API token 每次查 DB，但旧实现只检查 token revoked/expired，不检查 owner 生命周期；账号停用或软删除后，机器 token 仍可构造 `CurrentUser` 进入权限层。现把 owner 状态纳入中间件契约，避免 API token 测试只证明 token 行本身有效 |
| 2026-05-30 | N/A（API token secret-before-expiry 防枚举契约） | `tests/unit/test_auth_middleware.py` 29 passed；coverage unit 1269 passed；targeted ruff passed | 无新增 warning；API token 认证现在先验证 secret，再在 secret 匹配后返回 expired；伪造 secret 即使命中过期 token_id 也固定返回 `Invalid or revoked API token`，不更新 last_used | 测试 review 发现旧单测把“过期 token 不调用 `TokenService.verify_token()`”锁成期望，导致攻击者只要猜中 token_id，即使 secret 错误也能从 `expired` 响应枚举 token 存在和状态。现将测试改成能抓信息泄露，避免认证测试固化错误短路顺序 |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_repository_matrix.py::test_schedule_repository_due_query_ignores_soft_deleted_schedules` 1 passed | `tests/unit/test_project_repository_contracts.py tests/unit/test_worker/test_check_schedules.py` 10 passed；coverage unit 1268 passed；targeted ruff passed | 无新增 warning；`ScheduleRepository.find_due_schedules()` 现在生成 `FOR UPDATE OF schedule SKIP LOCKED`，并用查询契约锁住 enabled、非软删除、due time、`next_run_at` 排序和 limit；`qaplatform.infra.database.repositories.project_repo` 覆盖率从 51% 提升到 54% | 测试 review 发现 schedule worker 单测主要 mock repository，真实 PG 用例只证明软删除过滤，不能发现 due schedule 并发锁被删、锁错表或 due 条件放宽。现将实现收敛为只锁 `schedule` 主表，避免未来关系 eager load 时复现裸 `FOR UPDATE` 失败，也避免调度测试只证明 cron 调用了仓储 |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_repository_matrix.py::test_run_repository_find_waiting_orders_by_priority_then_fifo` 1 passed | `tests/unit/test_run_repository_contracts.py tests/unit/test_worker/test_scheduler.py` 18 passed；coverage unit 1267 passed；targeted ruff passed | 无新增 warning；`RunRepository.find_waiting()` 现在按 `priority, created_at` 取 waiting run 时生成 `FOR UPDATE OF run SKIP LOCKED`，并用查询契约锁住 global/project active-or-enqueued 统计与 waiting 计数；`qaplatform.infra.database.repositories.run_repo` 覆盖率从 28% 提升到 34% | 测试 review 发现 `FairScheduler.try_dequeue_waiting()` 文档要求 `SELECT ... FOR UPDATE SKIP LOCKED` 防止多 worker 重复捞等待队列，但现有 scheduler 单测全 mock repo，发现不了 `find_waiting` 实际没锁行。补锁后真实 PG 又暴露 ORM eager left join 上裸 `FOR UPDATE` 会失败，最终收敛为只锁 `run` 主表的 `FOR UPDATE OF run SKIP LOCKED`，避免调度测试只证明 mock 调用顺序 |
| 2026-05-30 | N/A（Dependency transaction boundary 契约） | `tests/unit/test_dependencies.py` 8 passed；coverage unit 1263 passed；targeted ruff passed | 无新增 warning；global/request DB session 成功后 commit、异常 rollback+reraise，`_get_repos` 使用 request container/session，`get_session_factory` 缺 DB 返回 503；`qaplatform.api.deps` 覆盖率从 85% 提升到 90%，`qaplatform.dependencies` 从 38% 提升到 54% | 测试 review 发现多数 route 单测 override `_get_db_session`，不能发现真实依赖层不 commit/rollback、异常被吞、repository bundle 绕过 request session 或 DB 未初始化返回 500。现补 8 条依赖边界契约，避免依赖测试只靠 lifespan mock 证明启动调用顺序 |
| 2026-05-30 | N/A（Webhook provider/dedup route 契约） | `tests/unit/test_api/test_p3.py` 60 passed；coverage unit 1255 passed；targeted ruff passed | 无新增 warning；webhook route 现在锁住项目级 dedup 预命中不创建 run、签名错误在 RBAC 前短路、GitHub provider 签名错误/歧义匹配不触发 run、provider 成功路径使用系统身份并保留 provider/delivery/repository metadata；`qaplatform.api.v1.webhooks` 覆盖率从 83% 提升到 92% | 测试 review 发现 provider webhook 多数测试停在 payload helper 和 missing-signature，项目级 webhook 只覆盖 create 后唯一约束冲突；无法发现 dedup 预检查被绕过、签名错误仍走 RBAC/创建、多个同仓库项目同时验签后误选一个项目，或 GitHub provider 触发时把用户身份/metadata 写错。现补五条 route 级契约，避免 webhook 测试只证明请求能返回某个状态码 |
| 2026-05-30 | N/A（Worker notification log write 异常语义契约） | `tests/unit/test_worker/test_notifications.py` 35 passed；coverage unit 1250 passed；targeted ruff passed | 无新增 warning；notification log 写入现在只把 `uq_notification_log_run_rule_channel` 唯一约束竞争当重复日志处理，其他 IntegrityError/RuntimeError 会在 send 后继续抛出且不 commit | 测试 review 发现旧实现对 `log_repo.create` 的所有异常都记录成 `notification_log_already_exists`，DB/FK/仓储故障会被吞掉，测试仍绿且质量证据失真。现补 duplicate/non-duplicate/write failure 三条契约并收紧实现，避免通知测试只证明发送成功而发现不了日志丢失 |
| 2026-05-30 | N/A（Worker notification project_name 懒加载契约） | `tests/unit/test_worker/test_notifications.py` 32 passed；coverage unit 1247 passed；targeted ruff passed | 无新增 warning；worker notification 多渠道发送现在锁住未引用 `project_name` 时不查 ProjectRepository，仍按渠道模板/规则模板渲染、归一 email config、写两条 sent log | 测试 review 发现已有用例证明引用 project_name 时会查项目，但没有反向契约；如果实现改成每次发送都查项目，旧测试仍绿，会引入隐性 DB 耦合/性能回退，甚至项目查询失败会把通知误记失败。现补懒加载 no-project-lookup 契约，避免 worker 通知测试只证明模板能渲染 |
| 2026-05-30 | N/A（Notification rule update 条件/渠道归一化契约） | `tests/unit/test_api/test_notifications.py` 21 passed；coverage unit 1246 passed；targeted ruff passed | 无新增 warning；notification rule update 成功路径现在锁住 project tenant lookup、`NOTIFICATION_EDIT` RBAC、conditions/channels/template 仓储参数、canonical webhook config 和 audit 脱敏；非法 update conditions 固定在请求体验证阶段 422 且不触碰项目/规则/audit | 测试 review 发现 notification rule create 和读取路径已有条件/渠道契约，但 update 成功路径主要证明改名/模板，不能发现 conditions/channels 归一化丢失、RBAC 漏掉、仓储参数漂移或 audit 泄露 webhook URL/template。现补成功和失败两条 update 契约，避免通知规则测试只证明接口能返回 200/422 |
| 2026-05-30 | N/A（Analytics 查询成功路径仓储/RBAC 契约） | `tests/unit/test_api/test_analytics.py` 6 passed；coverage unit 1244 passed；targeted ruff passed | 无新增 warning；analytics trends/flaky/test-history 成功路径现在锁住 project tenant lookup、`RUN_READ` RBAC、days/min_runs/suite/name/offset/limit/cutoff 仓储参数和响应映射 | 测试 review 发现 analytics 单测只证明 test-history 空白过滤参数会 422，不能发现 trends/flaky/test-history 成功路径漏掉项目租户校验、RBAC、分页/过滤参数传错或汇总字段映射漂移。现把三条查询成功路径契约补齐，避免 analytics 测试只覆盖请求体验证而不证明真实查询边界 |
| 2026-05-30 | N/A（Auth register password 文本边界短路契约） | `tests/unit/test_auth/test_auth_routes.py` 61 passed；targeted ruff passed | 无新增 warning；注册 password 短/空白/超长现在固定在请求体验证阶段 422，不打开 DB session、不创建 tenant/user、不写 audit、不发 token/cookie | 测试 review 发现既有注册请求体验证已经证明短 password 会 422 且零副作用，但未覆盖只靠 `min_length=8` 会放行的 8 个空格，也未锁住超长 password；旧测试仍绿却可能创建空白密码账户。现把注册 password 文本边界压到 Pydantic 边界，避免注册测试只证明 obvious short password 能失败 |
| 2026-05-30 | N/A（Auth login 请求体验证短路契约） | `tests/unit/test_auth/test_auth_routes.py` 59 passed；targeted ruff passed | 无新增 warning；登录 username 空/空白/超长/非法格式、password 空/空白/超长现在固定在请求体验证阶段 422，不打开 DB session、不构造 UserRepository/AuditEventRepository、不解析 tenant、不发 token/cookie | 测试 review 发现 register 有 username/password 形状约束，但 login 裸 `str`，畸形输入会进入 tenant/user 查询和 login_failed audit；既有 user_not_found/wrong_password 用例只证明业务失败 401，不能发现请求体验证缺失。现登录 body 边界前移，避免认证测试只证明正常/业务失败路径 |
| 2026-05-30 | N/A（API token create 空白文本请求体验证契约） | `tests/unit/test_auth/test_auth_routes.py` 53 passed；targeted ruff passed | 无新增 warning；API token create 的空白 `name` / `scopes[]` 现在固定在请求体验证阶段 422，且不实例化 ApiTokenRepository/AuditEventRepository、不调用 `generate_token` / `hash_token`、不 commit/rollback | 测试 review 发现既有 API token create 用例覆盖空字符串和超长值，但未覆盖只靠 `min_length=1` 会放行的空白字符串；`name="   "` 或 `scopes=["   "]` 会进入 token service、DB 和 audit，旧测试仍绿。现把空白文本挡在 Pydantic 边界，避免 API token 测试只证明明显空串和 happy path |
| 2026-05-30 | N/A（Project list tenant filter 默认列表契约） | `tests/unit/test_api/test_projects.py` 55 passed；targeted ruff passed | 无新增 warning；默认 `/projects` 列表现在锁住 `offset=0`、`limit=20`、`project.name ASC` 和 `tenant_id` filter | 测试 review 发现 q/status 路径已有请求参数和短路断言，但默认 list 只证明 200/序列化；如果实现删掉 tenant filter、默认 pagination 或 name order，mock 仍会绿。现把默认列表的 repository 参数压进用例，避免项目列表测试只覆盖响应壳并漏掉跨租户枚举风险 |
| 2026-05-30 | N/A（Auth middleware JWT claim shape 防泄漏契约） | `tests/unit/test_auth_middleware.py` 28 passed；targeted ruff passed | 无新增 warning；已签名 access token 的非 UUID `sub`/`tenant_id`、非字符串 `role`、空 `jti`、非 bool `is_platform_admin` 统一返回 401 `Invalid token claims`，且不查 Redis blacklist、不打开 DB session、不回显 claim secret | 测试 review 发现既有 auth middleware claims 用例锁住缺失 claim 和 platform-admin UUID，但非 admin 的非 UUID identity 与畸形 role/admin/jti 仍可穿透到后续 UUID 转换、权限矩阵或 blacklist/DB 副作用；旧测试会绿。现把 claims shape 校验前移到认证边界，避免 JWT 测试只证明明显缺字段会失败 |
| 2026-05-30 | N/A（Batch internal failure 防泄漏契约） | `tests/unit/test_api/test_p3.py::TestBatchCancel::test_batch_cancel_internal_failure_redacts_exception_without_side_effects tests/unit/test_api/test_p3.py::TestBatchRetry::test_batch_retry_internal_failure_redacts_exception_without_side_effects` 2 passed；batch cancel/retry slice 8 passed；`tests/unit/test_api/test_p3.py` 55 passed；targeted ruff passed | 无新增 warning；batch cancel/retry 的 repository/create 内部异常现在只返回 `<run_id>: operation failed`，不回显 SQL/secret/底层异常文本，且不 cancel、不 publish Redis cancel/status、不 create retry run、不 enqueue、不写 audit | 测试 review 发现 batch not-found / not-terminal / duplicate run_id 已有业务错误和副作用断言，但 `SQLAlchemyError` / `ValueError` 分支直接把异常文本拼进 `errors`；如果 DB driver、repository 或 create path 抛出带 SQL、token、内部字段的异常，旧测试仍可能绿且客户端可见。现用 cancel/retry 两条失败注入锁住通用错误和零副作用，避免 batch 测试只证明业务失败会列入 errors |
| 2026-05-30 | N/A（RBAC project membership lookup scope 契约） | `tests/unit/test_api/test_rbac_deps.py` 15 passed；targeted ruff passed | 无新增 warning；project-scoped RBAC 现在证明项目可见性查询按 project_id+tenant_id 收敛，ProjectMember lookup 按 project_id+user_id+tenant_id 收敛；缺失项目/跨租户项目固定 404 且不回显 project/user/tenant id，可见项目无成员固定 403 且不泄露标识 | 测试 review 发现现有 RBAC dependency 用例能证明状态码和查询次数，但 role lookup 没锁住 user/tenant 过滤；如果实现误删 tenant_id/user_id 条件、用别人的 ProjectMember 放行，或错误信息带出 project_id/user_id/tenant_id，旧测试仍可能绿。现把 SQL params 和响应脱敏压进既有用例，避免 RBAC 测试只证明“查过一次并返回 403/404” |
| 2026-05-30 | N/A（SSE ticket malformed payload 防泄漏契约） | `tests/unit/test_api/test_sse.py::test_authenticate_sse_ticket_rejects_malformed_payload_without_leaking` 8 passed；`tests/unit/test_api/test_sse.py` 23 passed；targeted ruff passed | 无新增 warning；malformed SSE ticket payload（非 JSON、非 object、缺字段、非法 UUID、role/scopes 类型错误）固定返回同一 401，不回显 payload、ticket 或 secret marker，且只使用原子 `getdel`，不回退 `get`/`delete` | 测试 review 发现 SSE ticket 已覆盖 legacy payload、scope 保留和 getdel 原子消费，但多类污染 payload 没有统一契约；如果后续错误处理回显 payload、ticket、secret，或为兼容坏 payload 回退到 racy get/delete，旧测试仍可能绿。现用参数化坏输入锁住 generic 401、防泄漏和 no-fallback，避免 SSE ticket 测试只证明一种 legacy 格式会失败 |
| 2026-05-30 | N/A（Ready degraded 双失败防泄漏契约） | `tests/unit/test_api/test_health.py::test_ready_checks_all_dependencies_and_redacts_probe_errors_when_degraded` 1 passed；`tests/unit/test_api/test_health.py` 6 passed；targeted ruff passed | 无新增 warning；`/ready` 在 DB 与 Redis probe 同时失败时固定返回 degraded，两个 probe 都会执行，响应只暴露 `{db: error, redis: error}`，不回显底层异常文案 | 测试 review 发现 readiness 已分别覆盖 DB 失败和 Redis 失败，但没有证明第一个 probe 失败后仍会检查第二个依赖，也没有防止把 `database probe failed` / `redis probe failed` 这类底层错误暴露给调用方。现补双失败与响应脱敏断言，避免 ready 测试只证明单点失败状态码 |
| 2026-05-30 | N/A（Artifact download 404 短路与 no-storage 契约） | `tests/unit/test_api/test_artifacts.py::TestDownloadArtifactRBAC::test_download_artifact_404_short_circuits_before_rbac_or_storage` 1 passed；`tests/unit/test_api/test_artifacts.py` 12 passed；targeted ruff passed | 无新增 warning；artifact 缺失或 artifact 指向跨租户 run 时，download route 固定返回同一 `Artifact not found` 404，在 RBAC 与 S3 presign 前短路，且响应不回显外部 artifact/run/storage path | 测试 review 发现 `_get_artifact_or_404` helper 已覆盖 missing 与 cross-tenant run，但 route 级 `download_artifact` 只覆盖 RBAC 403、成功 presign 和 storage 503；如果后续路由绕过 helper、404 后仍触发 RBAC 或预签名，旧 route 测试可能仍绿。现把缺失 artifact 与跨租户 artifact 放在同一用例比较响应与副作用，避免 artifact download 测试只证明 helper 和 happy path |
| 2026-05-30 | N/A（Project member update 缺失成员无副作用契约） | `tests/unit/test_api/test_project_members.py::test_update_nonexistent_member_returns_404_without_side_effects` 1 passed；`tests/unit/test_api/test_project_members.py` 10 passed；targeted ruff passed | 无新增 warning；项目成员 update 在项目存在但成员缺失时固定返回 `NOT_FOUND` envelope，不 update、不写 audit | 测试 review 发现 project member 已覆盖 delete 缺失成员、缺失项目、非法 role、跨租户 user 和 duplicate add，但 update 缺失成员没有独立断言；如果实现对不存在成员执行 update、返回错误来源漂移，或 404 前误写 audit，旧测试仍可能绿。现把 scoped lookup、404 响应和零副作用放进同一用例，避免成员 update 测试只证明 happy path role 变更 |
| 2026-05-30 | N/A（Credential rotate 缺失资源无副作用契约） | `tests/unit/test_api/test_credentials.py::test_rotate_credential_404_when_missing_without_side_effects` 1 passed；`tests/unit/test_api/test_credentials.py` 14 passed；targeted ruff passed | 无新增 warning；凭据 rotate 在项目存在但 credential 缺失时固定返回 `NOT_FOUND` envelope，不调用 crypto、不 update、不写 audit | 测试 review 发现 credentials 已覆盖 create/get/delete 缺失、rotate 成功、crypto 503 和明文不泄露，但没有单独锁住 rotate 的缺失 credential 分支；如果实现先加密再查资源、对 `None` 执行 update，或 404 前误写 audit，旧测试仍可能绿。现把缺失资源响应和副作用边界压到同一用例，避免 rotate 测试只证明 AAD happy path 与 crypto unavailable |
| 2026-05-30 | N/A（Run trigger environment 跨项目 404 等价性契约） | `tests/unit/test_api/test_runs.py::test_trigger_run_hides_missing_or_other_project_environment_without_side_effects` 1 passed；targeted ruff passed | 无新增 warning；手动触发 run 时显式传入缺失 environment 或跨项目 environment 都返回同一 `NOT_FOUND` envelope，不创建 run、不设置 retry group、不入队、不写 audit，且响应不回显外部 project_id | 测试 review 发现 trigger run 代码已有 `environment.project_id != project.id` 防护，但既有用例主要覆盖默认环境、正常触发、pipeline 不存在/跨租户、归档项目和 bad git input；如果后续漏掉 environment 父项目校验，会用外部环境创建 run 或泄露项目归属。现把缺失 env 与跨项目 env 放在同一用例中比较响应与副作用，避免 run trigger 测试只证明 pipeline 边界能隐藏 |
| 2026-05-30 | N/A（Pipeline item 跨项目 404 等价性契约） | `tests/unit/test_api/test_pipelines.py::test_pipeline_item_routes_hide_other_project_pipeline_without_side_effects` 1 passed；targeted ruff passed | 无新增 warning；pipeline get/update/delete 对跨项目 pipeline 与缺失 pipeline 返回同一 `NOT_FOUND` envelope，不 update/delete/audit，且响应不回显外部 project_id | 测试 review 发现 pipeline 详情已覆盖跨项目 404 等价性，但 update/delete 只覆盖正常路径；如果后续漏掉父项目校验，会把外部 pipeline 改掉或删掉，现三条 item 路由共用跨项目伪装断言，避免 pipeline 测试只证明详情页能隐藏跨项目 ID |
| 2026-05-30 | N/A（Notification rule item 跨项目 404 等价性契约） | `tests/unit/test_api/test_notifications.py::test_notification_rule_item_routes_hide_other_project_rule_without_side_effects` 1 passed；targeted ruff passed | 无新增 warning；notification rule get/update/delete 对跨项目 rule 与缺失 rule 返回同一 `NOT_FOUND` envelope，不 update/delete/audit，且响应不回显外部 project_id | 测试 review 发现 notification rule item 路由代码已有 `rule.project_id != project.id` 防护，但测试只覆盖 rule 不存在与 project 不存在，无法发现实现漏掉父项目校验、泄露外部 project_id，或在 404 前误 update/delete/audit。现三条 item 路由共用跨项目伪装断言，避免 notification rule 测试只证明缺失 ID 能 404 |
| 2026-05-30 | N/A（Schedule item 跨项目 404 等价性契约） | `tests/unit/test_api/test_schedules.py::test_schedule_item_routes_hide_other_project_schedule_without_side_effects` 1 passed；targeted ruff passed | 无新增 warning；schedule get/update/delete 对跨项目 schedule 与缺失 schedule 返回同一 `NOT_FOUND` envelope，不 update/delete/audit，不调用 `compute_next_run_at`，且响应不回显外部 project_id | 测试 review 发现 schedule item 路由代码已有 `schedule.project_id != project.id` 防护，但测试只覆盖 schedule 不存在，无法发现实现漏掉父项目校验、泄露外部 project_id，或在 404 前误 update/delete/audit。现三条 item 路由共用跨项目伪装断言，避免 schedule 测试只证明缺失 ID 能 404 |
| 2026-05-30 | N/A（前端登录 refresh-cookie 竞态与 worker-backed E2E 抗并行契约） | `tests/unit/test_frontend_security_contracts.py` 8 passed；frontend lint passed；frontend build passed；worker-backed Playwright `real-login-flow.spec.ts` + `real-run-trigger.spec.ts` 2 passed（35.0s） | Select controlled/uncontrolled warning 已消除；本轮真实 E2E 仅剩 `NO_COLOR` / `FORCE_COLOR` 环境提示 | 测试 review 发现登录页初始 `/auth/refresh` 的 401 清 cookie 响应可能晚于手动登录，导致整页重载后丢失 refresh cookie 并回到登录页；项目列表若不搜索新建项目，会被历史 E2E 数据分页污染；trigger modal pipeline Select 还会从 `undefined` 切到字符串并产生运行时 warning。现登录页跳过初始 refresh、登录成功结束 auth loading、E2E helper 等待 authenticated shell、trigger modal Select 全程受控，并用真实 worker-backed 并行组合验证登录、项目详情、触发 run、worker 结果、归档日志和 artifact 闭环 |
| 2026-05-30 | N/A（GitHub provider ref 空白 payload 短路契约） | `tests/unit/test_api/test_p3.py` 53 passed；coverage unit 1197 passed；targeted ruff passed；diff check passed | 无新增 warning；GitHub provider push payload 的空白 `ref` 现在固定返回 400 `Missing GitHub ref`，且不查 repo URL、不查 pipeline/environment、不查 dedup、不创建 run、不写 audit | 测试 review 发现 custom webhook schema 开始拒绝空白 `git_ref` 后，GitHub provider helper 若继续只判断 `not git_ref`，空白 ref 会落入 Pydantic ValidationError 或后续 provider 流程，测试仍绿。现 helper 用 `strip()` 明确短路，并加 route 级 no-repo/no-run 断言，避免 provider 测试只证明坏 SHA 会短路 |
| 2026-05-30 | N/A（Webhook git_sha 空白请求体验证短路契约） | `tests/unit/test_api/test_p3.py` 51 passed；coverage unit 1195 passed；targeted ruff passed；diff check passed | 无新增 warning；自定义 webhook 的空白 `git_sha` 现在固定在请求体验证阶段 422，且不查 project、不查 pipeline、不查 environment、不查 dedup、不创建 run、不写 retry group、不写 audit | 测试 review 发现 custom webhook 为兼容短 SHA 只限制 `git_sha` 长度，已有用例挡空串和超长，但空白 SHA 会继续进入 dedup key 和 Run 记录，测试仍绿。现补可选 `git_sha` 非空白校验，并复用字段定位和零副作用断言，避免 webhook SHA 测试只证明空字符串能被 Pydantic 拦住 |
| 2026-05-30 | N/A（Pipeline selector/retry 列表文本短路契约） | `tests/unit/test_api/test_pipelines.py` 27 passed；coverage unit 1194 passed；targeted ruff passed；diff check passed | 无新增 warning；pipeline create/update 的 selector `include_paths` / `exclude_paths` / `tags` 空白元素和 retry_policy `retry_on` 空白原因现在固定在请求体验证阶段 422，且不查 project、不读 pipeline、不创建/更新 pipeline、不写 audit | 测试 review 发现 selector/retry 已覆盖空字符串和列表长度，但空白路径、tag、retry reason 会通过 `min_length=1` 并进入发现规则或重试配置 JSONB，测试仍绿。现补列表元素非空白校验并保留下标错误信息，避免 pipeline selector/retry 测试只证明空字符串能被 Pydantic 拦住 |
| 2026-05-30 | N/A（Pipeline core text 请求体验证短路契约） | `tests/unit/test_api/test_pipelines.py` 23 passed；coverage unit 1190 passed；targeted ruff passed；diff check passed | 无新增 warning；pipeline create/update 的空白 `name`、stage `name/plugin`、collector `plugin` 和 trigger_config `type` 现在固定在请求体验证阶段 422，且不查 project、不读 pipeline、不创建/更新 pipeline、不写 audit | 测试 review 发现 pipeline 已覆盖空字符串、selector/retry/trigger_config 边界和审计脱敏，但多个执行配置文本字段只靠 `min_length=1`；空白值会进入 stages/collectors/trigger_config JSONB 或 pipeline name 并污染执行语义，测试仍绿。现补 schema 非空白约束和 create/update 零副作用用例，避免 pipeline 测试只证明明显空串会失败 |
| 2026-05-30 | N/A（Webhook git_ref 请求体验证短路契约） | `tests/unit/test_api/test_p3.py` 50 passed；coverage unit 1185 passed；targeted ruff passed；diff check passed | 无新增 warning；自定义 webhook 的空白 `git_ref` 现在固定在请求体验证阶段 422，且不查 project、不查 pipeline、不查 environment、不查 dedup、不创建 run、不写 retry group、不写 audit | 测试 review 发现 webhook 已有 `git_sha` 空/超长零副作用测试，但 `git_ref` 只靠 `min_length=1`；空白 ref 会继续进入 branch filter、dedup key 和 run 创建，测试仍绿。现补 webhook `git_ref` 非空白 schema 校验，并把 git 输入负向用例合并为字段定位断言，避免 webhook 测试只证明 SHA 边界能挡住 |
| 2026-05-30 | N/A（Run trigger git_ref 请求体验证短路契约） | `tests/unit/test_api/test_runs.py` 49 passed；coverage unit 1184 passed；targeted ruff passed；diff check passed | 无新增 warning；手动触发 run 的空白 `git_ref` 现在固定在请求体验证阶段 422，且不查 pipeline、不查 project、不查 environment、不创建 run、不写 retry group、不写 audit | 测试 review 发现已有用例覆盖空 `git_ref`、超长 `git_ref` 和非法 `git_sha`，但空白 `git_ref` 会通过 `min_length=1` 并作为真实 ref 写入 run，测试仍绿。现补 schema 非空白约束和零副作用参数用例，避免 run trigger 测试只证明明显空串能被 Pydantic 拦住 |
| 2026-05-30 | N/A（Schedule cron_expr 请求体验证短路契约） | `tests/unit/test_api/test_schedules.py` 10 passed；coverage unit 1183 passed；targeted ruff passed；diff check passed | 无新增 warning；schedule create/update 的非法 `cron_expr` 现在固定在请求体验证阶段 422，且不查 project、不查 pipeline、不查/写 schedule、不调用 `compute_next_run_at`、不写 audit | 测试 review 发现 create schema 会校验 cron，但 update schema 只靠 `min_length=1`；非法 cron 会继续进入 project lookup、权限、schedule lookup，再在 `compute_next_run_at` 里抛错，测试仍绿。现抽出共享 cron 校验 helper，并用 create/update 零副作用测试锁住，避免 schedule 测试只证明 create happy path 和 timezone 边界 |
| 2026-05-30 | N/A（Project core text 请求体验证短路契约） | `tests/unit/test_api/test_projects.py` 55 passed；coverage unit 1182 passed；targeted ruff passed；diff check passed | 无新增 warning；project create/update 的空白 `name` / `git_url` / `default_branch` / `root_path` 现在固定在请求体验证阶段 422，且 create 不查 slug、update 不查 project，不查 credential、不创建/更新 project、不写 audit | 测试 review 发现 project core 已覆盖空字符串、非法 slug、duplicate slug 和 credential binding，但空白 name/git_url/default_branch/root_path 会继续进入 slug 查重、credential 校验、project create/update 和 audit，测试仍绿。现补 schema 非空白约束和 create/update 零副作用测试，避免 project 测试只证明空字符串被 Pydantic 拦住 |
| 2026-05-30 | N/A（Environment name/cache_key 请求体验证短路契约） | `tests/unit/test_api/test_environments.py` 27 passed；coverage unit 1174 passed；targeted ruff passed；diff check passed | 无新增 warning；环境 create/update 的空白 `name` / `cache_key` 现在固定在请求体验证阶段 422，且不查 project、不查 environment、不加密 env_vars、不创建/更新 environment、不写 audit | 测试 review 发现 environment 已覆盖空字符串 name/cache_key、base image pinning、加解密失败和审计不泄密，但空白 name/cache_key 会继续进入 project lookup、env_vars 加密、environment create/update 和 audit，测试仍绿。现补 schema 非空白约束、schema 单测和路由零副作用测试，避免环境测试只证明空字符串被 Pydantic 拦住 |
| 2026-05-30 | N/A（Credential name 请求体验证短路契约） | `tests/unit/test_api/test_credentials.py` 13 passed；coverage unit 1172 passed；targeted ruff passed；diff check passed | 无新增 warning；凭据创建的空白 `name` 现在固定在请求体验证阶段 422，且不查 project、不查 credential name、不加密、不创建 credential、不写 audit | 测试 review 发现 credentials 已覆盖空 name、空 value、非法 type 和明文不泄漏，但空白 name 会继续进入 project lookup、name exists、AAD 加密上下文和 credential create，测试仍绿。现补 schema 非空白约束并复用零副作用 invalid-body 测试，避免凭据测试只证明合法 name 能绑定 AAD |
| 2026-05-30 | N/A（Notification rule name 请求体验证短路契约） | `tests/unit/test_api/test_notifications.py` 18 passed；coverage unit 1172 passed；targeted ruff passed；diff check passed | 无新增 warning；通知规则 create/update 的空白 `name` 现在固定在请求体验证阶段 422，且不查 project、不查 notification_rule、不写 audit | 测试 review 发现 notification rule 对 channel/condition 的负向测试很强，但 `name` 只靠 `min_length=1`，空白 name 会继续走项目权限并创建/更新规则，测试仍绿。现补 schema 非空白约束和零副作用测试，避免通知规则测试只证明合法 name 能创建/更新 |
| 2026-05-30 | N/A（Analytics test-history 文本过滤短路契约） | `tests/unit/test_api/test_analytics.py` 3 passed；coverage unit 1171 passed；targeted ruff passed；diff check passed | 无新增 warning；`/projects/{project_id}/analytics/test-history?suite=%20%20%20` 与 `name=%20%20%20` 现在固定返回 `ErrorResponse` 422，且不查 project、不查 test history | 测试 review 发现 analytics happy path 和前端契约已证明单用例历史能显示，但空白 suite/name 会继续查项目、走权限并打 TestResult 仓储，最后只表现为空历史，测试仍绿。现补 route 层空白文本短路和 OpenAPI 422 schema，避免 analytics 测试只证明正常 flaky 行能打开历史 |
| 2026-05-30 | N/A（Run list git_ref/created range 请求参数短路契约） | `tests/unit/test_api/test_runs.py` 48 passed；coverage unit 1168 passed；`tests/unit/test_release_quality_docs_contract.py` 30 passed；targeted ruff passed；diff check passed | 无新增 warning；`/runs?git_ref=`、空白 `git_ref`、超过 200 字符的 `git_ref`、以及 `created_from > created_to` 现在固定返回 `ErrorResponse` 422，且不查 project member、不查 run list | 测试 review 发现 run list 已覆盖 status/sort 短路和 results 文本过滤，但列表自身的 `git_ref` 与时间范围仍可把明显坏输入推进仓储层；空 ref 会形成无意义精确过滤，超长 ref/反向时间范围也会打到 DB，测试仍绿。现补 git_ref 长度/空白和 created range 前置校验，避免列表测试只证明合法过滤能拼条件 |
| 2026-05-30 | N/A（Audit events query 参数短路契约） | `tests/unit/test_api/test_audit.py` 13 passed；coverage unit 1164 passed；`tests/unit/test_release_quality_docs_contract.py` 30 passed；targeted ruff passed；diff check passed | 无新增 warning；`/audit-events?action=`、`resource_type=`、超过 200 字符的 action/resource_type、以及 `start_at > end_at` 现在固定返回 `ErrorResponse` 422，且不查 audit list、不查跨租户 match、不写 `audit_events.list` 自审计 | 测试 review 发现 audit-events 已覆盖正常过滤、自审计摘要、跨租户 404 和拒绝路径不写自审计，但空 action/resource_type 会扩大成更宽查询，反向时间范围也会照样打到 audit repository。现补手工短路校验和 OpenAPI 422 schema，避免审计查询测试只证明正常过滤能返回 |
| 2026-05-30 | N/A（Project list q/status 请求参数短路契约） | `tests/unit/test_api/test_projects.py` 47 passed；coverage unit 1158 passed；`tests/unit/test_release_quality_docs_contract.py` 30 passed；targeted ruff passed；diff check passed | 无新增 warning；`/projects?status=`、`q=`、空白 q 和超过 500 字符的 q 现在固定返回 `ErrorResponse` 422，且不查 project list | 测试 review 发现 project list 已拒绝未知 status、覆盖正常 q 搜索和 OpenAPI enum，但空 status 会退化成无过滤列表，空/空白 q 会查全量列表，超长 q 会进入 LIKE，测试仍绿。现补手工短路校验和零副作用测试，避免项目列表测试只证明合法搜索能拼过滤条件 |
| 2026-05-30 | N/A（Run list status 空段短路契约） | `tests/unit/test_api/test_runs.py` 44 passed；coverage unit 1154 passed；`tests/unit/test_release_quality_docs_contract.py` 30 passed；targeted ruff passed；diff check passed | 无新增 warning；`/runs?status=`、`,`、`queued,,running` 现在固定返回 `ErrorResponse` 422，且不查 project member、不查 run list | 测试 review 发现 run list 已拒绝未知 status，但空 status 或逗号空段会被当作“无过滤”或静默忽略，导致调用方拼错查询仍能查全量列表且测试绿。现补空段白名单前置校验和零副作用测试，避免 status 测试只证明 `typo` 被拒绝 |
| 2026-05-30 | N/A（Run list sort 请求参数短路契约） | `tests/unit/test_api/test_runs.py` 41 passed；coverage unit 1151 passed；`tests/unit/test_release_quality_docs_contract.py` 30 passed；targeted ruff passed；diff check passed | 无新增 warning；`/runs?sort=` 现在只接受 `created_at` / `-created_at`，非法 sort 固定返回 `ErrorResponse` 422，且不查 project member、不查 run list | 测试 review 发现 run list 已覆盖多 status 过滤、非法 status 422 和单 status equality，但 `sort=updated_at` 这类拼错字段会静默退化成 `created_at` 升序，调用方排序错误测试仍绿。现补 sort 白名单和零副作用测试，避免列表测试只证明默认排序能返回 |
| 2026-05-30 | N/A（Run results text filter 请求参数短路契约） | `tests/unit/test_api/test_runs.py` 39 passed；coverage unit 1149 passed；`tests/unit/test_release_quality_docs_contract.py` 30 passed；targeted ruff passed；diff check passed | 无新增 warning；`/runs/{run_id}/results?suite=` 与 `q=` 现在拒绝空字符串和超过 500 字符的文本过滤，坏 query 固定在参数校验阶段 422，且不查 Run、不查 TestResult | 测试 review 发现 results 列表已覆盖 status enum、suite/q happy path 和 LIKE wildcard 转义，但空 `q` 会退化成无过滤全量读，超长 q/suite 会进入大 LIKE 或精确匹配，测试仍绿。现补 Query 边界和零副作用测试，避免 results 过滤测试只证明正常关键词能拼条件 |
| 2026-05-30 | N/A（Webhook git_sha 请求体验证短路契约） | `tests/unit/test_api/test_p3.py` 49 passed；coverage unit 1145 passed；`tests/unit/test_release_quality_docs_contract.py` 30 passed；targeted ruff passed；diff check passed | 无新增 warning；自定义 webhook 保留短 SHA 兼容，但 `git_sha` 现在拒绝空字符串和超过 100 字符的值，坏 payload 固定在请求体验证阶段 422，且不查项目、不查 pipeline/environment、不 dedup、不创建 Run、不写 audit | 测试 review 发现 provider GitHub webhook 已有 40 位 SHA 强校验，手动 run 也有 40 位 SHA 验证，但自定义 project webhook 的 `git_sha` 仍可为空或超长；空字符串会绕过 dedup，超长值会污染 Run 记录，现用 schema 和零副作用测试锁住，避免 webhook 测试只证明短 SHA happy path 能触发 |
| 2026-05-30 | N/A（Pipeline trigger_config 请求体验证短路契约） | `tests/unit/test_api/test_pipelines.py` 18 passed；`tests/unit/test_domain_models.py` 5 passed；coverage unit 1143 passed；`tests/unit/test_release_quality_docs_contract.py` 30 passed；targeted ruff passed；diff check passed | 无新增 warning；Pipeline trigger_config `type` 现在拒绝空字符串，`dedup_window_seconds` 现在拒绝负数，create/update 坏 payload 固定在请求体验证阶段 422，且不查项目、不读写 pipeline、不写 audit；domain `TriggerConfig` 同步拒绝歧义触发配置 | 测试 review 发现 pipeline trigger_config 已覆盖 manual/webhook/schedule 正常读写和审计脱敏，但 `type` 仍可为空、dedup 窗口可为负数；坏配置会进入 JSONB，后续触发/去重语义才漂移，测试仍绿。现补 schema/domain 边界和路由零副作用测试，避免 trigger 配置测试只证明正常 payload 能保存 |
| 2026-05-30 | N/A（Pipeline retry policy 请求体验证短路契约） | `tests/unit/test_api/test_pipelines.py` 16 passed；`tests/unit/test_domain_models.py` 4 passed；coverage unit 1140 passed；`tests/unit/test_release_quality_docs_contract.py` 30 passed；targeted ruff passed；diff check passed | 无新增 warning；Pipeline retry policy `retry_on` 现在拒绝空原因和超过 20 项的列表，create/update 坏 payload 固定在请求体验证阶段 422，且不查项目、不读写 pipeline、不写 audit；domain `RetryPolicy` 同步拒绝歧义重试原因 | 测试 review 发现自动重试已有 `max_attempts` 边界、worker retry 行为和 API happy path，但 `retry_on` 仍是裸字符串列表；空原因或异常大列表会写入 JSONB，worker 再把非 infra 原因静默当作不重试，测试仍绿。现补 schema/domain 边界和路由零副作用测试，避免重试测试只证明 `infra` 正常配置能保存 |
| 2026-05-30 | N/A（Pipeline selector 请求体验证短路契约） | `tests/unit/test_api/test_pipelines.py` 14 passed；`tests/unit/test_services/test_discovery.py` 12 passed；coverage unit 1137 passed；`tests/unit/test_release_quality_docs_contract.py` 30 passed；targeted ruff passed；diff check passed | 无新增 warning；Pipeline selector `include_paths` / `exclude_paths` / `tags` 现在拒绝空条目和超过 100 项的列表，create/update 坏 payload 固定在请求体验证阶段 422，且不查项目、不读写 pipeline、不写 audit；domain `TestSelector` 同步拒绝歧义发现规则 | 测试 review 发现 pipeline create/update 已覆盖嵌套 payload happy path 和部分 schema 负向，但 selector path/tag 列表仍是裸字符串列表；空 glob 或超大列表会进入 JSONB/发现逻辑，测试仍绿。现补 schema/domain 边界和路由零副作用测试，避免 pipeline 测试只证明正常 selector 能保存 |
| 2026-05-30 | N/A（Batch run_ids 唯一性短路契约） | `tests/unit/test_api/test_p3.py` 47 passed；coverage unit 1132 passed；`tests/unit/test_release_quality_docs_contract.py` 30 passed；targeted ruff passed；diff check passed | 无新增 warning；batch cancel/retry 请求现在拒绝重复 run_id，固定在请求体验证阶段 422，且不读取 run、不 cancel、不 create retry run、不入队、不发 Redis 取消/状态事件、不写 audit | 测试 review 发现 `BatchRunRequest` 只限制 1-50 个 ID，重复 ID 会让同一个 Run 在 batch cancel/retry 中被循环处理，可能造成重复取消事件、重复 retry run 或重复审计。现补 schema 唯一性和两个端点的零副作用测试，避免 batch 测试只证明单 ID 能成功 |
| 2026-05-30 | N/A（GitHub provider webhook SHA 短路契约） | `tests/unit/test_api/test_p3.py` 45 passed；coverage unit 1130 passed；`tests/unit/test_release_quality_docs_contract.py` 30 passed；targeted ruff passed；diff check passed | 无新增 warning；GitHub provider push `after` 与 PR `head.sha` 现在只接受 40 位 hex SHA，无效 SHA 在 payload 映射阶段返回 400，且不会查项目、验签、建 Run 或写 audit；项目级自定义 webhook 的既有短 SHA 兼容语义不变 | 测试 review 发现 provider helper 已覆盖 40 位 SHA happy path 和缺失 SHA，但 `after=\"not-a-sha\"` / `head.sha=\"not-a-sha\"` 仍会进入 provider 路由后续 repo lookup/signature/run creation。现补 helper 显式错误和 route 级零副作用测试，避免 GitHub provider webhook 测试只证明正常 payload 可触发 |
| 2026-05-30 | N/A（API token create 请求体验证短路契约） | `tests/unit/test_auth/test_auth_routes.py` 51 passed；coverage unit 1127 passed；`tests/unit/test_release_quality_docs_contract.py` 30 passed；targeted ruff passed；diff check passed | 无新增 warning；API token 创建现在拒绝空/超长 name 与空/超长 scope 字符串，保留 `scopes=[]` 作为无权限 token 语义，坏 payload 固定在请求体验证阶段 422 | 测试 review 发现 API token happy path 已覆盖 secret 不入响应/审计，但 `CreateTokenRequest.name` 仍是裸 `str`，`scopes` 元素也无长度约束；空名称或空 scope 会进入 token service、DB 和 audit。现补 schema 与零副作用负向测试，避免 API token 测试只证明正常 token 能创建 |
| 2026-05-30 | N/A（Auth refresh audit resource_id/token 契约） | `tests/unit/test_auth/test_auth_routes.py` 51 passed；`tests/unit/test_architecture_boundaries.py` 17 passed；`tests/unit/test_release_quality_docs_contract.py` 30 passed；targeted ruff passed；diff check passed | 无新增 warning；`auth.refresh` 与 `auth.refresh_failed` 审计现在显式写 `resource_id=None`，成功路径锁住 tenant/user、old_jti/new_jti 元数据，失败路径锁住 reason 且 raw refresh/access token 不入审计 payload | logout 审计补强后的同型复扫发现 refresh 成功/失败审计也缺显式 `resource_id`，且部分测试只断言 action/reason。现把 refresh 审计契约提升到字段、脱敏和事务层，并用架构边界测试确认所有直接审计写入均显式携带 `resource_id` |
| 2026-05-30 | N/A（Auth logout audit payload/transaction 契约） | `tests/unit/test_auth/test_auth_routes.py::TestAuditLogout` 2 passed；`tests/unit/test_auth/test_auth_routes.py` 51 passed；`tests/unit/test_release_quality_docs_contract.py` 30 passed；targeted ruff passed；diff check passed | 无新增 warning；logout 审计现在锁住 tenant/user、`resource_id=None`、`had_access_token`、raw access/refresh token 不入审计 payload，以及 audit session commit/rollback 边界 | 测试 review 发现 logout 审计用例只证明写了 `auth.logout` 和部分 user_id，无法发现 tenant/resource_id 漏写、`had_access_token` 漂移、token 原文误入 AuditEvent，或审计 session 未提交。现补结构化 helper 与事务断言，避免认证退出测试只证明 204 和“调用过审计” |
| 2026-05-30 | N/A（Project list status OpenAPI enum 契约） | `tests/unit/test_api/test_projects.py` 43 passed；coverage unit 1122 passed；targeted ruff passed | 无新增 warning；`/projects?status=` 保留统一 `ErrorResponse` 422 运行时校验，同时 OpenAPI 参数 schema 暴露 `active` / `archived` enum | 测试 review 发现运行时已经拒绝非法 status，但 OpenAPI 仍是普通 string/null，客户端生成器和文档读者看不到枚举约束。现用单测锁住参数 enum，避免项目列表测试只证明“任意字符串可传” |
| 2026-05-30 | N/A（Notification validation error 结构化断言） | `tests/unit/test_api/test_notifications.py` 17 passed；`tests/unit/test_release_quality_docs_contract.py` 30 passed；targeted ruff passed；diff check passed | 无新增 warning；notification rule 创建/更新的 channels/conditions 校验失败测试现在断言 FastAPI/Pydantic `detail[].loc` 与错误消息，不再靠 `repr(resp.json())` 字符串包含 | 测试 review 发现 notification 用例虽然已经有 no-side-effect helper，但多条 422 只在整个响应 repr 中找错误文本，无法发现错误落点从 `channels` 漂到 `conditions` 或整体 body。现统一用结构化 helper 锁住字段 loc 与消息，保留 repository/audit 不应发生的副作用断言 |
| 2026-05-30 | N/A（Project list status filter 枚举拒绝契约） | `tests/unit/test_api/test_projects.py` 43 passed；`tests/unit/test_release_quality_docs_contract.py` 30 passed；targeted ruff passed；diff check passed | 无新增 warning；`/projects?status=` 现在只接受 `active` / `archived`，未知值返回 `ErrorResponse` 422 且不查询 project repository，OpenAPI 422 响应声明为 `ErrorResponse` | 测试 review 发现 `/projects?status=` 与 `/runs?status=` 有同型风险：单测原先只覆盖列表/search 的正常路径，裸字符串状态会把拼错值静默传给 DB 过滤，调用方或前端拼写错误只会表现为空列表。现补合法状态过滤断言、非法状态 no-repo 短路和 OpenAPI 错误响应契约，避免项目列表测试只证明 200/分页壳存在 |
| 2026-05-30 | N/A（SSE live event payload/schema 契约） | `tests/unit/test_api/test_sse.py` 15 passed；`tests/unit/test_release_quality_docs_contract.py` 30 passed；targeted ruff passed；diff check passed | 无新增 warning；日志/状态 SSE happy path 现在断言事件 id、真实 JSON payload 字段和首轮 Redis `xread` 游标/count/block 参数 | 测试 review 发现最早的 SSE happy-path 用例虽然已不再只是端点存在 smoke，但仍主要靠文本包含 `hello` / `running`，无法发现 payload 字段从 `line/stream` 退化成其他形状，或首轮 `xread` 游标/count/block 漂移。现把断言提升为事件帧和 Redis 参数级契约 |
| 2026-05-30 | N/A（Auth login last_login 写入契约） | `tests/unit/test_auth/test_auth_routes.py` 47 passed；`tests/unit/test_release_quality_docs_contract.py` 30 passed；targeted ruff passed；diff check passed | 无新增 warning；登录成功单测现在必须证明 `update_last_login(user, aware_datetime)` 被 await 且会 commit | 测试 review 发现登录成功用例只断言 200、access token 和 refresh cookie，无法发现实现退化为“能登录但不更新 last_login/不提交审计事务”。现把断言补到 repository await 参数和 session commit/rollback，避免认证测试只证明会发 token |
| 2026-05-30 | N/A（Worker stdout/stderr secret redaction 文档契约） | `tests/unit/test_release_quality_docs_contract.py` 30 passed；targeted ruff passed；diff check passed | 无新增 warning；功能目录/TODO 不再保留“用户主动打印密钥后的日志脱敏未实现”旧口径，历史台账也标明 2026-05-28 只证明被动不泄露、后续已补齐主动 stdout/stderr 脱敏证据 | 测试 review 发现代码和测试已经覆盖 executor 在写 Redis 前按环境变量敏感值脱敏，external-stack 还验证 live SSE、archived logs 与真实 `run.read` token 读面只出现 `[REDACTED]`，但 feature catalog/TODO 仍把该能力写成未实现。现统一文档口径并补 release-quality 文档契约，防止安全能力被文档降级或测试证据被误读 |
| 2026-05-30 | N/A（Artifact preview E2E 证据等级文档契约） | `tests/unit/test_release_quality_docs_contract.py::test_testing_docs_do_not_overclaim_generic_html_artifact_preview_e2e` 1 passed；相关 docs/frontend/runs 契约合并复跑 100 passed；targeted ruff passed | 无新增 warning；docs 现在明确 Allure HTML 预览有 E2E，普通 `html`/`report + text/html` 预览由前端契约测试覆盖，不再把普通 HTML 预览证据等级写成 E2E | 测试 review 发现 docs 把 HTML artifact 预览泛称为 E2E 证据，但当前 `special-regressions` 实际覆盖的是 `allure-report` 类型的 Allure HTML；普通 `html`/`report + text/html` 由本轮前端契约测试覆盖。现修正文档为“Allure HTML 有 E2E，普通 HTML/report 有前端契约”，并补 release-quality 文档契约防止证据等级再次被写高 |
| 2026-05-30 | N/A（Run list status filter 枚举拒绝契约） | `tests/unit/test_api/test_runs.py` 35 passed；targeted ruff passed；前端契约/安全契约合并复跑 36 passed | 无新增 warning；`/runs?status=` 现在复用 `RunStatusValue` 的 Literal 集合校验逗号状态，未知值返回 `ErrorResponse` 422 且不查询 run repository，OpenAPI 422 响应声明为 `ErrorResponse` | 测试 review 发现 `/runs?status=` 单测只证明合法 `queued` 和 `queued,running` 能生成过滤条件，但状态参数是裸字符串，拼错状态会静默查 DB 并返回空列表，无法暴露调用方或前端拼写错误。现用 `RunStatusValue` 的 Literal 集合校验逗号状态，未知值固定 422 且不查询 run repository |
| 2026-05-30 | N/A（Artifact preview URL 协议安全契约） | `tests/unit/test_frontend_security_contracts.py` 2 passed；`tests/unit/test_frontend_api_contract.py` 34 passed；targeted ruff passed；frontend `npx tsc --noEmit --pretty false` / `npm run lint -- --max-warnings=0` / `npm run build` passed | 无新增 warning；preview/download 共用 `isSafeArtifactUrl`，两条路径都会在 `setPreview` 或 `window.open` 前拒绝非 http/https URL | 测试 review 发现 artifact 下载按钮会拒绝非 http/https 预签名 URL，但预览按钮直接把返回值塞进 iframe；既有安全契约只检查 iframe sandbox/referrer，不能发现 unsafe URL 被打开。现把 preview/download 共用 `isSafeArtifactUrl`，预览和下载都必须在打开前通过 http/https 校验，并补安全契约测试锁住顺序 |
| 2026-05-30 | N/A（Analytics history xfail 视觉语义契约） | `tests/unit/test_frontend_api_contract.py::test_analytics_history_status_colors_match_result_status_semantics` 1 passed；`tests/unit/test_frontend_api_contract.py` 34 passed；frontend `npx tsc --noEmit --pretty false` / `npm run lint -- --max-warnings=0` / `npm run build` passed | 无新增 warning；analytics 历史表现在只有真实 `passed` 使用 passed 色，`skipped/xfail` 使用 skipped 色，`failed/error` 保持 failed 色 | 测试 review 发现结果列表与图标组件把 `xfail` 归入 skipped 语义，但 analytics 单用例历史表把 `xfail` 染成 passed，趋势页会把“预期失败”误显示成成功。现将 history 状态色改为 passed 仅代表真实 passed，`skipped/xfail` 使用 skipped 色，并用前端契约测试锁住，避免状态枚举覆盖只证明字段存在、不证明语义一致 |
| 2026-05-30 | N/A（Run detail HTML artifact 预览契约） | `tests/unit/test_frontend_api_contract.py::test_run_detail_previews_html_artifact_contract` 1 passed；`tests/unit/test_frontend_api_contract.py` 33 passed；frontend `npx tsc --noEmit --pretty false` / `npm run lint -- --max-warnings=0` / `npm run build` passed | 无新增 warning；Run detail 会对 Allure、`html` 类型、`text/html` MIME 或 `.html/.htm` 文件名显示 Preview，Allure 预览保留既有 iframe title 兼容现有 E2E | 测试 review 发现文档与集成用例都把 HTML/report artifact 当成可预览主路径，但 Run detail 只对 `allure-report` 显示 Preview，导致普通 HTML 报告只能下载不能预览而测试仍会绿。现把预览判定收紧为真实 HTML artifact 契约，并用前端契约测试锁住，避免 HTML 预览继续只靠文档口径存在 |
| 2026-05-30 | N/A（Projects/Runs 列表 retry 行为契约） | `tests/unit/test_frontend_api_contract.py::test_list_page_retry_buttons_refetch_current_failed_query` 1 passed；`tests/unit/test_frontend_api_contract.py` 32 passed；frontend `npx tsc --noEmit --pretty false` / `npm run lint -- --max-warnings=0` / `npm run build` passed | 无新增 warning；Projects 无搜索失败和 Runs 第 1 页失败时 Retry 会显式 `refetch()`，有搜索条件或非第 1 页时仍通过状态变更切换查询 | Settings retry 修复后继续扫同型错误态，发现 Projects/Runs 列表也只清搜索或页码，当前查询条件未变化时点击 Retry 不会重新请求。现统一补 `handleRetry` 并用前端契约测试锁住，避免列表错误态在 PR 级主链路外继续假绿 |
| 2026-05-30 | N/A（Settings API Token retry 行为契约） | `tests/unit/test_frontend_api_contract.py::test_settings_page_uses_real_api_token_contract` 1 passed；frontend `npx tsc --noEmit --pretty false` / `npm run lint -- --max-warnings=0` / `npm run build` passed | 无新增 warning；API token 列表加载失败时 Retry 在第 1 页会显式 `refetch()`，非第 1 页回到第 1 页重新请求 | 测试 review 发现 Settings token 错误态按钮只执行 `setPage(1)`，当用户已经在第 1 页时不会触发 React Query 重新请求，UI 看似有重试但实际不排障。现改为 `handleRetry` 并在前端契约测试中锁住 `refetch`，避免前端缺组件测试 runner 时错误态行为继续靠人工点击发现 |
| 2026-05-30 | N/A（mutating API route 审计写入架构契约） | `tests/unit/test_architecture_boundaries.py` 17 passed；targeted ruff/diff check passed | 无新增 warning；当前 API POST/PUT/PATCH/DELETE 路由必须直接写审计或委托 `_create_webhook_run` 这类已审计 helper；AST 检查要求实际调用 `write_audit` / `*.audit.create` / `AuditEventRepository(...).create`，仅出现 repository constructor 不算通过；直接审计写入还必须显式携带 `resource_id` | 测试 leader review 发现 README/TODO 仍把审计写路径描述成泛化缺口，但当前 mutating API route 已基本补齐，真正风险是后续新增写接口绕过审计或写入缺字段审计。现新增 AST 架构边界测试，扫描 `src/qaplatform/api` 的写路由并拒绝无审计写入的新增入口，避免审计覆盖继续靠人工记忆；同日收紧 false-positive 防线，防止“创建 AuditEventRepository 但未 create”或省略 `resource_id` 的假覆盖 |
| 2026-05-30 | N/A（frontend runtime dependency audit 复核） | `npm audit --prefix frontend --audit-level=moderate` 0 vulnerabilities；`dompurify` runtime 依赖锁定为 `3.4.7` | 无新增 warning；RISK-DEP-001 仍需 PR CI / default branch Dependabot alert 关闭证据，不能只凭本地 audit 直接关闭 | 测试 leader review 发现依赖风险台账要求 `npm audit`，但运营台账没有记录当前依赖树的真实 audit 结果，容易让安全测试停留在“升级了版本号”。现补本地 audit 证据，并保留默认分支告警关闭条件，避免把本地依赖检查误当成 GitHub alert 已闭环 |
| 2026-05-30 | N/A（Project member role 枚举验证短路契约） | `tests/unit/test_api/test_project_members.py::test_project_member_routes_reject_invalid_role_without_side_effects` 1 passed；targeted ruff/diff check passed | 无新增 warning；project member add/update 非法 role 固定在请求体验证阶段 422 | 测试 review 发现成员管理已有 add/update/remove happy path 和跨租户/duplicate/404 负向用例，但没有证明非法 role 不会进入项目可见性、用户查询或成员 repository。现用例锁住 `owner` 这类越权角色不查 project/user/member、不 create/update、不写 audit，避免成员权限测试只证明合法角色能流转 |
| 2026-05-30 | N/A（Credential create 请求体验证零副作用契约） | `tests/unit/test_api/test_credentials.py::test_create_credential_rejects_invalid_body_without_side_effects` 1 passed；targeted ruff/diff check passed | 无新增 warning；Credential create 现在拒绝空 name，并锁住空 value、非法 type 都在请求体验证阶段 422 | 测试 review 发现凭证用例重点覆盖 AAD 加密、重复名、删除 in-use 和审计脱敏，但空 name 可进入 credential repo；空 value/非法 type 虽有 schema 约束，却没有证明失败前不查项目、不查重复名、不加密、不创建、不写 audit。现负向用例补齐密钥入口的验证短路，避免 credential 测试只证明正常密钥会被加密 |
| 2026-05-30 | N/A（Environment name/cache_key 非空契约） | `tests/unit/test_api/test_environments.py::test_environment_schema_rejects_empty_name_or_cache_key tests/unit/test_api/test_environments.py::test_environment_routes_reject_empty_name_or_cache_key_without_side_effects` 3 passed；targeted ruff/diff check passed | 无新增 warning；Environment create/update 现在拒绝空 `name` 和空 `cache_key`，并在请求体验证阶段 422 | 测试 review 发现环境用例已覆盖镜像 pinning、资源限制、env_vars 加密和审计，但环境名与 cache key 只有 max_length；空值会污染 UI/审计，或让“显式空 cache key”和“未配置”语义混在一起。现 schema 与路由负向用例锁住坏 payload 不查 project、不查/写 environment、不写 audit，避免环境测试只证明正常配置能保存 |
| 2026-05-30 | N/A（Project core field schema 短路契约） | `tests/unit/test_api/test_projects.py::test_create_project_rejects_invalid_core_fields_without_side_effects tests/unit/test_api/test_projects.py::test_update_project_rejects_empty_core_fields_without_side_effects` 10 passed；targeted ruff/diff check passed | 无新增 warning；Project create/update 现在在后端请求体验证层拒绝空 name/git_url/default_branch/root_path，create 还拒绝空 slug 和非法 slug 格式 | 测试 review 发现前端已要求项目核心字段非空、slug 为小写字母数字连字符，但后端 schema 只有限长，unit 主要覆盖 happy path、duplicate slug 和 allowed_branches，无法发现空 slug/空 git_url 进入 repository。现用例锁住非法核心字段不查 slug、不创建/更新 project、不查 credential、不写 audit，避免项目测试只证明正常 payload 能保存 |
| 2026-05-30 | N/A（Run trigger Git 输入验证短路契约） | `tests/unit/test_api/test_runs.py::test_trigger_run_rejects_invalid_git_inputs_without_side_effects` 3 passed；targeted ruff/diff check passed | 无新增 warning；手动触发 run 现在拒绝空 `git_ref`、超长 `git_ref` 和非 40 位 hex `git_sha`，并固定在请求体验证阶段 422 | 测试 review 发现 run trigger 只覆盖 priority 越界和 happy-path git 字段，`git_ref=""` 会静默回退默认分支，超长 ref 也可能进入 DB/worker；`git_sha` 虽有正则但缺少验证层零副作用哨兵。现用例锁住坏 git 输入不查 pipeline/project/environment、不创建 run、不设置 retry group、不写 audit，避免触发测试只证明正常分支能跑 |
| 2026-05-30 | N/A（Notification send failure 错误脱敏契约） | `tests/unit/test_worker/test_notifications.py::TestEvaluateAndNotify::test_send_failure_logged` 1 passed；targeted ruff/diff check passed | 无新增 warning；通知投递异常写入 `NotificationLog.error_message` 前会按 channel config 脱敏，并保留可诊断的安全错误前缀 | 测试 review 发现 send failure 用例只要求原始异常文本进入日志，反而会锁住 webhook URL userinfo、header token 或渠道密钥泄露。现用例构造携带 credentialed URL 和 Bearer token 的底层异常，断言失败日志保留 `send failed` 但不含 URL、userinfo 或 token，避免通知失败测试把“泄密也能记日志”当成成功 |
| 2026-05-30 | N/A（Run cancel reason 审计与 schema 契约） | `tests/unit/test_api/test_runs.py::test_cancel_run tests/unit/test_api/test_runs.py::test_cancel_run_rejects_overlong_reason_without_side_effects` 2 passed；targeted ruff/diff check passed | 无新增 warning；Run cancel 现在限制 `reason` 最多 500 字符，合法 reason 进入 `run.cancel` audit after_state，超长 reason 在请求体验证层 422 | 测试 review 发现前端和接口向 `/runs/{id}/cancel` 发送 `reason`，但后端取消路径丢弃它，既有单测只证明 CAS、Redis 事件和 audit 调用发生，不能发现审计原因丢失或坏 payload 触碰 run repo。现用例锁住成功取消保留原因、超长 reason 不查询 run、不取消、不发 Redis、不写 audit，避免取消测试只证明“状态变 cancelled” |
| 2026-05-30 | N/A（Pipeline schema 非空/正 timeout 输入契约） | `tests/unit/test_api/test_pipelines.py` 10 passed；targeted ruff/diff check passed | 无新增 warning；Pipeline create/update schema 现在拒绝空 pipeline name、空 stage name、空 stage plugin 和非正 timeout | 测试 review 发现 pipeline 单测主要证明复杂嵌套 payload 能保存和审计脱敏，却没有覆盖坏 schema 边界；`timeout_seconds=0` 或空 plugin 可能进入 DB 后才在 executor/registry/timeout 逻辑里出问题。现负向用例锁住 create/update 在请求体验证层 422，且不触碰 project/pipeline repo 或 audit，避免 pipeline 测试只证明 happy path |
| 2026-05-30 | N/A（Environment resource limit 非正值拒绝契约） | `tests/unit/test_api/test_environments.py` 22 passed；docs contract 28 passed；targeted ruff/diff check passed | 无新增 warning；Environment create/update schema 现在拒绝非正 `memory_mb`、`cpu_cores`、`max_artifact_size_mb`、`max_artifacts_count` | 测试 review 发现环境资源限制用例只覆盖正常值持久化，schema 对多个资源字段只有类型约束，无法发现 0/负值一路进入 DB、worker 或 Docker resource config。现 schema 级参数化测试锁住 create/update 对同一字段的拒绝位置，避免环境配置测试只证明 happy path 能保存 |
| 2026-05-30 | N/A（Schedule quiet_windows API 规范化契约） | `tests/unit/test_api/test_schedules.py` 9 passed；docs contract 28 passed；targeted ruff/diff check passed | 无新增 warning；schedule API 现在会把 quiet window 规范化为 `start/end/timezone`，并拒绝非法 HH:MM 或非法 IANA timezone | 测试 review 发现 schedule API 把 `quiet_windows` 当裸 `list[dict]` 接收，既有 happy-path 测试还传入缺 `timezone` 的窗口；但 worker `should_fire` 假设每项都有合法 `timezone/start/end`，历史脏 payload 可能让扫描任务在路由之后才 KeyError。现测试锁住默认 timezone 补齐、坏时间/坏时区 422、且不触碰 repo/compute/audit，避免 quiet window 用例只证明 JSON 原样穿透 |
| 2026-05-30 | N/A（Schedule API timezone 输入验证短路契约） | `tests/unit/test_api/test_schedules.py` 8 passed；docs contract 28 passed；targeted ruff/diff check passed | 无新增 warning；`ScheduleCreate/Update.timezone` 现在校验 IANA timezone name，无效值固定 422 | 测试 review 发现 schedule API 只校验 cron，不校验 timezone，`Mars/Base` 会进入路由后在 `compute_next_run_at` 里抛错，容易变成 500。现 create/update 负向用例锁住无效 timezone 在请求体验证层短路，且不触碰 project/pipeline/schedule repo、compute 或 audit，避免 schedule 测试只覆盖 happy-path timezone |
| 2026-05-30 | N/A（Schedule cron timezone 基准转换契约） | `tests/unit/test_services/test_scheduling.py tests/unit/test_worker/test_check_schedules.py` 17 passed；targeted ruff/diff check passed | 无新增 warning；`compute_next_run_at` 现在会把 aware `base_time` 转换到 schedule timezone 后再交给 croniter 计算 | 测试 review 发现既有用例锁住了错误行为：`Asia/Shanghai` cron 在传入 UTC aware base 时仍按 UTC 计算，导致定时任务可能偏移 8 小时。现用例固定 00:30 UTC 对应上海 08:30 时，`0 9 * * *` 必须得到上海 09:00，避免调度测试只是证明函数返回一个 datetime |
| 2026-05-30 | N/A（API token malformed format DB 短路契约） | `tests/unit/test_auth/test_token_service.py tests/unit/test_auth_middleware.py` 41 passed；docs contract 28 passed；targeted ruff/diff check passed | 无新增 warning；`TokenService.parse_bearer_token` 现在拒绝空 token_id 或空 secret | 测试 review 发现 API token parser 只检查三段格式，`qap__secret` 会继续以空 token_id 查库，`qap_tokenid_` 会继续进入空 secret 校验。现 parser 和中间件测试锁住畸形 token 固定 401 format error 且不打开 DB session，避免认证入口测试只覆盖“少段数”而漏掉空段副作用 |
| 2026-05-30 | N/A（Webhook channel SSRF 非公网地址拦截契约） | `tests/unit/test_worker/test_channels.py` 35 passed；docs contract 28 passed；targeted ruff/diff check passed | 无新增 warning；Webhook URL 解析后改为拒绝所有 non-global IP，而不是只列部分私网 CIDR | 测试 review 发现 webhook 通知 SSRF 测试只覆盖 127.0.0.1，代码只显式列出部分 IPv4 私网和 `::1`，无法发现 `0.0.0.0`、IPv6 unique-local `fc00::/7`、link-local `fe80::/10` 这类非公网地址被放行。现用参数化用例锁住解析到非公网 IP 时不创建 HTTP client，避免通知渠道测试只证明一个私网样例安全 |
| 2026-05-30 | N/A（Scheduled run retry group 持久化契约） | `tests/unit/test_worker/test_check_schedules.py` 9 passed；docs contract 28 passed；targeted ruff/diff check passed | 无新增 warning；`check_schedules` 创建 schedule run 后改为通过 `run_repo.set_retry_group_id(run.id, run.id)` 显式持久化 retry group | 测试 review 发现 schedule worker 与 webhook 曾有同型问题：成功触发和 enqueue 失败路径只断言 `run.retry_group_id` 内存字段被赋值，无法发现实现绕过 repository update/flush 与 `updated_at` 契约。现用例锁住成功与 enqueue 失败两条路径都调用 repository 持久化 retry group，避免定时任务测试只证明 mock 对象字段变了 |
| 2026-05-30 | N/A（FairScheduler waiting queue 公平性契约） | `tests/unit/test_worker/test_scheduler.py` 14 passed；docs contract 28 passed；targeted ruff/diff check passed | 无新增 warning；本轮不改运行代码，只补等待队列项目配额满时的继续扫描断言 | 测试 review 发现 waiting dequeue 既有用例只证明全局容量满时停止在第一个可出队 run，未证明某个项目已达配额时调度器会跳过该项目并继续处理后续项目。新增用例锁住 `find_waiting(limit=10)`、逐项目配额检查顺序、只把可用项目 run 放入低优先级队列、且不把已等待 run 重新 `mark_waiting`，避免公平调度测试只证明“能出队一个” |
| 2026-05-30 | N/A（Webhook trigger retry group 持久化契约） | `tests/unit/test_api/test_p3.py::TestWebhookTrigger::test_webhook_trigger_creates_run tests/unit/test_api/test_p3.py::TestBatchCancel::test_batch_cancel_success tests/unit/test_api/test_p3.py::TestBatchRetry::test_batch_retry_success tests/unit/test_api/test_p3.py::TestWebhookTrigger::test_webhook_trigger_filtered_branch_returns_200_without_run tests/unit/test_api/test_p3.py::TestWebhookTrigger::test_webhook_trigger_dedup_integrity_error_returns_duplicate` 5 passed；targeted ruff/diff check passed | 无新增 warning；webhook run 创建后现在和手动触发一致，通过 `repos.run.set_retry_group_id(run.id, run.id)` 显式持久化 retry group | 测试 review 发现 webhook trigger 单测只断言 `run.create` 被调用和 dedup_key，不能发现 retry_group_id 只在内存对象上赋值、未走 repository update。现锁住 pipeline 查询 limit、run.create 关键入参、retry group 持久化和 enqueue 参数；batch retry 成功路径也补齐 `set_retry_group_id` 断言，避免自动重试链路测试只证明“创建了 run” |
| 2026-05-30 | N/A（Notification rule update repository 持久化契约） | `tests/unit/test_api/test_notifications.py` 17 passed；targeted ruff/docs contract/diff check passed | 无新增 warning；`update_notification_rule` 现在通过 `repos.notification_rule.update(...)` 写入更新，并用返回对象生成响应和审计 after_state | 测试 review 发现 notification rule update 与 schedule 同类：只改内存 ORM/mock 对象，测试只看字段变化，无法发现绕过 repository flush/refresh。现 name 更新、显式 `template: null` 清空、template 省略保持原值都锁住 update kwargs、响应和审计 before/after，避免通知规则更新测试只证明“对象被 setattr” |
| 2026-05-30 | N/A（Refresh token replay 拒绝契约） | `tests/unit/test_auth/test_auth_routes.py::TestRefreshRevokesOldToken tests/unit/test_auth/test_auth_routes.py::TestRefresh` 7 passed；targeted ruff/docs contract/diff check passed | 无新增 warning；`/auth/refresh` 现在在发新 token 前检查 refresh jti blacklist，已撤销 token 固定 401、清 refresh cookie 并写 `auth.refresh_failed` | 测试 review 发现 `test_refresh_with_revoked_token_returns_401` 名称写 401，实际断言 200，只证明“刷新后旧 jti 被拉黑”，无法发现已拉黑 refresh token 可被重放。现用例预置 revoked jti，断言不查用户库、不发新 token、写 `revoked_refresh_token` 失败审计，避免 refresh 安全测试只为覆盖率服务 |
| 2026-05-30 | N/A（Schedule update repository 持久化契约） | `tests/unit/test_api/test_schedules.py` 7 passed；targeted ruff/docs contract/diff check passed | 无新增 warning；`update_schedule` 现在通过 `repos.schedule.update(...)` 写入变更并用返回对象生成响应和审计 after_state | 测试 review 发现 schedule update 单测只断言 MagicMock 对象字段被改，无法发现路由绕过 repository flush/refresh 或响应/审计未基于持久化后的对象。现测试锁住 `compute_next_run_at` 入参、`schedule.update` kwargs、响应 JSON 和 audit before/after，避免 update 用例只证明“内存对象变了” |
| 2026-05-30 | N/A（backend integration JUnit 证据门禁） | `tests/unit/test_backend_integration_artifact_validator.py` 7 passed；release gate/docs contract 39 passed；targeted ruff passed | 无新增 warning；`validate_backend_integration_artifacts.py` 现在除 collect/JUnit nodeid 对齐外，也拒绝 failure/error、全 skip、默认 skip；workflow 调用该脚本时传入 release/nightly 的 skip 策略 | 集成证据门禁不再只证明“执行报告覆盖了收集清单”。新增用例能发现失败 JUnit、error JUnit、全 skipped JUnit，以及仅 nightly 允许的 OOM opt-in skip 策略漂移，避免 required integration artifact 被空跑或失败报告伪装成有效发布证据 |
| 2026-05-30 | N/A（GitSource URL userinfo 失败脱敏） | `tests/unit/test_plugins/test_git_source.py` 20 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只补无显式 secret 参数时的 credentialed URL 失败脱敏哨兵 | GitSource 既有测试覆盖 askpass token 不进入 clone URL、显式 token 失败不泄露，但没有证明用户把凭证嵌进 `git_url` 时，git stderr 回显的 URL 会被统一脱敏。新增用例直接让 auth-less clone 返回 `https://x-access-token:...@github.com/...` 失败文本，断言错误消息不含 token/userinfo，只保留 `https://***@github.com/...`，避免 clone 失败测试只证明“传了 secret 才安全” |
| 2026-05-30 | N/A（GitSource clone/private-IP 弱断言契约） | `tests/unit/test_plugins/test_git_source.py::TestGitSourceClone::test_clone_failure_raises_runtime_error tests/unit/test_plugins/test_git_source.py::TestGitSourceClone::test_clone_auth_failure_raises_runtime_error tests/unit/test_plugins/test_git_source.py::TestGitUrlValidation::test_rejects_https_hostname_that_resolves_to_private_ip` 3 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只把 GitSource clone/private-IP 用例从 raises-only 补成命令参数与拒绝来源契约 | 测试 review 发现 clone 失败用例只匹配 `git clone failed`，无法发现错误 exit code/stdout 来源漂移、clone 命令少了 `--depth 1 --branch main`，或无 auth 场景误带环境变量。private-IP 用例只匹配 `private IP`，无法发现没有按 hostname 调 DNS 或拒绝信息错误。现断言完整 RuntimeError、clone argv/env、完整 private IP 错误和 `getaddrinfo("github.example", None)`，避免 GitSource 失败路径测试只为异常覆盖率服务 |
| 2026-05-30 | N/A（PluginRegistry missing plugin 错误契约） | `tests/unit/test_plugins/test_registry.py::test_missing_plugins_raise_actionable_key_errors` 3 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只把 missing plugin 用例从 raises-only 补成错误参数与 registry 状态不变契约 | 测试 review 发现 PluginRegistry missing 用例只匹配 `Runner/Collector/Source plugin not found` 片段，无法发现 KeyError args 丢失插件名、错误类型泛化，或 miss 后污染已注册插件表。现先注册 runner/collector/source，再断言 `KeyError.args` 精确包含类型和 missing name，三类 names 列表不变且已注册插件仍可取回，避免插件查找测试只为异常覆盖率服务 |
| 2026-05-30 | N/A（内置 runner 报告路径工作区边界） | `tests/unit/test_plugins` 143 passed；targeted ruff passed | 无新增 warning；本轮新增内置 runner 输出路径保护，pytest/jest/playwright/go 均拒绝绝对路径和 `..` 越界路径 | Runner 与 JUnitCollector 的路径安全契约对齐：测试报告只能写到 workspace 内相对路径。这样能发现 `junit_xml="../..."`、绝对路径、Windows 绝对路径或 Go `json_output` 越界导致 collector 收不到结果、或直跑模式写出工作目录的回归，避免报告链路测试只证明命令能构造 |
| 2026-05-30 | N/A（内置 runner 越界路径错误契约） | `tests/unit/test_plugins/test_pytest_runner.py::TestPytestBuildCommandInternal::test_rejects_junit_xml_outside_workspace tests/unit/test_plugins/test_jest_runner.py::TestJestBuildCommand::test_rejects_junit_xml_outside_workspace tests/unit/test_plugins/test_playwright_runner.py::TestPlaywrightBuildCommand::test_rejects_junit_xml_outside_workspace tests/unit/test_plugins/test_go_test_runner.py::TestGoTestBuildCommand::test_rejects_output_paths_outside_workspace` 16 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只把四个 runner 越界报告路径用例从 raises-only 补成精确错误契约 | 测试 review 发现 runner 路径边界用例只匹配 `must be a relative path` 片段，无法发现错误字段泛化、错误消息漂移、或 Jest/Playwright 漏掉 `reports/../report.xml` 与 Windows 绝对路径。现四个 runner 均断言完整错误文案为 `must be a relative path inside the workspace`，且错误不回显原始越界路径，避免 runner 工作区边界测试只为异常覆盖率服务 |
| 2026-05-30 | N/A（Worker heartbeat reclaimer 契约） | `tests/unit/test_worker/test_tasks.py::TestHeartbeatLoop` 3 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只补 worker heartbeat 写入契约断言 | Worker heartbeat 测试不再只靠真实 sleep 证明 loop 没被 Redis 异常杀死，还锁住 Redis key 为 `worker:{worker_id}:heartbeat`、值为带时区 ISO 时间戳、TTL 为 90 秒。这样能发现 heartbeat key/TTL/时间格式漂移导致 reclaimer 误判 worker_lost 的回归 |
| 2026-05-30 | N/A（Jest runner JUnit 计数防崩契约） | `tests/unit/test_plugins/test_jest_runner.py` 15 passed；targeted ruff passed | 无新增 warning；本轮补强 Jest runner 测试，并让 malformed JUnit count 返回零计数而不是抛异常 | Jest runner 测试不再只用空 testcase 凑 suite 计数；fixture 现在包含 pass/failure/error/skipped 节点，run_tests 断言 pass/fail/error/skip、stdout/stderr 和 JUnit env 覆盖，直接 parser 测试锁住四类结果计数与坏计数字段防崩。这样能发现 jest-junit 报告计数漂移、错误结果漏计或第三方报告异常导致 runner 自身失败的回归 |
| 2026-05-30 | N/A（Go test runner JUnit 结果契约） | `tests/unit/test_plugins/test_go_test_runner.py::TestGoTestRunTests::test_run_success tests/unit/test_plugins/test_go_test_runner.py::TestGoTestRunTests::test_run_with_failures tests/unit/test_plugins/test_go_test_runner.py::TestGoTestRunTests::test_run_with_skipped tests/unit/test_plugins/test_go_test_runner.py::TestGoTestRunTests::test_package_level_fail_counts_as_error` 4 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只补 Go runner 解析结果对应的 JUnit XML 分类、耗时和错误文本断言 | Go runner 测试不再只证明 `TestRunResult` 的 pass/fail/skip/error 数字，也锁住生成的 JUnit XML 中 tests/failures/errors/skipped 汇总、classname/name/time，以及 failure/skipped/error 节点 message 和原始输出文本。这样能发现报告链路丢失失败原因、把 package error 误写成 failure、或 JUnit 汇总与 runner 结果漂移的回归 |
| 2026-05-30 | N/A（ExecutionService Run 创建契约） | `tests/unit/test_engine/test_execution_service.py::TestExecutionService::test_create_run_returns_queued tests/unit/test_engine/test_execution_service.py::TestConditionalTransitions::test_claim_for_worker_success` 2 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只补 domain execution service 的 Run 字段映射和 claim 返回对象断言 | ExecutionService 创建 Run 的测试不再只证明状态为 `queued`，还锁住 tenant/project/pipeline/environment、trigger、git_sha、source_run_id、chain_depth、dedup_key、默认 priority/attempt、retry_group_id 与 UTC 时间戳一致性；claim 成功路径也证明返回 repo.get 的同一对象。这样能发现字段漏传、重试链丢失、去重键丢失或 claim 返回占位对象的回归 |
| 2026-05-30 | N/A（Auth token unauth DB short-circuit） | `tests/unit/test_auth/test_auth_routes.py::TestTokenRoutes::test_token_routes_require_authentication` 3 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只补 auth token CRUD 未认证路径的 session 零触达断言 | API token create/list/revoke 未认证测试不再只看 401 和 `Missing Authorization header`，还锁住认证失败发生在打开 DB session / token repository 之前。这样能发现未认证请求仍触发数据库依赖或后续副作用的回归，避免 token 安全入口测试停留在最终状态码 |
| 2026-05-30 | N/A（Run missing 子资源统一短路） | `tests/unit/test_api/test_runs.py::test_run_resource_routes_hide_missing_run_without_side_effects` 1 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只补 run detail/cancel/results/artifacts/archive/notifications 缺失 run 的统一短路断言 | Run 子资源缺失路径不再只由单个 detail 404 用例间接覆盖；现在锁住六个入口统一返回 `Run not found`，且不执行项目权限、cancel 写入、result/artifact/notification 查询、S3 archived log 读取、Redis cancel/event 发布或 audit。这样能发现缺失资源路径继续触碰下游系统或泄露权限差异的回归 |
| 2026-05-30 | N/A（Readiness degraded probe completeness） | `tests/unit/test_api/test_health.py::test_ready_returns_degraded_when_database_probe_fails tests/unit/test_api/test_health.py::test_ready_returns_degraded_when_redis_probe_fails` 2 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只补 `/ready` 退化路径的响应元数据与依赖探测完整性断言 | Readiness 的 db/redis 单项失败测试不再只看 503/degraded/checks，还锁住 degraded 响应继续包含 `version`、`uptime`，并证明一项失败时另一项探测仍执行一次。这样能发现 readiness 被错误改成先失败先返回、遗漏依赖状态或退化响应缺少运维元数据的回归 |
| 2026-05-30 | N/A（Schedule negative path update/compute 短路） | `tests/unit/test_api/test_schedules.py::test_create_schedule_rejects_pipeline_outside_project_without_side_effects tests/unit/test_api/test_schedules.py::test_get_schedule_not_found_has_no_write_side_effects tests/unit/test_api/test_schedules.py::test_schedule_routes_hide_missing_project_without_side_effects` 3 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只补 schedule 负向路径的 update、schedule 查询和 next_run 计算零触达断言 | Schedule 的跨项目 pipeline、schedule missing、project missing 三类负向测试不再只证明 404 与 create/delete/audit 未发生，还锁住失败后不会触碰 schedule update、不会在父资源失败时继续查 schedule、不会计算 next_run。这样能发现短路顺序被破坏或失败路径误改 schedule 的回归，避免 schedule API 测试只为覆盖状态码 |
| 2026-05-30 | N/A（Schedule silent window naive now 短路契约） | `tests/unit/test_services/test_schedule_silent_windows.py::test_is_in_silent_window_rejects_naive_now` 1 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只把 naive now 用例从 raises-only 补成先校验时区、后窗口匹配的短路契约 | 测试 review 发现 `is_in_silent_window` 的 naive datetime 用例只证明会抛包含 `timezone-aware` 的 `ValueError`，无法发现实现先遍历 silent window 再失败，或错误原因从输入校验漂移到 datetime 比较异常。现传入一旦迭代就失败的窗口列表，断言错误文案固定且无 cause，避免静默窗口时间测试只为异常覆盖率服务 |
| 2026-05-30 | N/A（Notification rule update 副作用短路） | `tests/unit/test_api/test_notifications.py::test_get_rule_not_found tests/unit/test_api/test_notifications.py::test_notification_rule_routes_hide_missing_project_without_side_effects tests/unit/test_api/test_notifications.py::test_create_rule_rejects_invalid_conditions` 3 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只补 notification rule 负向路径的 update 零触达断言 | Notification rule 的 schema validation、rule not found、missing project 三类负向测试不再只证明 create/delete/audit 没发生，也锁住 `notification_rule.update` 不会被 await。这样能发现隐藏项目或请求体验证失败后仍误改规则对象的回归，避免负向用例只停在状态码和部分副作用 |
| 2026-05-30 | N/A（Pipeline 详情 404 等价性与父项目校验） | `tests/unit/test_api/test_pipelines.py::test_get_pipeline_returns_404_for_missing_or_other_project` 1 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只补强 pipeline 详情缺失/跨项目用例的断言面 | Pipeline 详情 404 测试不再只看状态码和同一 message，还锁住缺失 pipeline 与跨项目 pipeline 的完整 `NOT_FOUND` envelope、父项目 `get_for_tenant(project_id, tenant_id)` 先行校验、不触发 list/create/update/delete/audit，并证明响应不回显外部 project_id。这样能发现绕过父资源可见性、泄露跨项目归属或错误写审计的回归 |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_audit_events_api.py::test_audit_events_refused_queries_do_not_write_self_audit` 1 passed | targeted ruff passed | 无新增 warning；本轮不改运行代码，只补强 audit-events 拒绝路径真实 API 断言 | Audit events 拒绝查询不再只看 403/404 和 no self-audit，还锁住 member/viewer 固定 `Insufficient permissions`，跨租户资源固定 `Audit event not found` error envelope，并证明响应不回显跨租户 project/user ID。这样能发现权限错误来源漂移、404 信息泄露或错误自审计写入 |
| 2026-05-30 | N/A（Environment env_vars crypto failure 响应契约） | `tests/unit/test_api/test_environments.py::test_get_environment_decrypt_failure_returns_500_and_writes_audit tests/unit/test_api/test_environments.py::test_create_environment_encrypt_failure_returns_500_and_writes_audit` 2 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只补强 env_vars 加/解密失败测试的响应体与不泄密断言 | Environment env_vars crypto failure 测试不再只看 500 与审计写入，还锁住固定 `Environment env vars decrypt/encrypt failed` 响应文案，并用包含 `secret-value` 的底层异常证明响应不回显 secret。这样能发现错误处理把底层异常或明文环境变量泄给调用方的回归 |
| 2026-05-30 | N/A（Env vars AAD mismatch 解密失败契约） | `tests/unit/test_services/test_env_vars_crypto.py::test_env_vars_aad_mismatch_fails` 1 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只把 AAD mismatch 用例从 raises-only 补成 InvalidTag 与不泄密契约 | 测试 review 发现 env_vars AAD mismatch 用例只证明会抛统一 `ValueError`，无法发现实现把错误环境 ID 的密文按普通 payload 处理、抛错原因漂移，或把变量名/明文/密文写进错误信息。现断言底层 cause 为 `InvalidTag`，对外错误文案固定，且不包含 `TOKEN`、`secret`、ciphertext 或 wrong environment_id，避免 env_vars crypto 安全测试只为异常覆盖率服务 |
| 2026-05-30 | N/A（Domain pipeline 配置文本短路契约） | `tests/unit/test_domain_models.py tests/unit/test_services/test_discovery.py` 18 passed；coverage unit 1198 passed；targeted ruff passed | 无新增 warning；domain `TestSelector` include/exclude/tags、`RetryPolicy.retry_on`、`TriggerConfig.type`、`StageDefinition.name/plugin`、`CollectorDefinition.plugin`、`Pipeline.name` 现在同步拒绝空白文本 | 测试 review 发现 domain pipeline 配置用例名义上覆盖 blank，但旧断言只测空字符串，空白字符串仍会进入发现规则、重试原因、触发配置、stage/collector/pipeline 名称。现补空白串、长度/数量边界和下标错误信息断言，避免 domain 模型测试只证明 obvious empty string 能失败 |
| 2026-05-30 | N/A（Worker Git credential type mismatch 解密短路） | `tests/unit/test_worker/test_tasks.py::test_build_source_auth_rejects_type_mismatch` 1 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只把 Git 凭据类型不匹配用例从 raises-only 补成 no-decrypt 契约 | 测试 review 发现 `_build_source_auth` 的 type mismatch 用例只证明会抛 `RuntimeError`，无法发现实现先解密 `credential.encrypted_value` 再发现类型不匹配。现断言 credential lookup 发生一次且 `crypto.decrypt` 不被调用，避免 worker source auth 测试只为异常覆盖率服务 |
| 2026-05-30 | N/A（Worker encrypted env_vars 缺 crypto 短路契约） | `tests/unit/test_worker/test_tasks.py::test_build_pipeline_config_requires_crypto_for_encrypted_env_vars` 1 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只把 encrypted env_vars 缺 crypto 用例从 raises-only 补成 no-PipelineConfig 契约 | 测试 review 发现 `_build_pipeline_config` 缺 crypto 用例只证明会抛 `RuntimeError`，无法发现实现把加密 envelope 当普通 env_vars 继续构造 `PipelineConfig`，或把变量名/密文/明文写入错误信息。现断言 `PipelineConfig` 不被构造，错误信息固定且不包含 `API_TOKEN`、`secret-value` 或 ciphertext，避免 worker 环境变量加密测试只为异常覆盖率服务 |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_api_endpoints.py::TestProjectsCRUD::test_create_project` 1 passed | targeted docs contract passed；targeted ruff passed | 无新增 warning；project create 真实 API 用例现在验证响应 UUID、created_at/updated_at 可解析且有时区，并精确固定 tenant_id、created_by、默认 clone/config 字段与状态 | 测试 review 发现旧用例只断言 `id`/`created_at` 存在，不能发现 ProjectResponse 漏掉 tenant/creator/default_env/settings/silent_windows，或默认字段漂移。现把成功创建从字段存在升级成响应契约，避免 CRUD create 集成测试只为状态码和覆盖率服务 |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_api_write_state.py::test_notification_rule_api_persists_updates_and_hides_soft_deleted_rule` 1 passed | targeted docs contract passed；targeted ruff passed | 无新增 warning；notification rule 真实 API/DB 用例现在断言 soft-delete 后 repository `total=0` 且 `visible == []` | 测试 review 发现旧断言只检查被删 rule id 不在 visible set，不能发现 repository 错误混入其他可见规则。现改为精确空列表，避免 soft-delete 用例只证明“这个 id 被过滤” |
| 2026-05-30 | collect-only `tests/integration` 212 tests；`RUN_INTEGRATION_TESTS=1 tests/integration -m "not heavy_docker and not external_stack and not performance"` 161 passed / 51 deselected；Playwright `--list` 10 tests / 4 files；`npx playwright test tests/e2e/auth-flow.spec.ts --project=chromium` 1 passed；`npx playwright test tests/e2e/special-regressions.spec.ts --project=chromium` 7 passed | coverage unit 1379 passed；total 87.63%，statements 89.56%，branches 79.20%；`tests/unit/test_release_quality_docs_contract.py` 88 passed；backend ruff passed；tenant isolation self-test passed；frontend lint/build passed；`npm audit --prefix frontend --audit-level=moderate` 0 vulnerabilities；smoke shell syntax passed | 本轮复核发现新增 API token 请求体验证、API token create 空白文本请求体验证、Auth login 请求体验证短路验证、Auth register password 文本边界验证、Analytics 查询成功路径仓储/RBAC 验证、Notification rule update 条件/渠道归一化验证、Worker notification project_name 懒加载验证、Worker notification log write 异常语义验证、Webhook provider/dedup route 验证、Dependency transaction boundary 验证、Run repository scheduler lock 验证、Schedule repository due lock 验证、API token secret-before-expiry 验证、API token owner status 验证、JWT current-user lifecycle 验证、Webhook URL userinfo 防凭据嵌入验证、Admin status terminal denominator 验证、DingTalk/WeCom JSON object 响应验证、JUnit duration 输入容错验证、Jest/Playwright JUnit parent 目录验证、GitSource non-public DNS 防 SSRF 验证、GitSource rev-parse 失败/空 SHA 验证、GitSource URL scheme/SSH 判定顺序验证、Runner test_paths 工作区边界验证、Runner test_paths 空白路径验证、Go runner malformed event/elapsed 验证、Env vars truncated ciphertext 防泄漏验证、Encryption keys rotation 配置边界验证、Required config blank values 启动期验证、Log config invalid value 启动期验证、Numeric config invalid bounds 启动期验证、Trusted proxies CIDR 启动期验证、Default string config blank 启动期验证、审计架构边界测试、GitHub provider webhook SHA 验证、GitHub provider ref 空白 payload 验证、batch run_ids 唯一性验证、batch internal failure 防泄漏验证、Auth middleware JWT claim shape 防泄漏验证、pipeline selector/retry policy/trigger_config 请求体验证、pipeline core text 请求体验证、pipeline selector/retry 列表文本请求体验证、domain pipeline 配置文本验证、webhook git_sha 请求体验证、webhook git_sha 空白请求体验证、run results 文本过滤请求参数验证、run list sort/status/git_ref/created range 请求参数验证、project list tenant filter 与 q/status 请求参数验证、Project list tenant filter 默认列表验证、audit-events 查询参数验证、analytics test-history 文本过滤请求参数验证、RBAC project membership lookup scope 验证、SSE ticket malformed payload 防泄漏验证、ready degraded 双失败防泄漏验证、artifact download 404 短路与 no-storage 验证、notification rule name 请求体验证、credential name 请求体验证、credential rotate 缺失资源无副作用验证、project member update 缺失成员无副作用验证、environment name/cache_key 请求体验证、project core text 请求体验证、schedule cron_expr 请求体验证、run trigger git_ref 请求体验证、webhook git_ref 请求体验证后 `docs/backend-test-audit.md` 与质量台账仍停在旧 1122/1127/1130/1132/1137/1140/1143/1145/1149/1151/1154/1158/1164/1168/1171/1172/1174/1182/1183/1184/1185/1190/1194/1195/1197/1198/1226/1233/1239/1241/1242/1244/1246/1247/1250/1255/1263/1267/1268/1269/1272 unit baseline 和旧 85.51/85.52/85.56/85.58/85.59/85.60/85.61/85.65/85.68/85.69/86.11/86.16/86.17/86.18/86.45/86.47/87.74/87.75/87.78/87.80/87.82/87.83/87.87/87.89/87.90/88.23/88.26/88.27/88.28/88.48/88.51/75.66/75.69/75.72/75.74/75.75/75.77/75.80/75.86/75.89/76.74/76.85/76.88/76.91/77.49/78.25/78.56/86.80/87.14/87.28/87.32/87.33/88.73/89.09/89.26/89.31/89.32 coverage baseline；同日已刷新；Schedule、Notification Rule、Pipeline、Run trigger environment 跨项目 404、Batch internal failure 防泄漏、Auth middleware JWT claim shape 防泄漏、Project list tenant filter 默认列表验证、API token create 空白文本请求体验证、Auth login 请求体验证短路验证、Auth register password 文本边界验证、Analytics 查询成功路径仓储/RBAC 验证、Notification rule update 条件/渠道归一化验证、Worker notification project_name 懒加载验证、Worker notification log write 异常语义验证、Webhook provider/dedup route 验证、Dependency transaction boundary 验证、Run repository scheduler lock 验证、Schedule repository due lock 验证、API token secret-before-expiry 验证、API token owner status 验证、JWT current-user lifecycle 验证、Webhook URL userinfo 防凭据嵌入验证、Admin status terminal denominator 验证、DingTalk/WeCom JSON object 响应验证、JUnit duration 输入容错验证、Jest/Playwright JUnit parent 目录验证、GitSource non-public DNS 防 SSRF 验证、GitSource rev-parse 失败/空 SHA 验证、GitSource URL scheme/SSH 判定顺序验证、Runner test_paths 工作区边界验证、Runner test_paths 空白路径验证、Go runner malformed event/elapsed 验证、Env vars truncated ciphertext 防泄漏验证、Encryption keys rotation 配置边界验证、Required config blank values 启动期验证、Log config invalid value 启动期验证、Numeric config invalid bounds 启动期验证、Trusted proxies CIDR 启动期验证、Default string config blank 启动期验证、RBAC project membership lookup scope、SSE ticket malformed payload 防泄漏、Ready degraded 防泄漏、Artifact download 404 短路、Credential rotate 缺失资源与 Project member update 缺失成员契约补强后再次复核到 1379 passed，覆盖率为 87.63%；E2E 清单未漂移 | 刷新 backend 测试审计当前结果，并增加文档契约锁住最新 local baseline，避免测试负责人复核时把旧数字误当当前质量证据 |
| 2026-05-30 | N/A（run-e2e worker gate 复核） | `tests/unit/test_worker_test_asset_boundaries.py`；`bash -n scripts/run-e2e.sh`；targeted ruff passed | 无新增 warning；本地 real E2E 辅助脚本默认导出 `QAP_E2E_WORKER=1` 与 `QAP_WORKER_MAX_JOBS=1`，调用方仍可覆盖 | 测试 leader review 发现 `tests/e2e/real-run-trigger.spec.ts` 需要 `QAP_E2E_WORKER=1`，CI nightly/release_candidate 会设置，但 `scripts/run-e2e.sh` 直接跑 `real-*.spec.ts` 时未设置，导致本地最关键的 worker-backed 用例被 skip。现脚本默认启动 worker gate，并在 README/development 说明该脚本用于真实 worker-backed E2E |
| 2026-05-30 | collect-only: local no-env required lane `161/212 collected` but execution only `46 passed / 115 skipped / 51 deselected`; `RUN_INTEGRATION_TESTS=1` collect confirms required baseline `161`; `RUN_INTEGRATION_TESTS=1 tests/integration -m "not heavy_docker and not external_stack and not performance"` 161 passed / 51 deselected | `tests/unit/test_release_gate_workflow.py tests/unit/test_release_quality_docs_contract.py` 37 passed；targeted ruff passed | 未设置 `RUN_INTEGRATION_TESTS=1` 的本地 required 命令会产生大量 skip，不能作为真实集成覆盖证据；workflow 和 release review slices 仍停在旧 `147` baseline | 将 backend integration collect minimum 与 release evidence 文档提升到当前 `161`，并增加契约测试锁住 baseline，避免新增 required integration 用例后仍按旧门槛签字 |
| 2026-05-30 | N/A（OpenCLI smoke 聚合产物复核） | `tests/unit/test_smoke_report_contract.py`；`bash -n scripts/smoke/*.sh scripts/smoke/lib/*.sh` | 无新增 warning；`scripts/smoke/run-all.sh` 现在为每个子脚本传入 `${RESULTS_DIR}/<script-name>/`，`00-setup.sh` 也尊重外部 `RESULTS_DIR`；父脚本即使有子脚本失败/skip，也会先生成并打印父级产物目录后再返回非零退出码；README/development/feature-catalog 已同步当前 smoke 证据目录契约 | 手工 smoke 聚合报告不再只给出父级 pass/fail，同时把子脚本截图、report、meta 收进同一父目录树，避免排障时证据散落到多个时间戳目录或失败时找不到父级报告 |
| 2026-05-30 | N/A（OpenCLI smoke 密码口径复核） | `tests/unit/test_smoke_report_contract.py`；`bash -n scripts/smoke/*.sh scripts/smoke/lib/*.sh`；`py_compile scripts/*.py` | 无新增 warning；`scripts/smoke/00-setup.sh` 和 `02-login-smoke.sh` 不再硬编码 `admin123`，`scripts/seed_admin.py` 不再把实际密码打印到日志，README/development 已说明 `SMOKE_ADMIN_PASSWORD` 覆盖入口 | smoke setup、登录与 Playwright E2E 一样读取 `SMOKE_ADMIN_PASSWORD || E2E_ADMIN_PASSWORD || "admin123"`。这能发现真实登录配置漂移，同时避免自定义管理员密码在 smoke/CI 日志中泄露 |
| 2026-05-30 | N/A（run-e2e seed 密码口径复核） | `bash -n scripts/run-e2e.sh`；`tests/unit/test_release_quality_docs_contract.py` | 无新增 warning；`scripts/run-e2e.sh` 不再固定 `ADMIN_PASSWORD=admin123`，TODO/catalog/audit 中此前登记的 E2E 辅助脚本偏差已改为关闭口径 | 本地 real E2E 辅助脚本现在导出 `E2E_ADMIN_PASSWORD="${E2E_ADMIN_PASSWORD:-admin123}"`，seed 与 Playwright 登录读取同一个密码值。这样能发现真实登录失败，而不是因为 seed/login 密码漂移导致本地 E2E 假失败 |
| 2026-05-30 | N/A（manual worker smoke 脚本复核） | `tests/unit/test_worker_test_asset_boundaries.py` 4 passed；`py_compile scripts/e2e_worker_test.py`；targeted ruff passed；此前已跑 `py_compile scripts/*.py`、`ruff check scripts`、`bash -n scripts/smoke/*.sh scripts/smoke/lib/*.sh scripts/run-e2e.sh` | 无新增 warning；`scripts/e2e_worker_test.py` 不再调用过期 `PATCH /projects/{id}`，不再把 `passed/timed_out` 当 Run 终态，也不再依赖第三方仓库里存在 `tests/test_main.py`；现在 SSE 日志 `data:` 必须是带 `line` 的 JSON object，日志流结束但 0 行日志会失败，KeyboardInterrupt 以 130 退出 | 手动 worker smoke 现在与当前 E2E 策略一致：通过受控 setup script 写入 pytest simulator，触发 run 时显式传 `environment_id`，轮询 `done/failed/cancelled/timeout`，并验证 passed result、`junit.xml` artifact、下载 URL 和 SSE 日志证据。这样脚本能发现真实 worker 链路和日志流回归，而不是静默吞掉畸形日志、无限等待或中断假成功 |
| 2026-05-30 | N/A（Rate limit token isolation 复核） | `tests/unit/test_api/test_rate_limit.py::TestRateLimitMiddlewareDispatch::test_different_tokens_independent_quotas` | 无新增 warning；token A/B 独立 quota 用例不再只看 429/200 和 downstream 一次调用，还锁住 429 `TOO_MANY_REQUESTS` 响应体、`Retry-After` 窗口，以及 Redis bucket 只含 token hash 不含原始 token | per-token 限流是安全与隐私边界。现能发现错误响应契约漂移、错误 window、或把 bearer token 明文写入 Redis key 的回归，避免隔离测试停留在状态码 smoke |
| 2026-05-30 | N/A（E2E audit-events 403 响应契约） | `npx playwright test tests/e2e/special-regressions.spec.ts --grep "audit event access" --list` | 静态断言复核发现唯一直接 status-only API 拒绝断言；未跑浏览器 E2E | audit-events member 拒绝路径不再只看 403，补齐 `{ detail: "Insufficient permissions" }` 响应体断言。现能发现权限错误原因漂移或错误 403 来源，避免端到端安全回归只按状态码放行 |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_repository_matrix.py::test_user_repository_scopes_soft_delete_and_recovers_from_unique_violation tests/integration/test_real_repository_matrix.py::test_result_and_artifact_repositories_query_paginate_and_recover tests/integration/test_real_auth_results_artifacts.py::test_run_results_api_filters_real_rows_and_recovers_after_duplicate tests/integration/test_real_db_persistence.py::test_project_slug_unique_constraint_rolls_back_without_dirty_data` | N/A | 无新增 warning；真实 PostgreSQL 唯一约束测试不再只证明抛 `IntegrityError`，而是锁住 `uq_app_user_tenant_username`、`uq_test_result_run_suite_name`、`uq_project_tenant_slug` 具体约束，并在 rollback 后验证重复行没有残留 | 集成测试 review 发现剩余弱点集中在唯一约束/rollback 恢复。现能发现误触发其他 DB 约束、错误 rollback 或重复数据残留，避免集成测试只靠“抛异常”刷覆盖 |
| 2026-05-30 | N/A（异常路径余量收尾） | `tests/unit/test_worker/test_settings_tasks.py::test_on_shutdown_closes_docker_even_when_container_close_fails tests/unit/test_engine/test_log_stream.py::TestLogStream::test_read_archived_logs_missing_object_raises_not_found tests/unit/test_auth/test_jwt_service.py::TestDecodeToken tests/unit/test_auth/test_permissions.py::TestPermissionContext::test_frozen` 8 passed | 无新增 warning；shutdown 失败路径锁住原始异常和 docker close finally，归档日志缺对象锁住 run_id 与 S3 key，JWT decode 失败锁住 PyJWT 失败原因，PermissionContext frozen 锁住禁止写 user_id | `pytest.raises` 剩余样本不再只证明“有异常”，而是能发现错误原因泛化、归档 key 漂移或 cleanup finally 被破坏 |
| 2026-05-30 | N/A（PermissionContext frozen 字段保持契约） | `tests/unit/test_auth/test_permissions.py::TestPermissionContext::test_frozen` 1 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只把 PermissionContext frozen 用例从 raises-only 补成 FrozenInstanceError 与字段保持契约 | 测试 review 发现 PermissionContext frozen 用例只匹配 `user_id`，无法发现 frozen dataclass 退化成普通 AttributeError、错误字段名漂移，或赋值失败后原权限上下文字段被污染。现断言异常类型为 `FrozenInstanceError`，`args` 精确为 `cannot assign to field 'user_id'`，且 `user_id/role/tenant_id` 原值保持，避免权限上下文不可变测试只为异常覆盖率服务 |
| 2026-05-30 | N/A（API package lazy attr 错误契约） | `tests/unit/test_api/test_api_package.py::test_api_package_unknown_attribute_raises_attribute_error` 1 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只把 API package unknown attr 用例从 raises-only 补成 exact AttributeError 与 lazy export 保持契约 | 测试 review 发现 `qaplatform.api.__getattr__` unknown attr 用例只匹配 `missing`，无法发现模块名/属性名错误、AttributeError 被泛化，或 miss 后污染 `create_app` lazy export。现断言 `AttributeError.args` 精确为 `module 'qaplatform.api' has no attribute 'missing'`，并确认 miss 后 `api.create_app` 仍指向真实 `qaplatform.main.create_app`，避免 API package 测试只为异常覆盖率服务 |
| 2026-05-30 | N/A（Lifespan init_s3 失败启动短路契约） | `tests/unit/test_lifespan.py::TestLifespanInitializesS3::test_init_s3_failure_propagates` 1 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只把 lifespan init_s3 失败用例从 raises-only 补成启动短路和半初始化状态契约 | 测试 review 发现 lifespan init_s3 失败用例只证明异常会冒泡，无法发现失败后仍继续 init_crypto、注册插件、初始化 infra tracing，或把半初始化 container 放进 `app.state`。现断言 db/redis/arq/s3 初始化顺序已到 S3，`init_crypto`、`_ensure_plugin_registry` 均未调用，`setup_tracing` 只发生在 create_app 构造阶段，`instrument_infra` 未调用，且 `app.state.container` 不存在，避免 lifespan 启动失败测试只为异常覆盖率服务 |
| 2026-05-30 | N/A（JWT decode 失败原因与不泄密契约） | `tests/unit/test_auth/test_jwt_service.py::TestDecodeToken::test_raises_on_expired_token tests/unit/test_auth/test_jwt_service.py::TestDecodeToken::test_raises_on_wrong_secret tests/unit/test_auth/test_jwt_service.py::TestDecodeToken::test_raises_on_malformed_token` 3 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只把 JWT decode 三条失败用例从 raises-only 补成精确 PyJWT 原因与不泄密契约 | 测试 review 发现 JWT decode 失败用例只靠异常类型和 message 片段，无法发现 PyJWT 错误原因漂移、错误文本回显 token、subject/role claims 或不同 secret。现断言 exact exception type、`args`，并确认过期、错签名、畸形 token 错误不包含原 token、`u1`、`viewer` 或 `different-secret`，避免认证 token 解码测试只为异常覆盖率服务 |
| 2026-05-30 | N/A（异常路径弱断言复核） | `tests/unit/test_api/test_environments.py::test_environment_schema_rejects_unpinned_or_moving_base_images tests/unit/test_config.py::test_otel_sample_rate_bounds tests/unit/test_config.py::test_required_field_missing_raises tests/unit/test_worker/test_settings_tasks.py::test_get_worker_max_jobs_rejects_invalid_values tests/unit/test_api/test_sse.py::test_authenticate_sse_ticket_preserves_api_token_scopes` 17 passed | 无新增 warning；base image pinning、OTel sample rate、必需配置字段、worker max jobs 和 SSE ticket 二次消费不再只证明“抛了某类异常”，而是锁住字段 loc、错误消息、配置变量边界和 Redis `getdel` 调用序列 | `pytest.raises` 型用例容易退化成覆盖率测试：实现改成错误字段、泛化错误、或放宽校验仍可能通过。现把异常路径改成能发现具体契约漂移的断言 |
| 2026-05-30 | N/A（Worker max jobs env 错误契约） | `tests/unit/test_worker/test_settings_tasks.py::test_get_worker_max_jobs_rejects_invalid_values` 3 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只把 worker max jobs 非法值用例从 raises-only 补成 exact args 与不回显原始值契约 | 测试 review 发现 `_get_worker_max_jobs` 非法值用例只匹配错误片段，无法发现错误消息泛化、错误配置项名漂移，或把原始 env 值写进异常。现断言 `ValueError.args` 精确等于 `QAP_WORKER_MAX_JOBS must be ...`，错误包含配置项名但不包含 `0`、`-1`、`many`，避免 worker 并发配置测试只为异常覆盖率服务 |
| 2026-05-30 | N/A（T07 文档真相源防回流） | `tests/unit/test_release_quality_docs_contract.py::test_task_archives_reflect_webhook_and_result_filter_completion` 1 passed | 无新增 warning；`doc-conflict-audit.md` 不再保留“当前 main 仅 status / T07 分支未合入”的当前口径，release-quality contract 禁止旧 T07 状态回流 | 测试计划文档本身也会影响测试是否合理。现把 T07 已完成 status/suite/q 的真相源从任务包扩展到冲突审计档案，避免后续按过期文档重复补假任务 |
| 2026-05-30 | N/A（Rate limit fail-open 与 artifact RBAC 403） | `tests/unit/test_api/test_rate_limit.py::TestRateLimitMiddlewareDispatch::test_non_strict_endpoint_passes_when_redis_missing tests/unit/test_api/test_artifacts.py::TestDownloadArtifactRBAC::test_download_artifact_403_when_project_action_denied` 2 passed | 无新增 warning；非严格路径 Redis 缺失时必须原样返回 downstream response，artifact 下载 RBAC 拒绝固定 403 `Insufficient permissions`，并证明传入 RBAC 的 project_id/action/user/session 正确且不生成 S3 URL | 这两条是剩余 status-only 扫描的真实样本。现把“放行”和“拒绝”都落到调用边界，而不是只看最终状态码，避免限流 fail-open 或产物 RBAC 测试变成覆盖率 smoke |
| 2026-05-30 | N/A（Project duplicate/unauth 拒绝契约） | `tests/unit/test_api/test_projects.py::test_create_project_duplicate_slug tests/unit/test_api/test_projects.py::test_unauthenticated` 2 passed | 无新增 warning；重复 slug 固定返回 409 `Slug already exists` 且不 create/audit，未认证 project list 固定返回 401 `Missing Authorization header` 且不触碰 project repository | project 创建和列表是高频入口，状态码型测试无法发现错误信息漂移或未认证请求仍进入 repository。现补齐响应 detail 与副作用边界 |
| 2026-05-30 | N/A（Artifact 404 隐藏归属差异） | `tests/unit/test_api/test_artifacts.py::TestArtifactDownloadHelpers::test_get_artifact_or_404_not_found tests/unit/test_api/test_artifacts.py::TestArtifactDownloadHelpers::test_get_artifact_or_404_wrong_tenant tests/unit/test_api/test_artifacts.py::TestArtifactDownloadHelpers::test_get_artifact_or_404_run_not_found` 3 passed | 无新增 warning；artifact 缺失、artifact 指向跨租户 run、run 缺失三条 helper 路径均固定返回 404 `Artifact not found`，并保持 artifact/run repository 查询边界 | artifact 下载是对象访问安全边界，单纯 404 不能发现错误 detail 泄露 run/tenant 差异。现锁住统一隐藏响应和查询顺序，避免产物访问测试只停在状态码 smoke |
| 2026-05-30 | N/A（Run results status 查询枚举） | `tests/unit/test_api/test_runs.py::test_get_run_results_rejects_unknown_status` 1 passed | 无新增 warning；`/runs/{run_id}/results?status=unknown` 固定在 query.status 枚举校验阶段返回 422，错误信息列出合法 TestResult status，且不查询 run 或 test_result repository | T07 results 过滤依赖状态枚举契约，状态码型测试无法发现实现先查 run/RBAC 后才失败，或放宽 status 成任意字符串。现把请求参数校验和 repository 零触达一起锁住 |
| 2026-05-30 | N/A（SSE ticket legacy payload 原子消费） | `tests/unit/test_api/test_sse.py::test_authenticate_sse_ticket_rejects_legacy_payload_without_scopes` 1 passed | 无新增 warning；旧冒号分隔 ticket payload 固定返回 401 `Invalid or expired SSE ticket`，且通过 `sse_ticket:<ticket>` 调用 Redis `getdel` 一次，不回退到 `get`/`delete` 非原子消费 | SSE ticket 是一次性鉴权凭据，状态码型测试无法发现实现重新接受旧 payload、泄露解析原因，或破坏 GETDEL 原子消费。现锁住拒绝响应和 Redis 调用边界 |
| 2026-05-30 | N/A（RBAC project action 403 权限矩阵） | `tests/unit/test_api/test_rbac_deps.py::TestEnforceProjectAction::test_member_with_no_project_membership_is_denied tests/unit/test_api/test_rbac_deps.py::TestEnforceProjectAction::test_member_with_project_viewer_cannot_edit` 2 passed | 无新增 warning；member 无 project membership 与 project viewer 写操作两条拒绝路径不再只看 403，补齐 `Insufficient permissions` detail，并证明确实执行了带目标 project_id 的成员角色查询 | RBAC 是安全边界，状态码型测试无法发现实现未查询项目成员、查错项目或错误响应漂移。现锁住成员关系查询与权限矩阵拒绝结果，避免 project-scoped 权限测试只为覆盖率服务 |
| 2026-05-30 | N/A（RBAC non-project action 工厂边界契约） | `tests/unit/test_api/test_rbac_deps.py::TestEnforceProjectAction::test_non_project_scoped_action_raises_value_error tests/unit/test_api/test_rbac_deps.py::TestRequireProjectPermission::test_factory_rejects_non_project_scoped_action` 2 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只把 RBAC 非 project-scoped action 用例从 raises-only 补成 exact ValueError 与数据库零触达契约 | 测试 review 发现 RBAC 非 project-scoped action 用例只匹配 `non-project-scoped`，无法发现 imperative helper 和 FastAPI dependency factory 的错误来源被混淆、action 名丢失，或非法 action 仍继续进入 session 查询。现断言两条 `ValueError.args` 分别包含 `enforce_project_action` / `require_project_permission` 与 `Action.PROJECT_CREATE`，并证明 imperative helper 不 await `session.execute`，避免 RBAC 工厂边界测试只为异常覆盖率服务 |
| 2026-05-30 | N/A（RunExecutor setup/clone 失败短路契约） | `tests/unit/test_engine/test_executor.py::TestRunSetupContainerised::test_setup_nonzero_exit_raises tests/unit/test_engine/test_executor.py::TestRunSetupContainerised::test_setup_timeout_raises tests/unit/test_engine/test_executor.py::TestExecutorUsesSourcePlugin::test_clone_repo_propagates_error` 3 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只把 executor setup/clone 失败用例从 raises-only 补成 cleanup、active execution reset 和 repository 零写入契约 | 测试 review 发现 setup 失败用例只匹配 `Setup script failed/timed out`，无法发现非零退出或 timeout 后漏 cleanup、active execution id 残留，或 wait timeout 使用错误。clone 失败用例只匹配 `git clone failed`，无法发现 clone ref/URL 传错，或失败后仍 update git_sha/commit。现断言 exact RuntimeError args、create/start/wait/cleanup 参数、`_active_execution_id is None`，以及 clone 调用和 `update_git_sha/commit` 不被 await，避免 executor 失败路径测试只为异常覆盖率服务 |
| 2026-05-30 | N/A（Notification/Project 请求体验证字段级契约） | `tests/unit/test_api/test_notifications.py::test_update_rule_rejects_empty_channels tests/unit/test_api/test_projects.py::test_update_project_rejects_invalid_silent_windows` 6 passed | 无新增 warning；notification rule 空 channels 固定落在 `body.channels` min-length 错误，project silent_windows 五类非法 payload 固定落在字段级错误，并证明 project/credential/notification repository 与 audit 均无副作用 | 测试 leader 静态扫雷发现两条请求体验证用例仍偏状态码型。现补齐错误字段、错误原因和业务层零触达，避免实现先做项目访问/规则读取/凭据查询后才返回 422 时测试仍然假绿 |
| 2026-05-30 | N/A（Docker backend container 操作参数） | `tests/unit/test_engine/test_docker_backend.py::TestDockerBackend::test_cancel_sends_sigterm tests/unit/test_engine/test_docker_backend.py::TestDockerBackend::test_force_kill_sends_sigkill tests/unit/test_engine/test_docker_backend.py::TestDockerBackend::test_force_kill_ignores_already_stopped_conflict tests/unit/test_engine/test_docker_backend.py::TestDockerBackend::test_cleanup_deletes_container tests/unit/test_engine/test_docker_backend.py::TestDockerBackend::test_cleanup_ignores_already_removed_container` 5 passed | 无新增 warning；cancel/force_kill/cleanup 不再只证明 kill/delete 被 await，还锁住 Docker container lookup 使用传入 execution_id，409/404 幂等吞吐路径同样验证目标容器 id | engine 基础设施测试多为 mock 行为测试，容易漏掉传错 container id 的回归。现补齐容器操作参数证据，避免取消/清理测试只停留在“调用了某个 mock 方法” |
| 2026-05-30 | N/A（Docker backend 异常传播精确契约） | `tests/unit/test_engine/test_docker_backend.py::TestDockerBackend::test_network_mode_unknown_raises tests/unit/test_engine/test_docker_backend.py::TestDockerBackend::test_cancel_reraises_unexpected_docker_error tests/unit/test_engine/test_docker_backend.py::TestDockerBackend::test_cleanup_reraises_unexpected_docker_error` 3 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只把 Docker backend 三条异常用例从 raises-only 补成 exact network policy 与原 DockerError 透传契约 | 测试 review 发现 unknown network policy 只匹配错误片段，无法发现错误 policy 值丢失或错误默认到 bridge/none。cancel/cleanup unexpected DockerError 只匹配 `daemon exploded`，无法发现实现重包装异常、吞掉非 409/404 错误，或对错误 container id 调 kill/delete。现断言 `ValueError.args` 精确包含 `unknown`，cancel/cleanup 透传同一个 DockerError 对象，并锁住 `container("container-id")`、`kill(SIGTERM)`、`delete(force=True)`，避免 Docker backend 异常测试只为异常覆盖率服务 |
| 2026-05-30 | N/A（Rate limit strict auth 429 契约） | `tests/unit/test_api/test_rate_limit.py::TestRateLimitMiddlewareDispatch::test_register_endpoint_uses_strict_limit tests/unit/test_api/test_rate_limit.py::TestRateLimitMiddlewareDispatch::test_refresh_endpoint_uses_strict_limit tests/unit/test_api/test_rate_limit.py::TestRateLimitMiddlewareDispatch::test_sse_ticket_endpoint_uses_strict_limit` 3 passed | 无新增 warning；register/refresh/sse-ticket 超限不再只看 429，补齐 `TOO_MANY_REQUESTS` 响应体、`Retry-After` 使用 strict auth window、Redis bucket expire 写入同一 strict window，且 downstream 不执行 | 认证入口限流是安全控制面，状态码型测试无法发现响应契约漂移或错误使用普通 API window。现把 strict path 的窗口、bucket 和响应体一起锁住，避免限流测试只为覆盖率服务 |
| 2026-05-30 | N/A（SSE 拒绝路径 Redis 零副作用） | `tests/unit/test_api/test_sse.py::test_stream_logs_run_not_found_returns_404 tests/unit/test_api/test_sse.py::test_stream_logs_same_tenant_no_project_perm_returns_403 tests/unit/test_api/test_sse.py::test_stream_events_same_tenant_no_project_perm_returns_403` 3 passed | 无新增 warning；run 缺失固定 404 `Run not found`，同租户无项目权限固定 403 `Insufficient permissions`，三条拒绝路径均锁住 tenant-scoped run lookup，且不触碰 Redis `xread` / `hget` | SSE 日志/事件是长连接入口，状态码型测试无法发现权限拒绝后仍读取 Redis stream 或泄露 run 状态。现补齐响应契约和 Redis 零副作用，避免 SSE 拒绝路径测试退化成普通 HTTP smoke |
| 2026-05-30 | N/A（Project settings allowed_branches 请求体验证） | `tests/unit/test_api/test_projects.py::test_create_project_rejects_invalid_allowed_branches tests/unit/test_api/test_projects.py::test_update_project_rejects_invalid_allowed_branches` 6 passed | 无新增 warning；非法 `allowed_branches` 五类 create payload 与 update payload 不再只看 422，补齐 `body.settings` 字段级错误片段、project/credential repository 零调用和 audit 零副作用 | T06 webhook 分支过滤依赖 settings schema；静态扫雷发现这些负向测试只证明“返回 422”。现锁住请求体验证阶段短路，避免字符串被当作字符序列、空 pattern 或超长列表等回归被状态码型测试吞掉 |
| 2026-05-30 | N/A（Project member 跨租户拒绝路径） | `tests/unit/test_api/test_project_members.py::test_add_member_rejects_cross_tenant_user` 1 passed | 无新增 warning；跨租户 user 添加成员固定返回 422 `VALIDATION_ERROR` envelope，且锁住先验证项目租户、再查目标用户、不会查询 existing member、不会 create 或写 audit | 测试 leader 静态扫雷发现该用例只看 422，无法发现错误体契约漂移或实现先创建成员再失败。现补齐响应结构和 repository/audit 边界，避免成员管理负向测试停在状态码 smoke |
| 2026-05-30 | N/A（Credential 409 拒绝路径） | `tests/unit/test_api/test_credentials.py::test_create_credential_duplicate_name_returns_409 tests/unit/test_api/test_credentials.py::test_delete_credential_in_use_returns_409` 2 passed | 无新增 warning；duplicate name 与 in-use delete 两条 409 路径不再只看状态码，补齐精确 detail、project/credential 查询参数、crypto 不加密、delete/create/audit 零副作用 | 测试 leader 静态扫雷发现 credential 409 用例虽已有 no-write 断言，但错误契约和短路顺序仍不完整。现锁住 duplicate name 在加密前返回、delete in-use 在删除前返回，避免拒绝路径测试只证明“返回 409” |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_webhook_branch_dedup.py tests/integration/test_real_auth_results_artifacts.py::test_run_results_api_filters_real_rows_and_recovers_after_duplicate` | `tests/unit/test_release_quality_docs_contract.py::test_task_archives_reflect_webhook_and_result_filter_completion`、T06/T07 相关 API 单测 | 无新增 warning；T06/T07 任务包从过期待办改为验收档案，并由 release-quality contract 禁止“branch/dedup 两项缺失”或“results 只有 status 参数”的旧口径回流 | 整体 review 发现 webhook branch/dedup 与 test results suite/q 过滤已有生产代码、unit 和 required integration 证据，但任务包仍保留未勾选验收项，容易让后续测试计划重复验证已完成事项而漏掉真正剩余的 Git provider 矩阵。现同步任务索引和任务包状态，让文档测试也能发现这种“测试为覆盖率服务”的计划漂移 |
| 2026-05-30 | N/A（审计日志查询 API 文档真相复核） | `tests/unit/test_release_quality_docs_contract.py::test_docs_reflect_audit_events_query_api_completion` 1 passed | 无新增 warning；architecture、T02 任务档案、tasks index 和 doc-conflict-audit 均把 `/api/v1/audit-events` 标为已实现验收结果，剩余仅为正式 PRD 章节待补 | 整体 review 发现 audit-events 路由、unit/integration/performance 证据和 README 已存在，但 architecture 仍写“查询路由待 T02 补齐”，T02 任务包也保留未勾选验收项。现修正文档真相源并加 release-quality contract，避免后续继续按已完成路由补假任务 |
| 2026-05-30 | N/A（Git URL allow-path 断言复核） | `tests/unit/test_plugins/test_git_source.py::TestGitUrlValidation::test_allows_https_hostname_that_resolves_to_public_ip tests/unit/test_plugins/test_git_source.py::TestGitUrlValidation::test_allows_ssh_format_after_hostname_validation` 2 passed | 无新增 warning；允许公网 HTTPS/SSH Git URL 的用例不再只靠“不抛异常”，同时断言 hostname 被传入 DNS 解析校验 | 静态扫雷发现两个 no-assert 样本属于 SSRF 防护的 allow-path。现补 DNS 校验调用断言，避免实现绕过 hostname validation 时这些正向用例仍然假绿 |
| 2026-05-30 | N/A（状态码型弱断言复核） | `tests/unit/test_api/test_rate_limit.py::TestRateLimitMiddlewareDispatch::test_different_tokens_independent_quotas tests/unit/test_api/test_projects.py::test_unauthenticated tests/unit/test_auth/test_auth_routes.py::TestLogoutRevokesTokens::test_logout_no_tokens_still_returns_204` 3 passed | 无新增 warning；token A 被限流时只有 token B 会进入 downstream，未认证 project list 不触碰 project repository，无 token logout 不写 Redis revoke 且仍清理 refresh cookie | 测试 leader review 的静态扫雷发现少数状态码型样本；本轮区分误报 helper 断言与真实弱点，把剩余高信号样本补成副作用断言，避免这些用例只证明“返回了某个状态码” |
| 2026-05-30 | N/A（Webhook notification 错误脱敏） | `tests/unit/test_worker/test_channels.py` 32 passed | 无新增 warning；Webhook channel 的 HTTP 4xx/5xx 失败只记录 `webhook returned HTTP <status>`，网络异常只记录异常类型，notification log 不再接收 upstream response body 或异常文本中的 token/URL/userinfo | 测试 leader review 发现 DingTalk/WeCom 已有 token/key 泄露哨兵，但 generic Webhook 500 用例只断言包含状态码，允许把上游响应正文原样写进 `ChannelResult.error`。现补泄露断言并收窄错误信息，避免通知失败测试只证明“失败了”，却漏掉日志泄密 |
| 2026-05-30 | N/A（Auth register 422 请求体验证短路） | `tests/unit/test_auth/test_auth_routes.py::TestRegister::test_register_rejects_invalid_username tests/unit/test_auth/test_auth_routes.py::TestRegister::test_register_rejects_short_password tests/unit/test_auth/test_auth_routes.py::TestRegister::test_register_rejects_bad_email` 7 passed；`tests/unit/test_auth/test_auth_routes.py` 47 passed | 无新增 warning；非法 username/password/email 都固定为 body 字段级 422，且不打开 session_factory、不创建 tenant/user、不写 audit、不 commit/rollback | 测试 leader review 发现注册请求体验证用例只断言 422，无法发现实现把参数校验挪进业务层后先创建 tenant/user 或写审计再失败。现统一锁住 session、repository、audit 零副作用，避免注册校验测试只是覆盖率型状态码用例 |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_api_endpoints.py::TestProjectsCRUD::test_delete_project tests/integration/test_api_endpoints.py::TestNotificationRules::test_delete_notification_rule` 2 passed | N/A | 无新增 warning；project delete 与 notification rule delete 不再只证明 204/404，还证明真实 DB 行被 soft-delete、`audit.event` 写入删除前状态、删除审计没有 after_state，且 notification webhook URL 不泄露到审计 before_state | 测试 leader review 发现两个集成删除用例以前只看 HTTP 结果，无法发现实现改成硬删/未写审计/审计泄露渠道配置仍然绿。现把断言落到真实 PostgreSQL 行与 audit schema，避免 CRUD 集成测试退化成状态码冒烟 |
| 2026-05-30 | N/A（Rate limit UUID 路径归一化） | `tests/unit/test_api/test_rate_limit.py` 28 passed | 无新增 warning；不同 run UUID 的同一路径会写入同一个 `rate_limit:ip:...:/api/v1/runs/{id}/logs` Redis bucket，且 `zremrangebyscore`、`zadd`、`zcard`、`expire` 均不包含原始资源 ID | 测试 leader review 发现限流测试已覆盖 token/IP 分桶和严格认证路径，但“UUID 归一化防止按资源 ID 绕过”只有生产代码注释，没有用例能发现实现回退为 raw path。现补齐 Redis pipeline key 级断言，避免限流测试只证明某个请求被 429，而漏掉同一路由多资源枚举绕过 |
| 2026-05-30 | N/A（Notification rule 422 请求体验证短路） | `tests/unit/test_api/test_notifications.py` 17 passed | 无新增 warning；非法 notification channel/condition payload 在请求体验证阶段固定 422，不进入 project access、notification rule repository 或 audit 写入 | 测试 leader review 发现 notification rule 多条 422 用例虽已断言不 create/get rule，但还不能发现实现先做项目访问/RBAC 或读取规则后才返回请求体验证错误。现用统一 helper 锁住 duplicate/unsupported/non-object channel config、非法 condition、未文档化 condition alias、update 空 channels 等路径都不触碰业务 repository/audit，避免 422 测试只证明“报错了” |
| 2026-05-30 | N/A（Run trigger archived project 拒绝路径） | `tests/unit/test_api/test_runs.py::test_trigger_run_archived_project_returns_409` 1 passed；`tests/unit/test_api/test_runs.py` 28 passed | 无新增 warning；archived project 手动触发固定 409，且只完成 pipeline/project 归属查询，不查 environment、不创建 Run、不设置 retry_group、不入队、不写 audit | 测试 leader review 发现 archived project 用例只断言 409 和文案包含 archived，无法发现实现把拒绝检查挪到 Run 创建或入队之后。现补齐 repository、queue 和 audit 零副作用断言，避免归档项目保护测试只是状态码 smoke |
| 2026-05-30 | N/A（Admin status 403 安全契约） | `tests/unit/test_api/test_admin.py` 4 passed；`tests/unit/test_frontend_api_contract.py tests/unit/test_release_quality_docs_contract.py` 55 passed | 无新增 warning；`GET /api/v1/admin/status` 非平台管理员拒绝路径继续固定返回 403 `Platform admin required` 且不打开 repositories，OpenAPI 同步声明 403 `detail` 响应形状 | 测试 leader review 发现 admin/status 运行测试已证明非 admin no-repo 短路，但路由契约未声明 403，客户端和安全测试计划无法从 OpenAPI 识别该拒绝路径。现补齐 403 schema，并新增单测锁住契约，避免权限拒绝测试只停在运行时状态码 |
| 2026-05-30 | N/A（Webhook provider 非 Run 响应契约） | `tests/unit/test_api/test_p3.py::test_webhook_routes_document_actual_non_run_responses tests/unit/test_api/test_p3.py::TestProviderWebhookTrigger` 4 passed；`tests/unit/test_api/test_p3.py` 42 passed | 无新增 warning；项目 webhook 与 GitHub provider webhook 的 `200 filtered/duplicate`、provider `202 ignored`、`400/401/409 detail` 响应已在 OpenAPI 中声明为实际响应形状，provider 无效 JSON、忽略事件、缺签名都不会继续创建 Run 或写 audit | 测试 leader review 发现 webhook 单测覆盖了部分运行分支，但契约层仍只声明 201/少数错误，且 provider 入口的 400/202/401 拒绝路径缺少路由级短路证据，客户端和测试都可能误以为这些分支会返回 Run 或统一 ErrorResponse。现补齐真实 OpenAPI schema，并用路由级单测锁住 invalid JSON/unsupported event/missing signature 的响应体与 no repo/no run/no audit 边界 |
| 2026-05-30 | N/A（Project git credential 业务 422 契约） | `tests/unit/test_api/test_projects.py tests/unit/test_api/test_project_members.py` 37 passed；`tests/unit/test_frontend_api_contract.py tests/unit/test_release_quality_docs_contract.py` 55 passed | 无新增 warning；手动抛出的业务 422 不再被统一 handler 泛化为 `Request validation failed`，project create/update 的 git credential 绑定错误会返回具体 message，且 OpenAPI 声明 422 `ErrorResponse` | 测试 leader review 发现 projects 多个 422 用例只断言状态码，无法发现 `HTTPException(422, detail=...)` 被全局 422 handler 当成请求体验证错误处理，导致业务 detail 丢失。现 422 handler 区分 Pydantic validation list 与业务字符串 detail，测试锁住 create 阶段 credential 绑定、update credential type mismatch、token auth + SSH URL 三条业务错误的精确信息和无 update/audit 副作用 |
| 2026-05-30 | N/A（Credential/Environment crypto 503 契约） | `tests/unit/test_api/test_credentials.py` 12 passed；`tests/unit/test_api/test_environments.py` 18 passed | 无新增 warning；crypto service 未初始化时，credential create/rotate 与 environment list/create/get/update/delete 固定返回 503 `Crypto service not initialised`，且 OpenAPI 同步声明 503 `ErrorResponse` | 测试 leader review 发现 credentials/environments 的运行代码已有 crypto service unavailable 503，但路由 OpenAPI 未声明该失败路径，且单元层没有证明该路径不会创建/更新敏感资源或写 audit。现补齐 503 schema，并用单测锁住项目校验后的短路边界、credential/environment repository 零写入和 audit 零副作用 |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_cancel_e2e.py::test_repeated_http_cancel_is_idempotent --collect-only` 1 test collected | `tests/unit/test_api/test_runs.py::test_cancel_already_terminal tests/unit/test_release_quality_docs_contract.py::test_repeated_http_cancel_assertion_is_exact_terminal_conflict` 2 passed | 无新增 warning；重复 HTTP cancel 第二次固定 409，并要求响应 detail 为 `Run already in terminal status: cancelled` | 测试 leader review 发现 heavy cancel E2E 仍接受 `200 或 409`，这会把“重复取消被当成成功并可能重复发布 Redis 事件/审计”的回归吞掉；当前 API 契约是首次取消写入 terminal `cancelled` 后，再次取消应返回终态冲突 409。现收紧集成断言，并用轻量 release-quality contract 禁止 `{200, 409}` 宽断言回流 |
| 2026-05-30 | N/A（Artifact download 503 契约/拒绝路径） | `tests/unit/test_api/test_artifacts.py` 11 passed | 无新增 warning；对象存储未配置时，artifact download 会先完成 artifact/run 归属查询与 project RBAC，再返回 503 `Artifact download is not available`，且 OpenAPI 同步声明 503 `ErrorResponse` | 测试 leader review 发现真实集成已覆盖 storage unavailable 503，但 route OpenAPI 只声明 403/404，客户端契约层无法知道这条失败路径；同时单元层缺少“存储不可用发生在权限之后”的直接证据。现补 OpenAPI 503 响应声明，单测锁住查询/RBAC 顺序和响应 detail，避免产物下载拒绝路径只停留在状态码 smoke |
| 2026-05-30 | N/A（Auth middleware JWT claims 拒绝路径） | `tests/unit/test_auth_middleware.py` 21 passed | 无新增 warning；已签名但缺少 `sub` / `tenant_id` 的 access token、以及 platform-admin claim 携带非 UUID subject 时，统一返回 401 `Invalid token claims`，且不触发 Redis blacklist 或 DB session | 测试 leader review 发现 auth middleware 多个 401 用例只覆盖过期、无效签名、refresh token type，却没有覆盖“签名有效但 claims 结构坏”的路径；当前实现会在 `payload["sub"]` / `payload["tenant_id"]` 或 platform-admin UUID 转换处抛 500。现中间件先校验必需 claims，platform-admin DB 复核前校验 UUID，并用单测锁住坏 claims 不碰 Redis/DB |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_auth_audit_failure_paths.py::test_refresh_with_invalid_token_returns_401_not_500 tests/integration/test_auth_audit_failure_paths.py::test_refresh_invalid_token_returns_401_when_audit_write_fails` 2 passed | `tests/unit/test_auth/test_auth_routes.py::TestRefresh::test_refresh_missing_cookie tests/unit/test_auth/test_auth_routes.py::TestRefresh::test_refresh_with_access_token_rejected tests/unit/test_auth/test_auth_routes.py::TestRefresh::test_refresh_expired_token tests/unit/test_auth/test_auth_routes.py::TestRefresh::test_refresh_missing_user_clears_cookie_without_revoking_token` 4 passed | 无新增 warning；refresh 失败路径不再只看 401，补齐 detail、失败审计 reason、refresh cookie 清理、缺 cookie 不开 session、用户缺失不 revoke 旧 jti 且回滚 session | 测试 leader review 发现 access-token-as-refresh / expired refresh / missing user 分支调用 `_clear_refresh_cookie(response)` 后抛 `HTTPException`，异常响应实际没有携带删除 cookie header，旧 refresh cookie 会留在客户端。现失败 401 显式带 `Set-Cookie` 清理头，unit 锁住 decode 失败、token type 错误和用户缺失三条路径的副作用边界，真实 DB/audit 集成进一步验证 invalid token 及 audit-write-fails 分支都清理 refresh cookie |
| 2026-05-30 | N/A（RBAC project path 参数解析） | `tests/unit/test_api/test_rbac_deps.py::TestRequireProjectPermission::test_invalid_uuid_raises_422 tests/unit/test_api/test_rbac_deps.py::TestRequireProjectPermission::test_missing_project_id_param_raises_500` 2 passed | 无新增 warning；`require_project_permission` 的非法 UUID / 缺失 path param 不再只看状态码，补齐精确 detail 和 session 查询零调用 | 测试 leader review 发现 RBAC dependency 参数解析错误只断言 422/500，不能发现非法 path param 仍继续进入 project/member 查询，或错误信息漂移导致调用方排障困难。现两条路径都锁住解析阶段短路，不触碰数据库查询 |
| 2026-05-30 | N/A（Analytics pagination 请求参数短路） | `tests/unit/test_api/test_p3.py::TestAnalytics::test_trends_rejects_invalid_pagination tests/unit/test_api/test_p3.py::TestAnalytics::test_flaky_rejects_invalid_pagination` 2 passed | 无新增 warning；analytics trends/flaky 无效 `offset`/`limit` 不再只看 422，补齐 Pydantic query `loc` 和 project/trend/flaky/history repository 零调用 | 测试 leader review 发现 analytics invalid pagination 用例只断言 422，无法发现路由在参数非法时仍查 project、做 RBAC/session 查询或触发聚合 repository。现用例锁住请求参数校验在进入业务查询前短路，避免参数校验测试只证明“返回了一个 422” |
| 2026-05-30 | N/A（Run detail/archive 拒绝路径） | `tests/unit/test_api/test_runs.py::test_get_run_not_found tests/unit/test_api/test_runs.py::test_get_archived_run_logs_returns_503_without_s3` 2 passed | 无新增 warning；run 详情缺失不再只看 404，归档日志存储未配置不再只看 503，补齐统一 `NOT_FOUND / Run not found` error envelope、tenant-scoped run lookup，以及无 S3 client 时不进入 `LogStream` | 测试 leader review 发现 run detail 404 不能发现 `get_for_tenant(run_id, tenant_id)` 参数漂移或错误体契约漂移；归档日志 503 不能发现实现提前读取归档流、绕过 run/tenant 查询，或把存储未配置误包装成其他响应。现两条拒绝路径锁住资源查询顺序、响应契约和无外部日志读取副作用 |
| 2026-05-30 | N/A（Auth login failure 单元拒绝路径） | `tests/unit/test_auth/test_auth_routes.py::TestLogin::test_login_wrong_password tests/unit/test_auth/test_auth_routes.py::TestLogin::test_login_user_not_found tests/unit/test_auth/test_auth_routes.py::TestLogin::test_login_inactive_user tests/unit/test_auth/test_auth_routes.py::TestAuditLogin` 6 passed | 无新增 warning；登录失败路径不再只看 401，补齐响应体、无 access/refresh token、无 refresh cookie、无 last_login 更新，以及 `auth.login_failed` 审计 reason/username/user_id/tenant_id | 测试 leader review 发现 wrong-password / user-not-found / inactive-user 三条单测只断言 401，无法发现失败路径误发 token、错误设置 refresh cookie、更新 last_login、审计原因漂移，或 inactive 账户被错误归类为普通密码错误。现三条路径都锁住失败响应和副作用边界，审计专测继续覆盖成功/失败事件 |
| 2026-05-30 | N/A（Credential 404 单元拒绝路径） | `tests/unit/test_api/test_credentials.py::test_get_credential_404_when_missing tests/unit/test_api/test_credentials.py::test_delete_credential_404_when_missing` 2 passed | 无新增 warning；credential get/delete 404 不再只看状态码，补齐 `NOT_FOUND / Credential not found` error envelope、project/tenant/credential 查询参数和 delete/audit 零副作用 | 测试 leader review 发现凭据详情/删除缺失凭据时只断言 404，不能发现跨 project/tenant 查询参数漂移、删除缺失凭据仍调用 delete，或拒绝路径误写 audit。现两条路径锁住统一 error envelope 和 repository await 参数，删除路径额外断言无 delete/audit 副作用 |
| 2026-05-30 | N/A（Project 404 单元拒绝路径） | `tests/unit/test_api/test_projects.py::test_get_project_not_found tests/unit/test_api/test_projects.py::test_update_project_not_found tests/unit/test_api/test_projects.py::test_delete_project_not_found` 3 passed | 无新增 warning；project get/update/delete 404 不再只看状态码，补齐 `NOT_FOUND / Project not found` error envelope、租户查询参数和 update/delete/audit 零副作用 | 测试 leader review 发现 project not-found 单测只断言 404，无法发现 `get_for_tenant(project_id, tenant_id)` 参数漂移、错误体契约漂移、更新/删除不存在项目时仍调用 update/delete，或拒绝路径误写 audit。现三条路径都锁住统一 error envelope、tenant-scoped 查询参数，以及拒绝路径不触碰 credential、update/delete 或 audit |
| 2026-05-30 | N/A（RunTrigger 请求体验证短路） | `tests/unit/test_api/test_runs.py::test_trigger_run_priority_validation` 1 passed；`tests/unit/test_release_quality_docs_contract.py::test_testing_docs_capture_run_priority_validation_contract` 1 passed | 无新增 warning；非法 run priority 422 会在请求体校验阶段短路，不触碰 pipeline/project/environment/run repository 或 audit 写入 | 测试 leader review 发现 `priority=3` 用例只断言 422，不能发现校验顺序回退后先查 pipeline/project、创建 Run 或写审计再报错。现用例断言 Pydantic error 定位到 `body.priority`，并锁住 repository/audit 副作用为零，避免参数校验测试只证明“返回了 422” |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_worker_execute.py --collect-only` 8 tests collected | `tests/unit/test_release_quality_docs_contract.py::test_worker_external_stack_creation_assertions_are_exact` 1 passed | 无新增 warning；external-stack worker 创建/触发请求精确断言 201，不再接受 `200 或 201` | 测试 leader review 发现 worker 外部栈黑盒用例中 project/environment/pipeline/run 的 POST 创建请求允许 `200/201`，但对应 API 契约声明创建成功必须返回 201，且用例本身使用随机 suffix，不应以幂等复用为理由放宽。现所有 external-stack worker 创建/触发请求都固定断言 201，并用轻量 contract 测试禁止 `status_code in (200, 201)` 回流，避免创建接口状态码退化被重型黑盒链路漏掉 |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_auth_results_artifacts.py::test_auth_token_routes_require_authorization_without_side_effects` 1 passed | `tests/unit/test_auth/test_auth_routes.py::TestTokenRoutes::test_token_routes_require_authentication` 3 passed | 无新增 warning；API token create/list/delete 缺失 Authorization 固定 401，且真实 DB 不新增 token 或 create/revoke audit | 测试 leader review 发现 API token create 无认证单测接受 `401 或 403`，会把认证失败和授权失败混为一谈，且没有覆盖 list/delete。现单测参数化锁住 create/list/delete 均返回 `Missing Authorization header` 401；真实 FastAPI/PostgreSQL 集成同时断言无认证请求不会创建 ApiToken，也不会写 `auth.api_token_create` / `auth.api_token_revoke` 审计副作用 |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_auth_results_artifacts.py::test_real_sse_logs_resume_after_last_event_id_uses_ticket_rbac_and_redis` 1 passed；合并通知切片后 `RUN_INTEGRATION_TESTS=1 tests/integration/test_notification_delivery.py tests/integration/test_api_endpoints.py::TestNotificationRules tests/integration/test_real_api_write_state.py::test_notification_rule_api_persists_updates_and_hides_soft_deleted_rule tests/integration/test_real_auth_results_artifacts.py::test_real_sse_logs_resume_after_last_event_id_uses_ticket_rbac_and_redis` 17 passed | `tests/unit/test_api/test_sse.py` 15 passed；`tests/unit/test_api/test_sse.py tests/unit/test_release_quality_docs_contract.py` 35 passed；合并通知切片后 `tests/unit/test_api/test_notifications.py tests/unit/test_worker/test_notifications.py tests/unit/test_frontend_api_contract.py tests/unit/test_release_quality_docs_contract.py tests/unit/test_api/test_sse.py` 114 passed；targeted ruff passed；targeted diff/trailing whitespace check passed | 无新增 warning；SSE 缺失 ticket 统一返回 401，且不触碰 Redis GETDEL | 测试 leader review 发现 SSE “无 ticket”单测接受 `401 或 422`，参数校验错误也能过，无法发现认证入口语义退化。现 `_authenticate_sse_ticket` 对缺失/空 ticket 显式返回 `Invalid or expired SSE ticket` 401，unit 断言不调用 Redis，真实 JWT/Postgres/Redis 集成在日志续传路径中补缺失 ticket 401 证据，external-stack smoke 也收紧为只接受 401 |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_notification_delivery.py tests/integration/test_api_endpoints.py::TestNotificationRules tests/integration/test_real_api_write_state.py::test_notification_rule_api_persists_updates_and_hides_soft_deleted_rule` 16 passed | `tests/unit/test_worker/test_notifications.py tests/unit/test_api/test_notifications.py` 48 passed；`tests/unit/test_api/test_notifications.py tests/unit/test_worker/test_notifications.py tests/unit/test_frontend_api_contract.py tests/unit/test_release_quality_docs_contract.py` 99 passed；targeted ruff passed；targeted diff check passed | 无新增 warning；兼容旧扁平 channel payload，同时把 API 新写入统一成 worker/frontend 使用的 `config` 形状，把 OpenAPI 从自由 dict 收紧到 `NotificationChannelPayload` 和 `NotificationCondition*`，并让无效条件 fail-closed；历史脏 condition response 标记 invalid 而不是 500 | 测试 leader review 发现 notification API 测试只验证扁平 `webhook_url` / `address` 能存取，worker delivery 测试却直接手写 canonical `config` DB 行，无法发现 API 创建的 notification rule 实际发送时拿到空 config；若历史 DB 已存扁平 channel，list/get response 也可能继续给前端旧形状；同时 OpenAPI 仍把 `channels` 暴露成自由 object，前端 contract 无法证明 channel 字段。现将 API create/update/response 归一化为 `{type, config}`，worker 发送前也归一化历史扁平 channel，OpenAPI `NotificationRuleCreate/Response.channels` 改为 `NotificationChannelPayload` 引用，contract 测试锁住 `type/config/template` 和渠道枚举；真实 FastAPI → PostgreSQL → worker delivery 回归断言 API 创建的 webhook rule 会把 `config.url` 传给 `_send_channel` 且写入 sent log，另用真实 DB 旧 JSON 行证明 list API 会输出 canonical response。继续复核发现 condition typo 曾在 worker 中 warning 后返回 true，可能让写错的规则误触发；现 API create/update 拒绝未知字段/未知 operator/空 group，并把 OpenAPI condition schema 收紧到 canonical 字段/operator，未文档化 `consecutive_failed_runs` alias 也在 API 写入前 422；历史脏条件在 worker fail-closed 且不会写误导性 notification log。进一步复核 response schema 后发现旧库未知字段/坏 operator 会因 typed response validation 导致 list/get 500；现 response-only `NotificationInvalidCondition` 暴露 `invalid/reason/raw_field/raw_operator`，前端编辑表单只消费合法 leaf 条件，新写入仍保持 422，真实 DB 集成覆盖历史脏行可读 |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_repository_matrix.py::test_schedule_repository_due_query_ignores_soft_deleted_schedules` 1 passed；`RUN_INTEGRATION_TESTS=1 tests/integration/test_real_repository_matrix.py` 9 passed；`RUN_INTEGRATION_TESTS=1 tests/integration/test_silent_windows.py` 7 passed | `tests/unit/test_worker/test_check_schedules.py` 9 passed；targeted ruff passed | 无新增 warning；本轮不改 schedule worker 正常触发语义，只补 repository due-query 的 soft-delete 隔离 | 测试 leader review 发现 worker 单测把 `ScheduleRepository.find_due_schedules()` mock 掉，只证明“拿到 due schedule 之后怎么处理”，无法发现真实查询把已软删除 Schedule 继续返回给 cron tick。现为 due query 增加 `Schedule.deleted_at IS NULL`，并新增真实 Postgres 测试同时放入 visible due、soft-deleted due、future 三类 schedule，断言只返回未删除且到期的 schedule，避免调度测试只是覆盖 worker 外壳；同时复跑真实 schedule worker 集成切片确认正常触发/静默窗口路径未被误伤 |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_repository_matrix.py tests/integration/test_real_db_persistence.py::test_run_repository_conditional_updates_are_persisted_and_idempotent` 9 passed；`tests/integration/test_real_repository_matrix.py::test_run_repository_state_writes_ignore_soft_deleted_runs tests/integration/test_real_repository_matrix.py::test_run_repository_state_machine_persists_all_terminal_paths` 2 passed | `tests/unit/test_engine/test_executor.py tests/unit/test_engine/test_reclaim.py tests/unit/test_worker/test_scheduler.py tests/unit/test_worker/test_tasks.py tests/unit/test_worker/test_settings_tasks.py` 101 passed；targeted ruff passed | 无新增 warning；本轮不改正常运行状态机，只补 soft-deleted run 的状态/元数据写入隔离 | 测试 leader review 发现原 repository 状态机测试只证明 queued/preparing/running/collecting 到终态的正常流转，无法发现已软删除 Run 被 worker/cancel/reclaimer/queue metadata 写入继续改动。现为 `RunRepository` 的 claim、mark_running、mark_collecting、finish/fail/cancel/timeout、mark_worker_lost、mark_enqueued、mark_waiting、set_retry_group_id、update_git_sha、update_execution_id、release_worker 增加 `Run.deleted_at IS NULL` 写入保护，并新增真实 Postgres 测试断言 soft-deleted run 保持原状态、worker/queue/git/execution 字段不被污染 |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_api_endpoints.py::TestAnalytics` 3 passed | `tests/unit/test_api/test_p3.py::TestAnalytics tests/unit/test_architecture_boundaries.py tests/unit/test_release_quality_docs_contract.py` 38 passed；`tests/unit/test_api/test_p3.py tests/unit/test_api/test_runs.py tests/unit/test_architecture_boundaries.py tests/unit/test_release_quality_docs_contract.py` 97 passed；targeted ruff passed；targeted diff check passed；`src/qaplatform/api` 直接 SQLAlchemy 扫描无命中 | 无新增 warning；本轮不改 analytics 响应契约；除聚合 SQL 下沉外，补上 soft-deleted run 不得污染聚合结果的真实 DB 回归 | 测试 leader review 完成 T-ARCH-LAYERS route 层直查收敛：`api/v1/analytics.py` 的 trends/flaky/test-history 聚合改走 `RunRepository.list_trend_points()` 与 `TestResultRepository.list_flaky_tests()` / `list_test_history()`；repository 查询显式排除 `Run.deleted_at`，integration 在趋势、flaky、单用例历史三条路径都插入 soft-deleted run 并断言总数、失败数、flaky rate 和历史点不被污染；unit 测试禁止 analytics route 层 `session.execute()`，并断言 repository 参数、分页与响应形状；架构边界测试把 analytics 加入不得内联执行 SQLAlchemy 查询的清单 |
| 2026-05-30 | N/A（T-ARCH-LAYERS runs 查询下沉） | `tests/unit/test_api/test_runs.py::test_list_runs tests/unit/test_api/test_runs.py::test_list_runs_member_user_uses_project_member_repository tests/unit/test_api/test_runs.py::test_list_runs_member_user_with_no_projects_returns_empty_without_querying_runs tests/unit/test_architecture_boundaries.py tests/unit/test_release_quality_docs_contract.py` 33 passed；`tests/unit/test_api/test_runs.py tests/unit/test_architecture_boundaries.py tests/unit/test_release_quality_docs_contract.py` 58 passed；targeted ruff passed；targeted diff check passed；`api/v1/runs.py` / auth / deps / admin 直接 SQLAlchemy 扫描无命中；API 剩余直接查询扫描仅命中 `api/v1/analytics.py` | 无新增 warning；本轮不改运行列表业务语义，只把成员项目过滤查询下沉到 repository 层 | 测试 leader review 继续推进 T-ARCH-LAYERS：`api/v1/runs.py` 的跨项目列表成员项目 ID 查询改走 `ProjectMemberRepository.list_project_ids_by_user()`；新增非管理员列表测试禁止 route 层 `session.execute()`，同时覆盖“有成员项目时传入 `RunRepository.list` project_id IN filter”和“无成员项目时直接返回空分页且不查 runs”两条可发现问题的路径；架构边界测试同步锁住 `api/v1/runs.py` 不得重新内联 SQLAlchemy 查询 |
| 2026-05-30 | N/A（T-ARCH-LAYERS auth 查询下沉） | `tests/unit/test_auth_middleware.py tests/unit/test_auth/test_auth_routes.py tests/unit/test_architecture_boundaries.py tests/unit/test_release_quality_docs_contract.py` 90 passed；`tests/unit/test_auth tests/unit/test_auth_middleware.py tests/unit/test_api/test_rbac_deps.py tests/unit/test_api/test_admin.py` 147 passed；`tests/unit/test_release_quality_docs_contract.py tests/unit/test_architecture_boundaries.py` 29 passed；targeted ruff passed；targeted diff check passed；`api/auth/middleware.py` / `api/v1/auth.py` / `api/deps.py` / `api/v1/admin.py` 直接 SQLAlchemy 扫描无命中；API 剩余直接查询扫描仅命中 `api/v1/analytics.py` / `api/v1/runs.py` | 无新增 warning；本轮不改认证业务语义，只把 auth 直查下沉到 repository 层并把测试改成能发现分层回流 | 测试 leader review 继续推进 T-ARCH-LAYERS：`api/auth/middleware.py` 的平台管理员 claim 复核改走 `UserRepository.is_platform_admin()`，`api/v1/auth.py` 的租户注册冲突检查、租户创建、用户创建与单租户 fallback 改走 `TenantRepository` / `UserRepository`；auth middleware/register/fallback 测试现在显式禁止 route 层 `session.execute()`，架构边界测试同步锁住 auth/deps/admin 入口不得重新内联 SQLAlchemy 查询 |
| 2026-05-30 | N/A（T-ARCH-LAYERS API 查询下沉第一组） | `tests/unit/test_api` 264 passed；`tests/unit/test_api/test_admin.py tests/unit/test_api/test_rbac_deps.py tests/unit/test_architecture_boundaries.py tests/unit/test_release_quality_docs_contract.py` 45 passed；targeted ruff passed；`api/deps.py` / `api/v1/admin.py` 直接 SQLAlchemy 扫描无命中 | 无新增 warning；本轮不改业务语义，只把已知 API 直接查询下沉到 repository 层 | code-review 继续推进 T-ARCH-LAYERS 第二阶段：`api/deps.py` 的项目可见性和 ProjectMember 角色查询改走 `ProjectRepository` / `ProjectMemberRepository`，`api/v1/admin.py` 的 queue/in-flight/1h success-rate 计数改走 `RunRepository.count_by_statuses()` / `count_finished_since_by_statuses()`；admin 非平台管理员测试继续证明权限短路先于 repository 打开；新增架构边界测试锁住 `api/deps.py` 和 `api/v1/admin.py` 不得重新直接 `session.execute()` / `db.execute()`。当时剩余直接 SQLAlchemy 查询集中在 `api/auth/middleware.py`、`api/v1/auth.py`、`api/v1/analytics.py`、`api/v1/runs.py` |
| 2026-05-30 | N/A（T-ARCH-LAYERS engine 反向依赖收敛） | `tests/unit/test_architecture_boundaries.py tests/unit/test_lifespan.py tests/unit/test_worker/test_settings_tasks.py tests/unit/test_engine/test_executor.py tests/unit/test_engine/test_reclaim.py tests/unit/test_worker/test_tasks.py tests/unit/test_release_quality_docs_contract.py` 122 passed；targeted ruff passed；targeted diff check passed；engine import 扫描无 `qaplatform.api` / `qaplatform.worker` 命中 | 无新增 warning；本轮只做分层边界收敛，不改业务语义 | code-review 发现 TODO/architecture 记录的 `T-ARCH-LAYERS` 仍停在“engine 反向依赖 `api.metrics` / `worker._redact`”状态。现把 Prometheus 指标定义迁到 `observability.metrics`，`api.metrics` 只保留 `/metrics` endpoint 与兼容 re-export；`main.py` / `engine/executor.py` 改用中立指标模块，`engine/reclaim.py` 改用 `engine.redact`，worker 内部失败脱敏也直接走 `engine.redact`；新增架构边界单测防止 engine 重新 import API / worker。剩余技术债缩小为 API 路由直接 SQLAlchemy 查询下沉到 repositories / query service |
| 2026-05-30 | `RUN_INTEGRATION_TESTS=1 tests/integration -m "not heavy_docker and not external_stack and not performance"` 152 passed / 51 deselected；collect-only 203 tests；Playwright `--list` 10 tests / 4 files | `tests/unit` 980 passed；coverage 84.71%（statements 87.11%，branches 73.76%） | 无新增 warning；本轮不改运行代码，只修正测试审计文档中的陈旧用例数/覆盖率，并登记前端组件/Hook 测试缺口 | 测试 leader review 发现 backend-test-audit 仍引用 758 passed、83.37%、integration 186/138 等旧证据，且没有把前端缺组件级测试的风险落到测试策略。现将当前 unit/coverage/required integration/Playwright 清单写回文档，并明确 PR 级 mocked auth-flow 不能替代 trigger modal、settings token、notification rules、analytics panel 等组件行为测试 |
| 2026-05-30 | N/A（Maintainer 决策表待确认/已决策拆分复核） | `tests/unit/test_release_quality_docs_contract.py` 15 passed；`tests/unit` 980 passed；targeted ruff passed；targeted diff check passed | 无新增 warning；本轮不改运行代码，只拆分 TODO 与 doc-conflict-audit 的 maintainer 决策承接口径 | 测试 leader review 发现 TODO 的“Maintainer 决策待确认”表混有独立审计清理、Pipeline collector、F-NT-01 条件通知和 F-EX-05 归档日志回看等已实现/已决策事项，容易让后续测试计划把关闭项继续排成待确认。现拆成“仍需确认”和“已决策 / 已实现承接”，并用文档契约锁住已关闭事项不得回流到待确认区 |
| 2026-05-30 | N/A（Manual trigger 档案状态复核） | `tests/unit/test_release_quality_docs_contract.py` 14 passed；`tests/unit` 979 passed；targeted ruff passed；targeted diff check passed | 无新增 warning；本轮不改手动触发运行代码，只把 `doc-conflict-audit` §15 的 `T-MANUAL-TRIGGER` 状态从历史“未处理”对齐为当前已完成证据 | 测试 leader review 发现 TODO 已把 F-EX-01 手动触发参数与入队验收标为完成，源码和单测/integration 也已锁住 `pipeline_id`、`git_ref`、40 位 `git_sha`、`environment_id`、`priority`、queue/job id、`mark_enqueued` 与 `run.trigger` audit，但审计档案后续任务表仍保留未处理状态。现补 release-quality 文档契约，防止后续测试计划把已完成的手动触发验收再次当成缺口 |
| 2026-05-29 | N/A（OpenTelemetry 文档真相契约复核） | `tests/unit/test_release_quality_docs_contract.py` 13 passed；`tests/unit/test_observability/test_tracing.py` 8 passed；`tests/unit` 978 passed；targeted ruff passed；targeted diff check passed | 无新增 warning；本轮不改 OpenTelemetry 运行代码，只把 README、architecture、TODO、feature catalog、T10 任务包与 doc-conflict-audit 修复进度从“未实现/待装配”修正为“基础 tracing 已落地，OTLP exporter/部署验证待收口” | 测试 leader review 发现实时真源文档仍把 tracing provider 和 FastAPI instrumentation 描述成未落地，和 `src/qaplatform/observability/tracing.py`、`main.py`、`worker/settings.py` 及 tracing 单测相冲突，容易让后续测试继续围绕已完成缺口补假覆盖。现新增 release-quality 文档契约锁住 OpenTelemetry 的部分完成状态、已落地入口、历史档案进度段和剩余 exporter/接收端验证缺口，避免文档驱动的待办再次误导测试计划 |
| 2026-05-29 | N/A（Notification/ProjectMember 404/409 状态码式测试复核） | `tests/unit/test_api/test_notifications.py tests/unit/test_api/test_project_members.py` 20 passed；targeted API 404/409 3 passed；`tests/unit` 977 passed；targeted ruff passed | 无新增 warning；本轮不改 API 运行代码，只补通知规则详情缺失、成员重复添加和删除缺失成员拒绝路径的查询链、错误语义与无副作用证据 | 测试 leader review 发现部分 API 用例只断言 404/409 状态码，无法发现项目租户校验被跳过、rule/member 查询参数漂移或拒绝路径误写审计。现锁住 `project.get_for_tenant`、`notification_rule.get_by_id`、`user.get_by_id`、`project_member.get_existing` 的 await 参数，并断言 delete/create/audit 无副作用；同时按当前真实契约记录 409 仍是 FastAPI `detail` 响应而非统一 error envelope |
| 2026-05-29 | N/A（Artifact presigned URL custom expiry 返回值/上下文复核） | `tests/unit/test_api/test_artifacts.py` 9 passed；targeted S3 custom expiry passed；`tests/unit` 977 passed；targeted ruff passed | 无新增 warning；本轮不改 artifact/S3 运行代码，只补 presigned URL custom expiry 测试的返回值和 async context 证据 | 测试 leader review 发现 custom expiry 用例只断言 S3 `generate_presigned_url` 被 await，没证明 `S3Storage.generate_presigned_url()` 把签名 URL 原样返回，也没证明 async client context 被正确进入/退出。现补 `url == ...`、`__aenter__` 和 `__aexit__` await 断言，让测试能发现返回值丢失或 client context 泄漏 |
| 2026-05-29 | N/A（EmailChannel to_thread/wait_for 与 JWT Redis await 复核） | `tests/unit/test_worker/test_channels.py tests/unit/test_auth/test_jwt_service.py` 44 passed；targeted email/JWT 4 passed；`tests/unit` 977 passed；targeted ruff passed | 无新增 warning；本轮不改通知渠道或 JWT 运行代码，只把 email async 外壳和 Redis blacklist 写入断言收紧到 await 层 | 测试 leader review 发现 EmailChannel 是 async API 包同步 SMTP helper，但测试只 patch `_send_sync` 并读取同步 `call_args`，无法证明发送被放进 `asyncio.to_thread`，也无法证明 SMTP 超时保护仍包在 `wait_for` 上。现 patch `asyncio.to_thread` / `wait_for` 并断言 await 参数、timeout 与 MIME 内容，同时新增 SMTP timeout 错误断言；JWT revoke TTL floor 测试改为 `redis.set.assert_awaited_once_with(...)`，避免 Redis coroutine 未 await 时假绿 |
| 2026-05-29 | N/A（RateLimit/Cancel async no-side-effect 复核） | `tests/unit/test_api/test_rate_limit.py tests/unit/test_engine/test_cancel.py` 43 passed；targeted no-side-effect 10 passed；`tests/unit` 976 passed；targeted ruff passed | 无新增 warning；本轮不改 rate limit 或 cancel 运行代码，只把拒绝/早退路径的无副作用断言收紧到 AsyncMock await 层 | 测试 leader review 发现严格限流拒绝请求和无 active container 取消路径都在证明“不能继续触碰下游异步操作”，但仍用 `assert_not_called()` 检查 `call_next` / `backend.cancel` / `force_kill`。现改为 `assert_not_awaited()`，让测试能发现限流阻断后仍 await 下游 app、取消早退误 await 后端控制接口等真实问题 |
| 2026-05-29 | N/A（Runner/GitSource subprocess await 参数复核） | `tests/unit/test_plugins/test_pytest_runner.py tests/unit/test_plugins/test_go_test_runner.py tests/unit/test_plugins/test_jest_runner.py tests/unit/test_plugins/test_playwright_runner.py tests/unit/test_plugins/test_git_source.py tests/unit/test_auth/test_token_service.py` 119 passed；`tests/unit` 976 passed；targeted ruff passed | 无新增 warning；本轮不改 runner/source 运行代码，只把 subprocess 启动与 token repo 写入的参数证据收紧到 AsyncMock await 层 | 测试 leader review 发现 pytest/go/jest/playwright runner 与 GitSource 都通过 `await asyncio.create_subprocess_exec` 启动外部命令，但部分测试仍从 `mock_exec.call_args` / `call_args_list` 读参数，无法发现 coroutine 未 await 或 await 参数漂移。现统一改为 `await_args` / `await_args_list` / `assert_awaited_once()`，并把 Git full-SHA clone 从“至少调用一次”收紧为 clone + checkout 两步证据；TokenService hash 测试也改从 `repo.create.await_args` 验证 secret_hash |
| 2026-05-29 | N/A（RunExecutor wait/clone/artifact/status event await 边界复核） | `tests/unit/test_engine/test_executor.py` 42 passed；targeted executor 11 passed；`tests/unit` 976 passed；targeted ruff passed | 无新增 warning；本轮不改 RunExecutor 运行代码，只把 setup wait、source clone、git sha 更新、artifact row 写入和取消竞态状态发布断言收紧到 AsyncMock await 层 | 测试 leader review 发现 executor 用例覆盖的是高价值执行状态机，但若从 `call_args` / `call_args_list` 读取 AsyncMock 或检查不存在的 legacy `backend.create`，会漏掉 coroutine 未 await、await 参数漂移、mark_running 失败后仍发布状态事件等真实风险。现统一改为 `await_args` / `await_args_list` / `assert_awaited*`，并直接 patch `publish_status_event` 证明 RUNNING/COLLECTING 的发布边界 |
| 2026-05-29 | N/A（JUnit collector S3 upload await 参数复核） | `tests/unit/test_plugins/test_junit_collector.py` 19 passed；targeted ruff passed | 无新增 warning；本轮不改 JUnit collector 运行代码，只把 report 上传到 S3 的 `put_object` 参数读取收紧到 AsyncMock await 层 | 测试 leader review 发现 JUnit collector 已覆盖自定义路径边界、collect 语义和 upload metadata，但上传用例仍在 `assert_awaited_once()` 后从 `put_object.call_args` 读取 Bucket/Key/Body/ContentType。现改为 `put_object.await_args.kwargs`，让测试能发现上传 coroutine 未 await 或 S3 参数在异步边界漂移 |
| 2026-05-29 | N/A（LogStream xread await 参数复核） | `tests/unit/test_engine/test_log_stream.py` 18 passed；targeted ruff passed | 无新增 warning；本轮不改 LogStream 运行代码，只把 `read_logs(block_ms=...)` 的 Redis xread 参数断言收紧到 AsyncMock await 层；pipeline `xadd` 保持同步排队调用断言 | 测试 leader review 发现 LogStream 多数 Redis/S3 异步边界已用 `await_args`，但 block 读取仍从 `xread.call_args` 取参数。现改为 `assert_awaited_once_with({stream: "0"}, count=100, block=5000)`，避免阻塞读参数漂移或未 await xread 时测试假绿 |
| 2026-05-29 | N/A（Worker early-exit enqueue AsyncMock no-side-effect 复核） | `tests/unit/test_worker/test_settings_tasks.py::test_dequeue_waiting_exits_without_session_factory tests/unit/test_worker/test_check_schedules.py::TestCheckSchedules::test_no_session_factory` 2 passed；`tests/unit/test_worker/test_settings_tasks.py tests/unit/test_worker/test_check_schedules.py` 26 passed；targeted ruff passed | 无新增 warning；本轮不改 worker 运行代码，只把缺失 session factory 时不应 enqueue 的断言收紧到 AsyncMock await 层；同步 `session_factory` 早退断言保持 `assert_not_called()` | 测试 leader review 发现 worker dequeue/check_schedules 早退路径需要证明“不触碰 ARQ enqueue”这个异步下游，但两处仍用同步 mock 断言。现改为 `assert_not_awaited()`，并在 schedule 测试里把 `arq_pool` 替身改成 AsyncMock，避免早退测试只证明 mock 没同步登记而漏掉误 await enqueue |
| 2026-05-29 | N/A（RBAC dependency session.execute await 参数复核） | `tests/unit/test_api/test_rbac_deps.py` 15 passed；targeted ruff passed | 无新增 warning；本轮不改 RBAC 运行代码，只把权限依赖中的 session.execute 查询/绕过断言收紧到 AsyncMock await 层 | 测试 leader review 发现 RBAC 单测在证明 owner/admin/platform_admin 不查 ProjectMember、member 查询次数和 path dependency 可见性短路时，仍用同步 `call_args` / `call_count` / `assert_not_called()` 读取 `AsyncMock session.execute`。现改为 `await_args`、`await_count` 与 `assert_not_awaited()`，让测试能发现权限绕过误查库或查询顺序漂移 |
| 2026-05-29 | N/A（Artifact/SSE async I/O boundary await 参数复核） | `tests/unit/test_api/test_artifacts.py tests/unit/test_api/test_sse.py` 24 passed；targeted ruff passed | 无新增 warning；本轮不改 artifact/SSE 运行代码，只把 S3 presign、artifact missing 短路、SSE 404/403 短路和 SSE ticket atomic fallback 防回归断言收紧到 AsyncMock await 层 | 测试 leader review 发现 artifact helper 与 SSE 权限拒绝路径在证明“不能触碰下游异步 I/O”时仍用同步 mock 断言；S3 presign 成功路径也只看同步调用记录。现改为 `assert_awaited_once_with()` / `assert_not_awaited()`，并把 atomic ticket 测试里的 `get/delete` 替身改成 AsyncMock，确保未来有人退回 `await get` + `await delete` 或越权前读 Redis stream/S3 presign 时能被测试抓住 |
| 2026-05-29 | N/A（Projects API AsyncMock no-side-effect/await 参数复核） | `tests/unit/test_api/test_projects.py` 28 passed；targeted ruff passed | 无新增 warning；本轮不改 projects 运行代码，只把 create/update 拒绝路径无副作用和 silent window settings 入参读取收紧到 AsyncMock await 层 | 测试 leader review 发现 projects 单测已有 create/update/delete 审计和 git URL 脱敏证据，但部分拒绝路径仍用 `assert_not_called()` 检查异步 repository/audit，silent window settings 用 `update.call_args` 取参数。现改为 `assert_not_awaited()` 与 `update.await_args`，让测试能发现拒绝路径误 await 写操作或 settings 入参在异步边界漂移 |
| 2026-05-29 | N/A（Project members 与 run results 异步无副作用复核） | `tests/unit/test_api/test_runs.py::test_get_run_results_rejects_unknown_status tests/unit/test_api/test_project_members.py` 9 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只把 project member 拒绝路径和 run result invalid status 的无副作用断言从同步 mock 层收紧到 AsyncMock await 层 | 测试 leader review 发现成员管理路由已覆盖跨租户用户、重复成员、缺失 project 矩阵等拒绝路径，但仍用 `assert_not_called()` 表达异步 repo/audit 不应发生；run result 非法 status 也只用同步 mock 检查 result list 未调用。现统一改为 `assert_not_awaited()`，并给 add/update/remove 审计读取前补 `assert_awaited_once()`，让测试能发现拒绝路径误 await 仓储方法或审计 coroutine 没真正执行 |
| 2026-05-29 | N/A（Credential rotate audit AsyncMock await 参数复核） | `tests/unit/test_api/test_credentials.py` 10 passed；targeted ruff passed | 无新增 warning；本轮不改 credentials 运行代码，只把 rotate 审计 payload 读取从同步 mock 调用记录收紧到 AsyncMock await 层；保留同步 crypto service 的 `call_args` 断言 | 测试 leader review 发现凭证 create/delete 审计已使用 `await_args`，但 rotate 仍在 `assert_awaited_once()` 后用 `audit.create.call_args` 读取参数。现统一为 `await_args`，让 rotate 审计测试能发现异步审计写入未 await 或 payload 漂移，同时避免误改同步加密服务断言 |
| 2026-05-29 | N/A（Environment API AsyncMock await 参数复核） | `tests/unit/test_api/test_environments.py::test_create_environment_stores_disk_limit tests/unit/test_api/test_environments.py::test_update_environment_updates_and_clears_disk_limit tests/unit/test_api/test_environments.py::test_get_environment_decrypt_failure_returns_500_and_writes_audit tests/unit/test_api/test_environments.py::test_create_environment_encrypt_failure_returns_500_and_writes_audit` 4 passed；`tests/unit/test_api/test_environments.py` 16 passed；targeted ruff passed | 无新增 warning；本轮不改 environment 运行代码，只把 disk limit、resource limit 更新和 env_vars 加解密失败审计的参数证据收紧到 AsyncMock await 层 | 测试 leader review 发现环境变量/资源限制路径已有语义断言，但仍有 `environment.create/update.call_args`、`audit.create.call_args` 和 `assert_not_called()` 读异步仓储状态。现改为 `assert_awaited_once()`、`await_args`、`await_args_list` 与 `assert_not_awaited()`，让测试能发现环境创建/更新/审计 coroutine 未 await 或加密失败路径误创建环境 |
| 2026-05-29 | N/A（P3 webhook run.create AsyncMock await 参数复核） | `tests/unit/test_api/test_p3.py::TestWebhookTrigger::test_webhook_trigger_creates_run tests/unit/test_api/test_p3.py::TestWebhookTrigger::test_webhook_trigger_preserves_project_git_auth_metadata` 2 passed；`tests/unit/test_api/test_p3.py` 37 passed；targeted ruff passed | 无新增 warning；本轮不改 webhook 运行代码，只把 webhook 创建 run 的 dedup key 与 git auth metadata 断言从同步 mock 调用记录收紧到 AsyncMock await 层 | 测试 leader review 发现 P3 webhook 用例已覆盖创建 run、保留项目级 git 认证配置和 payload metadata 合并，但仍从 `run.create.call_args` 读取异步仓储参数。现改为 `assert_awaited_once()` + `await_args`，让测试能发现 run 创建 coroutine 未 await 或 await 参数漂移，避免 webhook 测试只证明 mock 被同步登记 |
| 2026-05-29 | N/A（Audit helper/self-audit AsyncMock await 参数复核） | `tests/unit/test_api/test_audit.py` 7 passed；targeted ruff passed | 无新增 warning；本轮不改 audit 运行代码，只把 `write_audit` helper、project create route wiring 和 `/audit-events` 自审计测试的参数读取收紧到 AsyncMock await 层 | 测试 leader review 发现审计 helper 是全局横切能力，但多条用例仍从同步 `call_args` 读取 `repos.audit.create` 或 patched `write_audit` 的 payload。现补齐 `assert_awaited_once()` 并统一改用 `await_args`，让测试能发现审计写入未 await、payload 漂移或自审计 summary 变形，而不是只证明 mock 有同步调用记录 |
| 2026-05-29 | N/A（Auth route audit AsyncMock await 参数复核） | `tests/unit/test_auth/test_auth_routes.py::TestAuditLogin tests/unit/test_auth/test_auth_routes.py::TestAuditRegister tests/unit/test_auth/test_auth_routes.py::TestAuditLogout tests/unit/test_auth/test_auth_routes.py::TestAuditRefresh tests/unit/test_auth/test_auth_routes.py::TestAuditApiTokens` 10 passed；`tests/unit/test_auth/test_auth_routes.py` 41 passed；targeted ruff passed | 无新增 warning；本轮不改 auth 运行代码，只把登录、注册、退出、刷新和 API token 审计测试的 payload 读取从同步 mock 调用记录收紧到 AsyncMock await 层 | 测试 leader review 发现认证审计测试已断言 `audit_repo.create.assert_awaited_once()`，但下一行仍用 `call_args` 取审计参数，导致测试对“有没有真正 await 这个 coroutine”和“await 参数是否漂移”的表达不一致。现统一改为 `await_args`，让审计测试真正验证异步写入边界，而不是在同步 mock 记录上拿假证据 |
| 2026-05-29 | N/A（Notification rule API audit/no-side-effect await 参数复核） | `tests/unit/test_api/test_notifications.py` 12 passed；targeted ruff passed | 无新增 warning；本轮不改 notification rule 运行代码，只把审计 payload 与拒绝路径无副作用断言从同步 mock 层收紧到 AsyncMock await 层 | 测试 leader review 发现通知规则 API 虽已覆盖创建/更新/删除审计脱敏和缺失 project 矩阵，但仍从 `audit.create.call_args` 读取异步审计参数，拒绝路径也用 `assert_not_called()` 表达异步仓储无副作用。现改为 `await_args` 与 `assert_not_awaited()`，让用例能发现审计 coroutine 未被 await、await payload 漂移或拒绝路径误 await 仓储方法 |
| 2026-05-29 | N/A（Run API filter/trigger AsyncMock await 参数复核） | `tests/unit/test_api/test_runs.py::test_get_run_results_filters_by_suite tests/unit/test_api/test_runs.py::test_get_run_results_filters_by_keyword tests/unit/test_api/test_runs.py::test_get_run_results_combines_suite_and_keyword tests/unit/test_api/test_runs.py::test_get_run_results_escapes_keyword_like_wildcards tests/unit/test_api/test_runs.py::test_list_runs_single_status_uses_equality tests/unit/test_api/test_runs.py::test_trigger_run_includes_git_auth_metadata_without_plaintext tests/unit/test_api/test_runs.py::test_trigger_run_with_high_priority tests/unit/test_api/test_runs.py::test_trigger_run_default_priority` 8 passed；`tests/unit/test_api/test_runs.py` 26 passed；targeted ruff passed | 无新增 warning；本轮不改 Run API 运行代码，只把结果过滤、状态过滤、触发优先级和执行 metadata 的参数断言从同步 mock 调用记录收紧到 AsyncMock await 层 | 测试 leader review 发现 runs 测试仍有多处从 `AsyncMock.call_args` 读取仓储参数，容易在 coroutine 未真正 await、await 次数漂移或异步传参变形时假绿。现全部改为 `assert_awaited_once()` + `await_args`，让这些用例真正验证异步边界和业务参数，而不是只证明 mock 曾被碰过 |
| 2026-05-29 | N/A（auth refresh cookie 安全属性单测补强） | `tests/unit/test_auth/test_auth_routes.py::TestLogin::test_login_success tests/unit/test_auth/test_auth_routes.py::TestRegister::test_register_success tests/unit/test_auth/test_auth_routes.py::TestRefresh::test_refresh_success tests/unit/test_auth/test_auth_routes.py::TestLogoutRevokesTokens::test_logout_revokes_refresh_token tests/unit/test_auth/test_auth_routes.py::TestLogoutRevokesTokens::test_logout_revokes_both_tokens` 5 passed；targeted ruff passed | 无新增 warning；本轮不改 auth 运行代码，只把已有 refresh cookie 契约从“存在即可”提升为安全属性断言 | 测试 leader review 发现登录/注册/刷新用例只断言响应里有 refresh cookie，无法发现 `HttpOnly`、`Secure`、`SameSite=strict`、`Path=/api/v1/auth` 或 `Max-Age` 被误删；logout 也只证明 token revoke，不证明浏览器 cookie 被按同一路径安全清除。现成功/刷新/清除路径都检查 `Set-Cookie` 安全属性，避免认证测试只为状态码和覆盖率服务 |
| 2026-05-29 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_api_endpoints.py::TestAnalytics` 3 passed | `tests/unit/test_frontend_api_contract.py::test_analytics_ui_exposes_single_test_history_contract` 1 passed；targeted ruff passed；frontend `npm run build` passed；frontend `npm run lint -- --max-warnings=0` passed | 无新增 warning；本轮补 F-RE-05，不改变 project trend / flaky 聚合口径，只新增单用例 history API 与 UI 入口 | 测试 leader review 发现 analytics 已有项目级趋势和 flaky 聚合，但没有“某个测试最近几次到底何时从 passed 变 failed”的证据链，容易让趋势测试只证明聚合 endpoint 200。现 `/analytics/test-history` 按 suite/name 返回真实 Run + TestResult join 的 run_id、run_created_at、run_status、用例 status、duration/error 和 git_ref；前端从 flaky 行进入历史表，契约测试锁住 DTO/hook/UI，避免单用例趋势再次退化成只看覆盖率 |
| 2026-05-29 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_silent_windows.py` 7 passed | `tests/unit/test_release_quality_docs_contract.py::test_backlog_docs_reflect_silent_windows_completion` 1 passed；`tests/unit/test_release_quality_docs_contract.py` 11 passed；`tests/unit` 972 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改静默窗口运行逻辑，只把产品化 API 入口补进真实 DB 证据并同步文档真源 | 测试 leader review 发现 F-EX-02 的 worker/cron 证据已经很实，但 TODO/catalog 仍把项目级静默窗口列为缺口，且真实 integration 主要通过直接写 DB 构造 `Project.settings`。现新增真实 API/DB integration，证明 `PUT /api/v1/projects/{project_id}` 会保存 `silent_windows`、保留既有 settings 并写 `project.update` audit；文档契约禁止“产品化入口未完成”和“T05 仍待决策”口径回流，避免验收矩阵误导后续测试计划 |
| 2026-05-29 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_notification_delivery.py` 5 passed | `tests/unit/test_worker/test_notifications.py::TestEvaluateConditions tests/unit/test_worker/test_notifications.py::TestEvaluateAndNotify::test_or_group_with_consecutive_failures_sends_notification` 12 passed；`tests/unit/test_frontend_api_contract.py::test_notification_rule_ui_matches_supported_channel_contract tests/unit/test_worker/test_notifications.py tests/unit/test_api/test_notifications.py` 42 passed；`tests/unit/test_worker/test_notifications.py tests/unit/test_frontend_api_contract.py tests/unit/test_api/test_notifications.py tests/unit/test_release_quality_docs_contract.py` 79 passed；`tests/unit` 971 passed；frontend `npm run build` passed；frontend `npm run lint -- --max-warnings=0` passed；Playwright mocked-API visual smoke passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮补 F-NT-01 条件表达，不改通知日志幂等键或渠道发送协议 | 测试 leader review 发现条件通知仍只支持 AND，且连续失败次数没有真实 DB 证据，容易让“失败时通知”测试看似充分却漏掉告警噪声控制和升级场景。现 `_evaluate_conditions` 支持嵌套 `any` / `all` 与 `consecutive_failures`，前端表单可保存全部/任一条件和连续失败字段，真实 DB integration 构造 done → failed → failed → current failed 链路，证明连续 3 次触发而 4 次门槛不触发 |
| 2026-05-29 | N/A（F-NT-03 真实失败用例 summary 复核） | `tests/unit/test_engine/test_executor.py::TestExecutorCommitsAfterStateTransitions::test_execute_includes_failed_test_names_in_summary tests/unit/test_engine/test_executor.py::TestExecutorCommitsAfterStateTransitions::test_execute_caps_failed_test_names_in_summary` 2 passed；`tests/unit/test_engine/test_executor.py tests/unit/test_worker/test_notifications.py tests/unit/test_release_quality_docs_contract.py` 77 passed；`tests/unit/test_release_quality_docs_contract.py` 10 passed；`tests/unit` 967 passed；targeted ruff passed | 无新增 warning；本轮把通知模板的 `failed_tests` 变量从“调用方可手工传 summary”提升为 executor 真实 collector 结果自动填充，失败/错误用例最多保留前 20 条并记录省略数 | 测试 leader review 发现通知模板虽然已有 `summary.failed_tests` 格式测试，但真实执行摘要不会自动带失败用例名，导致生产通知仍可能只显示计数。现 executor 终态 summary 会从 collector 结果写入 failed/error 用例名，并有正常失败/错误混合和 25 条截断两个单测锁住；文档契约同步把 F-NT-03 改回完成 |
| 2026-05-29 | N/A（F-NT-02 渠道唯一性契约复核） | `tests/unit/test_api/test_notifications.py tests/unit/test_frontend_api_contract.py::test_notification_rule_ui_matches_supported_channel_contract` 13 passed；`tests/unit/test_frontend_api_contract.py tests/unit/test_release_quality_docs_contract.py tests/unit/test_api/test_notifications.py` 50 passed；`tests/unit` 965 passed；frontend `npm run lint -- --max-warnings=0` passed；frontend `npm run build` passed；Playwright mocked-API visual smoke passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改通知日志表结构，明确产品契约为单条规则中每种一等渠道只能配置一次 | 测试 leader review 发现通知日志以 `run_id/rule_id/channel_type` 幂等，如果 UI/API 允许同一规则配置两个同类型渠道，测试仍可能全绿但实际第二个渠道会被跳过或无法可靠记账。现 API 负向测试拒绝重复/未知渠道类型，前端契约测试和 UI 冒烟锁住“新增渠道选择下一个未用类型、选项不提供重复类型、四类用尽后禁用添加”，避免多渠道测试只证明能显示四个渠道而漏掉少发事故 |
| 2026-05-29 | N/A（F-NT-02 前端渠道契约复核） | `tests/unit/test_frontend_api_contract.py::test_notification_rule_ui_matches_supported_channel_contract` 1 passed；frontend `npm run build` passed；frontend `npm run lint -- --max-warnings=0` passed；Playwright mocked-API visual smoke passed；targeted ruff passed | 无新增 warning；本轮不改后端 channel 发送逻辑，修复前端通知规则表单与后端发送契约偏差 | F-NT-02 review 发现 TODO/catalog 仍说钉钉/企微未实现，但后端 router 与 channel 单测已支持；真正缺口在 UI：表单只允许 email/webhook，且 email 写 `config.to`，后端实际要求 `to_addresses` 和 SMTP 配置。现前端支持 Email/Webhook/DingTalk/WeCom 四类渠道，email 保存 `smtp_host/from_address/to_addresses`，DingTalk/WeCom 暴露密钥输入和 text/markdown，契约测试锁住当前一等渠道集合并禁止 Slack 假枚举，避免 UI 测试只证明列表能显示已有规则 |
| 2026-05-29 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_notification_delivery.py` 4 passed | `tests/unit` 960 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮把通知模板从规则级单次渲染收敛为每 channel 独立渲染，并只在模板需要 `project_name` 时查项目表 | Notification template review 发现原测试只证明规则级基础变量可替换，无法发现 `channels[].template` 被忽略、项目名未进入消息或失败用例变量格式漂移。现 unit 同时断言 webhook channel 覆盖规则模板、email fallback 规则模板、项目名只查一次、`failed_tests` 格式；required integration 用真实 DB 规则和项目名证明发送消息完整等于预期，避免通知模板测试只为覆盖率服务 |
| 2026-05-29 | N/A（本轮 F-LS-03 + T-LOGGING 完整 unit 回归） | `tests/unit` 958 passed；F-LS-03 targeted required integration 1 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；完整 unit 较上一轮 955 增至 958，新增项目搜索排序、worker logging 顺序和两条文档契约进入基线 | 本轮从 TODO/catalog 真相源继续收敛两个明确缺口：F-LS-03 项目搜索排序和 T-LOGGING worker 结构化日志初始化。完整 unit 回归确认 API、worker startup、文档契约和既有质量门兼容；真实 DB/API integration 证明项目搜索不只是 mock 参数正确，而是乱序创建后按名称字母序返回 |
| 2026-05-29 | N/A（Worker 结构化日志初始化复核） | `tests/unit/test_lifespan.py` 9 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮只让 worker/arq startup 复用既有 `configure_logging`，不改变日志 schema 或引入 trace-log 关联 | T-LOGGING review 发现 API lifespan 已配置 structlog，但 worker `on_startup` 未调用统一日志初始化，导致后台任务启动失败与 worker/engine/plugin 的 stdlib logger 输出形态可能与 API 不一致。现 worker startup 在创建 Settings 后、依赖初始化前调用 `configure_logging(settings)`；unit 锁住调用参数和顺序，避免日志配置再次只停留在 API 进程 |
| 2026-05-29 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_api_endpoints.py::TestProjectsCRUD::test_list_projects_search_orders_results_by_name` 1 passed | `tests/unit/test_api/test_projects.py::test_list_projects_with_search` 1 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮修复 F-LS-03，不改搜索语义，只把项目搜索结果从默认 `created_at desc` 收敛到名称字母序 | Project search review 发现 TODO/catalog 明确记录 F-LS-03 未达 PRD：LIKE 转义已有，但 `/api/v1/projects` 仍靠 BaseRepository 默认 `created_at desc` 排序。现 API 调用 repository 时传 `ProjectORM.name.asc()`；unit 锁住 `order_by`，required integration 乱序创建 Alpha/Zulu/Beta 后按唯一 token 搜索并断言返回名称等于 `sorted(names)`，避免项目搜索只证明能搜到、不证明排序符合 PRD |
| 2026-05-29 | N/A（测试质量 review 完整 unit 回归） | `tests/unit` 955 passed；本轮改动文件 targeted ruff passed；`git diff --check` passed | 无新增 warning；完整 unit 较上一轮 954 增至 955，新增 schedule silent-window 单测进入基线 | 本轮从测试 leader 角度继续清理“只证明 mock 被碰过”的用例，覆盖 schedule worker、notification channels、cancel/status event、executor redaction、worker settings retention。完整 unit 回归确认这些补强与既有 API/engine/worker/doc 契约兼容，当前剩余 rg 命中多为已跟随 `await_args` 参数断言的良性痕迹或 Docker/外部栈显式 skip |
| 2026-05-29 | N/A（Worker settings retention cutoff/log 复核） | `tests/unit/test_worker/test_settings_tasks.py` 17 passed；targeted ruff passed | 无新增 warning；本轮不改 worker settings 运行代码，只补 retention cleanup 参数名与日志证据 | Worker settings follow-up review 发现 run/audit retention cleanup 虽已断言 cutoff 时间窗口，但删除 repo 和日志仍停在“调用一次”。现断言 `delete_terminal_older_than(cutoff=...)`、`delete_older_than(cutoff=...)`、commit 无参数，以及日志模板/删除数量/cutoff isoformat，避免维护任务测试漏掉 cutoff 参数名漂移或日志证据失真 |
| 2026-05-29 | N/A（Cancel/event/redaction await 参数复核） | `tests/unit/test_engine/test_cancel.py tests/unit/test_engine/test_events.py tests/unit/test_engine/test_executor_redaction.py` 30 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只补 Redis cancel/status event 与 fail-path redaction 的参数证据 | Engine infrastructure follow-up review 发现 cancel publish 仍用同步 `assert_called_once_with` 检查 AsyncMock，status event 只确认 xadd/hset 被 await，redaction fail-path 从同步 `call_args` 取失败消息。现断言 cancel channel/payload、status stream/hash key 与 payload、fail_if_current 的目标 run id 和脱敏 message 均来自 await 参数，避免基础设施测试只证明 mock 被碰过 |
| 2026-05-29 | N/A（Notification channels await/MIME 参数复核） | `tests/unit/test_worker/test_channels.py` 30 passed；targeted ruff passed | 无新增 warning；本轮不改 notification channel 运行代码，只补 Email/Webhook/DingTalk/WeCom 参数证据 | Channel follow-up review 发现多条 HTTP channel 用例仍从 AsyncMock 的同步 `call_args` 读取参数，Email happy path 只看部分 SMTP 参数，没有解析 MIME 主题和正文。现改为用 `await_args` 校验 webhook/dingtalk/wecom 的 method/url/params/json/headers，并解析 raw MIME 断言 subject/from/to/body，避免 channel 测试只证明请求 mock 被碰过或靠字符串包含误判 |
| 2026-05-29 | N/A（Schedule worker silent-window 与 await 参数复核） | `tests/unit/test_worker/test_check_schedules.py` 9 passed；targeted ruff passed | 无新增 warning；本轮不改 schedule 运行代码，只补静默窗口业务分支和 async 参数证据 | Schedule worker follow-up review 发现项目级 silent window 分支缺单测，主路径/异常路径仍有部分 AsyncMock 从同步 `call_args` 读取参数，容易只证明“被调用过”。现新增 silent window 用例，断言不创建 run、不 enqueue、不更新 fire 时间但写 `schedule_skipped_silent_window` audit；主路径锁住 `run_repo.create` 完整 kwargs，audit/update 参数改读 `await_args`，避免调度测试只覆盖 happy-path 外壳 |
| 2026-05-29 | N/A（Worker-lost reclaim 边界参数复核） | `tests/unit/test_engine/test_reclaim.py` 11 passed；targeted ruff passed | 无新增 warning；本轮不改 reclaim 运行代码，只补 worker_lost 边界分支的 mark/event/callback 参数证据 | Reclaim follow-up review 发现无 execution_id、cleanup 失败、retry callback、无 backend 分支仍只确认 `mark_worker_lost` 或 callback 被 await 一次，不能发现 worker_id/message 漂移、failed status event 缺失，或 callback 收到错误消息。现断言 `mark_worker_lost(run.id, worker_id, message)`、无 execution_id 仍发布 `failed previous=running`、cleanup 失败仍标记目标 run、callback 精确接收 `(run, message)`，避免 worker_lost 回收测试只验证“走过分支” |
| 2026-05-29 | N/A（FairScheduler await 参数与 conflict reason 复核） | `tests/unit/test_worker/test_scheduler.py` 13 passed；targeted ruff passed | 无新增 warning；本轮不改 scheduler 运行代码，只把队列参数读取切到 await 层并补 conflict/dequeue 目标证据 | Scheduler follow-up review 发现多条 queue 断言从 AsyncMock 的同步 `call_args` 读取 `enqueue_job` 参数，ARQ job id conflict 分支只检查 reason 包含片段，waiting dequeue 只看 mark_enqueued 次数。现改为 `await_args`，断言 conflict 精确 reason `arq job id conflict: run:{id}`，并确认 dequeue 只入队第一条 waiting run，避免优先级/冲突恢复测试在 mock 层假绿 |
| 2026-05-29 | N/A（RunExecutor timeout/artifact/status await 参数补强） | `tests/unit/test_engine/test_executor.py` 40 passed；targeted ruff passed | 无新增 warning；本轮不改 executor 运行代码，只补 setup timeout、artifact 上传限制和终态 summary 的 await 参数契约 | Executor follow-up review 发现 setup timeout 只确认 graceful/cleanup 被 await，没锁住 execution id 和 reason；artifact size/count limit 用例从同步 `call_args` 读取 S3/DB 参数，且没有显式锁 `.txt` artifact 类型；xfail summary 从同步 call 读终态 summary。现断言 `_graceful_stop("c", reason="setup timeout 0s")`、cleanup execution id、S3 Bucket/Key、Artifact run_id/type=`log`/name/storage_path/size，以及 `finish_if_current(str(run.id), status=DONE, summary=...)`，避免 executor 测试只证明外围步骤被碰过 |
| 2026-05-29 | N/A（Worker settings await 参数复核） | `tests/unit/test_worker/test_settings_tasks.py` 17 passed；targeted ruff passed | 无新增 warning；本轮不改 worker settings 运行代码，只补维护任务 await 参数和失败补偿 run id 证据 | Worker settings follow-up review 发现 shutdown/dequeue/after_job_end 仍有“只 await 一次”的断言：close/try_dequeue_waiting 没证明无额外参数，ARQ failed job 补偿没锁住 `fail_if_current` 目标 run id，且从同步 `call_args` 读取错误消息。现改为 `assert_awaited_once_with()`，并断言 `fail_if_current(run.id, message=ANY)` 后从 `await_args` 校验 URL userinfo 脱敏，避免维护任务测试在 async mock 层假绿 |
| 2026-05-29 | N/A（Worker notification await 参数复核） | `tests/unit/test_worker/test_notifications.py` 24 passed；targeted ruff passed | 无新增 warning；本轮不改 notification 运行代码，只把通知日志和发送 mock 的参数读取切到 await 层 | Notification follow-up review 发现成功、发送失败、模板失败和模板渲染用例虽已覆盖业务字段，但仍从 AsyncMock 的同步 `call_args` 读取 `_send_channel` / `log_repo.create` 参数。现改为 `await_args`，确保测试能发现 coroutine 未真正 await 或 await 参数漂移，而不是只看到 mock 被同步登记 |
| 2026-05-29 | N/A（LogStream await 参数与 batch payload 单测补强） | `tests/unit/test_engine/test_log_stream.py` 18 passed；targeted ruff passed | 无新增 warning；本轮不改 log_stream 运行代码，只把 Redis/S3 async mock 读取切到 await 参数并补 batch payload 证据 | LogStream follow-up review 发现写单条日志、归档 put_object 和截断用例仍从同步 `call_args` 读取 AsyncMock 参数；batch 只验证 xadd 次数和 trim kwargs，不能发现 pipeline 改成 transaction 默认值或写入内容顺序/stream 漂移。现用 `assert_awaited_once_with` / `await_args` 校验单条写入与 S3 put 参数，并断言 `pipeline(transaction=False)` 及三条 batch payload 精确值，避免日志测试在 mock 调用层假绿 |
| 2026-05-29 | N/A（Worker execute_run claim/config 单测补强） | `tests/unit/test_worker/test_tasks.py` 18 passed；targeted ruff passed | 无新增 warning；本轮不改 worker 运行代码，只补执行入口 claim 参数、active run config 和 release 证据 | Worker tasks review 发现 claim/no-claim 路径只断言 `claim_for_worker` 被 await，没证明 run id 与 `worker_id` 正确；active project 用例只断言 executor 被 await，无法发现传错 run、环境镜像/timeout/network/resource limits/collector/source_auth 漂移，或 finally release 参数错误。现断言 `claim_for_worker(run_id, worker_id="worker-test")`、executor 入参 run identity 与 PipelineConfig 核心字段，并锁住 active run 最终 `release_worker(str(run.id), worker_id=...)`，避免 worker 主入口测试只验证“执行过” |
| 2026-05-29 | N/A（Docker backend async 参数与 lifecycle 单测补强） | `tests/unit/test_engine/test_docker_backend.py` 33 passed；targeted ruff passed | 无新增 warning；本轮不改 Docker backend 运行代码，只补容器创建 async await 参数、容器名、HostConfig 与 start/wait 参数证据 | Docker backend review 发现 `create_execution` 用例从 AsyncMock 的同步 `call_args` 读取配置，且未锁住容器名、Cmd/Env、Memory/MemorySwap/NanoCpus、SecurityOpt、capabilities、devices、Init、binds 等关键 HostConfig；`start/wait` 也只看方法被 await，无法发现容器 ID 或 timeout 漂移。现改用 `await_args`，断言 `qap-run-{run_id}`、核心 sandbox 配置、`container("container-id")` 和 `wait(timeout=10)`，避免 Docker 单测只证明 aiodocker mock 被碰过 |
| 2026-05-29 | N/A（Worker auto retry 参数契约单测补强） | `tests/unit/test_worker/test_auto_retry.py` 24 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只补自动重试 run 创建和 scheduler 入队参数证据 | Auto retry review 发现 `_attempt_retry` 用例多处只断言 `create/enqueue` 被 await 一次，无法发现 tenant/project/pipeline/environment/git ref/trigger/metadata/source_run/retry_group/chain_depth 漂移，也无法发现 backoff `_defer_by` 被删或传错。现统一断言 retry Run 创建完整 kwargs、metadata 为拷贝而非原 dict，并断言 scheduler 以 retry run 和精确 `_defer_by` 入队，避免自动重试测试只证明“碰过 mock” |
| 2026-05-29 | N/A（Executor setup sandbox spec 单测补强） | `tests/unit/test_engine/test_executor.py::TestRunSetupContainerised::test_setup_runs_via_backend_not_subprocess` 1 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只补 setup container ExecutionSpec 断言 | Executor setup review 发现 `_run_setup` 用例使用 AsyncMock backend 但仍以同步 `assert_called_once/call_args` 读取，且只粗略检查 command/mount/phase。现改为 `assert_awaited_once`，并断言 image、完整 shell command、env、network policy、user、readonly_rootfs、workspace mount source/target/read_only 和 labels，避免 setup sandbox 测试只能证明 backend 被碰过 |
| 2026-05-29 | N/A（Notification channel router 参数单测补强） | `tests/unit/test_worker/test_channels.py` 30 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只补 channel router 与 webhook HTTP 调用的参数证据 | Channel review 发现 router 用例只断言各 channel `send` 被 await 一次，不能发现 config/message 被改坏；Webhook happy path 读取 `call_args` 而非 await args。现断言 email/webhook/dingtalk/wecom 都以原始 config 和 message 调用，并用 await args 校验 HTTP method/url/json，避免通知 channel 测试只停在“路由到了某个 mock” |
| 2026-05-29 | N/A（Rate limit XFF key 单测补强） | `tests/unit/test_api/test_rate_limit.py` 27 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只补限流 bucket key 证据 | Rate-limit review 发现 “no trusted proxy ignores XFF” dispatch 用例只断言请求通过且 `call_next` 被调用，没证明 Redis bucket key 真用 direct client IP 而不是伪造的 `X-Forwarded-For`。现断言 `call_next` 被 await，且 Redis zadd key 包含 `ip:203.0.113.1`、不含 `1.2.3.4`，避免代理信任边界测试只验证未被限流 |
| 2026-05-29 | N/A（Run status event 单测补强） | `tests/unit/test_engine/test_events.py` 4 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只补 Redis 状态事件基础设施契约 | Event publisher review 发现测试只断言 stream/hash 被写和状态字段存在，没证明 timestamp 是 timezone-aware，也没证明 stream event 与 status hash 使用同一个 timestamp。现用 await args 校验 `xadd` event 与 `hset` mapping 完全对齐，避免 SSE 事件和 current-status hash 出现静默漂移 |
| 2026-05-29 | N/A（Credentials async 断言与创建参数复核） | `tests/unit/test_api/test_credentials.py` 10 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只把凭证安全路径的 mock 断言从“被调用”提升为 await 与参数契约 | Credentials review 发现 create happy path 只断言加密 AAD，没锁住 `credential.create` 的 tenant/project/name/type/ciphertext/created_by；部分 async repo/audit/delete/update 仍用 `assert_called_once`，可能漏掉 coroutine 未 await 的问题。现改为 `assert_awaited_once(_with)`，并断言创建入参、duplicate/delete-in-use/missing-project 分支无 await 副作用，避免凭据测试停留在同步 mock 调用层 |
| 2026-05-29 | N/A（Run list/detail API 单测补强） | `tests/unit/test_api/test_runs.py::test_list_runs tests/unit/test_api/test_runs.py::test_get_run` 2 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只补 run 读路径 tenant/status 过滤证据 | 测试 review 发现 run 列表和详情单测只验证响应能返回，无法发现列表漏 tenant 过滤、status 过滤没传入 repository，或详情页用错 tenant 读取。现列表断言 offset/limit、tenant filter 和 status filter 的表达式右值，详情断言 `get_for_tenant(run.id, tenant_id)`，避免读路径测试只覆盖序列化外壳 |
| 2026-05-29 | N/A（Run trigger API 单测补强） | `tests/unit/test_api/test_runs.py::test_trigger_run tests/unit/test_api/test_runs.py::test_trigger_run_stays_queued_and_audited_without_arq_pool` 2 passed；targeted ruff passed | 无新增 warning；本轮不改运行代码，只补 run trigger 成功入队路径的参数与副作用证据 | 测试 review 发现 `test_trigger_run` 只断言 API 返回 201 和 arq 被调用一次，无法发现手动触发被发到错误队列、job id 漂移、`mark_enqueued` 未落库、waiting 分支误触发，或 `run.trigger` 审计丢失。现断言 run.create 入参、`set_retry_group_id`、`enqueue_job("execute_run", run_id, _queue_name="queue:medium", _job_id="run:{id}")`、`mark_enqueued` queue/job/timezone 时间、no `mark_waiting` 和审计 action/resource/status，避免核心触发用例只做“调用过”检查 |
| 2026-05-29 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_auto_retry_real_db.py` 7 passed；Playwright `--list` 10 tests / 4 files；targeted ruff passed | N/A | 无新增 warning；本轮不改产品运行代码，只收紧真实 executor retry integration 和 E2E helper 类型口径 | 自动重试 integration review 发现 setup infra error 和 clone failure 用例已有真实 DB/Redis/队列断言，但局部仍停在 `create_execution` / `source.clone` 被 await 一次，不能发现 setup 容器 spec、挂载、network policy、clone ref 或凭据参数漂移。现断言 setup `ExecutionSpec` 的 image/command/env/network/labels/mount，并断言 clone 使用 metadata git_url、`main` ref、per-run workspace 且不带额外 auth；同时修正 E2E helper 的 `network_policy` 类型为产品真实枚举 `allow/deny/restricted`，避免测试工具层保留已失效的 `block` 值 |
| 2026-05-29 | N/A（完整 unit 回归） | `tests/unit` 954 passed；文档/readme/smoke/release gate 契约 targeted 22 passed；`git diff --check` passed | 无新增 warning | 本轮新增的 backlog 文档语义契约进入 unit 套件，完整 unit 从此前 953 passed 增至 954 passed；用于收尾确认本轮 E2E、通知 integration、文档契约和 smoke 静态清理没有破坏既有 unit 基线 |
| 2026-05-29 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_notification_delivery.py` 3 passed；Python ruff targeted passed；frontend `npm run lint -- --max-warnings=0` passed | N/A | 无新增产品 warning；曾误把 `ruff` 跑到 TypeScript E2E 文件，已改用 ESLint 校验前端代码 | 通知 integration review 发现真实 DB 用例只证明 `_send_channel` 被 await 一次和消息包含 `failed=2`，还不能发现匹配规则选错 channel、配置传错或日志 channel 类型漂移。现断言发送参数必须是 `webhook` + `https://example.com/hook`，消息必须完整等于 `Run {run_id} failed=2`，并校验 `NotificationLog.channel_type`，让规则筛选、模板渲染、发送参数和落库日志形成一条可追踪证据链 |
| 2026-05-29 | Playwright `tests/e2e/real-login-flow.spec.ts --project=chromium` 1 passed；Playwright `--list` 10 tests / 4 files；frontend `npm run lint -- --max-warnings=0` passed | N/A | Playwright 本地输出 `NO_COLOR` / `FORCE_COLOR` Node 环境 warning，不影响断言；无产品代码 warning | E2E review 发现 `real-login-flow` 旧用例“有项目才打开”，没有项目时仍会绿，容易把“登录页可用”误当成“登录后项目导航可用”。现改为先经真实 API 创建指定项目，再要求 UI 登录后必须在项目列表看到并打开该项目，断言 URL 和详情标题都匹配，避免 real-login lane 只做松散冒烟 |
| 2026-05-29 | N/A（前端质量门与 smoke 脚本静态审查） | `tests/unit/test_smoke_report_contract.py tests/unit/test_release_gate_workflow.py` 13 passed；`frontend npm run build` passed；`frontend npm run lint -- --max-warnings=0` passed；`shellcheck -x scripts/smoke/*.sh scripts/smoke/lib/common.sh` passed；`bash -n` passed；`git diff --check` passed | 无新增 warning；本轮不改产品运行代码，只清理 smoke 脚本静态问题并验证前端质量门 | 文档已把前端验收口径提升为 build/lint 零 warning，故本轮用真实 `npm run build` 和 `npm run lint -- --max-warnings=0` 复验；同时发现 smoke 脚本有未使用变量、重复 redirect 和 `local line=$(...)` 可能掩盖返回值，容易让冒烟脚本维护质量下降。现删除无用采样变量、合并日志 redirect、拆分声明/赋值，并为动态 source 增加 shellcheck source 标注；`SMOKE_ALLOW_SKIPS` 语义和 release gate workflow 契约测试保持通过，避免 smoke/release 文档只靠文字承诺 |
| 2026-05-29 | `tests/integration/test_find_past_pipeline_deadline.py` 本地默认环境 2 skipped（缺少 `RUN_INTEGRATION_TESTS=1`）；targeted ruff passed；`git diff --check` passed | N/A | 无新增 warning；未启动真实 integration DB，本轮仅做测试说明与断言精度补强 | 测试 review 发现 deadline integration 顶部场景说明仍写旧的 60s/100s/30s，但实际夹具已改成 `timeout_seconds=300` 和 1800s floor，容易误导测试签收；同时“returns only overdue run”只断言 old 在、fresh 不在，没把本测试种子集合的命中结果锁成精确集合。现同步说明为 300/1800/2000/100/2100s，并断言 `{run_old, run_fresh}` 交集必须等于 `{run_old}`，避免该测试名义上验证 only、实际上只做部分包含判断 |
| 2026-05-29 | N/A（TokenService 与 worker-lost reclaimer 单测补强） | `tests/unit/test_auth/test_token_service.py tests/unit/test_engine/test_reclaim.py` 27 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改运行代码，只补 API token service 与 worker-lost reclaimer 的参数级契约证据 | 测试 review 发现 `TokenService.create_api_token` 只断言 repo.create 被 await，无法发现 token_id 传错、把整串 token 当 secret hash、scopes/expires_at 漂移或返回 token 不匹配；worker-lost reclaimer 只看 Redis `xadd` 被触发，没证明 `mark_worker_lost` 的 worker/message 参数或 failed 状态事件 previous 值。现 deterministic generate/hash 后断言 repo.create 完整 kwargs 与只 hash secret；reclaimer 断言 `mark_worker_lost(run.id, worker_id, message)`、`publish_status_event(redis, run.id, "failed", previous="running")` 和 orphan cleanup，避免这两处测试只看 mock 被调用 |
| 2026-05-29 | N/A（Worker 执行早退、取消顺序与 Docker 错误边界补强） | `tests/unit/test_worker/test_tasks.py tests/unit/test_engine/test_cancel.py tests/unit/test_engine/test_docker_backend.py` 67 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮测试打红后修正 `src/qaplatform/worker/tasks.py`，让 run 已认领后在 pre-execute 早退取消路径释放 `worker_id` | 测试 review 发现 worker archived/deleted project 用例只断言 executor 不执行，无法发现 claim 后早退把 `worker_id` 留在终态 run 上；取消处理用例只证明 force_kill 被调用，没锁住 SIGTERM→wait→SIGKILL 顺序和 grace 参数；Docker backend 只测正常 kill/delete，漏掉 409/404 幂等吞错和非预期 DockerError 透传。现早退路径断言 cancel/release/no executor/commit，并修复源码；取消路径断言调用顺序与 grace timeout；Docker backend 覆盖 already stopped/removed 与 unexpected daemon error，避免这些测试只验证“调用过” |
| 2026-05-29 | N/A（Worker settings 维护任务 wiring 单测补强） | `tests/unit/test_worker/test_settings_tasks.py` 17 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 worker settings 运行代码，只补 reclaim retry callback、dequeue scheduler 注入和 retention cutoff 证据 | 测试 review 发现 reclaim_resources 只断言传入了 `on_reclaimed=ANY`，没执行 callback，无法发现 worker_lost 自动 retry wiring 断掉；dequeue 只看 FairScheduler 被构造，retention cleanup 只看 repo 方法被调用，无法发现 arq/settings/run_repo 注入错或 cutoff 没按配置天数。现模拟 reclaimer 调用 callback，断言 `_schedule_retry_for_run(run, ConnectionError(message), run_repo/arq/settings)`；dequeue 锁住构造参数；run/audit cleanup 断言 timezone-aware cutoff 落在配置天数窗口内，避免维护任务测试只验证 cron 函数不报错 |
| 2026-05-29 | N/A（ExecutionService 状态转移参数单测补强） | `tests/unit/test_engine/test_execution_service.py` 26 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 execution service 运行代码，只补条件状态转移 helper 的 repository 参数证据 | 测试 review 发现 claim/start/finish/fail/cancel/timeout 多个用例只断言返回 True/False，无法发现目标状态传错、worker_id/summary/error_message/expected 丢失，或 claim 成功后没有回读 run。现逐个锁住 `repo.update_status` 的 run_id、RunStatus、worker_id、summary、error_message、expected，并断言 claim 失败不调用 `repo.get`，避免状态机单测只验证 mock 返回值 |
| 2026-05-29 | N/A（GitSource URL 校验与 clone 单测隔离补强） | `tests/unit/test_plugins/test_git_source.py` 19 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 GitSource 运行代码，只把 clone subprocess/auth 测试与 DNS URL 校验拆开 | 测试 review 发现 GitSource clone 单测在 mock git subprocess 的同时仍隐式跑真实 `_validate_git_url` DNS 解析，容易受网络/DNS 环境影响；而 SSRF 边界本身没有 deterministic 单测。现 clone 类测试 stub URL 校验，专门新增私网 IP 拒绝、public IP 允许、SSH hostname 校验、非法 scheme/no hostname 不触发 DNS 的用例，避免 source 插件测试既 flaky 又漏安全边界 |
| 2026-05-29 | N/A（FairScheduler 显式 trigger_type 与队列元数据单测补强） | `tests/unit/test_worker/test_scheduler.py` 13 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮测试打红后修正 `src/qaplatform/worker/scheduler.py`，让 `enqueue_run(..., trigger_type=...)` 真正参与队列优先级选择 | 测试 review 发现 `enqueue_run` 签名接收 trigger_type 但实现忽略，现有用例只看返回 True，无法发现调用方显式传 schedule/webhook 时仍按 run 对象旧 trigger 排队；容量满用例也没证明不会 enqueue，成功用例没锁住 mark_enqueued 元数据。现补显式 trigger override 低优先级队列用例、容量满 no enqueue、mark_enqueued queue/job_id/timezone-aware 时间，避免 scheduler 测试只验证“入队成功” |
| 2026-05-29 | N/A（Webhook channel SSRF 边界单测补强） | `tests/unit/test_worker/test_channels.py` 30 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 notification channel 运行代码，只补 webhook URL 安全校验和拒绝短路证据 | 测试 review 发现 webhook send 的成功/失败用例都 patch 掉 `_validate_webhook_url`，无法发现未来 SSRF 校验被删或私网解析仍创建 HTTP client。现用 deterministic `socket.getaddrinfo` 覆盖私网 IP 解析拒绝并断言 `httpx.AsyncClient` 未创建，同时覆盖非 http(s) scheme 和无 hostname URL，避免 channel 测试只验证正常发送参数 |
| 2026-05-29 | N/A（Check schedules 缺失 project 持久化与触发契约补强） | `tests/unit/test_worker/test_check_schedules.py` 8 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮测试打红后修正 `src/qaplatform/worker/settings.py`，让 project missing 分支的 `last_error="project not found"` 更新提交事务 | 测试 review 发现 check_schedules 成功用例只看 create/enqueue 被调用，无法发现 pipeline/project/env 查询、run.create 入参、enqueue trigger/settings、next_run_at 或 commit 次数漂移；project missing 分支完全没覆盖，源码更新 schedule 后未 commit，真实 DB 会丢失错误状态。现成功路径锁住查询链、run metadata、enqueue 参数、schedule update 和两次 commit；新增 missing project 用例要求 no env/no run/no enqueue/no audit 且 commit 一次，避免 cron 测试只证明“不报错” |
| 2026-05-29 | N/A（Worker notification 发送去重与日志契约单测补强） | `tests/unit/test_worker/test_notifications.py` 24 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 worker notification 运行代码，只补规则查询、delivery 去重、发送参数、成功/失败日志和多渠道无真实外部发送证据 | 测试 review 发现通知 worker 多条用例只断言 create 次数或状态，无法发现 project 查询错、delivery key 漏掉 run/rule/channel、重复通知仍发送、失败日志字段缺失，或多渠道测试悄悄走真实 channel 并只记录失败数量。现成功/失败/模板失败/重复/多渠道路径都锁住 `find_enabled_by_project`、`get_by_delivery`、`_send_channel` 参数、NotificationLog project/run/rule/channel/status/error，并在无规则路径断言 no commit，避免通知测试只验证“调用过” |
| 2026-05-29 | N/A（LogStream Redis trim 与归档 TTL 单测补强） | `tests/unit/test_engine/test_log_stream.py` 18 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 log_stream 运行代码，只补 Redis XADD trim 参数、归档 JSONL body、成功/失败 TTL 和失败 no-S3-write 证据 | 测试 review 发现 log write 只断言 key/data，无法发现 `maxlen`/`approximate` 被删导致 Redis stream 无界增长；archive success 只断言 bucket/key/content-type，不能发现 JSONL body 变形或 TTL 错；archive failure 只看返回 False，不能发现读取 Redis 失败后仍尝试写 S3 或没设置长 TTL/重试标记。现单条和 batch 写都锁住 `_MAXLEN` trim，归档解析 Body JSONL 内容并断言 `_STREAM_TTL`，失败路径断言 no put_object、`_ARCHIVE_FAILURE_TTL` 和 retry set，避免日志测试只验证 happy path 外壳 |
| 2026-05-29 | N/A（JUnit collector 路径边界与上传元数据单测补强） | `tests/unit/test_plugins/test_junit_collector.py` 19 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 JUnit collector 运行代码，只补 configured path 安全边界、collect 结果语义和 upload_report 参数证据 | 测试 review 发现 JUnit collect 的自定义路径只断言结果名称，未证明 suite/status 被正确解析；`_resolve_xml_path` 虽有绝对路径和 `..` 越界保护但没有单测，未来配置路径校验被删会读到 working dir 外报告；upload_report 只断言 bucket/key，无法发现 Body/ContentType 或 artifact metadata 漂移。现补 relative path 的 suite/status、拒绝 `../junit.xml` 和绝对路径，上传断言 Body、ContentType、artifact type/local_path/mime_type，避免 collector 测试只看结果数量 |
| 2026-05-29 | N/A（Auth middleware API token 校验副作用单测补强） | `tests/unit/test_auth_middleware.py` 17 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 auth middleware 运行代码，只补 API token 成功/拒绝路径的 secret verify、last_used 与事务证据 | 测试 review 发现 `_authenticate_api_token` 成功路径只断言返回用户和调用 update，无法发现解析出的 secret 没有用来校验 hash、last_used 时间无 timezone；拒绝路径只断言 401，无法发现 missing/revoked/expired token 仍继续做 verify、更新 last_used 或 commit。现成功路径锁住 `verify_token(secret, secret_hash)`、update 对象和 timezone-aware 时间；拒绝矩阵按记录状态断言 verify 是否应发生，并统一要求 no update/no commit，避免鉴权测试只覆盖异常码 |
| 2026-05-29 | N/A（Auth API token CRUD 单测补强） | `tests/unit/test_auth/test_auth_routes.py` 41 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 auth 运行代码，只补 API token 创建、列表、撤销的入参、审计、事务和脱敏证据 | 测试 review 发现 API token 创建只看 201/token prefix/name，无法发现 `TokenService` 没按当前用户创建、secret 未 hash、scopes/expires_at 错、明文或 hash 泄入响应/审计；列表只看默认分页 kwargs，撤销只看 204/404。现用 deterministic token 锁住 repo.create user/name/token_id/secret_hash/scopes/expires_at、响应不含 secret_hash、审计只含 name/scopes 且不含明文/哈希、commit/rollback 语义；列表改为 `page=2/per_page=1` 并断言 user_id+offset；撤销/缺失 token 断言 get/revoke/audit/commit/rollback，避免安全路由测试只验状态码 |
| 2026-05-29 | N/A（Schedule list/create 与拒绝路径单测补强） | `tests/unit/test_api/test_schedules.py` 7 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 schedules 运行代码，只补列表分页、创建入参/审计、跨 project pipeline、schedule missing 和 project missing 矩阵证据 | 测试 review 发现 schedule list 只断言空页，create 只断言 cron/pipeline 响应，get missing 只看 404；无法发现 project tenant lookup、offset/limit、pipeline 归属、`compute_next_run_at` 调用时机、schedule.create 入参或 audit create 写错。现 list 使用真实 schedule 与 `page=2/per_page=1` 断言入参和无审计；create 断言 compute、pipeline lookup、create kwargs 和 `schedule.create` audit；跨 project pipeline 与 missing schedule/project 要求统一 404 且无 schedule create/delete/audit/compute 副作用 |
| 2026-05-29 | N/A（P3 webhook/batch 拒绝路径副作用单测补强） | `tests/unit/test_api/test_p3.py` 37 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 webhook/batch 运行代码，只补缺失 project、签名缺失、archived project、batch cancel missing run、batch retry 非终态的无副作用证据 | 测试 review 发现 P3 混合测试里多条拒绝路径只断言 404/409 或 processed/failed 计数，无法发现实现返回错误前已经查 pipeline/env、创建 run、发 cancel/status 事件、入队或写审计。现对 webhook 缺失 project/签名缺失/archived project 锁住无 pipeline/env/dedup/run/audit 副作用；对 batch cancel not found 锁住 no CAS/no Redis publish/no audit；对 batch retry running 锁住 no create/no retry_group/no enqueue/no audit，避免这组测试只为状态码和覆盖率服务 |
| 2026-05-29 | N/A（Admin status 权限短路与指标查询单测补强） | `tests/unit/test_api/test_admin.py` 3 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮测试打红后修正 `src/qaplatform/api/v1/admin.py` 依赖顺序，让平台管理员校验先于 DB 指标 session | 测试 review 发现 `/admin/status` 非管理员用例只断言 403，无法发现实现为了返回 403 仍先打开 DB session；这类测试会漏掉权限短路和资源消耗问题。现新增“非管理员触碰 DB 即失败”的断言，并把 route 参数调整为先解析 `_require_platform_admin` 再进入 `_get_db_session`；admin 正向用例同时断言 4 次指标查询，避免状态页只验证 JSON shape |
| 2026-05-29 | N/A（Artifacts 归属链与预签名下载参数单测补强） | `tests/unit/test_api/test_artifacts.py` 9 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 artifacts 运行代码，只补 artifact->run 归属查询、404 短路和 S3 presign 参数证据 | 测试 review 发现 artifact helper 只断言 404/成功返回，无法发现 artifact missing 后仍查 run、run tenant lookup 参数错，或成功下载用错 bucket/key/TTL；download RBAC 成功路径也只证明 project_id 传给权限层。现断言 missing artifact 不查 run，wrong-tenant/run-missing 使用 `run.get_for_tenant(artifact.run_id, tenant)`，成功 helper 返回 artifact/run 且调用链正确；下载成功还锁住 `generate_presigned_url(get_object, Bucket=qa-platform, Key=artifacts/abc/report.html, ExpiresIn=3600)`，避免产物测试只覆盖异常码和权限 patch |
| 2026-05-29 | N/A（Projects create/update 审计与 git URL 脱敏单测补强） | `tests/unit/test_api/test_projects.py` 28 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 projects 运行代码，只补创建/更新路径的 repo 入参、审计 before/after 和 URL userinfo 脱敏证据 | 测试 review 发现 project create 只看 201 和 name，无法发现 slug 查重入参、create tenant/creator/git_url 入参、`project.create` 审计或 git URL token 脱敏被破坏；update 也只看响应 name，无法发现 repo update 入参和 `project.update` before/after 写错。现 create 使用带 userinfo 的 HTTPS git URL，断言 get_by_slug/create 入参、审计 action/resource/id、after_state name 以及 `https://***@...` 脱敏且不含 token；duplicate slug 断言不 create/不审计；update 断言 get/update 入参和审计 before/after 名称 |
| 2026-05-29 | N/A（Project members 列表入参与缺失 project 矩阵单测补强） | `tests/unit/test_api/test_project_members.py` 8 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 project-members 运行代码，只补成员列表 project/tenant 入参、跨租户用户短路和缺失 project 无副作用证据 | 测试 review 发现成员列表只断言分页 kwargs 和返回 role，无法发现 project_id/tenant_id 传错或 list 误写审计；跨租户用户拒绝没有证明不会继续查 existing member；project 不可见时也没有 list/add/update/remove 矩阵。现断言 list 的 project lookup、`list_by_project_tenant(project, tenant)` 和无审计；跨租户用户只查 user、不查 existing、不创建/审计；新增四路缺失 project 统一 `NOT_FOUND` 且 user/member repo/audit 全无调用，避免成员管理测试只覆盖状态码外壳 |
| 2026-05-29 | N/A（Notification rules 列表、审计与缺失 project 矩阵单测补强） | `tests/unit/test_api/test_notifications.py` 9 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 notifications 运行代码，只补通知规则路由的分页入参、审计 await 和 project 不可见无副作用证据 | 测试 review 发现通知规则列表只断言空 data，无法发现 project_id、tenant_id、offset/limit 传错或列表误写审计；create/update/delete 虽有脱敏检查，但没有严格证明 audit create 被 await；缺失 project 也没有覆盖五个入口统一短路。现把 list 改为带真实规则和 `page=2/per_page=1` 的语义用例，断言 project access、分页、响应字段和无审计；create/update/delete 使用 awaited 审计断言；新增 list/create/get/update/delete 缺失 project 矩阵，要求统一 `NOT_FOUND` 且 notification_rule repo/audit 全无调用 |
| 2026-05-29 | N/A（Credentials 路由隔离、分页与 rotate 审计单测补强） | `tests/unit/test_api/test_credentials.py` 10 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 credentials 运行代码，只补敏感凭证路由的 project/tenant 入参、rotate 审计和缺失 project 无副作用证据 | 测试 review 发现凭证列表只断言分页 kwargs，无法发现 project_id/tenant_id 传错；rotate 只证明重新加密和 update，无法发现 `credential.rotate` 审计被删、resource 写错或新 secret 进入审计；project 不可见时也缺少 list/create/get/update/delete 矩阵。现补 list positional args、rotate 响应/审计脱敏，以及缺失 project 五路统一 `NOT_FOUND` 且 credential repo、crypto、audit 全无调用，避免凭证测试只围绕状态码和调用次数 |
| 2026-05-29 | N/A（Audit events list 路由语义单测补强） | `tests/unit/test_api/test_audit.py` 7 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 audit-events 运行代码，只补审计查询路由的过滤、分页、跨租户掩蔽和自审计证据 | 测试 review 发现 Audit helper 有单测、project create 有审计烟测，但 `GET /audit-events` 路由没有 unit 级行为契约；若 tenant filter、offset/limit、跨租户 404 掩蔽或 `audit_events.list` 自审计摘要被删，状态码烟测难以及时发现。现新增成功列表用例，断言过滤参数透传、`page=3/per_page=2` 转为 `offset=4/limit=2`、响应包含目标事件、自审计只记录查询摘要不复制 data/敏感字段；同时新增跨租户匹配用例，要求统一 `NOT_FOUND` 响应且不写 list 自审计 |
| 2026-05-29 | N/A（Environment list 分页与解密单测补强） | `tests/unit/test_api/test_environments.py` 16 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 environment 运行代码，只补列表正向路径的分页、解密和字段映射证据 | 测试 review 发现 Environment list 在 unit 层没有正向语义用例，无法发现 offset/limit 算错、env_vars 未解密、resource_limits 映射丢 disk/artifact 限制，或普通读列表误写审计。现新增带加密 env_vars 的列表用例，断言 page/per_page/total、response 全字段、`list_by_project(project, offset=10, limit=5)`，并要求 audit/commit 无调用，避免列表接口只靠集成 smoke 兜底 |
| 2026-05-29 | N/A（Environment 缺失 project 路由矩阵单测补强） | `tests/unit/test_api/test_environments.py` 15 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 environment 运行代码，只补 project 不可见时 CRUD 矩阵的统一拒绝和无副作用证据 | 测试 review 发现 Environment 已覆盖 env 自身 missing/cross-project，但缺少 project 不可见时 list/create/detail/update/delete 的路由矩阵；若 create 先碰 crypto 或 environment repo，再返回 404，会泄露或污染审计。现用会主动失败的 crypto service 保护 create/update，覆盖五个入口统一 `Project not found` 响应体，并断言 environment list/get/create/update/delete 与 audit/commit 全无调用 |
| 2026-05-29 | N/A（Environment update/delete 404 隔离与无副作用单测补强） | `tests/unit/test_api/test_environments.py` 14 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 environment 运行代码，只补 update/delete 的 missing/cross-project 隐藏资源拒绝路径 | 测试 review 发现 get detail 已覆盖跨 project 坏密文不解密，但 update/delete 仍缺同类证据；若未来先解密、加密新 env_vars、更新、删除或写 audit，再返回 404，会泄露存在性或污染审计。现用 missing id 与 cross-project bad ciphertext 覆盖 PUT/DELETE，要求四种响应体一致为 `Environment not found`，并断言 create/update/delete/audit/commit 全无调用 |
| 2026-05-29 | N/A（Environment detail 404 隔离与解密短路单测补强） | `tests/unit/test_api/test_environments.py` 13 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 environment 运行代码，只补 get detail 的跨 project 隐藏和无副作用证据 | 测试 review 发现 Environment get 只有解密失败 500 用例，没有覆盖缺失或属于其他 project 的 404；若实现先解密再判归属，坏 env_vars 会返回 500 并写 crypto failure audit，泄露存在性。现新增 missing 与 cross-project 响应体一致断言，并给 cross-project 环境放入坏加密载荷，要求仍为 `Environment not found`、不 create/update/delete、不写 audit/commit，证明归属校验先于解密 |
| 2026-05-29 | N/A（Environment create 加密与审计单测补强） | `tests/unit/test_api/test_environments.py` 12 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 environment 运行代码，只把 create 用例从密钥脱敏扩展为完整创建契约 | 测试 review 发现 create env_vars 用例只证明响应回显和审计不含 secret，且 mock 会回默认 disk limit，无法发现 repository 入参丢 resource limit/network/cache、audit action/resource/id/tenant/user 错，或 env var key 泄入审计。现让 create mock 按入参回显，断言 encrypted env_vars 不含 key/secret 且可解密、resource_limits/network/cache 等 repo 入参正确、audit `environment.create` after_state 含环境核心字段并只记录 env_vars count |
| 2026-05-29 | N/A（Environment update env_vars 加密与审计单测补强） | `tests/unit/test_api/test_environments.py` 12 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 environment 运行代码，只把 env_vars update 从“响应正确且审计无明文”补成完整加密和审计契约 | 测试 review 发现 update env_vars 用例只验证响应返回新变量、审计 repr 不含 secret，无法发现 repo update 未按 env id 重新加密、audit action/resource/id 错、before/after 不是同一环境，或变量 key 泄入审计。现断言 get/update 调用、存储 envelope 不含新旧 key/secret 且可解密为新值、audit `environment.update` action/resource/tenant/user、before/after id/project/disk/env_vars count，并保证 `OLD_TOKEN`/`NEW_TOKEN` 与明文 secret 均不进审计 |
| 2026-05-29 | N/A（Environment delete 审计与脱敏单测补强） | `tests/unit/test_api/test_environments.py` 12 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 environment 运行代码，只补删除路径的审计和密钥脱敏证据 | 测试 review 发现 Environment create/update 覆盖了 env_vars 加密和审计脱敏，但 delete 路径没有单测，无法发现删除时没查旧环境、未调用 repository delete、`environment.delete` action/resource 错、before_state 丢 resource limit 或旧 env_vars 泄密。现新增删除用例，断言 get/delete 调用、audit action/resource/id、旧 id/project/base_image/disk/env_vars count、after_state 为空，并保证旧 `API_TOKEN`/secret 不进入审计 |
| 2026-05-29 | N/A（Pipeline 缺失 project 路由矩阵单测补强） | `tests/unit/test_api/test_pipelines.py` 6 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 pipeline 运行代码，只把 missing project 用例从单一路由扩展为 CRUD 矩阵证据 | 测试 review 发现 `test_pipeline_routes_hide_missing_project` 名字像覆盖所有 Pipeline 路由，但实际只测 list，无法发现 create/detail/update/delete 在 project 不可见时继续查 pipeline、写入或审计。现覆盖 list/create/detail/update/delete 五个入口，要求统一 404 响应体、每次只做 project tenant lookup，并断言 pipeline list/get/create/update/delete 与 audit 全部无调用，避免“路由矩阵”测试只有一个 GET |
| 2026-05-29 | N/A（Pipeline detail 404 隔离与无副作用单测补强） | `tests/unit/test_api/test_pipelines.py` 6 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 pipeline 运行代码，只把 get detail 的 404 用例从状态码扩展为隔离语义证据 | 测试 review 发现 get pipeline 缺失与“存在但属于其他 project”只断言 404，无法发现错误体差异造成枚举，也不能证明读路径不会误触发 create/update/delete/audit。现断言两种响应体完全一致、detail 为 `Pipeline not found`、按目标 id 查询，并要求 pipeline 写操作和 audit 全部无调用，避免 404 测试只是路由外壳 |
| 2026-05-29 | N/A（Pipeline update 审计 before/after 单测补强） | `tests/unit/test_api/test_pipelines.py` 6 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 pipeline 运行代码，只把 update 测试从脱敏断言扩展为完整审计语义契约 | 测试 review 发现 pipeline update 单测虽然覆盖了敏感配置脱敏，但没有证明写的是 `pipeline.update`、resource id 正确、tenant/user 正确，或 before_state 真的是更新前旧值。现断言 response/update 入参、audit action/resource、旧 name/enabled/stages/trigger_config/retry_policy、新 name/enabled/retry_policy 以及敏感配置脱敏，避免更新审计只证明“不泄密”却漏掉“记错对象或旧状态” |
| 2026-05-29 | N/A（Pipeline create 审计语义与脱敏单测补强） | `tests/unit/test_api/test_pipelines.py` 6 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 pipeline 运行代码，只把 create 测试从“audit 被调用”补成资源字段和脱敏契约 | 测试 review 发现 pipeline create 单测只确认 repo 入参和 audit 调用次数，无法发现 `pipeline.create` action/resource 写错、after_state 丢字段，或 stage/trigger/collector 配置中的 token 泄入审计。现用真实嵌套 payload 携带 token，断言 repo 仍保存原始配置，同时 audit after_state 记录 project/name/stages/retry_policy 并脱敏 `API_TOKEN`、`webhook_secret`、collector `api_token` 和 URL userinfo，避免审计测试只是覆盖率调用哨兵 |
| 2026-05-29 | N/A（Run trigger 跨租户 pipeline 拒绝副作用补强） | `tests/unit/test_api/test_runs.py` 26 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改运行代码，只补跨租户 pipeline 触发 run 的无副作用证据 | 测试 review 发现跨租户 pipeline 触发用例只断言 404 文案，无法证明实现不会继续查 environment、创建 Run、设置 retry group、入队或写 `run.trigger` audit。现断言跨租户 project lookup 后短路，并要求 environment/run/arq/audit 全部无调用，避免隔离路径只有状态码假绿 |
| 2026-05-29 | N/A（Run trigger/cancel 拒绝路径副作用补强） | `tests/unit/test_api/test_runs.py` 26 passed；targeted ruff passed；`git diff --check` passed；`tests/unit/test_api/test_runs.py tests/unit/test_api/test_pipelines.py` 32 passed | 无新增 warning；本轮补测试时发现并修正 terminal cancel 错误体枚举口径，运行代码现在返回 `done` 而不是 `RunStatusEnum.DONE` | 测试 review 发现 `cancel_already_terminal` 只断言 409，无法证明不会 CAS 取消、发 Redis cancel/status 事件或写 `run.cancel` audit；`trigger_run_pipeline_not_found` 只断言 404，无法证明不会继续查 project、创建 Run、设置 retry group、入队或写审计。现补无副作用断言，并把终态取消错误消息改为用户可读枚举 value，避免拒绝路径和错误契约假绿 |
| 2026-05-29 | N/A（Pipeline delete 与 Run cancel 审计/事件单测补强） | `tests/unit/test_api/test_pipelines.py` 6 passed；`tests/unit/test_api/test_runs.py` 26 passed；targeted ruff passed；`git diff --check` passed；组合单测 43 passed | 无新增 warning；本轮不改 pipeline/run 运行代码，只补删除与取消路径的审计、响应和事件语义断言 | 测试 review 发现 pipeline delete 单测只断言 audit 被调用，无法发现 action/resource/before_state 写错；run cancel 单测只断言 Redis 方法被调用，无法发现响应仍是 running、CAS 取消参数错误、Redis event 内容错或 audit before/after 错。现 pipeline delete 锁住 `pipeline.delete` resource、删除前 name/enabled/stages/trigger_config/retry_policy 和空 after_state；run cancel 模拟 running -> cancelled 重读，断言响应状态、`cancel_if_current` expected statuses、cancel pub/sub channel、status stream/hash 内容和 `run.cancel` audit |
| 2026-05-29 | N/A（Schedule API 审计单测补强） | `tests/unit/test_api/test_schedules.py` 5 passed；targeted ruff passed；`git diff --check` passed | 目标用例运行时仍有既存 rate-limit mock warning 文本，但不影响断言；本轮不改 schedule 运行代码，只补 update/delete 审计 before/after 契约 | 测试 review 发现 schedule update/delete 单测只看状态码、对象字段和 delete 调用，若 `write_audit` 被删或 action/resource/before/after 写错，单测仍会绿。现 update 断言 `schedule.update` 审计记录旧 cron、新 cron 和 `next_run_at` JSON 时间口径；delete 断言 `schedule.delete` 审计记录旧 cron/enabled 且 after_state 为空，避免调度接口审计只靠较慢 integration 兜底 |
| 2026-05-29 | N/A（Auth API token 跨用户撤销边界单测） | `tests/unit/test_auth/test_auth_routes.py::TestTokenRoutes` 6 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 auth 运行代码，只补 API token revoke 的跨用户拒绝副作用证据 | 测试 review 发现 token revoke 只覆盖自有 token 204 和不存在 token 404，没有证明攻击者猜到他人 `token_id` 时仍隐藏为 404，且不会调用 revoke 或写 `auth.api_token_revoke` audit。现新增跨用户 token 用例，断言 `Token not found`、只查 token_id、不 revoke、不写 audit、不 commit，并发生 rollback，避免撤销接口只靠状态码覆盖 |
| 2026-05-29 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_webhook_branch_dedup.py` 13 passed | targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 webhook 运行代码，只把 terminal same-commit 复触发用例从数量断言补成 Run 内容证据 | 测试 review 发现 terminal 后同 commit 可再次触发的用例只断言同 dedup key 的 webhook run 数量为 2，无法发现第二次返回旧 run、状态没重置、git ref/sha 错位或 triggered_by 丢失。现断言第一次 queued 后手动置 DONE，第二次返回不同 run id，两个真实 Run 分别为 DONE/QUEUED，保留原始 `refs/heads/...` git_ref、相同 git_sha/dedup key，并写入正确触发人 |
| 2026-05-29 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_audit_events_api.py` 5 passed | targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 audit-events 运行代码，只把 owner/admin 列表权限用例从 200-only 补成可见数据证据 | 测试 review 发现 `test_audit_events_owner_admin_can_list_member_viewer_forbidden` 只断言 owner/admin 返回 200，如果列表查询返回空页或遗漏目标审计事件仍会绿。现插入真实 `project.create` 审计事件后，要求 owner/admin 响应 total 非空且 data 中包含该 event id；member/viewer 仍保持 403，避免权限测试只覆盖状态码外壳 |
| 2026-05-29 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_auth_results_artifacts.py::test_api_token_scope_matrix_enforced_by_real_routes` 1 passed | targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 auth/scope 运行代码，只把真实 API token scope 矩阵从状态码表补成权限副作用证据 | 测试 review 发现 API token scope 矩阵只断言 200/201/403，无法发现只读 token 写入 project、run.trigger token 创建错 Run、或 schedule 拒绝后仍落库。现断言只读 token 能读到目标 project、拒绝写 project 不落库、触发响应和真实 Run 行匹配 project/pipeline/environment/queued/manual，且 schedule 拒绝前后 Schedule 计数不变 |
| 2026-05-29 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_api_endpoints.py` 19 passed | targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 pipeline 运行代码，只把 pipeline list smoke 从 seed 非空补成新建资源回读契约 | 测试 review 发现 `test_list_pipelines` 只断言列表里有 seed pipeline，如果列表端点漏掉刚创建的管线、丢嵌套字段或只返回旧数据仍会绿。现先 POST 创建 `list-pipeline`，再 GET 列表按 id 找回，并要求列表项的 stages/selector/trigger_config/collectors/retry_policy 与创建响应的规范化输出一致，避免管线列表测试只是路由覆盖 |
| 2026-05-29 | N/A（release gate workflow 契约单测） | `tests/unit/test_release_gate_workflow.py` 10 passed；targeted ruff passed | 无新增 warning；本轮不改 CI workflow 行为，只把无显式 assert 的 YAML 解析测试补成结构断言 | 测试 review 的 AST 扫描发现 `test_ci_workflow_yaml_has_no_duplicate_mapping_keys` 只依赖 `yaml.load` 不抛异常，虽然能抓重复 key，但无法证明 workflow 根对象和关键 jobs 仍可被解析。现断言解析结果是 dict、`jobs` 是 dict，并包含 backend-test/backend-integration/frontend-api/e2e/release-gate，避免 CI 证据门测试只做“没抛异常”的隐式通过 |
| 2026-05-29 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_auth_audit_failure_paths.py` 12 passed | targeted ruff passed | 无新增 warning；本轮不改 auth/rate-limit 运行代码，只把 login rate-limit 与 audit outage 测试补成审计计数证据 | 测试 review 发现 login 失败限流用例验证了 Redis token hash bucket 和 429，但没有证明第 6 次被限流请求没有继续进入 auth failure audit；audit outage 三条用例也只看状态码和 warning，无法证明审计事务没有半成功写入。现要求 5 次业务失败只新增 5 条 `auth.login_failed`，限流请求不新增审计噪声；审计写失败的 login/refresh/logout 路径前后 audit count 不变，避免测试只证明状态码、不证明 handler 短路与 rollback 边界 |
| 2026-05-29 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_api_endpoints.py` 19 passed | targeted ruff passed | 无新增 warning；本轮不改 core API 运行代码，只把列表与跨租户 smoke 从外壳断言补成业务语义证据 | 测试 review 发现 projects / notification rules 列表用例先创建资源，却只断言 `total >= 1`，如果列表只返回旧 seed 数据或丢字段仍会绿；核心跨租户 project smoke 也只看 tenant B ID 返回 404，无法抓 existence oracle 错误体漂移。现断言 create 成功、created id 出现在列表中并核对关键字段，同时要求 tenant B project 与随机 UUID 的 404 响应体完全一致，避免列表和隔离 smoke 只是覆盖路由外壳 |
| 2026-05-29 | `RUN_INTEGRATION_TESTS=1 RUN_PERFORMANCE_TESTS=1 tests/integration/test_performance_smoke.py::test_read_run_api_p99_smoke tests/integration/test_performance_smoke.py::test_write_run_api_p99_smoke` 2 passed | targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮不改 run 读写实现，只把 read/write run p99 smoke 从状态码采样补成语义采样 | 测试 review 发现 read run smoke 只断言 200，write run smoke 20 次循环只断言 201 和 p99，无法发现接口很快但读错对象、创建错 tenant/project/environment、未设置 retry group、误入队或漏写 `run.trigger` audit。现读路径每个样本断言目标 Run 核心字段，写路径每个样本断言响应字段，采样后批量核对真实 Run 行的 queued/manual/no-arq 状态和 audit after_state，避免性能 smoke 只证明“快”不证明“读写对” |
| 2026-05-29 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_webhook_branch_dedup.py` 13 passed | targeted ruff passed | 无新增 warning；本轮不改 webhook 运行代码，只把跨租户 404 用例补成无副作用证据 | 测试 review 发现跨租户 project webhook 用例只断言 404，无法发现实现先创建租户 B webhook run 或写 webhook decision audit 后再返回隐藏错误。现断言目标项目 webhook run 数不变，且不写 `webhook.filtered` / `webhook.duplicate` audit，避免安全隔离路径只有状态码假绿 |
| 2026-05-29 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_silent_windows.py` 6 passed | targeted ruff passed | 无新增 warning；本轮不改 silent window 运行代码，只把 manual/webhook 绕过静默窗口的弱断言补成响应、DB 和审计证据 | 测试 review 发现 manual/webhook ignores silent windows 只断言 201 和 run 数量 +1，无法发现 run 创建成错误 trigger_type、git_ref/git_sha、未写 retry group、误入队或审计 after_state 错位。现断言响应字段、新 Run 的 metadata/dedup_key/无队列池状态，以及 `run.trigger` audit，确保“绕过静默窗口”不是仅靠状态码假绿 |
| 2026-05-29 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_api_endpoints.py` 19 passed | targeted ruff passed | 无新增 warning；本轮不改 analytics 运行代码，只把 200-only 集成断言升级为真实聚合证据 | 测试 review 发现 trends/flaky 只断言 200 和 list，甚至注释说明只验证 endpoint returns 200；现在用真实终态 Run 和 TestResult 行断言日期趋势 total/passed/failed/pass_rate、pagination total，以及 flaky mixed outcomes 的 passed/failed/flaky_rate，避免聚合查询坏掉但测试仍绿 |
| 2026-05-29 | N/A（API 单测强化批次横向复核） | `tests/unit/test_api` 249 passed；changed API slices 80 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；本轮强化后的 API 单测目录复跑未出现 fixture 横向副作用 | 本批把 Artifact/Credentials/Project Members/Projects/Runs 的状态码型断言升级为副作用、审计、敏感字段、repository 参数与响应语义契约；横向复跑确认新增 fake Redis/audit mock 不影响同层 API 单测 |
| 2026-05-29 | N/A（Project Members 响应与审计语义复核） | `tests/unit/test_api/test_project_members.py` 7 passed；targeted ruff passed | 无新增 warning；fixture 显式关闭 Redis client，避免 rate-limit 误把裸 `MagicMock` 当 Redis | 测试 review 发现成员添加/更新/移除用例主要看状态码和 repo 方法调用，无法发现更新响应仍返回旧 role、审计 before/after role 错误，或拒绝路径仍创建成员/写审计。现让 update mock 模拟真实 repository mutation，并断言响应 role、add/update/remove audit、拒绝路径 no-create/no-delete/no-audit |
| 2026-05-29 | N/A（Artifact 下载拒绝路径副作用复核） | `tests/unit/test_api/test_artifacts.py` 9 passed；targeted ruff passed | 无新增 warning；本轮不改 Artifact 运行代码，只给 RBAC denied 单测补对象存储副作用哨兵 | 测试 review 发现 `download_artifact` 403 用例只断言权限错误，若实现先生成 S3 presigned URL 再做项目权限校验，测试仍可能通过。现为拒绝路径注入 S3 mock 并断言 `generate_presigned_url` 未调用，确保权限失败不会提前触碰对象存储或泄露产物路径存在性 |
| 2026-05-29 | N/A（Credentials API 密钥副作用复核） | `tests/unit/test_api/test_credentials.py` 9 passed；targeted ruff passed | 无新增 warning；fixture 显式关闭 Redis client，避免 rate-limit 误把裸 `MagicMock` 当 Redis | 测试 review 发现 duplicate credential 与 delete credential 用例只看状态码/删除调用，没证明重复名不会创建/写审计，也没证明删除审计不泄露 `value` / `encrypted_value`。现补充 no-create/no-audit、`credential.delete` audit 和敏感字段缺失断言，确保密钥类接口不只是状态码覆盖 |
| 2026-05-29 | N/A（Projects API 状态码型单测复核） | `tests/unit/test_api/test_projects.py` 28 passed；targeted ruff passed | 无新增 warning；本轮不改 Projects 运行代码，只把列表/删除弱断言升级为 repository 参数与审计契约 | 测试 review 发现项目搜索列表用例只断言 200 + repo 被调用，删除项目只断言 204，无法发现分页 offset/limit 丢失、搜索 filter 未传、delete 未调用或审计静默失效。现补充列表响应与 repository 参数断言，删除用例断言 `repos.project.delete(project)` 与 `project.delete` audit，避免状态码假绿 |
| 2026-05-29 | N/A（Run trigger 无队列池 fallback 语义复核） | `tests/unit/test_api/test_runs.py` 26 passed；targeted ruff passed | 旧用例使用裸 `MagicMock` Redis 会让 rate-limit pipeline 产生未 await warning；已改为显式 fake Redis，单文件复跑无 warning | 测试 review 发现 `test_trigger_run_enqueues_without_arq_pool` 只断言 201，无法发现 project mock 缺少 `git_url` / `git_auth_method` / `credential_id` / `shallow_clone` 时 metadata 被 truthy mock 污染，也不能证明无 `arq_pool` 时 run 仍保持 queued、retry group 已设置、审计已写入。用例改为断言响应状态、run create 参数、metadata、`set_retry_group_id`、Redis cancel/status 发布和 `run.trigger` audit，避免触发路径只为覆盖率服务 |
| 2026-05-29 | N/A（Settings API Token 撤销语义复核） | `tests/unit/test_auth/test_auth_routes.py::TestTokenRoutes::test_list_tokens` 1 passed；`tests/unit/test_frontend_api_contract.py` 27 passed；frontend lint `--max-warnings=0` passed；frontend build passed；`npx playwright test tests/e2e/auth-flow.spec.ts --project=chromium` 1 passed；Playwright headless visual smoke 断言 Active/Revoked 文案与 revoked 按钮 disabled | Playwright 本地启动 compose/API 前置时仅有 `NO_COLOR`/`FORCE_COLOR` 环境提示，不影响断言；无新增业务 warning | 测试 review 发现 PR 级 mocked E2E 把 `DELETE /auth/tokens/{id}` 模拟成从列表删除 token，但真实后端是软撤销并继续在列表中返回 `is_revoked=true`。这会让测试绿、真实 Settings 页面仍把已撤销 token 显示成普通可撤销 token。现改为页面展示 Active/Revoked 状态、已撤销 token 禁用撤销按钮，后端路由单测锁住列表返回 `is_revoked` 且不返回 `token`/`secret_hash`，E2E mock 改成保留行并标记 `is_revoked`，契约测试禁止再用 `apiTokens.filter(...)` 伪造删除语义 |
| 2026-05-29 | N/A（SSE endpoint-exists 弱断言复核） | `tests/unit/test_api/test_sse.py` 15 passed；targeted ruff passed | 无新增 warning；本轮不改 SSE 运行代码，只把“端点存在”型单测升级为事件 payload 契约 | 测试 review 发现日志/状态 SSE 单测只断言 200 和 `text/event-stream`，如果 Redis Stream 数据没有被 yield、事件类型写错或 payload 丢失，测试仍可能通过。将用例改为断言 `event: log` / `event: status_change` 以及真实 payload 文本，确保它们能发现序列化链路断裂，而不是只为路由覆盖率服务 |
| 2026-05-29 | N/A（Rate limit Redis client 缺失分支复核） | `tests/unit/test_api/test_rate_limit.py` 27 passed；`tests/unit/test_auth tests/unit/test_auth_middleware.py` 125 passed；targeted ruff passed | 无新增 warning；本轮不改变普通业务接口 fail-open 策略，只修正 auth 高风险端点在 Redis client 未注入/未就绪时的 fail-closed 行为 | 测试 review 发现既有用例只覆盖 Redis 操作异常，没有覆盖 middleware 启动期或容器未暴露 Redis client 的 `redis is None` 分支，导致登录、注册、token、refresh、SSE ticket 可绕过限流保护。新增缺失 client 回归用例，要求 auth 高风险端点返回 503 + `Retry-After: 5` 且不调用下游 handler，普通 `/api/v1/runs` 仍 fail-open；运行手册同步区分非认证端点与认证高风险端点的 Redis client 缺失行为 |
| 2026-05-29 | N/A（架构文档当前实现口径复核） | `tests/unit/test_release_quality_docs_contract.py` 5 passed；targeted ruff passed；旧口径短语扫描无命中；`git diff --check` passed | 无新增 warning；本轮不改运行代码，只修正 architecture / doc-conflict-audit 的 stale 状态并补文档契约测试 | `architecture.md` 不再声称 Git provider webhook 与审计清理任务尚未实现；当前口径改为 GitHub provider 已支持 `/webhooks/github` 与 `/api/v1/webhooks/github`、repository URL candidates 匹配和系统身份审计，审计日志由 worker `cleanup_old_audit_events` 按 `retention_audit_days` 清理。契约测试禁止这些旧句回流，并要求文档保留 GitLab/Gitee 等剩余增强边界 |
| 2026-05-29 | N/A（Settings API Token 管理占位页复核） | `tests/unit/test_frontend_api_contract.py` 27 passed；frontend lint `--max-warnings=0` passed；frontend build passed；`npx playwright test tests/e2e/auth-flow.spec.ts --project=chromium` 1 passed；targeted ruff passed；`bash -n scripts/smoke/07-settings.sh` passed；`git diff --check` passed | 无新增 warning；本轮不改变后端 API token 权限模型，只把已有 `/auth/tokens` 能力接到前端 Settings 并扩展 PR 级 mocked E2E；手工 smoke 从任意段落/skip 改为令牌创建入口和表格硬断言 | 测试 review 发现后端 token 创建/列表/撤销和集成测试已存在，但 Settings 仍显示 “API Tokens management” 占位文案，属于用户可见假完成。新增 `use-api-tokens`、真实管理页和前端契约测试，Playwright 登录流覆盖 token 列表、创建后一次性明文展示、列表刷新和撤销，避免文档/后端已有能力但前端入口空壳仍被覆盖率掩盖 |
| 2026-05-29 | N/A（CI workflow YAML 结构契约复核） | `tests/unit/test_release_gate_workflow.py` 10 passed；`tests/unit` 907 passed；ruff full passed；`git diff --check` passed | 无新增 warning；本轮不改变 workflow 行为，只补 release gate workflow 的重复 key 契约 | `ci.yml` 常规 YAML 解析不会可靠暴露重复 mapping key；新增单测使用唯一 key loader 解析 workflow，避免 `run`、`with`、`if` 等字段重复时被解析器静默保留最后一个值，导致 GitHub Actions 配置评审假绿 |
| 2026-05-29 | N/A（前端 Run 状态假绿与 E2E 弱断言复核） | frontend lint `--max-warnings=0` passed；frontend build passed；`tests/unit/test_frontend_api_contract.py` 21 passed；`npx playwright test tests/e2e/auth-flow.spec.ts --project=chromium` 1 passed；`npx playwright test tests/e2e/special-regressions.spec.ts --project=chromium` 7 passed | 无新增 warning；本轮只收紧前端 `done` 状态归一化和真实 E2E 的弱断言，不改变后端 run 状态枚举 | 后端返回 `done` 但 summary `total=0` 时，前端不再显示 `Passed`，而是显示 `Unknown`；若旧数据只有 passed/failed/skipped/error 计数但缺少 `total`，前端会从计数推导总数，避免把有效历史 summary 误显示为 `Unknown`；E2E mock 同时覆盖 failed summary、empty collector summary 和 legacy summary，避免采集器/JUnit 解析丢结果时在 Recent Runs 页面形成假绿；真实 special-regressions 不再用 `toBeTruthy()` 接受任意非空用户 id，而是校验 UUID 形态 |
| 2026-05-29 | N/A（Go runner 与默认 JUnit collector 产物契约复核） | `tests/unit/test_plugins/test_go_test_runner.py` 18 passed；`tests/unit/test_plugins` 123 passed；`tests/unit` 906 passed；ruff full passed；`git diff --check` passed；真实本机 Go smoke 证明 `run_tests()` 对 1 pass / 1 fail 解析为 `passed=1 failed=1 error=0` 并写出 JUnit；模拟 worker shell command smoke 证明 `build_command()` 可由 `go test -json` 生成 `results/go-test.json` 与 `results/junit.xml` | 无新增 warning；本轮不引入 `go-junit-report` 等外部依赖，只用 Go 标准库 helper 转换 worker 产物；worker shell 用 `mkfifo + tee` 保持 stdout 实时流，不因保存 JSON 延后日志 | Go runner 不再只在直接 `run_tests()` 路径解析 stdout；真实 worker 路径默认也会创建 `results/`、保存 `go-test.json` 并写出 collector 默认读取的 `results/junit.xml`。同时修正真实 `go test -json` 会在失败测试后追加 package-level `fail` 的双算风险，避免一个失败用例被平台记成 failed + error |
| 2026-05-29 | N/A（Jest/Playwright runner 与 JUnit collector 路径契约复核） | `tests/unit/test_plugins/test_jest_runner.py` 13 passed；`tests/unit/test_plugins/test_playwright_runner.py` 20 passed；`tests/unit/test_plugins` 121 passed；`tests/unit` 904 passed；ruff full passed；frontend lint/build passed；`git diff --check` passed | 无新增 warning；本轮不启动真实 Node 项目，只把 runner 命令和直接执行路径的报告位置契约锁住 | Jest runner 不再只声明 `jest-junit` reporter 却让报告落到默认 `junit.xml`；Playwright runner 不再把输出路径拼进 `--reporter=junit,path`，两者默认都会创建 `results/` 并通过官方 JUnit 输出 env 写到 collector 默认读取的 `results/junit.xml`，自定义 `junit_xml` 也会覆盖 env var，避免真实测试已执行但平台 summary/results 因报告路径不一致而全零 |
| 2026-05-29 | required integration 147 passed / 51 deselected；heavy Docker 9 passed / 3 skipped（本机 macOS OOM 语义前置）；external-stack 6 passed；external-stack performance 2 passed；release_candidate performance summary 39 SLO validator + trend passed | `tests/unit` 901 passed；runner env merge targeted 79 passed；README 端点/runner 文档契约 3 passed；release sign-off 文档一致性契约 2 passed；frontend API contract 21 passed；ruff full passed；frontend lint `--max-warnings=0` passed；frontend build passed；Playwright `--list` 10 tests / 4 files；`git diff --check` passed；README 端点清单经 `create_app()` 路由表自动核对 | 无新增 warning；本机已启动隔离 compose API/worker/MinIO 验证真实 worker 链路，临时失败定位为本地 Docker socket group 配置错误，改用容器内 socket 实际 root group 后复跑通过；release_candidate 完整发版门仍需 CI Linux 环境补 OOM 零 skip 证据 | 本轮测试 review 把“只为覆盖率”的弱断言转成可观察契约：integration schema 不再 metadata fallback、smoke skip 默认失败、前端 API contract 校验 enum/nullability/required/array item 且禁止前端把后端非 nullable 字段放宽成 null、真实 pytest JUnit `type="pytest.xfail"` 会保留 TestResult `xfail` 并进入后端 summary、前端状态展示、E2E 种子汇总与 results status OpenAPI enum/422 校验，pytest runner summary 不再把首个计数/单结果 summary 错当全零，runner shell command 校验参数带空格时不会被拆坏且统一在 `/workspace` 执行，内置 runner 直跑时 `env_vars` 会叠加保留基础环境而不是替换掉 `PATH` 等工具链变量，setup script 也固定从 workspace 根目录运行，README/architecture/protocol 的第三方 runner `/workspace` 与 shell quoting 契约被单测锁住，release slice 不再把未记录的 Linux CI release_candidate OOM 零 skip 证据写成已通过，rate limit 单测实际覆盖两个耗尽 token 与第三个 token hash bucket 隔离，lifespan/worker startup 测试证明 `s3_client` 真进入 app/worker 上下文，external-stack 真实 pytest package smoke 不再接受 failed 终态或 worker 不可访问的本地 `file://` fixture，必须验证 Run done、summary/results passed、JUnit artifact 与 archived logs，外部栈真实 worker 覆盖 SSE/live logs/artifact/archived logs/worker_lost/priority/clone-setup 失败与 10 容器并发 SLO；本地剩余证据缺口仅为 macOS Docker Desktop 下 OOMKilled 语义不可稳定证明 |
| 2026-05-29 | performance collect-only：`test_performance_smoke.py` 31 tests collected；`test_worker_execute.py -m "external_stack and performance"` 2 selected / 6 deselected | worker/config targeted unit 39 passed；targeted ruff passed；CI YAML parsed；SLO manifest 39 entries；performance summary validator positive/negative passed | 无新增 warning；本轮不启动真实 external stack 或完整 release_candidate，只把准入契约、collect 基线和样本校验先收紧 | release_candidate 性能准入从“内联 SLO 名称 + 单样本有效性”升级为版本化 `.github/performance-slo-manifest.json`：39 个 SLO 声明 `threshold_env` / `min_samples` / kind，CI 由 `scripts/validate_performance_summary.py` 拒绝缺项、意外项、阈值漂移、profile 不匹配、失败或样本不足；external-stack performance 新增单 worker 10 容器并发证据，CI 外部栈会把 `QAP_MAX_CONCURRENT_RUNS` / `QAP_MAX_CONCURRENT_PER_PROJECT` / `QAP_WORKER_MAX_JOBS` 覆盖为 10 |
| 2026-05-29 | required collect-only 147/197；`RUN_INTEGRATION_TESTS=1 tests/integration/test_webhook_branch_dedup.py` 13 passed；GitHub provider webhook targeted 3 passed；legacy duplicate targeted 1 passed | targeted ruff passed | 无新增 warning；本轮不启动完整 release_candidate，只把 webhook provider 正式入口和既有项目级 dedup 回归一起锁进 required integration | GitHub provider webhook 从“只能带 project_id 调项目级入口”补成 `/webhooks/github` 与 `/api/v1/webhooks/github`：按 GitHub repository URL candidates 匹配项目，用匹配项目 `webhook_secret` 验签，无登录用户时以系统身份触发并写 audit；无效签名不创建 Run，branch filter/duplicate 仍写决策 audit；backend integration collect baseline 同步收紧到 147 |
| 2026-05-29 | required collect-only 144/194；`RUN_INTEGRATION_TESTS=1 tests/integration/test_real_repository_matrix.py::test_audit_retention_hard_deletes_only_events_older_than_cutoff` 1 passed | `tests/unit/test_worker/test_settings_tasks.py` 14 passed；targeted ruff passed | 无新增 warning；本轮不启动完整 release_candidate，只补数据保留准入缺口的实现和目标证据 | `retention_audit_days` 从“只有配置”补成 worker `cleanup_old_audit_events` 定时清理；repository 方法按 `audit.event.created_at` 硬删超期审计，unit 锁住 cron 入口提交/日志，真实 Postgres 覆盖普通租户审计、系统审计超期删除和未超期保留；backend integration collect baseline 同步收紧到 144 |
| 2026-05-29 | collect-only 基线：required 144、heavy 12、external-stack 6、external-stack performance 早期单用例、performance 31；fake release_candidate backend integration manifest pass + collect-below-min negative；fake release_candidate E2E required-spec/title negative | `tests/unit` 798 passed，coverage 83.05%；backend-test manifest validation passed；CI YAML parsed；workflow validation scripts shell syntax passed | 无新增 warning；本轮不启动真实 external stack 或完整 release_candidate，只验证 CI 证据门逻辑和本地 unit 门禁 | `backend-test` 从裸 `pytest` 收口到 `tests/unit`，并解析 unit JUnit/coverage XML/JSON，禁止 unit failure/error/skip 或空覆盖产物；backend integration 每条 lane 先写 collect 清单并校验最低收集基线，避免 marker/删除导致跑错范围仍绿；nightly 只允许已知 OOM opt-in 用例 skip 且拒绝全 skip JUnit，release_candidate 继续零 skip；E2E release_candidate 强制关键 spec 存在，并把逐 spec title 集合纳入 JUnit/`--list` 匹配，避免同文件同数量弱测试替换关键路径 |
| 2026-05-29 | F-AU-04 targeted integration 2 passed | RBAC dependency targeted unit 2 passed | 无新增 warning；本轮不改运行代码，只复核当前 `require_project_permission` 行为与文档真相源 | 租户隔离 catalog 口径从历史 ⚠️ 修正为 ✅：Member/Viewer 的 path `project_id` 路由已先做项目租户可见性检查，跨 tenant、随机 UUID、软删除项目一致 404；无项目成员但同租户可见项目仍保持 403，避免把授权失败和资源枚举混在一起 |
| 2026-05-29 | OOM E2E collect-only 3 tests collected；release_candidate CI validation 脚本语法通过并用 skipped/fake-passed JUnit 负/正向模拟 | CI YAML parsed；targeted ruff / py_compile passed；`git diff --check` passed | 无新增 warning；本轮不在 Docker Desktop 上强跑 OOMKilled，而是把 release_candidate 发版环境改为显式 `QAP_TEST_OOM=1` 且禁止 OOM JUnit skip/fail | OOMKilled 从“有 DockerBackend 黑盒 + fake backend 平台语义但发版可静默 skip”收紧为 release_candidate 准入项：新增真实 Docker OOM 经 `RunExecutor` 落到 DB summary、Redis event/hash 和日志的 opt-in heavy 测试；`backend-integration-test` 在 release_candidate 会强制打开 `QAP_TEST_OOM=1`，manifest 解析 `test_oom_e2e.py` 三个 testcase，缺失、跳过或失败都会拒签 |
| 2026-05-29 | N/A（backend integration evidence gate 配置验证） | CI YAML parsed；Validate backend integration artifacts shell syntax passed；fake release_candidate backend JUnit 做 pass/failure/error/skip/no-testcase 模拟 | 无新增 warning；本轮不启动真实 external stack 或完整 integration，只验证 backend integration evidence validation 逻辑 | backend integration manifest 从“XML 非空即可”补强为“每个 expected JUnit 都必须有 testcase、failure/error 为 0”；release_candidate 下 skipped 也必须为 0，避免 required/heavy/external-stack/performance XML 文件齐全但测试失败、错误、全跳过或空跑时仍形成可签字证据 |
| 2026-05-29 | performance collect-only：`test_performance_smoke.py` 31 tests collected；`test_worker_execute.py -m "external_stack and performance"` 早期单用例 / 6 deselected | CI YAML parsed；Validate backend integration artifacts shell syntax passed；fake `release_candidate` performance summary 通过 gate/profile/threshold 校验并生成 `performance-thresholds.json`；performance helper JSONL 写出 `gate_profile=release_candidate`；targeted ruff / py_compile / `git diff --check` passed | 无新增 warning；本轮不启动真实 external stack 或完整性能测试，只验证 release_candidate 准入口径、阈值显式化和 manifest 校验脚本 | 性能 SLO 从“summary 里有名字”补强为“发版 gate 可拒签”：早期固定 SLO 阈值在 CI job env 显式列出，summary 每条记录必须匹配当前 `gate_profile`、期望 `threshold_ms`、基础 samples 字段且 `passed=true`，validation 会输出 `performance-thresholds.json` 作为签字时可核对的阈值来源 |
| 2026-05-29 | N/A（前端/API 契约层修复） | frontend `npm run lint -- --max-warnings=0` / `npx tsc --noEmit --pretty false` / `npm run build` passed；SSE unit 15 passed；targeted ruff passed | 无新增 warning；本轮不新增前端依赖，把手写类型债收敛到明确 DTO/view model 边界 | 前端 API 契约主线清理完成：Environment 与 Run 拆出后端 response DTO 和 UI view model，Environment create/update payload 提交 `base_image` / `env_vars`，Run trigger payload 和 summary `error` 对齐后端；notification 渠道类型覆盖当前一等渠道 email/webhook/dingtalk/wecom，Slack 通过通用 Webhook 覆盖；SSE reconnect 新 ticket 会携带 `last_event_id`，后端同时接受 header/query 游标 |
| 2026-05-29 | N/A（CI/E2E evidence gate 配置验证） | CI YAML parsed；Playwright list：release_candidate 10 tests / 4 files，nightly 9 tests / 3 files，pr_like 1 test / 1 file；E2E validation 脚本用 fake JUnit/list JSON 做 pass/failure/skip/count-mismatch/spec-without-tests/wrong-junit-spec 模拟 | 无新增 warning；本轮不启动本地 compose E2E 栈，只验证 workflow 配置、spec 分档清单、Playwright 可发现用例和 evidence validation 逻辑 | E2E job 现在会写入本次 spec 清单和 Playwright `--list` 文本/JSON 测试清单，校验每个预期 spec 至少发现 1 个 testcase、JUnit 总 testcase 数和逐 spec testcase 数都等于 `--list` 期望数，且 failure/error/skipped 必须为 0；同时校验 HTML report、spec 清单非空且符合 gate 模式，并上传 E2E evidence manifest，避免 release sign-off 只拿到空 artifact、跑错 spec 子集、少跑用例或把 skip/fail 结果当成可签字证据 |
| 2026-05-29 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_worker_resource_termination.py` 1 passed；`RUN_INTEGRATION_TESTS=1 tests/integration/test_docker_resource_usage_e2e.py` 1 passed | executor/resource targeted unit 77 passed；targeted ruff passed；Docker stats blackbox targeted ruff / py_compile passed | 无新增 warning；本轮不新增依赖，用 Docker stats 作为可选 backend 能力，普通 fake/backend 缺 stats 时保持原行为；真实 Docker stats stream 已有 heavy 黑盒；OOMKilled 平台语义由 release_candidate opt-in gate 承接 | OOM/timeout backend 结果除 Run `timeout`、Redis event 和收尾日志外，现在还会写 `summary.resource_termination`：记录 reason、exit_code、oom/timed_out 标记、耗时、开始/结束时间和 `resource_usage` 峰值 CPU/内存采样；新增真实 Docker 容器 stats stream 黑盒证明内存/限制/timestamp 可采样，剩余缺口是多资源报告加载体验 |
| 2026-05-29 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_api_write_state.py::test_environment_api_persists_encrypted_env_vars_limits_and_soft_delete tests/integration/test_real_api_write_state.py::test_environment_api_reads_resource_limits_without_audit_secret_leak` 2 passed | Environment disk quota targeted unit 47 passed；targeted ruff passed；frontend `npx tsc --noEmit --pretty false` / `npm run build` passed；`git diff --check` passed | 无新增 warning；本轮不新增依赖，把既有内部 `disk_mb` 传递提升为 API/DB/audit 契约 | Environment resource limit 从 `memory_mb` / `cpu_cores` / artifact limit 扩到 `disk_mb`：create/get/list/update 响应、真实 DB `resource_limits`、audit before/after 均保持同口径，worker 继续复用既有 `disk_mb -> ResourceLimits.disk_bytes -> Docker StorageOpt.size` 链路 |
| 2026-05-29 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_real_api_write_state.py::test_project_git_token_binding_reaches_trigger_metadata_without_secret` 1 passed | F-PM-01/02 targeted unit 143 passed；targeted ruff passed；`git diff --check` passed | 无新增 warning；required integration 走真实 FastAPI 项目/凭证/Run 路由、真实 PostgreSQL Run/AuditEvent 行和真实项目绑定状态，不 mock repository、DB session、权限或审计链路 | Git 凭证执行闭环从“只存 credential_id”补到 API/worker/GitSource：项目绑定校验归属/类型/URL，manual/webhook/schedule metadata 只带引用，worker 执行时解密并通过 askpass/SSH 临时 key 注入 clone；Run metadata 与 AuditEvent 均不含 token 明文 |
| 2026-05-29 | `RUN_INTEGRATION_TESTS=1 tests/integration/test_api_endpoints.py::TestPipelines` 3 passed | F-PL-01 targeted unit 71 passed；targeted ruff passed；frontend `npx tsc --noEmit --pretty false` passed；`npm run build` passed | 无新增 warning；`npm run typecheck` 不存在，已按项目脚本改用 `npx tsc --noEmit` 和 `npm run build` 验证 | Pipeline collector 配置从文档缺口变成 API/DB/worker/executor 闭环：`collectors[]` 持久化、审计脱敏、worker 映射、executor 非硬编码 collector、JUnit 自定义相对 report path 和前端表单/类型均有目标证据 |
| 2026-05-28 | collect-only 186 tests；required integration 138 passed / 48 deselected；`test_worker_execute.py` collect-only 7 tests；`test_real_worker_persists_artifacts_and_archived_logs` 本机缺 `api, worker` 时 1 skipped；`external_stack and not performance` 本机 6 skipped / 1 deselected | ruff full passed；tenant isolation self-test passed；`py_compile test_worker_execute.py` passed；CI YAML parsed；`git diff --check` passed | 无新增 warning；本轮增强既有 nightly/manual external-stack worker smoke 和 CI evidence manifest，不新增测试框架/依赖，不改变 API 行为；真实 compose 栈未在本机启动时保持前置条件 skip，CI nightly/manual 在 `QAP_EXTERNAL_STACK_REQUIRED=1` 下 stack 不 ready 会 fail | 真实 worker artifact 数量限制从“API/DB 不暴露超额文件”补强到“直接 head-object 证明 MinIO 不存在超额对象”；CI evidence manifest 从“证据文件非空”补强到校验早期固定 SLO name 都写入 `performance-smoke-summary.jsonl`，避免 nightly/manual 只上传缺项 JSONL |
| 2026-05-28 | collect-only 186 tests；required integration 138 passed / 48 deselected；`test_worker_execute.py` collect-only 7 tests；`test_real_worker_persists_artifacts_and_archived_logs` 本机缺 `api, worker` 时 1 skipped；`external_stack and not performance` 本机 6 skipped / 1 deselected | ruff full passed；tenant isolation self-test passed；`py_compile test_worker_execute.py` passed；`git diff --check` passed | 无新增 warning；本轮增强既有 nightly/manual external-stack worker smoke，不新增测试框架/依赖，不改变 API 行为；真实 compose 栈未在本机启动时保持前置条件 skip，CI nightly/manual 在 `QAP_EXTERNAL_STACK_REQUIRED=1` 下 stack 不 ready 会 fail | 真实 worker artifact 限制从单元/required DB 证据扩到 external-stack 黑盒：fake pytest 产出 JUnit/HTML/log/Allure 四类允许 artifact 和 1 个超额 `zz-over-limit.txt`，环境 `max_artifacts_count=4`；管理员 JWT 与真实 `run.read` token 的 artifact list 都只看到 4 个允许产物，并直接对 MinIO `reports/{run_id}/zz-over-limit.txt` 做 head-object 负断言；归档日志能回看 count limit skip，空 scope 403 不回显超额文件名、内容片段或日志 key |
| 2026-05-28 | collect-only 186 tests；required integration 138 passed / 48 deselected；`test_worker_execute.py` collect-only 7 tests；`test_real_worker_persists_artifacts_and_archived_logs` 本机缺 `api, worker` 时 1 skipped；`external_stack and not performance` 本机 6 skipped / 1 deselected | ruff full passed；tenant isolation self-test passed；`py_compile test_worker_execute.py` passed；`git diff --check` passed | 无新增 warning；本轮增强既有 nightly/manual external-stack worker smoke，不新增测试框架/依赖，也不改变 PR required integration 数量；本机缺 compose API/worker 时保持前置条件 skip，CI nightly/manual 在 `QAP_EXTERNAL_STACK_REQUIRED=1` 下 stack 不 ready 会 fail | 真实 worker 大日志归档从“API 预置 1500 行对象可分页”扩到“compose worker 实际打印 1505 行后由真实 MinIO 归档并经 API 深页回看”：管理员 JWT 和真实 `run.read` token 都读取 `page=1/2&per_page=1000`，断言 bulk 行顺序完整、bulk secret 被 `[REDACTED]` 替换、worker secret 不泄露；空 scope token 403 不回显 bulk marker、日志 key、artifact 路径或脱敏 marker |
| 2026-05-28 | collect-only 186 tests；required integration 138 passed / 48 deselected；`test_worker_execute.py` collect-only 7 tests；`external_stack and performance` 本机缺 `api, worker` 时 1 skipped；`external_stack and not performance` 本机 6 skipped / 1 deselected | ruff full passed；tenant isolation self-test passed；`py_compile test_worker_execute.py` passed；CI YAML parsed；`git diff --check` passed | 无新增 warning；新增测试仍在 nightly/manual external-stack + performance lane，CI 会在 compose API/worker/MinIO 仍存活时单独跑 `test_worker_execute.py -m "external_stack and performance"` 并上传独立 JUnit/log，同时把 elapsed SLO 写入 performance summary；本机缺 stack 按前置条件 skip，CI 设置 `QAP_EXTERNAL_STACK_REQUIRED=1` 后 stack 不 ready 会 fail | 真实 worker SLO 从“无自动化证据”变成 nightly/manual 可执行证据：POST `/runs` 后用 live SSE 读首条 worker log 且 Run 尚未终态，再等待 done、轮询 archived logs、artifact list、artifact download URL，并通过预签名 URL 从真实 MinIO GET JUnit 内容；记录 first live log、terminal、archive ready、artifact ready、download、E2E 总耗时阈值 |
| 2026-05-28 | collect-only 185 tests；required integration 138 passed / 47 deselected；performance smoke 31 passed；SSE ticket/create + logs/events delivery target 3 passed | ruff full passed；`test_performance_smoke.py` target ruff passed | 无新增 warning；新增证据仍在 nightly/manual performance lane，走真实注册、真实 `run.read` API token、真实 `/auth/sse-ticket` 路由、真实 Redis ticket payload 和真实 PostgreSQL AuditEvent；手写 Redis SSE ticket smoke 同步改为当前 JSON payload，避免旧 legacy ticket 在 scope 反序列化后误报 | SSE ticket 创建从只在 required integration 证明“能签发且 audit 不存 ticket”扩到 SLO/副作用哨兵：20 次采样验证 Redis payload 携带 `run.read` scopes，`auth.sse_ticket_create` audit 的 after_state 只记录 TTL/single_use，且 before/after/resource_id 不保存 ticket、access token 或 API token；实时 logs/events SSE 性能 smoke 也使用当前 JSON ticket 格式，避免 nightly/manual 因测试夹具过期而 401 |
| 2026-05-28 | collect-only 184 tests；required integration 138 passed / 46 deselected；`test_real_worker_persists_artifacts_and_archived_logs` 本机未启动 external stack 时 1 skipped | `test_engine/test_executor_redaction.py` + `test_engine/test_executor.py` 41 passed；ruff full passed | 无新增 warning；产品修复位于 executor 写 Redis log 前的脱敏路径，外部栈红线测试仍在 nightly/manual compose API/worker/MinIO ready 后执行，本机缺 stack 为前置条件 skip | 主动 stdout/stderr secret 脱敏从未决策略变成自动化证据：fake pytest 会把解密后的 worker env secret 打到 stdout/stderr；live `/logs` SSE、archived logs 和真实 `run.read` API token 读面只允许 `[REDACTED]`，空 scope ticket/归档读面 403 且不回显原文或 marker |
| 2026-05-28 | collect-only 184 tests；required integration 138 passed / 46 deselected；`test_real_auth_results_artifacts.py` SSE targets 3 passed；`test_auth_sse_ticket_rate_limit_uses_real_redis_token_hash_bucket` 1 passed；`test_worker_execute.py` collect-only 6 tests，未启动 stack 时 6 skipped（新增目标场景单独 1 skipped） | `test_sse.py` 13 passed；ruff target passed；`py_compile` passed | 无新增 warning；required integration 走真实 JWT/API token、真实 `/auth/sse-ticket`、PostgreSQL Run/RBAC 和 Redis Stream；external-stack 新场景仍只在 compose API/worker/MinIO ready 后执行，本机缺 stack 为前置条件 skip | SSE ticket 从只携带 user/role/tenant 修正为 JSON payload 并携带 API token scopes，旧格式 ticket 直接 401 避免 scope 丢失：`run.read` API token 可续读 `/logs`，空 scope token 403；nightly/manual 新增真实 worker 运行中 SSE marker 黑盒，验证 ticket 单次消费、`Last-Event-ID` 不重放、空 scope 拒绝不泄露 marker/log key/artifact path，并继续回看归档日志和 JUnit artifact |
| 2026-05-28 | collect-only 183 tests；required integration 138 passed / 45 deselected；完整 local integration 146 passed / 37 skipped；performance smoke 30 passed；`test_real_api_write_state.py` 13 passed；worker external-stack collect-only 5 tests | ruff target/full passed；`git diff --check` passed | 无新增 warning；required integration 仍走真实 FastAPI `/runs`、真实 PostgreSQL Run/AuditEvent 行和 FairScheduler 入队元数据；performance smoke 仍是 nightly/manual 趋势哨兵，ARQ test double 只记录外部队列副作用；external-stack worker helper 只增强触发审计断言，本地未启动 compose 时仍由 nightly/manual 产出真实 worker 证据 | Manual trigger 审计从“字段子集正确”扩到“完整 after_state 等于 API response”：priority 0/1/2 同时验证 high/medium/low queue metadata 落库、`run.trigger` audit 不夹带 `queue_name` / `arq_job_id` / `git_url` / `credential_id`；performance trigger/preemption 和 external-stack worker/worker_lost/priority/clone-failure 也复用同一完整 payload 口径 |
| 2026-05-28 | collect-only 183 tests；required integration 138 passed / 45 deselected；完整 local integration 146 passed / 37 skipped；`test_webhook_branch_dedup.py` 10 passed；targeted webhook audit 2 passed | ruff target/full passed；`git diff --check` passed | 无新增 warning；本轮不新增测试数量，增强既有 PR required integration，走真实 FastAPI webhook 路由、真实 PostgreSQL Run/AuditEvent 行、真实 project settings/HMAC 校验和 ARQ conflict test double；不 mock repository、DB session、权限链路或审计链路 | Webhook `run.trigger` audit 从字段抽样扩到 after_state 与真实 API response 对齐：签名成功路径继续证明攻击性 `git_url` / `credential_id` / `default_branch` 不覆盖执行配置或审计；enqueue conflict 路径证明 waiting Run 仍有完整可追溯审计 payload |
| 2026-05-28 | collect-only 183 tests；required integration 138 passed / 45 deselected；完整 local integration 146 passed / 37 skipped；`test_real_api_write_state.py` 13 passed；`test_schedule_and_run_apis_persist_next_run_metadata_and_audit_rows` 1 passed | ruff target/full passed；`git diff --check` passed | 无新增 warning；本轮不新增测试数量，增强既有 PR required integration，走真实 FastAPI schedule API、真实 PostgreSQL Schedule/AuditEvent/Run 行，不 mock repository、DB session、权限链路或审计链路 | Schedule audit 从“只证明 create/update/delete action 存在”扩到 create/update payload 真实性：create before_state 为空，after_state 包含 pipeline、cron、timezone、missed_fire_policy、quiet_windows、enabled 和 next_run_at；update before/after 对齐 API response，避免审计行存在但内容漂移 |
| 2026-05-28 | collect-only 183 tests；required integration 138 passed / 45 deselected；完整 local integration 146 passed / 37 skipped；performance smoke 30 passed；`test_performance_smoke.py` collect-only 30 tests；`test_real_api_token_concurrent_read_paths_p99_smoke` 1 passed | ruff target/full passed；`git diff --check` passed | 无新增 warning；本轮不新增测试数量，增强既有 nightly/manual performance smoke，走真实注册、真实 `run.read` + `audit.read` API token、真实 FastAPI run detail / archived logs / artifact list / artifact download / audit-events 路由和真实 PostgreSQL Run/Artifact/AuditEvent 行；S3 test double 只记录对象存储副作用，不替代 DB/API/RBAC 链路 | 真实 API token 并发读路径从 archived logs + artifact download + audit-events list 三路，扩到 run detail、archived logs、artifact list、artifact download、audit-events list 五路同批并发；继续断言只有日志读取触发 `logs/{run_id}.jsonl` get_object，只有下载触发 artifact presign，artifact list 保持 DB-only，audit-events 成功查询写足自审计且 after_state 仅含查询过滤、分页与 total，不包含返回 data、日志片段或 artifact 名称/路径 |
| 2026-05-28 | collect-only 183 tests；required integration 138 passed / 45 deselected；完整 local integration 146 passed / 37 skipped；`test_worker_execute.py` collect-only 5 tests；`test_priority_queues_wait_for_matching_external_workers_then_finish` 本机未启动 external stack 时 1 skipped | ruff full passed；`git diff --check` passed | 无新增 warning；本轮不新增测试数量，增强既有 nightly/manual external-stack priority queue smoke，测试仍走真实 admin 登录、真实 `/auth/tokens` 创建 `run.read` 和空 scope API token、真实 compose high/medium/low worker 与 MinIO 产物链路；本机未启动 compose 栈时保持前置条件 skip，CI nightly/manual 栈不完整时由 `QAP_EXTERNAL_STACK_REQUIRED=1` 失败 | high/low priority Run 的读面从管理员 JWT 扩到机器 token 黑盒证据：匹配 worker 完成后 `run.read` token 可读取 run detail、archived logs、artifact list 和 JUnit artifact download；空 scope token 对 logs/list/download 均 403，且响应不回显日志 key、artifact 路径/名称、JUnit 内容片段或关键日志行 |
| 2026-05-28 | collect-only 183 tests；required integration 138 passed / 45 deselected；完整 local integration 146 passed / 37 skipped；`test_worker_execute.py` collect-only 5 tests；`test_worker_clone_and_setup_failures_do_not_retry_or_leak_external_stack` 本机未启动 external stack 时 1 skipped | ruff full passed；`git diff --check` passed | 无新增 warning；本轮不新增测试数量，增强既有 nightly/manual external-stack clone/setup failure smoke，测试仍走真实 admin 登录、真实 `/auth/tokens` 创建 `run.read` 和空 scope API token、真实 compose API/worker/MinIO 失败日志链路；本机未启动 compose 栈时保持前置条件 skip，CI nightly/manual 栈不完整时由 `QAP_EXTERNAL_STACK_REQUIRED=1` 失败 | clone/setup failure archived logs 读面从管理员 JWT 扩到机器 token 黑盒证据：`run.read` token 可读取 clone failure 归档响应（允许为空但不含 clone secret/userinfo）和 setup failure stderr 日志；空 scope token 对失败日志回看 403，且响应不回显日志 key、credentialed URL secret/userinfo、clone error 细节或 setup stderr |
| 2026-05-28 | collect-only 183 tests；required integration 138 passed / 45 deselected；完整 local integration 146 passed / 37 skipped；`test_worker_execute.py` collect-only 5 tests；`test_worker_lost_retry_completes_with_artifacts_and_archived_logs` 本机未启动 external stack 时 1 skipped | ruff full passed；`git diff --check` passed | 无新增 warning；本轮不新增测试数量，增强既有 nightly/manual external-stack worker_lost retry smoke，测试仍走真实 admin 登录、真实 `/auth/tokens` 创建 `run.read` 和空 scope API token、真实 compose API/worker/MinIO/reclaimer 链路；本机未启动 compose 栈时保持前置条件 skip，CI nightly/manual 栈不完整时由 `QAP_EXTERNAL_STACK_REQUIRED=1` 失败 | `worker_lost` attempt=2 retry Run 的读面从管理员 JWT 扩到机器 token 黑盒证据：`run.read` token 可读取 retry run archived logs、artifact list 和 JUnit artifact download，空 scope token 对 logs/list/download 均 403，且响应不回显日志 key、artifact 路径/名称、JUnit 内容片段或关键日志行 |
| 2026-05-28 | collect-only 183 tests；required integration 138 passed / 45 deselected；完整 local integration 146 passed / 37 skipped；`test_worker_execute.py` collect-only 5 tests；`test_real_worker_persists_artifacts_and_archived_logs` 本机未启动 external stack 时 1 skipped | ruff full passed；`git diff --check` passed | 无新增 warning；本轮不新增测试数量，增强既有 nightly/manual external-stack worker smoke，测试走真实 admin 登录、真实 `/auth/tokens` 创建 `run.read` 和空 scope API token、真实 compose API/worker/MinIO 产物链路；本机未启动 compose 栈时保持前置条件 skip，CI nightly/manual 栈不完整时由 `QAP_EXTERNAL_STACK_REQUIRED=1` 失败 | `run.read` 机器 token 产物读面从 JUnit 抽样扩到 JUnit、HTML report、trace log、Allure index 四类真实 worker artifact：四类产物都逐个经预签名 URL 下载并校验内容；空 scope token 对 logs/list/四类 download 均 403，且响应不回显 worker secret、artifact 名称/路径、artifact 内容片段或日志行 |
| 2026-05-28 | collect-only 183 tests；required integration 138 passed / 45 deselected；完整 local integration 146 passed / 37 skipped；`test_worker_execute.py` collect-only 5 tests；`test_real_worker_persists_artifacts_and_archived_logs` 本机未启动 external stack 时 1 skipped | ruff full passed；`git diff --check` passed | 无新增 warning；本轮不新增测试数量，增强既有 nightly/manual external-stack worker smoke，测试走真实 admin 登录、真实 `/auth/tokens` 创建 `run.read`、`project.read` 和空 scope API token、真实 compose API/worker/MinIO 产物链路；本机未启动 compose 栈时保持前置条件 skip，CI nightly/manual 栈不完整时由 `QAP_EXTERNAL_STACK_REQUIRED=1` 失败 | 真实 worker 产物读面从管理员 JWT 扩到机器 token 黑盒证据：worker 完成后 `run.read` token 可读取 run detail、archived logs、artifact list 和 JUnit artifact download，`project.read`/空 scope token 对 detail 拒绝，空 scope token 对 logs/list/download 也 403，且响应不回显 worker secret、artifact 名称/路径或日志行 |
| 2026-05-28 | collect-only 183 tests；required integration 138 passed / 45 deselected；完整 local integration 146 passed / 37 skipped；performance smoke 30 passed；`test_run_read_empty_scope_denied_logs_and_artifacts_no_s3_p99_smoke` 1 passed；相关拒绝/并发读 performance 子集 5 passed / 25 deselected | ruff full passed；`git diff --check` passed | 无新增 warning；新增测试仍在 nightly/manual performance lane，走真实注册、真实 `/auth/tokens` 创建空 scope API token、真实 FastAPI 归档日志/artifact 路由和真实 PostgreSQL Run/Artifact 行；S3 test double 只记录对象存储副作用，不替代 DB/API/RBAC 链路 | `run.read` 保护的归档日志、artifact 列表、artifact 下载空 scope 拒绝进入性能哨兵：同 tenant 三端点共 21 次采样均 403，不回显日志 key/行内容或 artifact name/path，且全程不触发 S3 get_object/presign；默认阈值 `PERF_RUN_READ_EMPTY_SCOPE_DENIED_P99_MS=1000` |
| 2026-05-28 | collect-only 182 tests；required integration 138 passed / 44 deselected；完整 local integration 146 passed / 36 skipped；targeted run.read/audit scope 4 passed | ruff targeted/full passed；`git diff --check` passed | 无新增 warning；本轮不增加测试数量，增强既有 required integration，走真实注册、真实 `/auth/tokens` 创建 `run.read`/`project.read`/空 scope API token、真实 FastAPI 权限链路、真实 PostgreSQL AuditEvent/Run/Artifact 行和 S3 副作用哨兵 | Audit events 空 scope 拒绝从 nightly/manual performance smoke 下沉到 PR 必跑证据：403 不回显本次 action/resource_id，拒绝前后不误写自审计；归档日志、artifact 列表、artifact 下载同步补空 scope 拒绝：403 不回显日志 key/行内容或 artifact name/path，且不触发 S3 get_object/presign |
| 2026-05-28 | collect-only 182 tests；required integration 138 passed / 44 deselected；完整 local integration 146 passed / 36 skipped；performance smoke 29 passed；`test_audit_events_api_token_denied_no_self_audit_p99_smoke` 1 passed | ruff targeted/full passed；`git diff --check` passed | 无新增 warning；新增测试仍在 nightly/manual performance lane，走真实注册、真实 `run.read`/`project.read`/空 scope API token、真实 FastAPI `/audit-events` 权限链路和真实 PostgreSQL AuditEvent 计数，不 mock repository/session/current-user | Audit events 拒绝查询 SLO 从角色/跨租户拒绝扩到真实 API token scope 不足：三类 token 共 21 次请求都返回 403，响应不回显审计查询细节，并断言拒绝前后 `audit_events.list` 自审计计数不变；默认阈值 `PERF_AUDIT_EVENTS_API_TOKEN_DENIED_P99_MS=1000` |
| 2026-05-28 | collect-only 181 tests；required integration 138 passed / 43 deselected；完整 local integration 146 passed / 35 skipped；performance smoke 28 passed；`test_real_auth_results_artifacts.py` 28 passed；`test_run_read_token_replays_logs_and_presigns_artifact_without_extra_s3_reads` 1 passed；`test_worker_execute.py` 5 skipped（本机 external stack 不完整，预期）；`QAP_EXTERNAL_STACK_REQUIRED=1` 缺 `api, worker` 负向验证 fail | ruff targeted/full passed；`git diff --check` passed | 无新增 warning；新增 required 测试走真实 FastAPI/JWT/API token/PostgreSQL Run+Artifact/Redis LogStream 链路，S3 test double 仅记录对象存储读取与 presign 副作用；external-stack preflight 已从端口存活收紧到 compose 服务 + QA Platform `/ready` JSON，CI nightly/manual 设置 `QAP_EXTERNAL_STACK_REQUIRED=1`，本地可 skip，CI 栈不完整或不 ready 会 fail | 同一个真实 `run.read` API token 串联同一 Run 的 archived logs、artifact list 与 artifact download：日志回看只读 `logs/{run_id}.jsonl`，artifact 列表不触发 S3，下载只生成一次 presign 且不读对象；同时治理本地完整 integration 被非 QA Platform 8000 服务误判为外部栈的误报 |
| 2026-05-28 | collect-only 180 tests；required integration 137 passed / 43 deselected；完整 local integration 145 passed / 35 skipped；performance smoke 28 passed；`test_real_api_write_state.py` 13 passed；`test_environment_api_reads_resource_limits_without_audit_secret_leak` 1 passed | ruff targeted/full passed；`git diff --check` passed | 无新增 warning；新增测试进入 PR required integration，走真实 FastAPI environment API、真实 PostgreSQL Environment 行和真实 AuditEvent，不 mock repository、DB session、权限链路或审计链路；external-stack secret 口径收紧为“平台产物/归档不被动泄露”，不再把用户进程主动打印密钥后的日志脱敏误记为已覆盖 | Environment resource limit 从“创建时落库”扩到 create/get/list/update 读回：验证 `memory_mb`、`cpu_cores`、`max_artifact_size_mb`、`max_artifacts_count` 的 API 响应、真实 DB 状态和 audit before/after，同时保证 `env_vars` 明文不进入 DB repr 或审计状态；剩余缺口仍是 API 磁盘配额暴露、资源用量记录和主动 stdout/stderr secret 脱敏策略 |
| 2026-05-28 | collect-only 179 tests；required integration 136 passed / 43 deselected；完整 local integration 144 passed / 35 skipped；performance smoke 28 passed；`test_artifact_list_large_collection_page_p99_smoke` 1 passed；`test_audit_events_large_filtered_page_api_token_p99_smoke` 1 passed | ruff targeted/full passed；`git diff --check` passed | 无新增 warning；新增测试继续留在 nightly/manual performance lane，PR required integration 只增加 2 个 deselected；S3 test double 仅作为 artifact 列表 no-presign/no-read 副作用哨兵，不替代真实 FastAPI/API token/RBAC/PostgreSQL 链路 | Artifact 列表从小集合第一页扩到真实 `run.read` API token 下 1000 条大集合深页，仍验证 DB-only 不触发 S3；audit-events 从基础查询扩到真实 `audit.read` API token 下大量过滤深分页，验证 total、排序窗口、自审计增量和 bounded after_state 不含返回 data |
| 2026-05-28 | collect-only 177 tests；required integration 136 passed / 41 deselected；完整 local integration 144 passed / 33 skipped；performance smoke 26 passed；`test_performance_smoke.py` collect-only 26 tests；`test_dequeue_waiting_prioritizes_newer_high_runs_over_older_low_backlog_slo_smoke` 1 passed | ruff targeted/full passed；`git diff --check` passed | 无新增 warning；新增测试仍在 nightly/manual performance lane，PR required integration 只多 1 个 deselected；ARQ 使用 test double 仅记录 queue side effect，不替代真实 API/DB/audit 链路 | F-EX-08 长队风险从“waiting 能恢复入队”扩到“容量受限时 newer high priority 越过 older low backlog”：API 创建的 low/high run 都写 `run.trigger` audit，释放有限容量后 high 写入 `queue:high`，low 保持 waiting 且不新增 audit |
| 2026-05-28 | collect-only 176 tests；required integration 136 passed / 40 deselected；完整 local integration 144 passed / 32 skipped；performance smoke 25 passed；`test_worker_execute.py` collect-only 5 tests；`test_worker_clone_and_setup_failures_do_not_retry_or_leak_external_stack` 1 skipped（API server not reachable，本地未启动 external stack，预期） | ruff targeted/full passed；`git diff --check` passed | 无新增 warning；本轮不新增测试数量，增强既有 nightly/manual external-stack smoke；本地未启动 compose API/worker/MinIO 时保持前置条件 skip | credentialed clone failure 的防泄漏观察面从 run error/audit/artifact 扩到 archived logs API：worker finally 归档后，空或非空归档响应都必须不含 clone secret、`x-access-token` 或完整 credentialed URL |
| 2026-05-28 | collect-only 176 tests；required integration 136 passed / 40 deselected；完整 local integration 144 passed / 32 skipped；performance smoke 25 passed；`test_real_auth_results_artifacts.py` 27 passed；`test_audit_events_api_token_requires_audit_read_scope_without_self_audit_on_denial` 1 passed | ruff targeted/full passed | 无新增 warning；新增 required integration 走真实注册、真实 `run.read`/`project.read`/`audit.read` API token、真实 PostgreSQL AuditEvent、真实 `/api/v1/audit-events` 路由，不 mock current-user/repository/session | Audit events 的 API token 权限边界从 nightly/manual 成功路径下沉到 PR required integration：`run.read`/`project.read` 403 且不写 `audit_events.list`，自有 `audit.read` 才能查询，成功自审计 after_state 只记录查询过滤字段、分页与 total，不写返回 data |
| 2026-05-28 | collect-only 175 tests；required integration 135 passed / 40 deselected；完整 local integration 143 passed / 32 skipped；performance smoke 25 passed；`test_real_api_token_concurrent_read_paths_p99_smoke` 1 passed | ruff targeted/full passed | 无新增 warning；新增 nightly/manual performance smoke 启动无 current-user override 的真实 app，走真实注册、真实 `run.read` + `audit.read` API token、真实 PostgreSQL Run/Artifact/AuditEvent、真实 FastAPI 归档日志/artifact/audit-events 路由；S3 test double 只记录归档读取与 artifact presign 副作用，不替代 DB/API/RBAC | 日志回看、artifact 下载与审计查询从串行 p99 扩到小并发趋势哨兵：6 批并发请求持续验证归档日志分页窗口、artifact presign-only、audit_events.list 自审计增量和 no extra S3 副作用 |
| 2026-05-28 | collect-only 174 tests；required integration 135 passed / 39 deselected；完整 local integration 143 passed / 31 skipped；performance smoke 24 passed；`test_real_auth_results_artifacts.py` 26 passed；`test_artifact_download_soft_deleted_row_returns_404_without_presign` 1 passed | ruff targeted/full passed | 无新增 warning；新增 required integration 走真实 JWT 注册、真实 `run.read` API token、真实 PostgreSQL Artifact 软删除、真实下载 API；S3 test double 只记录副作用，不替代 DB/API/RBAC，且测试直接查询原始 Artifact 行确认 `deleted_at` 已写入 | Artifact 软删除下载从“列表不可见”补到“下载入口不可换预签名 URL”：软删除 artifact ID 与随机 UUID 返回一致 404，且拒绝路径不触发 S3 presign/get_object |
| 2026-05-28 | collect-only 173 tests；required integration 134 passed / 39 deselected；完整 local integration 142 passed / 31 skipped；performance smoke 24 passed；`test_dequeue_waiting_runs_slo_smoke` 1 passed | ruff targeted/full passed | 无新增 warning；新增 nightly/manual performance smoke 直接调用 `dequeue_waiting` cron 入口，走真实 PostgreSQL Run 行、RunRepository/FairScheduler 和 ARQ test double 记录，不 mock repository 或 DB session | Waiting Run 恢复入队从 repository ordering 证据扩到 SLO 趋势哨兵：10 个 waiting Run 按 priority 被写入 `queue:high` / `queue:medium` / `queue:low`、`arq_job_id` 和 `enqueued_at`，证明容量恢复后 worker cron 能把 DB waiting 队列重新推回外部队列 |
| 2026-05-28 | collect-only 172 tests；required integration 134 passed / 38 deselected；完整 local integration 142 passed / 30 skipped；performance smoke 23 passed；`test_schedule_tick_enqueue_slo_smoke` 1 passed | ruff targeted/full passed | 无新增 warning；新增 nightly/manual performance smoke 直接调用 schedule worker cron 入口，走真实 PostgreSQL Schedule/Run/AuditEvent、FairScheduler 入队元数据与 ARQ test double 记录，不 mock repository、DB session 或审计链路 | Schedule worker tick 从 required integration 的功能证据扩到 SLO 趋势哨兵：10 个 due schedule 分别创建 schedule Run、落 `queue:low`、写系统身份 `run.trigger` audit，并验证审计 after_state 中 schedule/project/pipeline/environment/git_ref/enqueued 与真实 DB 行一致 |
| 2026-05-28 | collect-only 171 tests；required integration 134 passed / 37 deselected；完整 local integration 142 passed / 29 skipped；performance smoke 22 passed；`test_webhook_trigger_enqueue_slo_smoke` 1 passed | ruff targeted/full passed | 无新增 warning；新增 nightly/manual performance smoke 走真实 FastAPI webhook 路由、PostgreSQL Run/AuditEvent、FairScheduler 入队元数据与 ARQ test double 记录，不 mock repository、DB session 或审计链路 | Webhook 触发入队从“required integration 验证创建/审计/保留 metadata”扩到 SLO 趋势哨兵：10 次采样均要落 `queue:medium`、写 `run.trigger` audit，攻击性 `git_url` / `credential_id` / `default_branch` 不能覆盖执行配置，delivery_id 不进入审计状态 |
| 2026-05-28 | collect-only 170 tests；required integration 134 passed / 36 deselected；完整 local integration 142 passed / 28 skipped；performance smoke 21 passed；`test_real_auth_results_artifacts.py` 25 passed；强化 artifact/log 拒绝路径副作用断言 3 tests | ruff targeted/full passed | 无新增 warning；本轮不增加测试数量，强化现有真实 DB/RBAC/API 用例的 S3 副作用哨兵：archived logs `project.read` 拒绝断言 no get_object/no presign，artifact list 拒绝断言 no presign/no get_object，artifact download 拒绝断言 no presign/no get_object | 拒绝路径证据从“只看状态码/响应或只看 no-presign”补强为完整 no-S3-read/no-presign，防止权限失败后仍提前触碰对象存储、泄露路径存在性或制造无谓外部依赖 |
| 2026-05-28 | collect-only 170 tests；required integration 134 passed / 36 deselected；完整 local integration 142 passed / 28 skipped；performance smoke 21 passed；`test_real_auth_results_artifacts.py` 25 passed；新增多 artifact 下载 presign-only required integration 1 test | ruff targeted/full passed | 无新增 warning；新增测试创建 5 条真实 Artifact PostgreSQL 行，走真实 JWT 注册、真实 `run.read` API token、真实 RBAC/DB/API 下载路由，S3 test double 只记录 presign/get_object 副作用 | Artifact 下载 burst/presign-only 从 nightly/manual SLO 下沉到 PR required integration：每个 artifact 返回对应 bucket/key/TTL 的预签名 URL，按请求顺序逐条 presign，且成功下载链接路径不读取 S3 对象 |
| 2026-05-28 | collect-only 169 tests；required integration 133 passed / 36 deselected；完整 local integration 141 passed / 28 skipped；performance smoke 21 passed；`test_real_auth_results_artifacts.py` 24 passed；新增归档日志大页 required integration 1 test | ruff targeted/full passed | 无新增 warning；新增测试直接预置 1500 行 JSONL 归档对象，走真实 JWT、真实 Run/RBAC、真实 `/runs/{id}/logs/archive` API 和分页窗口，S3 test double 只记录对象读/签名副作用 | 归档日志 1500 行大对象第 15 页从 nightly/manual SLO 下沉到 PR required integration：成功路径只读取 `logs/{run_id}.jsonl` 一次，不生成 presign，并断言 total/page/per_page/首尾行窗口正确 |
| 2026-05-28 | collect-only 168 tests；required integration 132 passed / 36 deselected；完整 local integration 140 passed / 28 skipped；performance smoke 21 passed；`test_real_auth_results_artifacts.py` 23 passed；新增存储未配置 required integration 2 tests | ruff targeted/full passed | 无新增 warning；新增测试走真实 JWT 注册、真实项目/环境/pipeline、真实 Run/Artifact PostgreSQL 行和真实 FastAPI 路由，S3 client 仅置空模拟配置缺失，不 mock repository、DB session、RBAC 或 API token 服务 | 归档日志回看和 artifact 下载的对象存储未配置边界从 nightly/manual SLO 下沉到 PR required integration：真实 RBAC/DB 通过后稳定返回 503 与明确错误信息，避免对象存储配置缺失被误报为 404、500 或权限失败 |
| 2026-05-28 | collect-only 166 tests；required integration 130 passed / 36 deselected；完整 local integration 138 passed / 28 skipped；performance smoke 21 passed；`test_real_auth_results_artifacts.py` 21 passed；新增 artifact 列表分页 DB-only 1 test | ruff targeted/full passed | 无新增 warning；新增测试走真实 JWT 注册、真实项目/环境/pipeline、真实 Run/Artifact PostgreSQL 行和真实 FastAPI `/runs/{id}/artifacts` 路由；S3 仅作为副作用哨兵，不替代 DB/API/RBAC 链路 | Artifact 列表分页从 nightly/manual SLO 证据下沉到 PR required integration：第二页按真实 DB `created_at desc` 返回稳定窗口，越界页返回空列表但保留 total，且成功列表不生成 presign、不读取 S3 |
| 2026-05-28 | collect-only 165 tests；required integration 129 passed / 36 deselected；完整 local integration 137 passed / 28 skipped；performance smoke 21 passed；新增 strict auth rate-limit 3 tests；`test_auth_audit_failure_paths.py` 12 passed | ruff targeted/full passed | 无新增 warning；新增 register/refresh/API token create 测试走真实 FastAPI app、真实 Redis sliding window、真实 refresh cookie/JWT 和真实 PostgreSQL AuditEvent 计数，不 mock rate-limit middleware、Redis client、token service 或 current-user | 认证高风险 strict rate-limit 矩阵补齐：register、refresh、API token create、login、SSE ticket 均有 required integration 证据；第 1-5 次真实业务副作用成功并写对应 audit，第 6 次 429 + `Retry-After`，Bearer 路径 Redis bucket 使用 token hash，IP/cookie 路径 Redis key 不包含 username/password/refresh token，限流请求不额外写审计 |
| 2026-05-28 | collect-only 162 tests；required integration 126 passed / 36 deselected；完整 local integration 134 passed / 28 skipped；performance smoke 21 passed；`test_auth_sse_ticket_rate_limit_uses_real_redis_token_hash_bucket` 1 passed；`test_auth_audit_failure_paths.py` 9 passed | ruff targeted/full passed | 无新增 warning；新增测试走真实 FastAPI app、真实 JWT 注册与 `/auth/sse-ticket`、真实 Redis sliding window 和真实 PostgreSQL AuditEvent 计数，不 mock rate-limit middleware、Redis client 或 current-user | 认证高风险 SSE ticket rate limit 进入 required integration：同一真实 access token 的第 1-5 次 `/auth/sse-ticket` 请求签发 ticket 并写 5 条 `auth.sse_ticket_create` audit，第 6 次返回 429 + `Retry-After`，Redis bucket 使用 `token:<sha256前16位>`，key 中不保存原始 Bearer token，限流请求不额外写审计 |
| 2026-05-28 | collect-only 161 tests；required integration 125 passed / 36 deselected；完整 local integration 133 passed / 28 skipped；performance smoke 21 passed；`test_realtime_status_event_sse_delivery_latency_smoke` 1 passed | ruff targeted/full passed | 无新增 warning；新增 performance smoke 走本地 uvicorn 真实 HTTP SSE 路由、一次性 ticket、真实 PostgreSQL Run/RBAC 查询和真实 Redis status stream，不 mock `/runs/{id}/events` | 状态事件 SSE 实时推送进入 nightly/manual SLO：5 个独立 run 打开 `/runs/{id}/events` 后发布 `collecting`/`done` 状态事件，客户端需在 `PERF_SSE_EVENT_DELIVERY_P99_MS` 默认 2000ms 内看到 `status_change` |
| 2026-05-28 | collect-only 160 tests；required integration 125 passed / 35 deselected；完整 local integration 133 passed / 27 skipped；`test_real_sse_events_resume_after_last_event_id_uses_ticket_rbac_and_redis` 1 passed；`test_real_auth_results_artifacts.py` 20 passed | ruff targeted/full passed | 无新增 warning；新增测试走真实 FastAPI app、真实 JWT SSE ticket、真实 PostgreSQL Run/RBAC 查询和真实 Redis status event stream，不 mock SSE ticket 或 Redis Stream | 状态事件 SSE `/runs/{id}/events` 进入 required integration：`Last-Event-ID` 只回放断点后的 `status_change`，返回 `done` sentinel，并验证 ticket 单次消费；补齐日志流 `/logs` 之外的 run 状态事件续传证据 |
| 2026-05-28 | collect-only 159 tests；required integration 124 passed / 35 deselected；完整 local integration 132 passed / 27 skipped；`test_auth_login_rate_limit_uses_real_redis_token_hash_bucket` 1 passed；`test_auth_audit_failure_paths.py` 8 passed | ruff targeted/full passed | 无新增 warning；新增测试走真实 FastAPI app、真实 Redis sliding window、真实 auth login 失败路径和真实 PostgreSQL audit failure 夹具，不 mock rate-limit middleware 或 Redis client | 认证高风险端点 rate limit 进入 required integration：同一 Bearer token 的第 1-5 次 `/auth/login` 请求返回 401，第 6 次返回 429 + `Retry-After`，Redis bucket 使用 `token:<sha256前16位>`，key 中不保存原始 Bearer token |
| 2026-05-28 | collect-only 158 tests；required integration 123 passed / 35 deselected；完整 local integration 131 passed / 27 skipped；performance smoke 20 passed；`test_artifact_download_storage_unavailable_p99_smoke` 1 passed | ruff targeted/full passed | 无新增 warning；新增 skip/deselected 只来自 performance opt-in；存储未配置场景走真实 FastAPI/RBAC/PostgreSQL Artifact/Run 查询后在 S3 client 边界短路，不替代 DB/API 权限链路 | Artifact 下载存储未配置进入 nightly/manual SLO：真实 artifact 请求 `/artifacts/{id}/download` 在对象存储客户端不可用时稳定返回 HTTP 503 与 `Artifact download is not available`，3 次 warmup + 20 次采样由 `PERF_ARTIFACT_DOWNLOAD_UNAVAILABLE_P99_MS` 管控 |
| 2026-05-28 | collect-only 157 tests；required integration 123 passed / 34 deselected；完整 local integration 131 passed / 26 skipped；performance smoke 19 passed；`test_archived_log_storage_unavailable_p99_smoke` 1 passed | ruff targeted/full passed | 无新增 warning；新增 skip/deselected 只来自 performance opt-in；存储未配置场景走真实 FastAPI/RBAC/PostgreSQL Run 查询后在 S3 client 边界短路，不替代 DB/API 权限链路 | 归档日志存储未配置进入 nightly/manual SLO：真实 run 请求 `/logs/archive` 在对象存储客户端不可用时稳定返回 HTTP 503 与 `Archived logs are not available`，3 次 warmup + 20 次采样由 `PERF_LOG_ARCHIVE_UNAVAILABLE_P99_MS` 管控 |
| 2026-05-28 | collect-only 156 tests；required integration 123 passed / 33 deselected；完整 local integration 131 passed / 25 skipped；performance smoke 18 passed；`test_archived_log_missing_object_p99_smoke` 1 passed | ruff targeted/full passed | 无新增 warning；新增 skip/deselected 只来自 performance opt-in；S3 test double 对齐真实 NoSuchKey 异常形状，只替代对象存储服务，不替代真实 FastAPI/RBAC/PostgreSQL Run 查询 | 归档日志对象缺失进入 nightly/manual SLO 与副作用哨兵：真实 run 请求 `/logs/archive` 在对象不存在时稳定返回 `NOT_FOUND / Archived logs not found`，3 次 warmup + 20 次采样只读取 `logs/{run_id}.jsonl` 共 23 次，且不生成 presign URL |
| 2026-05-28 | collect-only 155 tests；required integration 123 passed / 32 deselected；完整 local integration 131 passed / 24 skipped；performance smoke 17 passed；`test_trigger_run_enqueue_slo_smoke` 1 passed | ruff targeted/full passed | 无新增 warning；本轮不新增测试数量，增强既有 nightly/manual performance smoke；触发链路走真实 FastAPI、PostgreSQL Run/AuditEvent、FairScheduler 入队元数据与 ARQ test double 记录，不替代 DB/API/审计链路 | 触发入队 SLO 从“10 次采样均入队 < 5s”扩到“每次采样也写可追溯审计”：10 个 run 都断言 `run.trigger` 的 before_state 为空，after_state 的 queued/manual/git_ref/priority/pipeline_id 与触发请求一致 |
| 2026-05-28 | collect-only 155 tests；`test_worker_execute.py` collect-only 5 tests；本地 external-stack 文件 5 skipped（未启动 compose 外部栈）；required integration 123 passed / 32 deselected；完整 local integration 131 passed / 24 skipped；performance smoke 17 passed | ruff targeted/full passed | 无新增 warning；本轮不新增测试数量，增强既有 nightly/manual external-stack smoke；本地未启动 compose API/worker/MinIO，真实执行证据由 nightly/manual 跑 `external_stack` 时产出 | 真实 worker 黑盒从“完成后有 artifact/归档日志”扩到“触发时也必须有可查询审计链路”：成功 worker、worker_lost retry 原始 run、high/low priority run、clone/setup 失败 run 都通过审计 API 校验 `run.trigger` 的 queued/manual/git_ref/priority 状态；worker secret 或 clone secret 不得进入该审计事件 |
| 2026-05-28 | collect-only 155 tests；required integration 123 passed / 32 deselected；完整 local integration 131 passed / 24 skipped；performance smoke 17 passed；`test_archived_log_replay_api_p99_smoke` / `test_archived_log_replay_large_page_p99_smoke` 2 passed | ruff targeted/full passed | 无新增 warning；本轮不新增测试数量，增强既有 nightly/manual performance smoke；归档日志回看走真实 FastAPI 权限依赖、PostgreSQL Run/RBAC、分页和 S3 get_object 副作用哨兵 | 归档日志成功回看 SLO 从“能读回且够快”扩到“只读正确归档对象”：小样本与 1500 行大对象分页都断言 3 次 warmup + 20 次采样只读取 `logs/{run_id}.jsonl` 共 23 次，且不生成 presign URL |
| 2026-05-28 | collect-only 155 tests；required integration 123 passed / 32 deselected；完整 local integration 131 passed / 24 skipped；performance smoke 17 passed；`test_artifact_download_url_api_p99_smoke` / `test_artifact_download_url_burst_p99_smoke` 2 passed | ruff targeted/full passed | 无新增 warning；本轮不新增测试数量，增强既有 nightly/manual performance smoke；artifact 下载链接走真实 FastAPI 权限依赖、PostgreSQL Artifact/Run 表和 S3 presign 副作用哨兵 | Artifact 下载链接 SLO 从“能生成 URL 且够快”扩到“成功路径 presign-only”：单 artifact 3 次 warmup + 20 次采样断言 23 次 `get_object` presign、burst 断言 3+40 次按 artifact 顺序 presign，且两条成功路径都没有 S3 `get_object` 调用 |
| 2026-05-28 | collect-only 155 tests；required integration 123 passed / 32 deselected；完整 local integration 131 passed / 24 skipped；performance smoke 17 passed；`test_artifact_list_api_p99_smoke` 1 passed | ruff targeted/full passed | 无新增 warning；本轮不新增测试数量，增强既有 nightly/manual performance smoke；artifact 列表走真实 FastAPI 权限依赖、PostgreSQL Artifact/Run 表、分页和 S3 副作用哨兵 | Artifact 列表 SLO 从“元数据查询快”扩到“成功查询仍是 DB-only”：3 次 warmup + 20 次 p99 采样后，断言 S3 test double 没有 `generate_presigned_url` 或 `get_object` 调用，避免列表页退化成隐性对象存储读/签名路径 |
| 2026-05-28 | collect-only 155 tests；required integration 123 passed / 32 deselected；完整 local integration 131 passed / 24 skipped；performance smoke 17 passed；`test_audit_events_list_api_p99_smoke` 1 passed | ruff targeted/full passed | 无新增 warning；本轮不新增测试数量，增强既有 nightly/manual performance smoke；审计查询走真实 FastAPI 权限依赖、PostgreSQL AuditEvent 表、分页/过滤和自审计写入链路 | Audit events 查询 SLO 从只看响应扩到成功副作用：3 次 warmup + 20 次 p99 采样后，断言同租户同用户新增 23 条 `audit_events.list` 自审计行，避免性能 smoke 只证明读快、不证明审计可追溯 |
| 2026-05-28 | collect-only 155 tests；required integration 123 passed / 32 deselected；完整 local integration 131 passed / 24 skipped；`test_real_worker_persists_artifacts_and_archived_logs` 本地 1 skipped（未启动 external stack，预期） | ruff targeted/full passed | 无新增 warning；本轮不新增测试数量，新增断言位于 nightly/manual external-stack 黑盒 lane，本地未启动 compose API/worker/MinIO 时保持前置条件 skip，业务断言不会被 PR required integration skip 掩盖 | 真实 worker 密钥防泄露证据收紧：同一 external-stack smoke 在验证 env var 解密注入、artifact 下载和归档日志读回后，额外断言 worker 注入密钥不出现在平台产出的 artifact 列表元数据、预签名下载内容或 archived logs 响应；当时该行只证明被动不泄露，后续 2026-05-30 已补齐主动 stdout/stderr 打印密钥的 live SSE、archived logs 与 `run.read` 读面脱敏证据 |
| 2026-05-28 | collect-only 155 tests；required integration 123 passed / 32 deselected；完整 local integration 131 passed / 24 skipped；performance smoke 17 passed；`test_artifact_list_denied_no_metadata_p99_smoke` 1 passed | ruff targeted passed | 无新增 warning；新增 skip/deselected 只来自 performance opt-in；拒绝路径走真实 FastAPI 权限依赖、PostgreSQL Artifact/Run 表和跨租户过滤链路，不触碰 S3 | Artifact 列表拒绝路径进入 nightly/manual SLO 与元数据泄露哨兵：member/viewer 无项目角色请求 run artifact 列表返回 403、owner 查询 tenant_B run artifact 列表返回 404，20 次采样 p99 受 `PERF_ARTIFACT_LIST_DENIED_P99_MS` 管控，响应不包含 artifact 名称或 `reports/` 存储路径 |
| 2026-05-28 | collect-only 154 tests；required integration 123 passed / 31 deselected；完整 local integration 131 passed / 23 skipped；performance smoke 16 passed；`test_audit_events_denied_queries_no_self_audit_p99_smoke` 1 passed | ruff targeted passed | 无新增 warning；新增 skip/deselected 只来自 performance opt-in；拒绝路径走真实 FastAPI 权限依赖、PostgreSQL AuditEvent 表和跨租户过滤链路，不依赖 mock repository/session | audit-events 拒绝查询进入 nightly/manual SLO 与副作用哨兵：member/viewer 请求 `/audit-events` 返回 403、owner 查询 tenant_B resource_id 返回 404，21 次采样 p99 受 `PERF_AUDIT_EVENTS_DENIED_P99_MS` 管控，且拒绝路径不会误写 `audit_events.list` 自审计行 |
| 2026-05-28 | collect-only 153 tests；required integration 123 passed / 30 deselected；完整 local integration 131 passed / 22 skipped；`test_audit_events_api.py` 5 passed；`test_audit_events_refused_queries_do_not_write_self_audit` 1 passed | ruff targeted passed | 无新增 warning；新增证据进入 required integration，真实 FastAPI 权限依赖、PostgreSQL AuditEvent 表和跨租户过滤链路，不依赖 mock repository/session | 安全审计查询拒绝路径进入真实 DB 证据：member/viewer 请求 `/audit-events` 返回 403、owner 查询 tenant_B resource_id 返回 404，且三条拒绝请求都不会误写 `audit_events.list` 自审计行，避免把探测 ID 污染进审计查询日志 |
| 2026-05-28 | collect-only 152 tests；required integration 122 passed / 30 deselected；完整 local integration 130 passed / 22 skipped；performance smoke 15 passed；`test_archived_log_denied_no_s3_read_p99_smoke` 1 passed | ruff targeted passed | 无新增 warning；新增 skip/deselected 只来自 performance opt-in；S3 test double 只记录 get_object 副作用，不替代真实 DB/API/RBAC/租户过滤链路 | 归档日志拒绝回看进入 nightly/manual SLO 与副作用哨兵：tenant A 请求 tenant B run 的 archived logs 与随机 UUID 返回一致 404，20 次采样 p99 受 `PERF_LOG_ARCHIVE_DENIED_P99_MS` 管控，且拒绝路径全程不触发 S3 get_object |
| 2026-05-28 | collect-only 151 tests；required integration 122 passed / 29 deselected；完整 local integration 130 passed / 21 skipped；performance smoke 14 passed；`test_artifact_download_denied_no_presign_p99_smoke` 1 passed | ruff targeted passed | 无新增 warning；新增 skip/deselected 只来自 performance opt-in，业务断言未被 skip；S3 test double 仅记录 presign 调用，不替代真实 DB/API/RBAC/租户过滤链路 | Artifact 拒绝下载进入 nightly/manual SLO 与副作用哨兵：tenant A 请求 tenant B artifact 与随机 UUID 返回一致 404，20 次采样 p99 受 `PERF_ARTIFACT_DOWNLOAD_DENIED_P99_MS` 管控，且拒绝路径全程不触发 S3 presign |
| 2026-05-28 | collect-only 150 tests；required integration 122 passed / 28 deselected；完整 local integration 130 passed / 20 skipped；`test_auth_refresh_logout_audits_do_not_store_tokens_or_passwords` 1 passed | ruff targeted passed | 无新增 warning；新增 required integration 不依赖 mock current-user，走真实 JWT、secure refresh cookie 值、FastAPI auth 路由和 PostgreSQL AuditEvent | Auth 凭证生命周期审计防泄密外扩：register audit 只记录 username/email，refresh audit 只记录 jti 元数据，logout audit 只记录是否携带 access token；password、access token、旧/新 refresh token 均不进入 AuditEvent before/after |
| 2026-05-28 | collect-only 149 tests；required integration 121 passed / 28 deselected；完整 local integration 129 passed / 20 skipped；`test_worker_execute.py` 5 collected；新增 external-stack clone/setup 用例本地 1 skipped（未启动外部栈，预期） | ruff full passed | 无新增 warning；新增 skip/deselected 只来自 nightly/manual external-stack，PR required integration 业务断言未受影响；heavy Docker 镜像 fixture 改为先复用本地镜像，缺镜像才 pull，避免 registry/network 波动把可运行用例误判为 skip | F-EX-07 worker 失败黑盒外扩：nightly/manual 真实 API/worker 栈会触发带 userinfo/token 的 clone 失败和 setup script 非 0 两条用户失败路径，断言它们保持 failed、不创建 attempt=2 retry Run、不落 artifact；clone 错误不泄露 `x-access-token`/secret，setup 失败的归档日志可读回 clone、setup 和 stderr 证据 |
| 2026-05-28 | collect-only 148 tests；required integration 121 passed / 27 deselected；完整 local integration 129 passed / 19 skipped；`test_worker_execute.py` 4 collected；新增 external-stack priority 用例本地 1 skipped（未启动外部栈，预期） | ruff full passed | 无新增 warning；新增 skip/deselected 只来自 nightly/manual external-stack，PR required integration 业务断言未受影响 | F-EX-08 优先级 worker 黑盒证据外扩：nightly/manual 会停掉 `worker-high`/`worker-low` 后触发 high/low priority run，证明 medium worker 不会误消费 high/low queue；再启动匹配 worker，真实 worker 完成执行、产出 JUnit artifact、预签名下载成功并可从归档日志读回上传和完成记录 |
| 2026-05-28 | collect-only 147 tests；required integration 121 passed / 26 deselected；完整 local integration 129 passed / 18 skipped；`test_auto_retry_real_db.py` 7 passed | `test_engine/test_executor.py` + `test_worker/test_auto_retry.py` 55 passed；ruff full passed | 无新增 warning；新增证据仍在 required integration，真实 PostgreSQL/Redis/worker 入口不依赖 external-stack；修复空 `Run.metadata_` clone fallback 误读 SQLAlchemy `metadata` 的真实回归 | 真实 `RunExecutor` 重试边界收紧：setup 阶段 Docker daemon `ConnectionError` 会冒泡到 worker 并创建/入队 attempt=2 retry Run；setup script 非 0 与 git clone 失败保持原 Run failed、不误触发 retry；clone 错误消息继续脱敏 userinfo/token |
| 2026-05-28 | collect-only 144 tests；required integration 118 passed / 26 deselected；完整 local integration 126 passed / 18 skipped；`test_auto_retry_real_db.py` 4 passed | ruff full passed | 无新增 warning；新证据进入 required integration，不依赖 external-stack 前置条件；发现并修复 Redis status event `previous` 被 SQLAlchemy 同步成 `failed` 的真实回归 | worker_lost reclaimer 从单测/黑盒外扩到真实 DB/Redis 证据：heartbeat 过期会把原 Run 标记 failed、清 worker、发布 `previous=running` 的 Redis status event、调用 orphan cleanup，并创建/入队 attempt=2 retry Run |
| 2026-05-28 | collect-only 143 tests；required integration 117 passed / 26 deselected；完整 local integration 125 passed / 18 skipped；performance smoke 13 passed | ruff full passed | 无新增 warning；新增 skip/deselected 只来自 performance opt-in 大日志回看哨兵，required integration 业务断言未受影响 | 归档日志回看 SLO 从小样本扩到大对象分页：nightly/manual performance smoke 预置 1500 行 JSONL 归档对象，走真实 `/runs/{id}/logs/archive` API/RBAC/分页链路读取第 15 页，并用 `PERF_LOG_ARCHIVE_LARGE_PAGE_P99_MS` 输出 p99 趋势信号 |
| 2026-05-28 | collect-only 142 tests；required integration 117 passed / 25 deselected；完整 local integration 125 passed / 17 skipped；`test_worker_execute.py` 3 skipped（本地未启动 external stack，预期） | ruff full passed | 无新增 warning；external-stack 只在 nightly/manual 启动 compose API/worker/MinIO 后实跑，本地 skip 不掩盖 required integration 业务断言 | 真实 worker artifact 黑盒证据扩到多产物：external-stack smoke 会让 worker 产出 JUnit、HTML report、trace log、Allure index，逐个经 API 换取真实 MinIO 预签名 URL 并下载内容，同时归档日志断言每个 artifact 上传记录 |
| 2026-05-28 | collect-only 142 tests；required integration 117 passed / 25 deselected；完整 local integration 125 passed / 17 skipped；`test_auth_audit_failure_paths.py` + `test_real_auth_results_artifacts.py` 25 passed | `test_auth_routes.py` + `test_jwt_service.py` 53 passed；ruff full passed | 无新增 warning；审计写入 outage 与 Redis revoke blip 用 failure injection 制造，真实 FastAPI/auth/DB/Redis 依赖仍走 integration app；新增 skip 未扩大 | Auth 安全审计故障进入 required integration：login/refresh/logout 失败审计写入异常仍保持 401/204，refresh token revoke Redis 抖动仍签发新 access token，且告警日志使用 `extra` 不再因 logging kwargs 二次抛错 |
| 2026-05-28 | collect-only 138 tests；required integration 113 passed / 25 deselected；完整 local integration 121 passed / 17 skipped；performance smoke 12 passed；`test_artifact_download_url_burst_p99_smoke` 1 passed | ruff full passed | 无新增 warning；新增 deselected/skip 均来自 performance opt-in，业务断言未被 skip；burst smoke 使用 S3 test double 只替代对象存储签名服务，真实 DB/API/RBAC 链路仍走 integration app | Artifact 下载链接 burst 进入 nightly/manual SLO 趋势哨兵：真实 PostgreSQL 预置 40 条 artifact 行，逐个走真实 `/artifacts/{id}/download` FastAPI/RBAC 链路并通过 S3 test double 生成预签名 URL，覆盖列表页连续换取下载链接的退化风险 |
| 2026-05-28 | required integration 113 passed / 24 deselected；完整 local integration 121 passed / 16 skipped；`test_worker_execute.py` 3 skipped（未启动 external stack，预期） | `test_env_vars_crypto.py` + `test_007_encrypt_environment_env_vars.py` + `test_worker/test_tasks.py` + `test_environment_env_vars_encryption.py` 19 passed；ruff full passed | 无新增 warning；真实 worker env var 注入证据位于 nightly/manual external-stack lane，本地未启动 compose 栈时保持 skip；setup script 只保存 secret SHA-256 摘要，不把测试 secret 字面量写入 audit 可见字段 | F-PL-02 环境变量加密从 API/DB/migration 扩到真实 worker 黑盒证据：external-stack worker smoke 会创建加密 env var，并要求容器内 `python -m pytest` 看到解密后的 `QAP_REAL_WORKER_SECRET` 后才生成 JUnit artifact 和归档日志；文档同步标记 F-PL-02 达标 |
| 2026-05-28 | required integration 113 passed / 24 deselected；完整 local integration 121 passed / 16 skipped | `test_engine/test_docker_backend.py` + `test_worker/test_tasks.py` 37 passed；ruff full passed | 单元与 integration 路径无新增 warning；真实 Docker storage driver 平台差异仍需后续 Docker lane 验证 | 内部磁盘限制传递进入自动化证据：`Environment.resource_limits["disk_mb"]` 会映射为 `ResourceLimits.disk_bytes`，Docker backend 在该值存在时写入 `HostConfig.StorageOpt.size`；API 暴露磁盘配额和资源用量记录仍保持为 F-PL-03 剩余缺口 |
| 2026-05-28 | collect-only 137 tests；required integration 113 passed / 24 deselected；完整 local integration 121 passed / 16 skipped；`test_worker_resource_termination.py` 1 passed | `test_engine/test_executor.py` 31 passed；ruff full passed | 无新增 warning；OOM/timeout 使用 backend test double 注入终止结果，真实部分是 PostgreSQL、Redis status/event、RunExecutor 状态机和 run log 收尾 | 资源终止进入 required integration 真实状态证据：`oom_killed=True` 与 `timed_out=True` 都会写 Run `timeout` 终态、summary、Redis `timeout` status event 和收尾日志；磁盘限制与资源用量记录仍保持为 F-PL-03 剩余缺口 |
| 2026-05-28 | collect-only 136 tests；required integration 112 passed / 24 deselected；完整 local integration 120 passed / 16 skipped；performance smoke 11 passed；`test_artifact_list_api_p99_smoke` 1 passed | ruff full passed | 无新增 warning；新增 skip 为 performance opt-in，artifact 列表性能 smoke 不进入 PR 硬门禁 | Artifact 列表元数据进入 nightly/manual SLO 趋势哨兵：真实 PostgreSQL 预置 80 条 artifact 行，走真实 `/runs/{id}/artifacts` FastAPI/RBAC/分页链路，失败时输出 p50/p99/max 摘要，默认阈值 `PERF_ARTIFACT_LIST_P99_MS=1000` |
| 2026-05-28 | collect-only 135 tests；required integration 112 passed / 23 deselected；完整 local integration 120 passed / 15 skipped；`test_real_auth_results_artifacts.py` 17 passed；`test_artifact_list_api_token_requires_run_read_scope` 1 passed | ruff full passed | 无新增 warning；artifact 列表断言顺序无关，避免无显式排序导致分页误报 | Artifact 元数据枚举进入 API token scope 真实证据：`project.read` token 请求 `/runs/{id}/artifacts` 返回 403 且不泄露 artifact 名称/路径，`run.read` token 才能看到真实 DB artifact 列表 |
| 2026-05-28 | required integration 111 passed / 23 deselected；`test_real_auth_results_artifacts.py` 16 passed；`test_real_jwt_created_api_token_authenticates_updates_last_used_and_revokes` 1 passed | ruff full passed | 无新增 warning；只加强真实 DB 审计断言，不改变 API 行为 | API token 创建/撤销审计进入真实 DB 防泄密证据：create 只记录 name/scopes，revoke 只记录 name，full token、secret、secret_hash 均不落 AuditEvent before/after |
| 2026-05-28 | collect-only 134 tests；required integration 111 passed / 23 deselected；完整 local integration 119 passed / 15 skipped；`test_real_auth_results_artifacts.py` 16 passed；`test_archived_logs_api_token_requires_run_read_scope` 1 passed | ruff full passed | 无新增 warning；完整 local integration 的 skip 仍来自 macOS Docker Desktop OOM 语义、performance opt-in、未启动 external stack，业务断言未被 skip | 归档日志回看进入 API token scope 真实证据：`project.read` token 请求 `/runs/{id}/logs/archive` 返回 403 且不读取 S3，`run.read` token 才可分页读回 JSONL |
| 2026-05-28 | collect-only 133 tests；required integration 110 passed / 23 deselected；完整 local integration 118 passed / 15 skipped；`test_real_api_write_state.py` 12 passed；`test_manual_run_priority_persists_worker_queue_metadata_with_real_db` 1 passed | ruff full passed | 无新增 warning；required integration 曾暴露配额顺序相关风险，已在测试内隔离 scheduler 容量边界，业务断言未被 skip | Worker 队列入队元数据进入真实 API/DB 证据：manual priority 0/1/2 经 `/api/v1/runs` 和 FairScheduler 写入 `queue:high` / `queue:medium` / `queue:low`、`arq_job_id`、`enqueued_at`，ARQ 仅作为外部队列 test double |
| 2026-05-28 | collect-only 132 tests；required integration 109 passed / 23 deselected；完整 local integration 117 passed / 15 skipped；performance smoke 10 passed；`test_cancel_run_api_p99_smoke` 1 passed | ruff full passed | 无新增 warning；完整 local integration 的新增 skip 来自 performance opt-in，业务断言未被 skip | 取消控制面进入 nightly/manual 性能趋势哨兵：真实 FastAPI cancel 路由、PostgreSQL Run 终态、Redis status hash 和 AuditEvent before/after 一起计入 p99 smoke，默认阈值 `PERF_CANCEL_RUN_P99_MS=1000` |
| 2026-05-28 | collect-only 131 tests；required integration 109 passed / 22 deselected；完整 local integration 117 passed / 14 skipped；`test_real_api_write_state.py` 11 passed；single/batch cancel targeted 2 passed | `test_api/test_runs.py` + `test_api/test_p3.py` 41 passed；ruff targeted passed | 无新增 warning；完整 local integration 的 skip 仍来自 macOS Docker Desktop OOM 语义、performance opt-in、未启动 external stack，业务断言未被 skip | 取消控制面进入真实 DB/API/Redis/audit 证据：单 run cancel 与 batch cancel 都断言 DB 终态、Redis status event `previous=running`、AuditEvent before=`running` / after=`cancelled` |
| 2026-05-28 | collect-only 130 tests；required integration 108 passed / 22 deselected；完整 local integration 116 passed / 14 skipped；`test_real_auth_results_artifacts.py` 15 passed；`test_artifact_download_api_token_cross_tenant_returns_same_404` 1 passed | ruff full passed（使用 `/Users/raylee/.local/bin/ruff`；`.venv` 未安装 ruff 模块） | 无新增 warning；完整 local integration 的 skip 仍来自 macOS Docker Desktop OOM 语义、performance opt-in、未启动 external stack，业务断言未被 skip | Artifact 下载跨租户枚举风险进入真实 API token 证据：tenant A 的 `run.read` token 请求 tenant B artifact 与随机 UUID 返回一致 404，且不会调用 S3 presign |
| 2026-05-28 | collect-only 129 tests；required integration 107 passed / 22 deselected；完整 local integration 115 passed / 14 skipped；`test_real_auth_results_artifacts.py` 14 passed；`test_artifact_download_api_token_requires_run_read_scope` 1 passed | ruff full passed（使用 `/Users/raylee/.local/bin/ruff`；`.venv` 未安装 ruff 模块） | 无新增 warning；完整 local integration 的 skip 仍来自 macOS Docker Desktop OOM 语义、performance opt-in、未启动 external stack，业务断言未被 skip | Artifact 下载权限进入 API token scope 真实证据：`project.read` token 请求 `/artifacts/{id}/download` 返回 403 且不调用 S3 presign，`run.read` token 才返回 bucket/key/TTL 正确的预签名 URL |
| 2026-05-28 | collect-only 128 tests；required integration 106 passed / 22 deselected；完整 local integration 114 passed / 14 skipped；`test_real_auth_results_artifacts.py` 13 passed；`test_log_archive_retry_worker_uses_real_redis_marker` 1 passed | ruff full passed（使用 `/Users/raylee/.local/bin/ruff`；`.venv` 未安装 ruff 模块） | 无新增 warning；完整 local integration 的 skip 仍来自 macOS Docker Desktop OOM 语义、performance opt-in、未启动 external stack，业务断言未被 skip | 归档失败补偿进入真实 Redis/worker 证据：首次 S3 put 失败后真实 Redis retry set 与 24h TTL 落下，worker cron 入口重试成功后清 marker/1h TTL，并可从 JSONL 归档读回 |
| 2026-05-28 | collect-only 127 tests；required integration 105 passed / 22 deselected；完整 local integration 113 passed / 14 skipped；performance smoke 9 passed；`test_audit_events_list_api_p99_smoke` 1 passed | ruff targeted/full passed | 无新增 warning；完整 local integration 新增 1 个 skip 为 performance opt-in，业务断言未被 skip | Audit events 查询 API 进入 nightly/manual SLO 趋势哨兵：真实 PostgreSQL 预置 120 条审计行，走真实 `/api/v1/audit-events` 权限/分页/过滤链路，失败时输出 p50/p99/max 摘要 |
| 2026-05-27 | collect-only 126 tests；required integration 105 passed / 21 deselected；完整 local integration 113 passed / 13 skipped；`test_cross_tenant_isolation.py` 14 passed | ruff targeted/full passed | 无新增 warning；完整 local integration 的 skip 来自 macOS Docker Desktop OOM 语义、performance opt-in、未启动 external stack，业务断言未被 skip | 归档日志回看进入跨租户隔离矩阵：`GET /runs/{id}/logs/archive` 对 tenant_B 真实 Run ID 与随机 UUID 返回一致 404，避免 archived log 是否存在成为资源枚举侧信道 |
| 2026-05-27 | collect-only 125 tests；required integration 104 passed / 21 deselected；完整 local integration 112 passed / 13 skipped；`test_real_auth_results_artifacts.py` 12 passed | ruff targeted/full passed | 无新增 warning；完整 local integration 的 skip 来自 macOS Docker Desktop OOM 语义、performance opt-in、未启动 external stack，业务断言未被 skip | Artifact S3 上传失败进入真实 DB 证据：executor 调用失败 S3 test double 后，用真实 ArtifactRepository/PostgreSQL 断言没有孤儿 Artifact 行；外部对象存储可 fake，但 DB 状态必须真实验证 |
| 2026-05-27 | collect-only 124 tests；required integration 103 passed / 21 deselected；完整 local integration 111 passed / 13 skipped；`test_real_api_write_state.py` 10 passed | `test_api/test_pipelines.py` 6 passed；ruff full passed | 无新增 warning；完整 local integration 的 skip 来自 macOS Docker Desktop OOM 语义、performance opt-in、未启动 external stack，业务断言未被 skip | Pipeline 复杂配置审计进入真实 DB 证据：stages/trigger_config 中 token/password/secret/credential/Authorization 等值会照常落业务表，但 `pipeline.create/update/delete` AuditEvent before/after 会递归脱敏，URL userinfo 也会保留 host/path 后脱敏 |
| 2026-05-27 | collect-only 124 tests；required integration 103 passed / 21 deselected；完整 local integration 111 passed / 13 skipped；`test_silent_windows.py` 6 passed | `test_worker/test_check_schedules.py` 7 passed；ruff full passed | 无新增 warning；完整 local integration 的 skip 来自 macOS Docker Desktop OOM 语义、performance opt-in、未启动 external stack，业务断言未被 skip | Schedule worker 的 missing pipeline 分支进入真实 DB audit 证据：软删除 pipeline 后 due schedule 不创建 Run，更新 `last_error=pipeline not found`，并以系统身份写 `schedule_skipped_missing_pipeline` AuditEvent，after_state 只包含 schedule/project/pipeline/reason/next_run_at 等排障字段 |
| 2026-05-27 | collect-only 123 tests；required integration 102 passed / 21 deselected；完整 local integration 110 passed / 13 skipped；`test_webhook_branch_dedup.py` 10 passed | `test_api/test_p3.py::TestWebhookTrigger` 4 passed；ruff full passed | 无新增 warning；完整 local integration 的 skip 来自 macOS Docker Desktop OOM 语义、performance opt-in、未启动 external stack，业务断言未被 skip | Webhook filtered/duplicate 这种“不创建 Run 但请求已发生”的分支进入真实 DB audit 证据：`webhook.filtered` / `webhook.duplicate` 写项目级 AuditEvent，after_state 只保留 project、git_ref、git_sha、branch、provider、delivery_id 等决策字段，不落 repo URL、dedup_key 或任意 metadata |
| 2026-05-27 | collect-only 123 tests；required integration 102 passed / 21 deselected；完整 local integration 110 passed / 13 skipped；`test_silent_windows.py` 5 passed | `test_worker/test_check_schedules.py` 7 passed；ruff full passed | 无新增 warning；完整 local integration 的 skip 来自 macOS Docker Desktop OOM 语义、performance opt-in、未启动 external stack，业务断言未被 skip | Schedule worker 自动触发进入真实 DB audit 证据：`check_schedules` 创建 schedule Run 后以系统身份写 `run.trigger` AuditEvent，集成测试覆盖成功入队与 arq job conflict 等待队列两条路径，并断言 `schedule_id`、trigger_type、project/pipeline/environment、git_ref、enqueued 状态 |
| 2026-05-27 | collect-only 123 tests；required integration 102 passed / 21 deselected；完整 local integration 110 passed / 13 skipped；`test_webhook_branch_dedup.py` 10 passed | ruff full passed | 无新增 warning；完整 local integration 的 skip 来自 macOS Docker Desktop OOM 语义、performance opt-in、未启动 external stack，业务断言未被 skip | 签名 webhook 进入 required integration：缺失 HMAC 签名不会创建 Run；签名成功后用真实 Project settings、FastAPI 路由、PostgreSQL Run/AuditEvent 验证 branch filter、dedup_key、`run.trigger` audit，并证明 payload 不能覆盖 `git_url` / `credential_id` / `shallow_clone` / `default_branch` 等保留执行配置 |
| 2026-05-27 | collect-only 121 tests；required integration 100 passed / 21 deselected；完整 local integration 107 passed / 14 skipped；`test_real_auth_results_artifacts.py` 11 passed；performance smoke 8 passed | ruff full passed | 无新增 warning；完整 local integration 的 skip 来自 Docker registry/image 前置条件不可用、performance opt-in、macOS OOM 语义、未启动 external stack，业务断言未被 skip | SSE `Last-Event-ID` 断点续传进入 required integration；用真实 JWT 注册、真实 `/auth/sse-ticket`、Redis GETDEL 单次消费、PostgreSQL Run/RBAC 查询和 Redis Stream ID 验证只回放断点后的日志 |
| 2026-05-27 | collect-only 120 tests；required integration 99 passed / 21 deselected；完整 local integration 101 passed / 19 skipped；performance smoke 8 passed | ruff full passed | 无新增 warning；完整 local integration 的 skip 来自 Docker registry/image 前置条件不可用、performance opt-in、macOS OOM 语义、未启动 external stack，业务断言未被 skip | SSE 实时日志推送 < 2s 进入 nightly/manual performance smoke；用真实本地 HTTP SSE 流、一次性 ticket、RBAC/PostgreSQL Run 查询和 Redis Stream 写入证明客户端可收到实时日志 chunk |
| 2026-05-27 | collect-only 119 tests；required integration 99 passed / 20 deselected；完整 local integration 106 passed / 13 skipped；performance smoke 7 passed | ruff targeted passed | 无新增 warning；完整 local integration 的额外 skip 来自 Docker registry/image 前置条件不可用，业务断言未被 skip；其余 skip 为 performance opt-in、macOS OOM 语义、未启动 external stack | 执行摘要生成 < 3s 进入 nightly/manual performance smoke；用真实 RunRepository/PostgreSQL、Redis LogStream、JUnitCollector 和终态 summary DB 写入证明，不把该 SLO 只留在文档里 |
| 2026-05-27 | collect-only 118 tests；required integration 99 passed / 19 deselected；完整 local integration 107 passed / 11 skipped；`test_real_auth_results_artifacts.py` 10 passed | ruff targeted passed | 无新增 warning；skip 均为既有 performance opt-in、macOS OOM 语义、未启动 external stack；本轮发现同文件多次真实注册会触发 auth register 每 IP 限流，已在 real-auth integration fixture 中提高测试阈值避免顺序误报 | artifact 列表到下载链接的真实 JWT/RBAC/API/DB 行与预签名 bucket/key/TTL 参数进入 required integration；保留 rate-limit middleware 开启，不改变生产限流行为 |
| 2026-05-27 | collect-only 117 tests；required integration 98 passed / 19 deselected；完整 local integration 106 passed / 11 skipped；`test_real_api_write_state.py` 10 passed | `test_api/test_projects.py` + `test_engine/test_executor_redaction.py` 30 passed；ruff targeted passed | 无新增 warning；skip 均为既有 performance opt-in、macOS OOM 语义、未启动 external stack | project/member/pipeline 写路径进入真实 DB audit 证据；项目 `git_url` userinfo 仅在 audit state 脱敏，不改变 API 响应 |
| 2026-05-27 | collect-only 114 tests；required integration 95 passed / 19 deselected；完整 local integration 103 passed / 11 skipped；`test_real_auth_results_artifacts.py` 9 passed | ruff targeted passed | 无新增 warning；skip 均为既有 performance opt-in、macOS OOM 语义、未启动 external stack | 归档日志 API 从 happy path 扩到分页窗口和 S3 对象缺失 404，仍不把真实 DB/API/RBAC 链路替换成 mock |
| 2026-05-27 | `tests/integration/test_worker_execute.py` collect-only 3 tests；local run 3 skipped（未启动 compose API/worker/MinIO） | ruff targeted passed | 无新增 warning | external-stack worker smoke 与 worker_lost retry 将在 nightly/manual 进一步校验预签名 URL 可下载真实 JUnit artifact 内容 |
| 2026-05-27 | collect-only 113 tests；required integration 94 passed / 19 deselected；完整 local integration 102 passed / 11 skipped；performance smoke 6 passed | ruff targeted passed | 无新增 warning | artifact 下载链接 API 进入 nightly/manual performance smoke，和读/写 run、入队、Redis 日志、归档日志一起输出 p50/p99/max 失败摘要 |
| 2026-05-27 | `tests/integration/test_real_api_write_state.py` 7 passed | `tests/unit/test_api/test_notifications.py tests/unit/test_api/test_schedules.py` 10 passed | 无新增 warning；ruff targeted passed | 审计敏感值不落库和 delete before_state 进入真实 DB/API 证据，覆盖 credentials、environments、notification rules、schedules |
| 2026-05-27 | E2E targeted `special-regressions -g "archived logs"` 1 passed | frontend lint `--quiet` passed | 本地旧 Uvicorn/Vite 复用会造成归档接口路由级假阴性；验证时已用 `CI=1` 强制新进程 | 归档日志回看 + artifact 预览进入 nightly/manual 真实 E2E 证据；本地复现该链路时优先停旧服务或使用 `CI=1` |
| 2026-05-27 | required integration 94 passed / 18 deselected；完整 integration 102 passed / 10 skipped（未启动外部 API/worker 栈）；performance smoke 5 passed | unit + coverage 758 passed，coverage 83.37% | 已清理项目内 warning；第三方 testcontainers warning 精确过滤并登记；nightly/manual external-stack 主动启动 compose 后跑真实 worker smoke 与 worker_lost retry 黑盒 | 可继续作为 PR 门禁，nightly 承担重型/性能路径 |

## 发版候选签字清单

测试负责人不能只看 GitHub Actions 绿色状态签字。每个发版候选必须用目标分支或目标 commit 触发 `workflow_dispatch`，并选择 `gate=release_candidate`；`pull_request`、`push`、`nightly` 或本地测试都只能作为补充证据，不能替代发版签字证据。

| 阶段 | 必须证据 | 拒签条件 |
| --- | --- | --- |
| 触发 | GitHub Actions run URL、目标分支、commit SHA、`gate=release_candidate` | run 不是目标 commit；使用 PR/default CI 代替；workflow 输入不是 `release_candidate` |
| Backend unit / lint | `backend-test` 通过，unit JUnit 有 testcase，failure/error/skipped 均为 0，coverage XML/JSON 可解析 | unit 空跑、coverage 缺失、tenant isolation audit 缺失或不可解析 |
| Frontend/API contract | OpenAPI schema 导出成功，frontend API contract、typecheck/build 通过 | 前后端 DTO/关键 hook 映射漂移，或 build/typecheck 失败 |
| Backend integration | required/heavy/external-stack/performance JUnit 均有 testcase；`collect_validation=passed`、`strict_skips=True`、`integration_skip_inventory=written skipped=0`、`nodeid_validation=passed` | 任一 lane 空跑；failure/error/skipped 非 0；unknown skip；collect 基线低于预期 |
| Worker/runtime evidence | external-stack worker 能写回终态、logs、artifacts、archived logs；`release_candidate_oom_tests=passed` | OOMKilled opt-in 用例缺失、skip 或失败；worker 结果没有真实入库/入对象存储证据 |
| E2E evidence | `mode=release_candidate`；关键 spec 均存在；`testcase_count` 与 `actual_testcase_count` 一致；failure/error/skipped/unexpected/flaky 均为 0 | 跑错 spec 子集；JUnit 与 Playwright list 不一致；任何 skip/fail/flaky |
| Performance SLO | `performance_summary_gate_profile=release_candidate`、`performance_trend_validation=passed`，summary 与 manifest/baseline 匹配 | SLO 名称缺失/意外新增、阈值漂移、样本不足、`passed=false`、超过 baseline 回退预算 |
| 签字记录 | PR 或 release note 记录 run URL、commit SHA、主要 artifact 名称和已知非阻塞风险 | 只写“CI 通过”但没有可追溯 run/artifact；非阻塞风险没有 owner/复核点 |

## 合并后复验

合并 PR #12 或任何修改 CI gate、测试分层、E2E、worker 结果入库、性能 SLO manifest/baseline 的 PR 后，必须在 `main` 上重新运行一次 `workflow_dispatch.gate=release_candidate`。复验通过后，把新的 run URL 写回 release 记录或后续 PR 描述；如果 `main` 复验失败，即使 PR 分支曾经通过，也要按 P0 release blocker 处理，直到同一 `main` commit 的 release candidate run 重新通过。

复验时先看 `Release Candidate Gate` 汇总，再抽查 artifacts：backend integration evidence manifest、integration skip inventory、E2E evidence manifest、performance trend JSON 和 release evidence markdown。若 GitHub runner、Docker registry、external stack 启动等基础设施故障导致失败，可以登记为 infra blocker，但不能把失败 run 改判为可签字。

## 测试负责人后续完善路线图

当前主干已经具备 release candidate evidence gate，测试负责人后续关注点不是单纯增加用例数，而是把发布签字、真实依赖、非功能专项和剩余 PRD 验收都变成可追溯证据。实施顺序以 [`TODO.md`](TODO.md) 和 [`feature-catalog.md`](feature-catalog.md) 为功能真相源；本表定义测试侧签收物。

| 优先级 | 主题 | 当前证据 | 仍需完善 | 测试侧签收标准 |
| --- | --- | --- | --- | --- |
| P0 | 发版签字记录闭环 | `release-gate` 已聚合 `release-gate-evidence/release-evidence.md`、E2E manifest、performance trend 和 skip inventory | PR / release note 仍需人工记录 run URL、commit SHA、artifact 名称和非阻塞风险；签字包尚未形成固定模板或 checklist artifact | 每个候选版本都有一份可追溯签字记录，包含目标 commit、release_candidate run、关键 artifact、风险 owner 和合并后 `main` 复验结果 |
| P0 | 可控真实依赖 | external-stack / worker-backed E2E 已覆盖真实 API、worker、MinIO、Docker 和公开 Git fixture | 公开 GitHub fixture 不足以证明企业私有仓库、凭据轮换和 provider 差异；GitLab/Gitee/PR/fork 事件矩阵仍缺 | 自有公开 fixture + 受控私有仓库 fixture 进入 nightly/manual；凭据轮换、成功 clone、失败脱敏、GitLab/Gitee/PR/fork 策略都有黑盒证据 |
| P0 | 剩余 PRD 验收矩阵 | TODO 已列出 Git provider 扩展、多资源报告加载体验等缺口；项目搜索排序、通知、项目级静默窗口和单用例历史趋势已完成 | 缺口分散在 TODO/catalog/tasks 中，容易只按功能实现而漏掉验收路径 | 每个剩余 PRD 项都有验收矩阵：API/DB 证据、前端路径、权限/审计、失败路径、是否进入 PR/nightly/release_candidate |
| P1 | 容量与长稳专项 | nightly/manual performance smoke 已覆盖 39 个 SLO 与趋势校验 | smoke 不能替代专项容量压测、长时间 soak、真实长队公平性、对象存储大文件和数据库冷归档规模验证 | 建立独立专项环境，输出容量基线、退化预算、soak 报告、队列公平性报告、对象存储/DB 生命周期报告 |
| P1 | 故障演练与恢复 | heavy Docker、OOMKilled、worker_lost、clone/setup failure 已有多条自动化证据 | Docker daemon 扰动、Redis/Postgres/MinIO 抖动、备份恢复和 Docker socket 生产替代方案还缺演练证据 | 每类关键依赖至少有一次故障注入记录、预期用户影响、告警信号、恢复步骤和自动化回归入口 |
| P1 | 运维可观测性 | structlog 已覆盖 API 与 worker 入口，Prometheus 与 CI evidence manifest 已覆盖部分链路 | OpenTelemetry 装配、部署 checklist、告警/runbook 仍未闭环 | 关键 run 生命周期、队列、对象存储、审计和通知路径都有 trace/log/metric 对应项，部署前 checklist 可操作且可复验 |
| P2 | 风险覆盖率提升 | unit coverage gate 约 83%，required integration 覆盖高风险 DB/API/RBAC/worker 路径 | 分支覆盖仍低于语句覆盖；不应靠空断言提升数字 | 覆盖率提升只接受真实风险路径：越权、拒绝副作用、通知降级、外部服务异常、归档/产物失败和审计边界 |
| P2 | 前后端契约与 E2E 广度 | frontend API contract 已校验关键 DTO 字段存在、OpenAPI type/nullable/enum/required/array item 对齐与 hook 映射；PR 轻量 E2E 已覆盖 mocked run 状态渲染；nightly/release_candidate 真实 E2E 已建立 | OpenAPI 生成 DTO/client、更多低成本真实后端 smoke、跨浏览器/移动端兼容仍可增强；PR 级 mocked API 仍不能替代真实后端证据 | 前端手写 DTO 漂移被生成契约替代；关键用户路径在 PR 有轻量真实烟测，在 release_candidate 有完整浏览器证据 |

## Skip 与 SLO 治理规则

- `release_candidate` 下 skipped 必须为 0；nightly 只允许已登记的环境门控或 OOM opt-in 类 skip，且必须能被 `scripts/report_integration_skips.py` 归类。
- 新增 skip 时先写原因、owner、恢复条件和对应 marker；unknown skip 不得进入发版候选签字。
- 集成测试清单变化要同步更新 collect 基线和 review slice 说明，避免删除/改 marker 后空跑仍绿。
- 手工 `scripts/smoke` 默认把 skip 计为失败；只有探索性运行才允许显式设置 `SMOKE_ALLOW_SKIPS=1`，此时报表状态为 `PASS_WITH_SKIPS`，不得作为签字证据。
- `.github/performance-slo-manifest.json` 新增或删除 SLO 时必须说明用户路径、阈值来源、`min_samples` 和归属 lane。
- `.github/performance-slo-baseline.json` 只能随证据更新：放宽 baseline 需要关联退化原因或容量变化；收紧 baseline 需要引用最近稳定 run；不得为了让单次 run 通过而临时改 baseline。

### Nightly skip inventory review cadence

测试负责人每天工作日检查最近一次 schedule 或 `workflow_dispatch.gate=nightly` 的 `backend-integration-artifacts`，以 `integration-skip-inventory.json` 为准，不用日志肉眼估算。复核步骤：

1. 下载 `backend-integration-artifacts`，确认 `evidence-manifest.txt` 包含 `integration_skip_inventory=written skipped=<n>`。
2. 对每条 skip 归类为 `environment_gate`、`infrastructure_gate` 或 `unknown`；分类依据必须能从 marker、pytest skip reason 或 issue 链接追溯。
3. `unknown` skip 必须当天补 owner、原因和恢复条件；如果当天无法解释，按 release blocker 候选登记，不得进入下一次发版候选签字。
4. 如果 skip 覆盖的是 release-critical 语义，要么移入 `release_candidate` 必跑路径，要么删除 skip 条件；不能只留在 nightly 里长期观察。
5. 每周复盘 nightly skip 趋势：重复出现的 infra/environment skip 超过 2 次，需要有独立 issue 和恢复日期。

| Skip 分类 | 允许出现场景 | 必填字段 | 升级条件 |
| --- | --- | --- | --- |
| `environment_gate` | 本地或 nightly 缺少外部服务、Docker 能力、registry 镜像或平台特性 | owner、环境前置条件、恢复命令/恢复信号 | 同一环境门控连续 2 次影响 nightly，转 infra issue |
| `infrastructure_gate` | GitHub runner、Docker registry、外部栈启动、网络/对象存储类故障 | owner、外部依赖、run URL、恢复条件 | 影响 release_candidate 时直接 P0 release blocker |
| `unknown` | 无法从 marker/reason 判断是否业务断言被跳过 | owner、初步怀疑、当天处理截止时间 | 不得过夜；不得作为 release sign-off 证据 |

### Performance SLO baseline governance

性能 baseline 是发版证据的一部分，不是让 CI 变绿的调参文件。涉及 `.github/performance-slo-manifest.json` 或 `.github/performance-slo-baseline.json` 的 PR 必须满足：

1. PR 描述包含触发方式、run URL、commit SHA、`backend-integration-artifacts/performance-trend.json` 和 `release-gate-evidence/release-evidence.md`。
2. 放宽 baseline 必须链接退化 issue，说明用户路径、容量/环境变化、owner、恢复或复核日期；没有退化 issue 的放宽视为拒签条件。
3. 收紧 baseline 需要最近至少 3 次同 profile 运行稳定优于旧 baseline，并记录样本 run URL；单次改善不能直接收紧。
4. 新增 SLO 必须同步 manifest、summary 输出、baseline、验证脚本期望和 release review slice；删除 SLO 必须说明产品路径已移除或被更强 SLO 覆盖。
5. `release_candidate` 失败时禁止先改 baseline 再补 issue；必须先确认是否产品退化、环境故障或测试夹具问题。

## 非阻塞风险台账

| ID | 风险 | 来源/信号 | Owner | 状态 | 下次复核 |
| --- | --- | --- | --- | --- | --- |
| RISK-CI-001 | GitHub Actions Node.js 20 deprecation 预警可能影响未来 hosted runner action 执行 | 2026-05-29 远端 CI run 日志中出现 action runtime deprecation 类 warning；GitHub changelog 已把 hosted runner 默认 Node 24 切换更新到 2026-06-16，并要求用户更新到 Node 24-ready action 版本 | CI 维护者 | mitigating / PR validation required | `.github/workflows/ci.yml` 必须保持 `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true`；官方 actions 需使用 Node 24 major：`actions/checkout@v6`、`setup-node@v6`、`setup-python@v6`、`upload-artifact@v7`、`download-artifact@v8`；关闭风险前必须引用 PR CI run 与 `workflow_dispatch.gate=release_candidate` run |
| RISK-DEP-001 | 默认分支仍有 Dependabot moderate vulnerabilities 提示 | 2026-05-29 GitHub Dependabot alerts #1-#9 均指向 `frontend/package-lock.json` 的 runtime dependency `dompurify`，最高修复版本要求 `>=3.4.0`；2026-05-30 当前分支 `npm audit --prefix frontend --audit-level=moderate` 返回 0 vulnerabilities | 依赖维护者 | mitigating / PR validation required | 本轮升级 `dompurify` 到 `^3.4.7` 并移除废弃 stub `@types/dompurify`；关闭风险前仍必须引用 frontend typecheck/build、PR CI，以及 Dependabot alerts 在默认分支关闭的证据 |

### GitHub Actions Node.js 24 readiness

CI workflow 需要主动在 Node 24 action runtime 下验证，而不是等 hosted runner 默认切换后被动暴露问题。每次修改 `.github/workflows/ci.yml` 时，测试负责人按以下顺序签收：

1. 确认 workflow 顶层保留 `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true`。
2. 确认官方 JavaScript actions 使用 Node 24-ready major：`actions/checkout@v6`、`actions/setup-node@v6`、`actions/setup-python@v6`、`actions/upload-artifact@v7`、`actions/download-artifact@v8`。
3. PR CI 必须全绿；如果 action major 升级影响 artifact 上传/下载、checkout、缓存或 Python/Node 安装行为，不能只用本地测试替代远端 runner 证据。
4. 因为该变更影响 CI gate 行为，合并前或合并后必须额外运行一次 `workflow_dispatch.gate=release_candidate`，并把 run URL、commit SHA 和 `release-gate-evidence` 记录到 issue 或 release 记录。

### Dependabot alert triage

本轮 Dependabot 清单是单一前端 runtime dependency 聚合风险，而不是 9 个互不相关的升级项：

| Alert | Package | Ecosystem | Manifest | 修复决策 |
| --- | --- | --- | --- | --- |
| GHSA-v8jm-5vwx-cfxm / GHSA-v2wj-7wpq-c8vv / GHSA-h8r8-wccr-v5f2 | `dompurify` | npm | `frontend/package-lock.json` | 升级到 `^3.4.7`，覆盖 `>=3.2.7` / `>=3.3.2` patched 要求 |
| GHSA-cj63-jhhr-wcxv / GHSA-cjmm-f4jc-qw8r | `dompurify` | npm | `frontend/package-lock.json` | 升级到 `^3.4.7`，覆盖 `>=3.3.2` patched 要求 |
| GHSA-39q2-94rc-95cp / GHSA-v9jr-rg53-9pgp / GHSA-crv5-9vww-q3g8 / GHSA-h7mw-gpvr-xq4m | `dompurify` | npm | `frontend/package-lock.json` | 升级到 `^3.4.7`，覆盖 `>=3.4.0` patched 要求 |

验证口径：这是前端 log viewer 的 XSS 防护依赖，属于 runtime security dependency；修复 PR 至少跑 `npm audit --prefix frontend --audit-level=moderate`、`npx tsc --noEmit`、`npm run build`。如果未来 Dependabot alerts 涉及后端 runtime dependency，再追加 backend unit/integration 证据。

## 缺陷回归流程

| 阶段 | 要求 |
| --- | --- |
| 发现 | 记录缺陷来源、影响租户/项目、失败日志、是否涉及安全/数据隔离 |
| 定位 | 先写能复现失败的自动化测试；跨租户、真实 DB、API 写入、worker 状态优先放 required integration |
| 修复 | 不为覆盖率添加无行为断言；外部 SMTP/S3/webhook 可 fake，但 DB/API/RBAC 链路必须真实 |
| 验证 | 本地跑新增测试、相关 suite、ruff；CI 失败摘要必须能指向具体测试和数据 |
| 关闭 | 在 PR/提交说明里写明回归测试路径；若只用 unit，说明为什么不需要 integration |

## 性能与非功能 Smoke

PR 不跑性能硬门禁，避免把环境抖动伪装成产品失败。`workflow_dispatch.gate=nightly` 与 schedule 记录趋势；`workflow_dispatch.gate=release_candidate` 使用 `.github/performance-slo-manifest.json` 中的 PRD SLO 阈值环境变量与最小样本数，并与真实 E2E 一起作为发版准入。性能 summary 每条记录必须带当前 `gate_profile`、manifest 对应阈值、满足 `min_samples` 且 `passed=true`，CI manifest 会拒绝缺项、意外项、profile 不匹配、阈值漂移、样本不足或失败的 SLO 证据；发现退化后再升格为专项性能测试。

| 项 | Nightly/Manual 入口 | 观察信号 | 升级条件 |
| --- | --- | --- | --- |
| API 与真实 DB smoke | `backend-integration-test` required + nightly/manual `tests/integration/test_performance_smoke.py` | pytest `--durations=20`、读/写 API p99 smoke、手动/webhook/schedule 触发入队 SLO、waiting dequeue 恢复入队 SLO、取消 API p99 smoke、真实 API token 五路并发读路径 p99 smoke、DB 写入/查询/审计链路；performance artifact 保存每个 SLO 断言的 p50/p99/max、`threshold_ms`、`gate_profile` 与 `passed` JSONL/Markdown 摘要 | 同一测试连续 3 次进入慢榜前 5 且耗时翻倍，或 smoke 超过阈值 |
| Redis / SSE 实时流 smoke | nightly/manual `tests/integration/test_performance_smoke.py` | Redis Stream 写入 + 读回 round trip；本地 uvicorn HTTP 流验证 `/runs/{run_id}/logs` 日志 SSE 和 `/runs/{run_id}/events` 状态事件 SSE 的真实路由、一次性 ticket、RBAC/PostgreSQL Run 查询和客户端 chunk 到达延迟，默认阈值 `PERF_SSE_LOG_DELIVERY_P99_MS=2000` / `PERF_SSE_EVENT_DELIVERY_P99_MS=2000` | 连续 2 次超过阈值，转日志/状态事件链路性能专项 |
| 归档日志回看 smoke | nightly/manual `tests/integration/test_performance_smoke.py` | `GET /runs/{run_id}/logs/archive` p99 smoke，覆盖小样本回看、1500 行归档对象第 15 页分页，成功路径只读取 `logs/{run_id}.jsonl` 且不 presign，对象缺失稳定 404，存储未配置稳定 503，跨租户拒绝路径 404 收敛且不触发 S3 get_object，真实空 scope API token 同租户拒绝不回显日志 key/行内容且不触发 S3 get_object/presign，并在真实 API token 五路并发读路径中验证分页窗口和无额外 S3 读取；失败摘要含 p50/p99/max | 连续 2 次超过阈值，转日志归档/对象存储专项 |
| Artifact 列表/下载链接 smoke | nightly/manual `tests/integration/test_performance_smoke.py` | `GET /runs/{run_id}/artifacts` p99 smoke 验证真实 DB artifact 行、RBAC 和分页，且成功列表只读 DB、不触发 S3 presign 或 get_object；`GET /artifacts/{artifact_id}/download` p99 smoke 验证单次与 burst 权限链路、每请求一次预签名 URL 生成且不读取对象、存储未配置稳定 503，跨租户拒绝路径 404 收敛且不触发 presign，真实空 scope API token 同租户拒绝不回显 artifact name/path 且不触发 S3 get_object/presign，并在真实 API token 五路并发读路径中验证 artifact list DB-only 与 artifact 下载 presign-only；软删除 artifact 下载 no-presign 已下沉 required integration | 连续 2 次超过阈值，转对象存储/API 性能专项 |
| Audit events 查询 smoke | nightly/manual `tests/integration/test_performance_smoke.py` | `GET /audit-events` p99 smoke，验证真实 DB 审计行、Owner/Admin/API token 权限链路、分页与过滤查询，并断言成功查询持续写入 `audit_events.list` 自审计行；拒绝路径覆盖 member/viewer、跨租户资源过滤，以及 `run.read`/`project.read`/空 scope API token，均不写 `audit_events.list` 自审计；真实 API token 五路并发读路径还验证 audit-events list 与 run detail、归档日志、artifact list、artifact 下载同批并发时仍写足自审计，且 self-audit after_state 不含返回 data、日志片段或 artifact 名称/路径 | 连续 2 次超过阈值，转审计查询/索引专项 |
| 执行摘要生成 smoke | nightly/manual `tests/integration/test_performance_smoke.py` | 真实 RunRepository/PostgreSQL 终态写入、Redis LogStream、JUnitCollector 解析 1000 条 JUnit testcase 后生成 summary；默认阈值 `PERF_EXECUTION_SUMMARY_P99_MS=3000` | 连续 2 次超过阈值，转 collector/summary/DB 写入性能专项 |
| Worker/container smoke | `heavy_docker` + `external_stack` integration | worker lost、cancel、state visibility、真实容器生命周期、真实 worker 后 artifact 列表、预签名下载内容与归档日志读回、真实 `run.read` API token 对 worker 产物的 detail/logs/list/JUnit/HTML/log/Allure 四类 download 读回、空 scope token 对 detail/logs/list/四类 download 拒绝且不泄露 worker secret/artifact/log 片段、worker_lost 后自动 retry 完成，且 attempt=2 retry Run 也用真实 `run.read`/空 scope token 覆盖 logs/list/JUnit download 三面；clone/setup failure archived logs 也用 `run.read`/空 scope token 覆盖成功读回与 403 不泄露；priority queue high/low 完成 Run 也用 `run.read`/空 scope token 覆盖 detail/logs/list/JUnit download | 同一 worker 路径连续 2 次超时、状态未收尾，或 artifact/log/retry/API token scope 回看缺证据 |
| E2E 用户路径 | PR/pr_like 轻量 auth-flow，nightly/schedule 固定 real login / run trigger / special regressions，release_candidate 全量 Playwright | 登录权限、创建项目到触发 run、结果/产物查看、归档日志回看、artifact 预览；PR auth-flow 使用 mocked API 验证 Recent Runs 的后端 `done` + failed summary 能渲染为 `Failed`，`done` + `total=0` empty collector summary 只能渲染为 `Unknown`；release_candidate 校验并上传 html/junit/test-results、spec 清单、Playwright `--list` 文本/JSON 测试清单和 E2E evidence manifest，强制关键 spec 存在，且每个预期 spec 至少有 1 个 testcase，JUnit 总数、逐 spec testcase 数和逐 spec title 集合都必须匹配 `--list`，failure/error/skipped 必须为 0 | 任一路径 nightly 连续失败 2 次，转 P0 缺陷并补后端回归 |
| CI 稳定性 | 所有 job timeout + pytest durations + Docker pull retry + backend artifacts + tenant isolation audit | 超时、registry 前置失败、warning 摘要；backend tenant isolation audit log、unit/required/heavy/external-stack/performance collect/JUnit、pytest log、coverage XML/JSON、performance p50/p99/max JSONL/Markdown、external-stack Docker compose ps/log、evidence manifest 非空校验，backend-test unit JUnit/coverage 可解析且无 failure/error/skip，backend integration collect 最低基线与 JUnit testcase/failure/error/skip 计数校验，nightly 只允许已知 OOM opt-in skip，release_candidate skipped 必须为 0，nightly/release_candidate performance summary 按 SLO manifest 校验固定名称、`gate_profile`、阈值、`min_samples` 和 `passed=true`，release_candidate 额外校验 OOMKilled heavy-docker testcase 未 skip/fail | 非业务前置失败超过 1 周内 2 次，登记 flaky/infra 项；artifact 缺失或无法定位失败用例时登记 CI evidence 缺陷；静态审计发现 artifact/test_result/run_event 绕过 Run join 时按安全隔离缺陷处理 |
