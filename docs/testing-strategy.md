# 测试分层、风险矩阵与质量运营

更新日期：2026-05-28

## 分层口径

| 层级 | CI 入口 | 证明什么 | 不证明什么 |
| --- | --- | --- | --- |
| Unit | `backend-test` 跑 `ruff`、tenant isolation 静态审计、`tests/unit` + coverage | 分支逻辑、错误处理、权限 helper、worker/service 小闭环；mock 用于隔离外部系统和制造失败分支；静态审计阻止 artifact/test_result/run_event 这类无 tenant_id 表绕过 Run join | 真实数据库约束、跨请求事务、Redis/Docker/S3/SMTP 的真实可用性 |
| Required integration | `backend-integration-test` 在 PR/push 跑 `tests/integration -m "not heavy_docker and not external_stack and not performance"` | 真实 PostgreSQL/Redis/Testcontainers、FastAPI 路由、repository 约束、API 写入后的 DB 状态、RBAC/租户隔离、auth 失败审计 outage 不变成 500、auth 高风险端点 login/register/refresh/API token create/SSE ticket 真实 Redis rate limit、Bearer token hash bucket 与限流后无额外 audit 副作用、refresh revoke Redis blip 不阻断 token 轮换、register/refresh/logout 审计防泄露 password/access token/refresh token、worker 入口异常重试、真实 `RunExecutor` setup Docker daemon 基础设施错误 retry、setup script/clone 业务失败不误 retry、worker_lost reclaimer 的真实 Redis status event 与 retry Run 入队、manual priority 到 high/medium/low worker queue 元数据落库且 `run.trigger` audit payload 与 API response 对齐、单 run/batch cancel 的 Redis status event 与 audit before/after、schedule create/update 审计 payload 与 API response 对齐、audit-events 成功自审计与拒绝路径不误写自审计、audit-events API token `audit.read` scope 与 `run.read`/`project.read`/空 scope 拒绝 no self-audit、schedule worker 自动触发 audit 与 missing-pipeline skip audit、SSE logs/events `Last-Event-ID` 断点续传、归档失败真实 Redis retry marker/worker cron 重试、归档日志读回分页/1500 行大页/对象缺失/存储未配置、API token `run.read` 成功、`project.read`/空 scope 拒绝和跨租户 404、同一个真实 `run.read` API token 串联归档日志回看、artifact 列表与下载 URL 且验证日志只读 S3 对象、artifact 只 presign 不读对象、artifact 限制落库、S3 上传失败无孤儿 Artifact 行、artifact 列表元数据分页/越界页/DB-only 无 S3 副作用和下载链接 bucket/key/TTL/多 artifact presign-only/存储未配置/软删除下载 no-presign、artifact API token `run.read` 成功、`project.read`/空 scope 拒绝和跨租户 404 收敛、签名 webhook 的 HMAC/branch filter/dedup/`run.trigger` audit payload 与 API response 对齐、filtered/duplicate 决策 audit、保留 metadata 保护 | Docker 容器执行、外部 API+worker compose 栈、平台相关 OOM 语义与 performance smoke |
| Heavy Docker integration | nightly/manual 跑 `tests/integration -m "heavy_docker and not external_stack"` | worker/container/cancel/state visibility 等需要真实 Docker 镜像和容器生命周期的链路 | 单独启动的 API/worker compose 栈；macOS/GitHub Actions 不稳定的 OOMKilled 语义默认不作为 PR 门禁 |
| External-stack integration | nightly/manual 先启动 compose API/worker/MinIO，再跑 `tests/integration -m "external_stack"` | 真实 API 触发后由 compose worker 消费队列、执行容器、写回终态；验证 worker 运行中 `/runs/{id}/logs` SSE 可读到唯一实时 marker、ticket 单次消费、`Last-Event-ID` 续读不重放、空 scope API token 被 `run.read` scope 拒绝且不回显日志/对象 key；验证真实 worker 后的多 artifact 列表、逐个预签名下载 URL 与 MinIO 对象内容、平台产出的 artifact 元数据/下载内容/归档日志不被动泄露 worker 注入密钥；fake pytest 主动把该密钥打印到 stdout/stderr 时，live `/logs` SSE、archived logs 和真实 `run.read` API token 读面只出现 `[REDACTED]` 且不回显原文；并用真实 `run.read` API token 重读 run detail、archived logs、artifact list 和 JUnit/HTML/log/Allure 四类 worker artifact download，空 scope token 对 detail/logs/list/四类 download 均 403 且不回显 worker secret、artifact 名称/路径、内容片段或日志行；触发 run 后可经审计 API 查到 `run.trigger` 且审计状态不夹带 worker/clone secret、归档日志 API、credentialed clone failure 后归档日志 API 响应即使为空也不泄露 clone secret/userinfo，clone/setup failure 的 archived logs 也用真实 `run.read` API token 重读且空 scope token 403 不回显日志 key、clone secret/userinfo 或 setup stderr、worker_lost 后自动 retry 再完成、high/low priority queue 只被匹配 worker 消费，且匹配 worker 完成后的 high/low Run 也用真实 `run.read`/空 scope token 覆盖 detail、logs、artifact list 和 JUnit download 读面，以及 clone/setup 用户失败不误 retry/不泄密/不落 artifact 的黑盒链路 | 不是 PR 门禁；不替代 required integration 的快速 DB/API/RBAC 覆盖；Docker daemon 基础设施错误边界已有真实 DB/worker 入口测试，进一步黑盒扰动可作为 nightly/manual 增强 |
| Performance smoke | nightly/manual 跑 `tests/integration/test_performance_smoke.py -m performance` | 读 API、写 API、手动/webhook/schedule 触发入队 SLO（每次采样都写 `run.trigger` 审计且状态字段一致，webhook 额外验证保留 metadata 不覆盖执行配置且不进审计，schedule 额外验证系统身份审计和 `queue:low` 元数据）、waiting dequeue 恢复入队 SLO（priority 对应 high/medium/low queue metadata）、容量受限长队中 newer high 越过 older low backlog 的 priority preemption smoke、取消 API p99、Redis 日志写读、SSE ticket 创建 API p99（真实 `run.read` API token 签发，Redis JSON payload 携带 scopes，`auth.sse_ticket_create` audit 的 after_state 只记录 TTL/single_use，且 before/after/resource_id 不保存 ticket/token）、SSE 实时日志推送 < 2s、SSE 状态事件推送 < 2s、真实 API token 并发读路径（run detail、归档日志分页、artifact list DB-only、artifact 下载 presign、audit-events list 同批并发，且验证自审计增量、自审计 after_state 只含查询过滤/分页/total 而不含返回 data 或日志/artifact 片段，并验证 S3 no-extra-read/no-extra-presign 副作用）、真实空 scope API token 对归档日志、artifact 列表、artifact 下载的同租户 403 拒绝路径（不回显日志 key/行内容或 artifact name/path，且不触发 S3 get_object/presign）、归档日志读回 API（小样本与 1500 行大对象分页均只读 `logs/{run_id}.jsonl` 且不 presign，对象缺失稳定 404 且只读目标归档对象，存储未配置稳定 503，跨租户拒绝不读 S3）、artifact 列表元数据 API（成功路径 DB-only 不 presign/读 S3，并覆盖真实 `run.read` API token 下 1000 条大集合深页）、artifact 列表拒绝不返回元数据、artifact 下载链接 API（单次/burst 成功路径 presign-only 不读对象，存储未配置稳定 503，跨租户拒绝不 presign）、audit events 查询 API 与成功自审计写入、真实 `audit.read` API token 下大量过滤深分页、audit-events 角色/跨租户拒绝和 `run.read`/`project.read`/空 scope API token 拒绝都不写自审计、执行摘要生成 < 3s 的趋势哨兵；失败时输出 p50/p99/max 摘要 | 不是 PR 阻塞性 SLO 证明；发现退化后升格专项压测 |
| E2E | PR 跑 `auth-flow.spec.ts`，nightly 固定跑 `real-login-flow` / `real-run-trigger` / `special-regressions`，manual 跑完整 Playwright | 浏览器登录/权限/导航冒烟；nightly/manual 覆盖创建项目、触发 run、查看 result/artifact、归档日志回看、HTML artifact 预览、特殊回归 | PR 级 `auth-flow` 是 UI mock 冒烟，不作为后端真实数据正确性的证据 |

## 稳定性标准

- 可重复：测试数据使用唯一 slug/name/UUID，不依赖固定执行顺序。
- 顺序无关：integration 通过 fixture 初始化真实 schema 和 seed 数据，测试间不共享可变业务状态。
- 失败日志清楚：CI pytest 使用 `--tb=short --durations=20`，并上传 JUnit XML、pytest 原始输出、coverage XML/JSON 和 tenant isolation audit log；nightly/manual external-stack 失败时额外上传 compose PostgreSQL/Redis/MinIO/API/worker 日志；performance smoke 额外上传 p50/p99/max JSONL 与 Markdown 摘要。heavy Docker fixture 先复用本地镜像；只有缺镜像时才进入 pull 前置条件检查。
- 数据隔离：真实 DB 测试必须断言 tenant/project 过滤、soft-delete 隐藏、唯一约束 rollback 后 session 可恢复。
- 不掩盖真实失败：仅把 Docker daemon/registry/API stack/OOM 平台语义这类前置条件缺失记为 skip；业务断言失败仍然失败。

## 业务风险矩阵

| 风险面 | 当前证据 | 状态 |
| --- | --- | --- |
| RBAC：tenant role、project role、platform admin、API token scope | audit events、cross-tenant API、SSE 403/404、SSE logs/events ticket + `Last-Event-ID` 真实续传、SSE ticket 透传 API token scopes、run detail/artifact list/download 与 archived logs 的 `run.read` scope、audit-events API token `audit.read` scope、真实 JWT/API token integration、auth 失败审计 outage、auth rate limit 真实 Redis bucket 与 refresh revoke Redis blip 回归、register/refresh/logout/API token 审计防泄密 | 已覆盖主干；API token scope 已补真实路由矩阵，只读/错误/空 scope 都会被门禁，SSE `/logs` 的 `run.read` API token 能续读而空 scope ticket 被 403 拒绝，run detail、artifact 元数据列表、预签名 URL 和归档日志读回都不能由 `project.read`/空 scope token 换取，tenant A `run.read` token 不能枚举 tenant B artifact；audit-events 只能由 `audit.read` API token 查询，`run.read`/`project.read`/空 scope 返回 403 且不写误导性 `audit_events.list`，成功查询写自审计且 after_state 只保留查询过滤字段、分页与 total，不保留返回 data，member/viewer 403 和跨租户过滤 404 不写误导性自审计，nightly/manual 还观察角色拒绝、跨租户拒绝、audit-events API token scope 拒绝查询 p99，以及空 scope token 对 run detail/归档日志/artifact/SSE 日志读面的同租户拒绝 p99 与 no-S3/no-presign 副作用；login/refresh/logout 在审计写入异常下不把 401/204 变成 500，认证高风险 login/register/refresh/API token create/SSE ticket 路由用真实 Redis 验证第 6 次请求返回 429，Bearer 路径 Redis key 只保存 token hash，IP/cookie 路径 Redis key 不包含 username/password/refresh token，各端点限流前 5 次写对应 audit、第 6 次不额外写审计，refresh revoke Redis 抖动不阻断 token 轮换；register/refresh/logout 不把 password/access token/refresh token 写入 AuditEvent before/after，API token 创建/撤销审计只记录 name/scopes 等可追溯元数据 |
| 数据隔离：跨 tenant/project、soft-delete、随机 UUID | `test_cross_tenant_isolation.py`、repository/API 真实 DB 测试、artifact/result/归档日志隐藏测试、真实 API token 跨租户 artifact 下载 404 收敛、软删除 artifact 下载与随机 UUID 404 收敛、artifact 列表拒绝 no-metadata p99 smoke、artifact 拒绝下载 no-presign p99 smoke、archived logs 拒绝 no-S3-read p99 smoke | 已作为 PR integration 门禁；Member/Viewer 项目级路由先归一 404 再做 RBAC；artifact 列表拒绝路径不返回名称/路径，artifact 下载对软删除/跨租户真实 ID 与随机 UUID 不暴露差异，归档日志回看对跨租户真实 ID 与随机 UUID 不暴露差异，拒绝路径不调用 S3 presign/get_object，nightly/manual 还观察这些拒绝路径的 p99 |
| Repository：Audit/User/Artifact/TestResult/Run 查询、分页、唯一约束、级联/retention | `test_real_repository_matrix.py`、`test_real_auth_results_artifacts.py`、`test_real_db_persistence.py` | 已补真实 Postgres 直测；retention 覆盖超期 `done/failed/cancelled/timeout` 与 result/artifact/event 级联 |
| API 写入后的真实 DB 状态 | projects、project members、pipelines、credentials、environments、notification rules、runs、schedules、auth/token、single/batch cancel audit 链路、batch retry audit 链路 | 已补 required integration；environment `memory_mb` / `cpu_cores` / artifact limit 已覆盖 create/get/list/update 读回和真实 DB 持久化，项目 `git_url` userinfo、pipeline 复杂配置密钥、凭据明文、环境变量值、通知 channel/webhook/template 不落 audit before/after，manual run trigger 与 schedule create/update 审计状态逐字段对齐 API response，project/pipeline/member/notification/schedule delete 和 run cancel 有删除/取消前快照 |
| Worker/queue 状态机与产物链路 | 真实 PG repository 状态机、run claim、cancel、state visibility、execute_run 基础设施异常重试、真实 `RunExecutor` setup Docker daemon 基础设施错误 retry、setup script/clone 业务失败不误 retry、worker_lost reclaimer 真实 Redis status event 与 retry 入队、RunExecutor OOM/timeout 资源终止写真实 DB/Redis、manual priority 到 `queue:high/medium/low` 的真实 API/DB 入队元数据和 `run.trigger` audit payload 对齐 API response、`dequeue_waiting` cron 恢复 waiting Run 的 high/medium/low queue metadata SLO、容量受限长队中 newer high 越过 older low backlog 的 priority preemption smoke、executor/task 单测、cancel API 真实 Redis status event `previous` 与 p99 smoke、状态事件 `/events` SSE `Last-Event-ID` 续传、artifact size/count/recursive upload、S3 上传失败无孤儿 DB 行、artifact 列表元数据分页/越界页/DB-only 无 S3 副作用、同一 `run.read` token 下归档日志回看 + artifact 列表 + 下载 URL 联合路径、下载链接 bucket/key/TTL/多 artifact presign-only/存储未配置/软删除 404 no-presign/API token scope/跨租户 404 真实 DB/API 测试、artifact 列表拒绝 no-metadata p99 smoke、artifact 拒绝下载 no-presign p99 smoke、归档失败真实 Redis retry marker/TTL + worker cron 重试、归档日志默认页/分页/1500 行大页/缺失对象/存储未配置/API token scope/跨租户 404 API 测试、归档日志拒绝 no-S3-read p99 smoke、heavy Docker cancel/OOM、compose high/medium/low worker 配置、external-stack worker smoke、real worker SSE live logs、worker_lost retry、priority queue worker isolation 和 clone/setup failure 黑盒 | 已覆盖 queued/preparing/running/collecting/done/failed/cancelled/timeout；priority+FIFO 排序、manual priority 队列元数据和审计 payload、waiting queue cron 恢复入队 SLO、容量受限时 API 创建的 newer high priority waiting runs 会经 `dequeue_waiting` 进入 `queue:high` 并越过 older low backlog，older low 保持 waiting 且不产生额外 audit、worker retry 落库、worker_lost 原 Run 失败、Redis status event `previous=running`、orphan cleanup 和 retry Run 入队、setup Docker daemon `ConnectionError` 会创建 attempt=2 retry Run，setup script 非 0 与 clone failure 不误 retry，clone 错误脱敏、`oom_killed` / `timed_out` backend 结果到 Run `timeout` 终态和 Redis status event、`/events` 状态流只回放断点后的 Redis status_change 并要求 ticket 单次消费、artifact 递归落库、失败上传不落库、归档失败补偿、cancel 控制面的 Redis previous/audit 前后态、API p99 趋势和 artifact 列表/下载链接参数有真实 DB/API/RBAC/Redis 测试；artifact 元数据列表、预签名 URL 与归档日志读回都需要 `run.read` scope，同一个真实 `run.read` API token 已覆盖同一 Run 的 archived logs、artifact list 和 artifact download，列表分页/越界页保持 DB-only 不调用 S3，日志回看只读取 `logs/{run_id}.jsonl`，artifact 下载只生成 presign 且不读取对象，多 artifact 下载链接逐条只生成 presign 且不读取对象，软删除 artifact 行保留 `deleted_at` 后下载入口与随机 UUID 同样 404 且不触发 presign/get_object，归档大页只读 `logs/{run_id}.jsonl` 且不生成 presign，存储未配置稳定返回 503，拒绝列表不返回 artifact 名称/路径，跨租户 artifact/log ID 与随机 ID 同样 404，拒绝路径不调用 presign 或 S3 get_object；nightly/manual 真实外部栈覆盖 worker 解密环境变量后注入容器、真实 worker 运行中通过 `/logs` SSE 读到唯一 marker 且 run 未终态、ticket 单次消费、`Last-Event-ID` 续读不重放、多 artifact 内容下载、平台产出的 artifact 元数据/下载内容/归档日志不被动泄露 worker 密钥；用户测试进程主动把该密钥打印到 stdout/stderr 后，live SSE、archived logs 与真实 `run.read` token 读面只保留 `[REDACTED]` marker，空 scope ticket/归档读面仍 403 且不回显原文或 marker；并用真实 `run.read` API token 读取 worker 产出的 archived logs、artifact list、JUnit/HTML/log/Allure 四类 worker artifact download，空 scope token 对 SSE logs、归档 logs、artifact list 和四类 download 均拒绝且不回显 worker secret、artifact 名称/路径、内容片段或日志行；同时覆盖 worker_lost 后自动 retry、停掉 high/low worker 时 high/low run 保持 queued、启动匹配 worker 后完成的队列隔离链路，并对匹配 worker 完成后的 high/low Run 用真实 `run.read`/空 scope token 覆盖 detail、logs、artifact list、JUnit download 与 403 不泄露；并覆盖 credentialed clone 失败与 setup 非 0 不创建 retry、不落 artifact、clone error 与 clone 失败归档日志 API 不泄密、setup 失败归档日志可读回，且 clone/setup failure archived logs 也用真实 `run.read`/空 scope token 覆盖成功读回与 403 不泄露 |
| Webhook/schedule 幂等与失败路径 | schedule worker 成功触发与 enqueue conflict 的系统 `run.trigger` audit、schedule missing pipeline 的系统 skip audit、HMAC 缺失/成功签名、branch filter、dedup、filtered/duplicate 决策 audit、terminal same commit、cross-tenant 404、archived project、missing pipeline/environment、enqueue conflict、保留 metadata 防覆盖、webhook 与 schedule 入队 SLO | Webhook/API/schedule worker 主风险已进 required integration；签名 webhook 成功和 enqueue conflict 的 `run.trigger` 审计 after_state 与真实 API response 对齐，filtered/duplicate 和 schedule missing-pipeline 这类不创建 Run 的分支已有 AuditEvent 且 after_state 只含非敏感决策/排障字段；nightly/manual 额外观察 webhook 和 schedule worker tick 入队 p99 |
| 三条关键 E2E 冒烟 | 登录/权限、创建项目到触发 run、查看 run/result/artifact/归档日志 | 已有 Playwright 用例；`special-regressions` 已覆盖真实 DB+S3 准备后的归档日志回放、搜索和 HTML artifact 预览；PR 只跑轻量 auth-flow，nightly 固定跑真实链路，完整链路 manual |

补充：`worker_lost` external-stack smoke 还会对 reclaimer 创建的 attempt=2 retry Run 使用真实 `run.read` API token 重读 archived logs、artifact list 和 JUnit download；空 scope token 对三面均 403，且响应不回显日志 key、artifact 路径/名称或 JUnit 内容片段。

补充：clone/setup failure external-stack smoke 会对失败 Run 的 archived logs 追加真实 `run.read` API token 读回；空 scope token 对失败日志回看 403，且响应不回显 `logs/{run_id}.jsonl`、credentialed URL secret/userinfo、clone error 细节或 setup stderr。

补充：priority queue external-stack smoke 会对 high/low 两个匹配 worker 完成 Run 使用真实 `run.read` API token 重读 detail、archived logs、artifact list 和 JUnit download；空 scope token 对 logs/list/download 均 403，且响应不回显日志 key、artifact 路径/名称、JUnit 内容片段或关键日志行。

补充：real worker SSE external-stack smoke 会在 fake pytest 进程打印唯一 start/end marker 时直接消费 `/runs/{id}/logs`，断言 start marker 到达时 Run 仍未终态，第二张 ticket 通过 `Last-Event-ID` 只续读 end marker，复用 ticket 返回 401，空 scope API token 换出的 ticket 返回 403 且不回显 marker、日志归档 key 或 artifact 路径。

## Warning 登记

| Warning | 来源 | Owner / 截止条件 | 状态 |
| --- | --- | --- | --- |
| `testcontainers.redis.wait_container_is_ready` deprecation | 第三方 testcontainers Redis 模块 import | 测试平台维护者；2026-06-17 前在依赖升级窗口评估是否可去掉精确 filter，或迁移 Redis fixture 到结构化 wait strategy | 已登记并在 `pyproject.toml` 精确过滤第三方 warning，避免 CI 噪音掩盖真实失败 |
| SQLAlchemy `Run.project` relationship overlap | ORM mapper 配置 | 后端维护者；2026-05-27 已完成，后续若改 Run/Project/Pipeline 关系需保留 mapper 无 warning 验证 | 已清理：`Run.project` 明示 `overlaps`，integration mapper 配置不再产生该 warning |
| Redis pubsub `close()` deprecation | heavy cancel integration 触发 `engine.cancel` 关闭 pubsub | 后端维护者；2026-05-27 已完成，后续 Redis client 升级需保留 `aclose()` 优先路径 | 已清理：取消监听器优先调用 `pubsub.aclose()`，单测锁定该路径 |

已清理项：

- SSE 单测的 `AsyncMockMixin._execute_mock_call was never awaited`：改为带同步 `pipeline()` 的轻量 Redis fake。
- httpx per-request `cookies=` deprecation：改为在 `AsyncClient.cookies` 上设置 refresh cookie。
- JWT wrong-secret 测试的 31 字节 HMAC key warning：换成 32 字节测试密钥。

## Flaky 与回归机制

- 台账入口：`docs/testing-quality-ops.md`。新 flaky、线上/测试缺陷回归、性能/nonfunctional smoke 都在该文件按表登记。
- 新 flaky 先登记：记录测试名、失败环境、复现命令、最近失败日志、owner 和下一步。
- 只允许因外部前置条件 skip：Docker daemon 不可用、本地缺镜像且 registry 拉取失败、本地未启动 external stack、平台 OOM 语义不稳定；CI nightly/manual 会主动启动 external stack，并以 `QAP_EXTERNAL_STACK_REQUIRED=1` 要求 `/ready` 返回 QA Platform 健康 JSON，否则失败而不是 skip。
- 不引入测试重跑来掩盖业务失败；如需 retry，只用于 Docker image pull 这类外部前置条件。
- 每个线上/测试发现缺陷都要补一条能失败再变绿的回归测试，优先放在 required integration；只有纯分支逻辑才放 unit。
- Coverage 门槛按真实风险路径逐步提高；不得为了百分比添加无行为断言或重 mock 覆盖。
- 后端静态检查由 CI `backend-test` 执行 `ruff check src tests` 和 `scripts/check_tenant_isolation.py --self-test`；tenant isolation audit 使用 AST 识别多行/别名 SQLAlchemy 查询，要求 Artifact/TestResult/RunEvent 直接查询具备 `Target.run_id == Run.id` 关系和有效的 `Run.tenant_id == ...` / `Run.project_id == ...` scope，并审计 `repos.artifact`/`repos.test_result` generic read 是否有变量级 `repos.run.get_for_tenant` guard 与空结果检查，与单元 coverage 门禁一起阻止新增 lint 债务和高风险租户隔离查询债务。
- CI 证据产物：`backend-test` 上传 `backend-test-artifacts`，包含 tenant isolation audit log、unit JUnit、pytest log、coverage XML/JSON 和 evidence manifest；`backend-integration-test` 上传 `backend-integration-artifacts`，包含 required/heavy/external-stack/performance JUnit 和 pytest log，external-stack 失败时包含 Docker compose ps/log，performance lane 包含每个 SLO 断言的 p50/p99/max JSONL 与 Markdown 摘要；manifest 会校验当前 lane 期望的证据文件非空，避免只上传空目录或初始化占位文件。
